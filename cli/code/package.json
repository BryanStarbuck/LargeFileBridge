{
  "name": "@lfb/cli",
  "version": "0.1.0",
  "private": true,
  "description": "Large File Bridge CLI (lfb) — thin REST wrapper over the local web app backend (pm/cli.mdx)",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}

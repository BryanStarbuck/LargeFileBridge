---
title: LargeFileBridge — Page Actions (the action-links row under the page title, selected-or-all, background-queued)
description: The cross-cutting spec for the page-level action-links row every LFBridge screen carries directly UNDER its page title — a single horizontal row of blue text hyperlinks, each with a leading icon, that operate on the WHOLE PAGE's set of files (or the checked subset), as opposed to the per-row ⋮ kebab (menus.mdx §3) or the single-entity "more" menu (menus.mdx §4). This design REPLACES the earlier "Actions ▾" dropdown: page actions are now VISIBLE inline links, not hidden behind a menu, matching the sister-app processing-queue action row (~/BGit/all/marketing/ai code/ui/Q/page_processing_queue.js — the .q-actions row). Defines the three LOCKED behavior rules every page action still obeys: (1) SCOPE — with nothing checked the action applies to ALL files on the page, recursively into sub-directories; with rows checked it applies to exactly the checked set. (2) SKIP-ALREADY-DONE — an action that PRODUCES an output (Create Transcriptions, Create AI descriptions) first drops every file that already has that output, so only the remainder is queued. (3) BACKGROUND QUEUE — the eligible set is handed to the server-side async job queue (job_queue.mdx) and the request returns immediately; the work drains in the background and surfaces in the progress dock. The user is told what happened with a single toast: "28 files will have their Transcriptions created". Names the per-page action catalog (which links appear on which page), the visual row (blue link, leading icon, gap spacing, "·" separators, optional "More ▾" overflow), and locks the notification wording. Sibling of menus.mdx (per-row/per-entity menus), job_queue.mdx (the async queue), Transcribe.mdx / ai_description.mdx (the two producing actions), file_system.mdx (the File System hub that also carries this row), and webapp.mdx §10–§14 (the progress dock the queue surfaces through).
---

# LargeFileBridge — Page Actions (the action-links row under the page title)

Every LFBridge screen that shows a **list of files** carries a single **horizontal row of action-links**
**directly beneath its page title**. It is the visible, discoverable home for the actions that operate on
the **whole page's set of files at once** — "transcribe everything here", "describe everything here",
"compress everything here" — rather than on one row.

This is the third and last of LFBridge's action surfaces, and it must not be confused with the other two
([menus.mdx](./menus.mdx) owns both of those):

| Surface | Scope | Where | Owner spec |
| --- | --- | --- | --- |
| **Row `⋮` kebab** | **one** entity (the row) | last cell of each table row / right-click | [menus.mdx §3 / §5](./menus.mdx) |
| **Entity `⋮` "more" menu** | **one** entity (the page's subject) | top-right of a single-entity page | [menus.mdx §4](./menus.mdx) |
| **Page action-links row** | the **whole page's files** (or the checked subset) | a row directly **under the title** of a **list** page | **this file** |

## 0. Design reversal — links, not a dropdown (LOCKED)

An earlier version of this spec put the page-level actions inside an **`Actions ▾` dropdown menu** in the
page header. **That is superseded.** Page actions are now a **visible horizontal row of hyperlinks** sitting
directly **under the page title** — the user sees every available action at a glance and clicks it in one
step, with **no menu to open first**.

The pattern is taken directly from the sister web app's Processing-Queue page — study it as the reference:

* **`~/BGit/all/marketing/ai/code/ui/Q/page_processing_queue.js`** — the `.q-actions` row built by
  `setupActions()`, and the `.q-actions` / `.q-actions a` CSS (blue link text, a leading `.q-icon` glyph,
  `gap: 14px`, thin `·` separators, and an optional `More ▾` overflow that only renders when there are
  overflow items).

We adopt that exact look across **every page of this web app** so the action affordance is identical
everywhere. The one difference from the sister page: that page identifies itself with tabs; **ours always
shows the page title**, and the action-links row sits **immediately below it**.

## 0.1 Second reversal — producing actions open the batch confirm popup (LOCKED)

An earlier version of this spec had the two **producing** links — **Create Transcriptions** and **Create AI
descriptions** — enqueue the work **on click** and show only a toast (Rules 1–3 ran silently on the server,
[§1.3](#13-rule-3--background-queue-return-immediately) / [§2](#2-the-notification-locked-wording)). **That
is superseded.** Per the product owner:

> *"We want the 'Create Transcriptions' action to bring up the pop-up window that the 'Transcribable' metric
> panel being clicked pops up on the View-one-repo page. We want 'Create AI descriptions' to also use the
> great pop-up."*

So clicking **Create Transcriptions** / **Create AI descriptions** now **opens the unified batch-confirm
popup** ([dialogs.mdx §5](./dialogs.mdx)) — the **same** `WarningPopup` the `Transcribable` / `Describable`
metric tile opens on the View-one-repo page ([task_tabs.mdx §5.1](./task_tabs.mdx)): the candidate list
**checked by default**, a **Continue** into the checklist, uncheck anything, then a solid-blue **"Transcribe
N files"** / **"Describe N files"** confirm. **This changes only WHERE the enqueue happens** — it moves from
the link's click to the popup's Confirm. The three LOCKED rules below are otherwise **unchanged**: they now
run at the popup's Apply. [dialogs.mdx](./dialogs.mdx) owns the popup contract and the scope→popup builders;
this file owns the *row* that opens it.

Siblings this builds on and does not repeat:

* [dialogs.mdx](./dialogs.mdx) — **the unified batch-confirm popup** the producing links now open (§0.1),
  and the app-wide "no native browser dialogs" rule the row's confirm modal already obeys (§3).

* [menus.mdx](./menus.mdx) — the per-row and per-entity menus (the other two surfaces). The page
  action-links row is **not** a per-entity catalog; it never appears on a single-entity page.
* [file_system.mdx](./file_system.mdx) — the File System left-bar hub; carries this action-links row and
  owns the interesting-directory folder coloring.
* [job_queue.mdx](./job_queue.mdx) — the **server-side background queue** every producing page action hands
  its eligible set to. Enqueue returns immediately; the queue drains in the background (§3).
* [Transcribe.mdx](./Transcribe.mdx) / [ai_description.mdx](./ai_description.mdx) — the two **producing**
  page actions (**Create Transcriptions** / **Create AI descriptions**); this file owns the *row*
  behaviour (scope, skip, queue, toast), those files own the *engine*.
* [webapp.mdx §10–§14](./webapp.mdx) — the **progress dock** + job registry the queued work surfaces
  through, and the bounded-concurrency model.
* [compression.mdx §12](./compression.mdx) / [git_ignore.mdx](./git_ignore.mdx) — the **pop-over
  dialogs** the **Compress videos / Compress images** and **Git ignore** links open (this file owns the
  *links*; those own the *dialogs + engines*).

---

## 1. The three LOCKED rules every page action obeys

A page action is defined by **what set it runs over** and **how it runs**. All three rules below are
LOCKED — every action in the per-page catalog (§4) obeys them unless its row says otherwise. (The design
reversal in §0 changed only the *presentation*, not these rules.)

### 1.1 Rule 1 — SCOPE: checked subset, else the whole page recursively (LOCKED)

When the user clicks an action link, the **set of files it runs over** is:

* **If one or more rows are checked** → **exactly the checked rows.**
* **If nothing is checked** → **every file on the page**, walked **recursively** into sub-directories.
  On a repo page that is the repo's whole working tree; on a File-System directory it is that directory
  and everything under it; on a storage it is the whole storage.

> **Containment (LOCKED).** The recursive walk is **downward only** — it descends into sub-directories of
> the resolved root and **never ascends to a parent or crosses into a sibling**. On the File-System column
> browser the root is the **deepest (rightmost) open column**, so an action there stays strictly inside
> that one directory even though ancestor columns are still open to its left ([file_system.mdx
> §4.1](./file_system.mdx), [directories.mdx §1.2](./directories.mdx)). A missing root is a `400`, never a
> silent fall-back to home or `/`.

So "click the action with nothing selected" means **"do it for all of them"** — the common case. The user
does not have to select-all first. (This mirrors the way a shell glob or a "Run all" button behaves.)

> **Same skip set as the scan (LOCKED).** The recursive root-walk skips exactly the directories the
> discovery scan and FS browser skip — `HARD_SKIP` ([scan.mdx §4](./scan.mdx)): `.git`, `node_modules`,
> and **build outputs** (`build/`, `dist/`, `.docusaurus/`, …) whose media are duplicates of the source
> files the page already shows. This keeps the action's *considered* count equal to what the page
> displays — a repo page showing 4 videos must never queue 8 because `build/` mirrored them.
> The walk and its skip set live in code:
> `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/transcribe/transcribe.service.ts` — `walkMedia()` / `SKIP_DIRS` (twin: `describe.service.ts` — `walkDescribable()`), sharing
> `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/shared/scan-filters.ts` — `HARD_SKIP`.

> Example (LOCKED walk-through). A repo page shows **40** files, **none checked**. The user clicks
> **Create Transcriptions**. The set to consider is **all 40** (recursive). Had **30** been checked, the
> set would be those **30**. The set is then narrowed by Rule 2.

### 1.2 Rule 2 — SKIP-ALREADY-DONE for producing actions (LOCKED)

An action that **produces an output for a file** — a transcript, an AI description — is **idempotent by
existence**: if the output already exists, the file is **dropped from the set** before anything is queued.
This is the same "already transcribed → skipped" / "already described → skipped" idempotency the per-file
paths already enforce ([Transcribe.mdx §4](./Transcribe.mdx), [ai_description.mdx §2.1](./ai_description.mdx)),
lifted to the batch.

The narrowing is applied **before** enqueue, so the queue only ever holds real work:

> Continuing the example. Of the **40** files, **12** already have transcripts. The set narrows to the
> **28** that do **not**. Only those **28** are enqueued. The 12 are never touched.

A producing action also drops files it **cannot** act on (a non-media file for Create Transcriptions, a
non-image/-video for Create AI descriptions) — those are silently excluded from the count, never queued.

**The drop is applied silently; the popup must not report it silently (LOCKED).** This rule keeps
already-done files out of the set, but a user staring at a large candidate count cannot see a filter that
left no trace — a big number reads as "it is redoing everything I already did". So wherever this narrowing
feeds the batch-confirm popup, the popup **states the excluded `alreadyDone` count** in its sub line
([dialogs.mdx §5.2.1](./dialogs.mdx)). Filtering correctly and *showing* that you filtered are two separate
requirements, and both are LOCKED.

**First-time gate (LOCKED).** Before a producing action queues anything, the planner resolves **where each
output would land** ([Transcribe.mdx §3.4](./Transcribe.mdx)). If **every eligible file** resolves to
`needsSetup` — the user has **no Personal storage configured** and the files are owned by no repo/company
storage — the planner **queues nothing** and returns `needsSetup: true` with a representative `setupPath`;
the page action opens the **first-time setup wizard** ([Transcribe.mdx §3.5](./Transcribe.mdx)) instead of
silently writing beside the media, then **re-runs the same page action** once a storage exists. (A *mixed*
scope — some files owned, some not — proceeds normally; the unowned files each report their own
`needs_setup` per file.) The producing actions that consult placement this way are **Create Transcriptions**
and **Create AI descriptions** (and any per-file analysis action added later). Pasted `~/…` paths are
home-expanded before this resolution (Transcribe.mdx §3.6).

The plan's `needsSetup` / `setupPath` fields, the all-eligible-need-setup detection, and the page action
routing to the wizard are implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/transcribe/transcribe.service.ts` — `enqueueTranscribe()` · `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/describe/describe.service.ts` — `enqueueDescribe()` · `~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/lib/pageActions.ts` — `createTranscriptions()` / `createDescriptions()`.

Producing actions further honour the two sticky flags ([menus.mdx §6.6](./menus.mdx)) where relevant: a
**Do not compress** flag excludes a file from a Compress-all; **Never IPFS** excludes it from an
Add-to-IPFS-all. (Transcribe/Describe are not gated by those flags — they don't pin or compress.)

### 1.3 Rule 3 — BACKGROUND QUEUE, return immediately (LOCKED)

Because a page action can add **many** items (a big repo can hold hundreds of media files), it **must not**
run them inline in the request. The narrowed eligible set is handed to the **server-side background job
queue** ([job_queue.mdx](./job_queue.mdx)); the endpoint **returns immediately** with the count that was
enqueued, and the queue **drains in the background** with bounded concurrency.

> **When the enqueue fires (§0.1).** For the two **producing** actions the enqueue is now triggered by the
> **batch popup's Confirm** ([dialogs.mdx §5](./dialogs.mdx)), not by the link's click — the click opens the
> popup (a plan/preview of the eligible files, [§5](#5-the-enqueue-rest-surface-plan-then-queue)), and the
> popup's **"Transcribe/Describe N files"** button is where the eligible **checked** set is handed to this
> queue. Everything below (return-immediately, background drain, one shared core-budgeted media-aware pool)
> is identical — only the trigger moved from the click to the Confirm. Each item, as it starts,
registers a progress-dock job (kind `transcribe` / `describe`, [webapp.mdx §11](./webapp.mdx)) so the user
watches the batch drain live — even after navigating away, and even in another tab.

This is the load-bearing difference from a per-file click: a per-file **Transcribe** may keep its request
open for the single run; a **page action** never does — it **enqueues and returns**.

> **One core-budgeted, media-aware drain (LOCKED).** Every "do this to many files" producer — **Create
> Transcriptions**, **Create AI descriptions**, **Compress videos / Compress images** — feeds the **one** shared
> background drain, not a private per-action worker. That drain runs **massively parallel** against the
> **Core Budget** ([job_queue.mdx §3](./job_queue.mdx), [parallelization.mdx](./parallelization.mdx)): the
> mass-compute budget is `round(cores × max_core_fraction)`, default **0.9**, so a bulk page action uses
> **≈90% of cores** — not a fixed tiny cap of two-at-a-time. It is **media-aware**: image compress fans
> **wide** (one thread per core), video compress and transcribe fan **narrow** with capped threads, and
> describe overlaps **network** waits — so N page actions firing at once still share the single budgeted pool
> instead of oversubscribing the box.

---

## 2. The notification (LOCKED wording)

The instant a producing page action's **batch popup is confirmed** (§0.1 — the popup opens on click; this
toast fires on its **"Transcribe/Describe N files"** Confirm), LFBridge shows **one toast** stating how many
files were queued — computed **after** Rules 1 + 2 (the eligible remainder = the checked set the user
confirmed), never the raw page count:

* **Create Transcriptions** → **"{N} files will have their Transcriptions created"**
  (equivalently phrased for one file: "1 file will have its Transcription created").
* **Create AI descriptions** → **"{N} files will have their AI descriptions created"**.
* Any other producing/queuing action follows the same shape: **"{N} files will …"**.

Edge cases, still one honest toast (never a silent no-op — [pin_process.mdx §6](./pin_process.mdx)):

* **Nothing eligible** (every candidate already had the output, or none were media) →
  **"All {M} files already have transcriptions — nothing to do"** (or the describe equivalent). `M` is the
  considered set size so the user sees *why* nothing queued.
* **Tools/credentials missing** — the action still queues, but the toast names the blocker the queue will
  hit (e.g. "Whisper isn't installed — install it in Settings → Tools", [Transcribe.mdx §5.2](./Transcribe.mdx);
  or the AI-credentials popup for describe, [ai_credentials.mdx](./ai_credentials.mdx)) so the user isn't
  surprised when cards fail.

The count in the toast is the **`willProcess`** figure the enqueue endpoint returns (§5) — the same number
that becomes queue items — so the toast and the dock never disagree.

---

## 3. Anatomy on the page — the action-links row (LOCKED)

```
┌───────────────────────────────────────────────────────────────────────────┐
│  ‹ Repos  /  Our_Movies                                    ┌ ⟳ Pin now  ┐ ⋮ │ ← header: title + primary + entity ⋮
│  ~/BGit/…/Our_Movies                                       └────────────┘   │
│                                                                             │
│  🎬 Create Transcriptions · ✨ Create AI descriptions · 🗜 Compress videos   │ ← ONE measured row, UNDER the title;
│  · 🗜 Compress images · 🚫 Git ignore                        ⋯ More ▾        │   what fits stays inline, the rest
│                                                                             │   collapses into More ▾ (pinned right)
│  128 Pin · 5 Undecided · 40 Ignore               search…       ⇅  ⛛  ⋮bulk  │ ← table control row (unchanged)
│  ☐  render/talk.mp4     …                                                   │
│  ☐  render/final.mov    …                                                   │
└───────────────────────────────────────────────────────────────────────────┘
```

The row is a **single flex line** of hyperlinks placed **directly beneath the page title** (above the
table's own control row). Its anatomy — LOCKED to match the sister-app `.q-actions` reference:

* **Each item is a hyperlink**, not a button: **blue link text** (`var(--lfb-primary)` / the link color),
  no border, no fill; `text-decoration: none` at rest, **underline on hover**; `cursor: pointer`.
* **A leading icon in front of every label** — a `lucide-react` glyph sized to the text (~14px), rendered
  as an inline icon before the label with a small gap (`gap: 5px` inside the link).
* **Even spacing, ONE line — never wraps** — the row is `display: flex; flex-wrap: nowrap; gap: 14px;
  align-items: center;` with a thin **`·` separator** (`color: var(--lfb-border)`) between consecutive
  items, exactly like `.q-actions .sep`. It is a **single row**; it does **not** wrap to a second line.
  When the items don't all fit, the overflow collapses into a trailing **`More ▾`** menu pinned to the
  **right** (§3.1) — never a second row. **The labels are short** so more fit inline: **Compress videos**,
  **Compress images**, **Git ignore** (not the older "Compress all videos…" / "Git-ignore big files…").
* **Always visible, never gated on a selection** — that is the whole point: the user can act on "all of
  them" without selecting first. When rows **are** checked, the producing items append the count to their
  label so the user knows the scope changed, e.g. **`🎬 Create Transcriptions (12)`** (12 checked); with
  nothing checked they read plainly and the scope is "all, recursive". (This replaces the old
  `Actions (12) ▾` button-label behaviour.)
* **Destructive / irreversible offers are tinted red** (`var(--lfb-bad)`, matching the reference's
  `color:#b91c1c` "Delete All Content" link) and always carry a **confirm step** (§4, [menus.mdx
  §6.1](./menus.mdx)) — a small web-based confirmation modal (never `window.confirm`), never a one-click
  mutation.
* **Measured `More ▾` overflow, pinned right (§3.1).** The row **measures what fits** and keeps that many
  actions inline (left → right, in catalog order); everything that does not fit collapses into a trailing
  **`More ▾`** menu at the **right** end of the row. When everything fits, no `More ▾` renders. This is the
  reference's `.q-more-wrap` / `.q-more-menu` made **width-aware** rather than a hand-set list.
* **Items that make no sense for the current page/selection are omitted**, not shown disabled.

**Placement vs. the header.** The page **title** (and any breadcrumb + the page's single **primary**
button such as **Pin now** / **Index files**, plus the single-entity `⋮` "more" menu when present) stays
in the [`PageHeader`](../code/packages/frontend/src/components/ui/PageHeader.tsx). The **action-links row is
its own row directly under that title block** — not in the header's right-aligned slot. The primary button
remains the page's one emphasised call-to-action in the header; the action-links row is the flat list of
everything else.

**Distinct from the `⋮` bulk menu.** The table's existing selection **`⋮` bulk menu** (set decision
Add to IPFS (pin)/Ignore/Undecided across checked rows, [one_repo.mdx §4.4](./one_repo.mdx)) stays where it is, in the
control row above the table, and only appears when rows are checked. The action-links row is the
page-level home and is always present. The two never duplicate an item.

The action-links row (always present on a list page, producing-item labels reflecting the checked count) is rendered in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/components/menu/PageActions.tsx` — `PageActions` component (rewritten from the old dropdown into the inline links row; fed a `scope` = a root path + the checked paths, and an action list), rendered beneath the title by `~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/components/ui/PageHeader.tsx` — `PageHeader` (its below-title `actions` slot).

### 3.1 One measured row, overflow into `More ▾` (LOCKED)

The action-links row is **exactly one line high** and **calculates what fits**. The user's rule: *"We only
have one row of action links, and then the overflow goes into the more. It calculates what fits and whatever
does not fit rolls into the more, and the more is on the right."* Concretely:

* **Measure, then split.** On layout and on every resize, the row measures its available width and walks the
  actions **left → right in catalog order**, keeping each action inline while it still fits. The **first
  action that would not fit — and every action after it — moves into the overflow.**
* **`More ▾` is pinned to the right.** When any action overflows, a trailing **`More ▾`** link (a down
  chevron `▾`) renders at the **right end** of the row and opens a small popover listing the overflowed
  actions **in the same catalog order**. The width reserved for `More ▾` is included in the fit math so it
  never itself causes a clipped last item.
* **No overflow → no `More ▾`.** If every action fits, the `More ▾` link is **not** rendered at all.
* **Order is preserved.** Overflow is purely a function of width, not importance: the actions keep their
  catalog order across the inline row and the `More ▾` popover, so an action never jumps position. (Producing
  actions — Create Transcriptions / Create AI descriptions — come first in the catalog, so they are the last
  to overflow.)
* **Selection-count labels are measured too.** When rows are checked and a producing label grows to
  `Create Transcriptions (12)`, the row re-measures, so a wider label can push a later action into `More ▾`.
* **Red-tinted / confirm-gated actions keep their treatment in the popover** — Compress / Git ignore look and
  behave identically whether inline or under `More ▾`.

Measuring the available width, splitting the actions into an inline set + an overflow set, and rendering the right-pinned `More ▾` popover only when the overflow set is non-empty are implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/components/menu/PageActions.tsx` — `PageActions` (a `ResizeObserver` on the row container drives a `useLayoutEffect` fit pass that computes `inline[]` / `overflow[]` from each item's measured width + the `·` separators + the reserved `More ▾` width; the popover renders only when `overflow.length > 0`).

---

## 4. The per-page action catalog (LOCKED)

Which links appear in the row is per-page. Every **file-list** page carries the two **producing** actions;
pages then add their own domain actions. Producing actions obey §1–§2; the others are the existing
page-level operations, now shown as inline links (they already run through the progress dock,
[webapp.mdx §11](./webapp.mdx)).

| Page (spec) | Action-links (left → right) | Primary button (header, outside the row) |
| --- | --- | --- |
| **One repo** ([one_repo.mdx](./one_repo.mdx)) | Create Transcriptions · Create AI descriptions · Compress videos · Compress images · Git ignore · Rescan | **Pin now** |
| **File System / a directory** ([file_system.mdx](./file_system.mdx), [directories.mdx](./directories.mdx)) | Create Transcriptions · Create AI descriptions · Show what could be transcribed · Compress videos · Compress images · Git ignore · Track / Pin this directory | — |
| **Full paths** (flat file table, [full_paths.mdx](./full_paths.mdx)) | Create Transcriptions · Create AI descriptions · Compress videos · Git ignore · IPFS pin · Unpin | — |
| **Storage detail** ([storages.mdx](./storages.mdx)) | Create Transcriptions · Create AI descriptions · Show what could be transcribed · Compress videos · Re-index files | **Index files** |
| **View one directory** ([directories.mdx](./directories.mdx)) | Create Transcriptions · Create AI descriptions · Compress all… (its files, recursive) | — |
| **Repos list** ([repos.mdx](./repos.mdx)) | Rescan all · Pin all | **+ Add repo** (whole-list actions; rows are repos, not files) |
| **IPFS pins** ([ipfs.mdx](./ipfs.mdx)) | Publish IPFS list… · Re-verify pins | — |

Non-list pages (settings, sign-in, IPFS dashboard, single-file viewers, etc.) do **not** carry the
page action-links row — their actions are page-specific controls documented in each page's own spec, and a
single media file uses the **entity `⋮` menu** ([menus.mdx §4](./menus.mdx)) and the Media-Viewer action
bar ([media_viewer.mdx](./media_viewer.mdx)), not this page row. (View-one-file's own header buttons —
[files.mdx §7.1](./files.mdx) — are that page's local controls, not this shared row.)

> **Producing actions are the LOCKED core of this spec.** **Create Transcriptions** and **Create AI
> descriptions** appear on **every file-list page** and behave identically everywhere (§1–§2). The other
> links are the page's pre-existing operations shown inline; their engines are specified in their own files
> ([compression.mdx §12](./compression.mdx), [scan.mdx](./scan.mdx), [ipfs_share_files.mdx](./ipfs_share_files.mdx)).

> **Compress links open the Compress Inside dialog (LOCKED).** The **Compress videos** / **Compress images**
> action-links do **not** run inline or fire a bare `window.confirm` — they open the **Compress Inside**
> pop-over dialog ([compression.mdx §12](./compression.mdx)) scoped to the page's directory, with the
> **matching kind pre-checked** (videos-only or images-only). They stay **red-tinted, confirm-gated offers**
> (§3, [menus.mdx §6.1](./menus.mdx)): the dialog *is* the confirm step, and its **Continue**
> background-queues the per-file-safe batch.

> **The Git ignore link opens the Git Ignore dialog (LOCKED).** The **Git ignore** action-link does **not**
> run inline or fire a bare confirm — it opens the **Git Ignore** pop-over dialog
> ([git_ignore.mdx](./git_ignore.mdx)) scoped to the page's **rightmost column that has a selection** (else
> the deepest open column's directory). The dialog adapts to the selection (one file / many files /
> directories / a mix), shows a **Recursive** checkbox when a directory is in the set, previews the exact
> anchored `.gitignore` lines, and its **Apply Git ignore ›** / **Recursive apply ›** primary button writes
> them into each owning repo's `.gitignore` (skip-already-ignored, multi-repo aware). It stays a **red-tinted,
> confirm-gated offer** (§3, [menus.mdx §6.1](./menus.mdx)): the dialog *is* the confirm step
> (charter: "Never add a `.gitignore` entry automatically").

The producing actions **open the batch popup** (§0.1, [dialogs.mdx §5–§6](./dialogs.mdx)) — they no longer
enqueue-then-toast on click. The click resolves the scope and opens the popup; the popup's Confirm enqueues
and toasts. Wired in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/lib/pageActions.ts` — `createTranscriptions()` / `createDescriptions()` (now call `openTranscribeBatch` / `openDescribeBatch` from `~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/lib/batchPopup.ts`, which fetch the `/plan` preview and open the popup whose Apply calls `api.transcribeEnqueue` / `api.describeEnqueue` and toasts `willProcess`).

### 4.1 "Show what could be transcribed" — a calc-engine action (LOCKED)

**Show what could be transcribed** is a page action on the **Storage detail** and **File System / directory**
pages (§4) — but it is a **calc-engine action, not a producing action**: clicking it **runs the
[What-Can-Be-Transcribed Calc Engine](./transcribe_calc_engine.mdx)** over the page's storage/directory,
which **lists** the transcribable-but-untranscribed media (a read-only scan) and writes a **transcribe
batch** ([to_do_batches.mdx §4](./to_do_batches.mdx#4-transcribe-batches--a-sibling-kind-locked)). It does
**not** itself transcribe anything.

* **Distinct from Create Transcriptions.** **Create Transcriptions** (§1) obeys Rules 1–3 and **queues the
  work now**; **Show what could be transcribed** only **surfaces** what *could* be transcribed. It is
  deliberately **opt-in and heavier** — opening every media file's sidecar to check for a transcript is more
  expensive than the default rollup, so it runs **only on this explicit click**, never as part of a scan
  ([transcribe_calc_engine.mdx §1](./transcribe_calc_engine.mdx#1-when-it-runs-locked)).
* **Scope like any page action (Rule 1).** With nothing checked it scans the whole storage/directory
  recursively; the link shows a brief **"Looking…"** state while the (cancelable) scan runs. It honours the
  same `HARD_SKIP` walk (§1.1).
* **What it produces.** One **transcribe batch** per storage with candidates → a **transcribe slug** on the
  [To Do page](./to_do.mdx#7-show-what-could-be-transcribed-locked); its chevron opens the **~80%-screen**,
  default-checked popup whose **"Transcribe N files"** Apply is the point where transcription is finally
  **enqueued as background jobs** ([job_queue.mdx §4b](./job_queue.mdx), [processing.mdx §4.6](./processing.mdx#46-applying-a-to-do-batch-is-a-processing-producer-locked)).
  So this link *finds* the work; the transcribe slug's Apply *does* it.
* **No skip-already-done toast.** Because it queues nothing, it shows no `willProcess` toast; its result is
  the transcribe slug(s) it produces (or a calm "nothing untranscribed here").

The link triggers the on-demand engine rather than the enqueue path in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/lib/pageActions.ts` — `showWhatCanBeTranscribed()` (calls `POST /api/todo/transcribe-scan?scope=<id>`, [transcribe_calc_engine.mdx §1](./transcribe_calc_engine.mdx#1-when-it-runs-locked)), served by `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/todo/todo.router.ts` — `POST /api/todo/transcribe-scan`.

---

## 5. The enqueue REST surface (plan-then-queue)

Each producing action maps to **one** allow-list-gated endpoint that does Rules 1+2 **on the server**
(so the "how many already have output" truth is computed where the files live) and hands the remainder to
the queue (Rule 3):

| Method + path | Body | What |
| --- | --- | --- |
| `POST /api/transcribe/plan` | `{ paths?: string[], root?: string }` | **Preview only, nothing queued** ([dialogs.mdx §5.2](./dialogs.mdx)). Applies Rules 1+2 on the server and returns the eligible **candidate file list** (`{ files: {path, sizeBytes}[], considered, alreadyDone, unsupported }`) so the batch popup (§0.1) can list them checked-by-default. |
| `POST /api/describe/plan` | `{ paths?: string[], root?: string, provider? }` | Same preview for AI descriptions. |
| `POST /api/transcribe/enqueue` | `{ paths?: string[], root?: string, overwrite?: boolean }` | Plan + queue transcriptions — called by the **popup's Confirm** with the **checked** `paths`. `root` = a directory/repo to walk recursively when nothing is checked. Returns the **plan**, not the results. |
| `POST /api/describe/enqueue` | `{ paths?: string[], root?: string, overwrite?: boolean, provider? }` | Same for AI descriptions. |

Both return the **plan** immediately (no waiting for the work):

```ts
export interface EnqueuePlan {
  considered: number;   // set size after Rule 1 (checked set, or the recursive walk)
  eligible: number;     // after Rule 2 — media that does NOT already have the output
  alreadyDone: number;  // dropped because the output already exists
  unsupported: number;  // dropped because not the right media kind
  queued: number;       // handed to the queue (== eligible unless overwrite re-queues done ones)
  willProcess: number;  // the number the toast shows (== queued)
}
```

* Exactly **one** of `paths` / `root` is used per call: a non-empty `paths` is the checked set (Rule 1a);
  otherwise `root` is walked recursively (Rule 1b). Sending neither is a `400`.
* `overwrite: true` (a future "Re-create all" variant) skips Rule 2, re-queuing files that already have the
  output; the default is `false` (skip-already-done).

The enqueue planners (walk-or-take the set, drop already-done + unsupported, push the remainder onto the queue, return the plan) are implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/transcribe/transcribe.service.ts` — `planTranscribe()` / `enqueueTranscribe()` · `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/describe/describe.service.ts` — `planDescribe()` / `enqueueDescribe()`; both push through `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/jobqueue/jobqueue.service.ts` — `enqueue()`.

The **plan/preview** endpoints (same Rules 1+2 walk, but return the candidate list instead of queuing — the batch popup's data source, [dialogs.mdx §5.2](./dialogs.mdx)) are implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/transcribe/transcribe.service.ts` — `previewTranscribe()` · `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/describe/describe.service.ts` — `previewDescribe()`; routes `POST /plan` on `transcribe.router.ts` / `describe.router.ts`; client methods `api.transcribePlan` / `api.describePlan`.

The routes are mounted on the existing feature routers:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/transcribe/transcribe.router.ts` — `POST /enqueue` · `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/describe/describe.router.ts` — `POST /enqueue`; API client methods `api.transcribeEnqueue` / `api.describeEnqueue` in `~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/api/client.ts`.

---

## 6. Acceptance criteria

1. Every **file-list** page (One repo, File System, a directory, Full paths, Storage detail, Repos list,
   IPFS pins) shows a single **horizontal action-links row directly beneath the page title** — a flex row
   of **blue text hyperlinks**, each with a **leading icon**, `gap: 14px`, thin `·` separators, wrapping
   when narrow. It is **always present** (not gated on a selection) and is **not** a dropdown.
2. **One measured row, `More ▾` overflow (§3.1).** The actions are **visible inline links** in a **single,
   non-wrapping row** that **measures what fits**; whatever does not fit collapses into a **right-pinned
   `More ▾`** menu — rendered **only** when something overflows — never a second row. Labels are the **short
   forms** (**Compress videos**, **Compress images**, **Git ignore**). No `Actions ▾` dropdown remains.
3. **Scope (Rule 1):** with rows checked, an action runs over **exactly** the checked set and its label
   shows the count, e.g. `Create Transcriptions (N)`; with nothing checked it runs over **all files on the
   page, recursively**.
4. **Skip-already-done (Rule 2):** **Create Transcriptions** / **Create AI descriptions** drop every file
   that already has the output (and every unsupported file) **before** queuing; only the remainder is
   queued.
5. **Background queue (Rule 3):** the eligible set is handed to the server-side queue
   ([job_queue.mdx](./job_queue.mdx)); the endpoint **returns immediately** with the plan, and the work
   drains in the background, each item surfacing a progress-dock card ([webapp.mdx §11](./webapp.mdx)).
6. **Notification (§2):** one toast, **"{N} files will have their Transcriptions created"** /
   **"… AI descriptions created"**, where **N is the eligible remainder** (the `willProcess` figure);
   nothing-eligible gives the honest "All {M} … already have … — nothing to do".
7. The action-links row is **distinct** from the row `⋮` kebab, the entity `⋮` more-menu, and the
   selection `⋮` bulk menu; it never duplicates their items, and it appears **only** on list pages (not on
   single-entity or settings pages), per the catalog (§4).
8. Compress/git-ignore/delete links remain **explicit-click offers with a confirm step**
   ([menus.mdx §6.1](./menus.mdx)) and are **tinted red**; no page action compresses, converts,
   git-ignores, or deletes without the user's explicit confirm in a small web modal.
9. The visual matches the sister-app reference (`~/BGit/all/marketing/ai/code/ui/Q/page_processing_queue.js`
   `.q-actions`): blue links, leading icon, `·` separators, hover underline, red tint for destructive.
10a. **Producing actions open the batch popup (§0.1):** clicking **Create Transcriptions** / **Create AI
    descriptions** opens the **same** unified batch-confirm popup the `Transcribable` / `Describable` metric
    tile opens ([dialogs.mdx §5–§6](./dialogs.mdx)) — candidates checked by default, uncheck, a solid-blue
    **"Transcribe/Describe N files"** confirm — **not** a fire-and-forget enqueue+toast and **never** a
    `window.confirm`. The enqueue and the LOCKED toast (§2) fire on the popup's Confirm; Rules 1–3 and the
    first-time-storage gate are unchanged (they run at Apply). The link fetches its candidate list from the
    plan/preview endpoint (§5).
11. **Show what could be transcribed (§4.1):** the **Storage detail** and **File System / directory** pages
    carry a **Show what could be transcribed** action-link that is a **calc-engine action, not a producing
    one** — it runs the [What-Can-Be-Transcribed Calc Engine](./transcribe_calc_engine.mdx) over the page's
    scope (opt-in, read-only, `HARD_SKIP`-walked, cancelable, no `willProcess` toast), writing a **transcribe
    batch** → a **transcribe slug**; transcription is enqueued only later, by that slug's Apply.

<!--
  HUB spec for the THIRD action surface: the page action-links ROW rendered directly UNDER the page title
  (whole-page/checked-subset), distinct from menus.mdx's row ⋮ kebab (one row) and entity ⋮ more-menu (one
  entity) and the selection ⋮ bulk menu (decision on checked rows).

  SECOND REVERSAL (§0.1, LOCKED): the two PRODUCING links (Create Transcriptions / Create AI descriptions)
  no longer enqueue-on-click+toast — they now OPEN THE UNIFIED BATCH-CONFIRM POPUP (dialogs.mdx §5-§6), the
  SAME WarningPopup the Transcribable/Describable metric tile opens (task_tabs §5.1). Only WHERE the enqueue
  fires changed (link click → popup Confirm); Rules 1-3 + the storage-setup gate are unchanged (they run at
  Apply). pageActions.ts createTranscriptions/createDescriptions call openTranscribeBatch/openDescribeBatch
  (lib/batchPopup.ts), which fetch the NEW /plan preview endpoint and open the popup. NEW endpoints POST
  /api/transcribe/plan + /api/describe/plan ({paths?|root?} → {files[],considered,alreadyDone,unsupported},
  Rules 1+2, NOTHING queued) = previewTranscribe()/previewDescribe(). Toast fires on Confirm.

  DESIGN REVERSAL (§0, LOCKED): this REPLACES the earlier "Actions ▾" DROPDOWN. Page actions are now VISIBLE
  inline blue hyperlinks with a leading icon, matching the sister-app processing-queue .q-actions row
  (~/BGit/all/marketing/ai/code/ui/Q/page_processing_queue.js — setupActions()/.q-actions CSS). Row sits
  UNDER the title (PageHeader below-title slot), NOT in the header's right slot.
  ONE MEASURED ROW (§3.1, LOCKED): the row is a SINGLE non-wrapping line (flex-wrap:nowrap) that MEASURES
  what fits (ResizeObserver + useLayoutEffect) and splits actions into inline[] + overflow[]; the overflow
  collapses into a RIGHT-PINNED "More ▾" popover rendered only when overflow.length>0 — never a second row.
  Labels are SHORT: "Compress videos" / "Compress images" / "Git ignore" (was "Compress all videos…" /
  "Git-ignore big files…"). Destructive links (compress/git-ignore/delete) tinted red + confirm modal.
  Compress links → compression.mdx §12 dialog; the Git ignore link → git_ignore.mdx dialog.

  THREE LOCKED behavior rules (unchanged by the reversal):
   (1) SCOPE — nothing checked => ALL files on the page RECURSIVELY; ≥1 checked => exactly the checked set
       (producing labels show the count, e.g. "Create Transcriptions (12)").
   (2) SKIP-ALREADY-DONE — producing actions (Create Transcriptions / Create AI descriptions) drop files
       that already have the output (and unsupported kinds) BEFORE queuing. Idempotent-by-existence.
   (3) BACKGROUND QUEUE — hand the eligible set to job_queue.mdx; endpoint RETURNS IMMEDIATELY with the
       plan; queue drains in background; each item = a progress-dock card (kind transcribe/describe).
       Every "do this to many files" producer (Create Transcriptions / Create AI descriptions / Compress all
       videos… / images…) feeds the ONE core-budgeted, media-aware parallel drain (job_queue.mdx §3,
       parallelization.mdx): mass-compute budget = round(cores × max_core_fraction), default 0.9 → bulk page
       actions use ≈90% of cores (image=wide/1thr, video+transcribe=narrow/capped, describe=network), NOT a
       fixed tiny cap.
  §4.1 SHOW WHAT COULD BE TRANSCRIBED (LOCKED) — a CALC-ENGINE action (NOT a producing action) on the Storage
  detail + File System/directory pages. Runs the What-Can-Be-Transcribed Calc Engine (transcribe_calc_engine.mdx)
  over the page scope: opt-in/heavier (never a scan), read-only, HARD_SKIP-walked, cancelable, "Looking…" state,
  NO willProcess toast. Writes a transcribe batch → a transcribe SLUG (to_do.mdx §7); transcription is enqueued
  ONLY later by that slug's "Transcribe N files" Apply (job_queue.mdx §4b, processing.mdx §4.6). Wired via
  pageActions.ts showWhatCanBeTranscribed() → POST /api/todo/transcribe-scan.

  Notification (LOCKED wording): "{N} files will have their Transcriptions created" / "… AI descriptions
  created", N = eligible remainder (willProcess). Nothing eligible => "All {M} files already have … —
  nothing to do". Enqueue endpoints POST /api/transcribe/enqueue + /api/describe/enqueue take
  {paths?|root?, overwrite?} and return EnqueuePlan {considered, eligible, alreadyDone, unsupported,
  queued, willProcess}. Per-page catalog in §4 (producing actions on EVERY file-list page).

  Code: components/menu/PageActions.tsx (the action-links ROW — rewrite from dropdown), components/ui/
  PageHeader.tsx (renders the row beneath the title), lib/pageActions.ts (createTranscriptions/
  createDescriptions), transcribe.service planTranscribe/enqueueTranscribe, describe.service planDescribe/
  enqueueDescribe, jobqueue.service enqueue(), transcribe.router/describe.router POST /enqueue,
  api.transcribeEnqueue/describeEnqueue. Keep consistent with file_system.mdx, job_queue.mdx (§3 Core Budget),
  parallelization.mdx (the ≈90%-of-cores media-aware drain the bulk producers feed), Transcribe.mdx,
  ai_description.mdx, menus.mdx (the other two surfaces), webapp.mdx §11–§14 (dock + concurrency), and the
  five per-page cross-refs, which have now been reworded from "Actions ▾ menu" to "action-links row":
  directories.mdx §1.2 + §7.1, full_paths.mdx §3.6, repos.mdx §6.1, storages.mdx §7.1.
-->

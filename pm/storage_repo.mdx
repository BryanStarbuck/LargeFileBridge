---
title: LargeFileBridge — Repo Storage
description: The Repo storage spec. A repo storage is any Git repository on the machine that LFBridge tracks large files for. Its root is the repo root (e.g. ~/BGit/xyz/), and its tracking lives in a hidden .lfbridge/ folder in that same root (~/BGit/xyz/.lfbridge/), git-ignored, holding a storage.yaml descriptor (name + type: repo + repo_root), a per-file fingerprint index (files.yaml), the large-file → IPFS CID manifest, and per-file media-analysis outputs (transcript / description / visuals-by-time). This is the canonical directory-based storage: git carries the small files + a committed manifest, IPFS carries the big git-ignored bytes across the user's computers, and the repo may also clone to Google Drive / Dropbox. Sibling of storage_personal / storage_company / storage_community; shares the storage.yaml schema and .lfbridge/ layout defined in storages.mdx. Links to repos.mdx / one_repo.mdx (the repos list + per-repo screen) which are the UI over this storage.
---

# LargeFileBridge — Repo Storage

A **repo storage** is the most common kind: **any Git repository on the machine** that LFBridge tracks
large files for. It is the **canonical directory-based storage** — the other three (personal / company /
community) follow the same shape.

Read the shared model first: the storage family, the `storage.yaml` schema, the `.lfbridge/` tracking
layout, cloud clones, and media analysis are all defined once in **[storages.mdx](./storages.mdx)**. This
spec covers only what is **specific to a repo storage**; it does not restate the shared parts.

Siblings this builds on and does not repeat:

* [storages.mdx](./storages.mdx) — the umbrella: `storage.yaml` (§3), `.lfbridge/` (§4), clones (§5),
  media analysis (§6).
* [Repos](./repos.mdx) / [One repo](./one_repo.mdx) — the **UI over a repo storage**: the repos list and
  the per-repo screen where the user makes the **Add to IPFS (pin)** / Ignore / Undecided decision
  (stored as the `sync` decision value) per large file.
* [storage_local.mdx](./storage_local.mdx) — the local index/manifest model and the IPFS pin model this
  reuses; **Local Storage stays settings/config**, the authoritative per-repo tracking is here.
* [repo_tracking_scheme.mdx](./repo_tracking_scheme.mdx) — the **on-disk scheme** of three files this
  spec's `.lfbridge/` now includes: **`repo_storage.yaml`** (repo-wide settings + special-file counts,
  single `repo_storage:` key), the per-file **`files/<path>.yaml` sidecars** (identity + append-only event
  history + perceptual fingerprint), and the per-computer **`history/<device>.txt`** audit log. §5 there
  draws the ownership boundary between these (working, local) and `manifest.yaml`/`decisions.yaml` (committed,
  travelling).
* [special_files.mdx](./special_files.mdx) — the classifier (large · already-pinned · video/image/audio,
  plus compressible/transcribable) that decides which files earn a sidecar and fill `repo_storage.yaml`'s
  counts.
* [use_case_two_computers.mdx](./use_case_two_computers.mdx) — the month-by-month tour that walks a repo
  storage across two computers, exercising every write above.

---

## 1. Anatomy of a repo storage

```
~/BGit/xyz/                 ← the repo root = the storage root ("XYZ")
├── .git/                   ← Git (small files + the committed manifest travel here)
├── .gitignore             ← lists .lfbridge/ AND the large files (they pin via IPFS, not Git)
├── .lfbridge/              ← hidden per-storage tracking area (storages.mdx §4) — git-ignored
│   ├── storage.yaml        ← descriptor: name + type: repo + repo_root (storages.mdx §3)
│   ├── repo_storage.yaml   ← repo-WIDE settings + special-file counts (single key: repo_storage) — repo_tracking_scheme.mdx §2
│   ├── files.yaml          ← per-file fingerprint index (storages.mdx §4.1)
│   ├── files/<path>.yaml   ← per-file <name>.yaml sidecars: identity + append-only event history — repo_tracking_scheme.mdx §3
│   ├── history/<device>.txt ← one UTC-timestamped audit log PER COMPUTER — repo_tracking_scheme.mdx §4
│   ├── manifest.yaml       ← large-file → IPFS CID + pin state (storage_local.mdx §9) — committed, travels
│   ├── decisions.yaml      ← the team-SHARED per-file decision ledger (decisions.mdx) — committed, travels
│   ├── <rel-dir>/<name.ext>.transcription   ← transcript — path-mirrored, ext APPENDED (storages.mdx §6)
│   ├── <rel-dir>/<name.ext>.ai_description  ← AI description — path-mirrored, ext APPENDED (storages.mdx §6)
│   └── analysis/           ← visuals-by-time + compression record only (storages.mdx §6)
└── …the repo's tracked + git-ignored files…
```

* The **storage root is the repo root** — there is no separate directory; a repo *is* the storage.
* The tracking area is **`~/BGit/xyz/.lfbridge/`** — inside the repo, hidden, and **git-ignored** so it
  never bloats commits. (Contrast: the small **committed** manifest of [storage_local.mdx §9.2](./storage_local.mdx)
  still lives in-repo and travels via Git; `.lfbridge/` is the *local* working tracking area.)
* `storage.yaml` here is minimal — see §2.

---

## 2. `storage.yaml` for a repo

```yaml
# ~/BGit/xyz/.lfbridge/storage.yaml
name: "XYZ"                 # defaults to the repo folder name; user-editable
type: repo
created: 2026-05-01T10:12:00Z
repo:
  repo_root: "~/BGit/xyz"   # the Git repo this storage tracks
clones:
  google_drive: null        # or "My Drive/LFB/XYZ" once mirrored (storages.mdx §5)
  dropbox: null
```

* **`name`** defaults to the repo folder name (`xyz` → `XYZ`) and is user-editable in
  [Repo settings](./repo_settings.mdx).
* **`type: repo`** with a **`repo`** block naming the `repo_root`. A repo storage never carries a
  `company`/`community`/`personal` block.

Placing `storage.yaml` (and `.lfbridge/`) at the repo root when a repo is registered, and adding `.lfbridge/` to the repo `.gitignore`, is to be implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/storage/storage.service.ts` — `ensureRepoStorage(repoRoot)`.

---

## 3. What "tracking a repo" means here

A repo storage is the on-disk backing for everything the [One repo](./one_repo.mdx) screen shows:

* **Discovery** — the scanner finds every Git repo under the configured roots
  ([storage_local.mdx §7](./storage_local.mdx), [scan.mdx](./scan.mdx)); each becomes a repo storage.
* **Per-file decision** — the **two-axis** decision (Add to IPFS *and/or* Add to git ignore) per large
  file, recorded in the **team-shared** `.lfbridge/decisions.yaml` ledger with who/when/SID
  ([decisions.mdx](./decisions.mdx)); its IPFS axis projects onto the frozen `sync`/`ignore`/`undecided`
  enum ([one_repo.mdx §1](./one_repo.mdx#1-the-decision-model-locked)). Because the ledger is committed and
  union-merged, the whole team shares one set of decisions and no teammate is re-asked about a file another
  already triaged. The machine-local `pin/r/<repo>/config.yaml → decisions:` map is now a reconciled cache
  of that ledger's IPFS axis, not the source of truth.
* **Per-file fingerprints** — `.lfbridge/files.yaml` records hash + size + created + modified, and the
  compressed/uncompressed variant hashes for media ([storages.mdx §4.1](./storages.mdx)), so LFB knows what
  changed since last pin.
* **IPFS bytes** — files marked **Add to IPFS (pin)** (the `sync` decision value) are pinned and moved between the user's computers over IPFS
  ([storage_local.mdx §9](./storage_local.mdx)); the small committed manifest travels via Git so a fresh
  clone knows what to fetch.

The repos list and per-repo UI over this storage are rendered in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/OneRepoPage.tsx` — `OneRepoPage` (served by `repos.router.ts`).

---

## 4. Media analysis + cloud clones (per repo)

* A repo storage can produce the [storages.mdx §6](./storages.mdx) **media-analysis** outputs for its
  video/image files: the **transcript** (`.lfbridge/<rel-dir>/<name.ext>.transcription`) and
  **description** (`.lfbridge/<rel-dir>/<name.ext>.ai_description`) live **inside the repo's `.lfbridge/`,
  path-mirrored, with the media's full filename and the extension APPENDED**, while
  **visuals-and-action-by-time** (`visuals_by_time.yaml`, time × visual × story) stays under
  `~/BGit/xyz/.lfbridge/analysis/<relpath>/`. They are artifacts that ride along with the storage —
  because `.lfbridge/` is **committed and travels with the repo**, the transcript/description are committed
  and synced by git (no `*.transcription` / `*.ai_description` git-ignore nudge — retired), while the
  multi-GB media stays git-ignored and rides IPFS.
* A repo storage **may also clone** to Google Drive / Dropbox ([storages.mdx §5](./storages.mdx)) — the
  same repo hierarchy mirrored to a cloud drive as a second, human-reachable copy — recorded in the
  `clones` block.

---

## 5. Where it shows in the UI

* In the **[Storages](./storages.mdx#2-the-storages-left-bar-tab)** left-bar tab, repos are represented by
  the single **Repos** child, which routes to the dedicated **[Repos](./repos.mdx)** tab (the Storages tab
  does not re-list every repo).
* The **[Repos](./repos.mdx)** tab is the long list; each row opens its **[One repo](./one_repo.mdx)**
  screen — the working UI over one repo storage.

---

## 6. TO DO Batch for this storage (LOCKED)

A repo storage participates in the product-wide **TO DO Batch** system
([to_do_batches.mdx](./to_do_batches.mdx)). A **TO DO Batch is a per-storage bundle of recommended file
actions** — there is exactly **one** batch per repo, persisted as hierarchical YAML at
**`~/T/_large_files_bridge/_do_batches/repo_<repo-slug>_2_do.yaml`** (the `<repo-slug>` derived from the
repo root). Like every batch it is **local, disposable, and uncommitted** — it lives only in the
machine-local state root, is **never** written into the repo's `.lfbridge/`, and is **never** team-shared.
(Contrast the **committed, travelling** `decisions.yaml` ledger of §1: the batch *reads* that ledger; it is
not part of it.)

* **This is the richest batch — all five categories apply.** A repo is the **only** storage where
  **`git_ignore`** ("big files that aren't a good idea to check in") is meaningful, so a repo batch can
  carry **`compress_video`**, **`compress_image`**, **`git_ignore`**, **`pin`**, and **`pull_down`** — the
  full set — plus the transcription discovery below.
* **Produced from the repo's classified files + the folded decisions ledger.** The calc engine
  ([to_do_batch_calc_engine.mdx](./to_do_batch_calc_engine.mdx)) builds the batch from the repo's
  **classified special files** ([special_files.mdx](./special_files.mdx)) and then **folds in the
  team-shared decisions ledger** ([decisions.mdx](./decisions.mdx)): it **recommends only** — never writes a
  decision, never moves a byte — and **skips any file/axis the team already decided**, so a teammate's
  earlier triage is never re-asked.
* **Recalculate-and-replace each scan.** The batch is **recomputed from scratch and replaced** on every
  scan pass; it holds no durable state of its own.
* **Same model, two surfaces (see [to_do.mdx §8](./to_do.mdx#8)).** This same batch is what the per-repo
  screen surfaces as its **yellow warning banner** ([warnings.mdx](./warnings.mdx)) *and* what the To-Do
  page renders as a **slug**. The repo page and the To-Do page are two views of one underlying batch, not
  two computations. Opening the slug's chevron shows the recommendations as two-axis **decision toggles**
  pre-set to the recommended state ([decision_toggles.mdx](./decision_toggles.mdx)), which the user confirms
  and **Applies**.
* **Transcription discovery for the repo.** The on-demand **"Show what could be transcribed"** link runs
  the **What Can Be Transcribed Calc Engine** ([transcribe_calc_engine.mdx](./transcribe_calc_engine.mdx)),
  which walks this repo for **video/audio that has no `.transcription` artifact under `.lfbridge/`** and emits a per-repo
  **transcribe batch** → a **transcribe slug** (a ~80%-width, default-checked popup: *"Transcribe N
  files"* → background jobs on [processing.mdx](./processing.mdx)).

Producing the repo batch during scan recalc, and naming/replacing its YAML, is handled in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/todo/todo-batch.engine.ts` —
`TodoBatchCalcEngine.recalcForStorage(storage)` writing via
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/todo/todo-batches.store.ts` —
`batchFileName(storage)` (→ `repo_<repo-slug>_2_do.yaml`); the transcribe discovery walk is
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/todo/transcribe-scan.engine.ts`.

---

## 7. Acceptance criteria

1. Any Git repo LFB tracks is a **repo storage** whose **root is the repo root** (`~/BGit/xyz/`) and whose
   tracking lives in a hidden, **git-ignored** **`~/BGit/xyz/.lfbridge/`**.
2. Its `.lfbridge/storage.yaml` has **`name`** (default = repo folder name), **`type: repo`**, and a
   **`repo`** block with `repo_root`; no other type block.
3. `.lfbridge/` holds the per-file fingerprint index (hash + size + dates; compressed/uncompressed variant
   hashes for media), the IPFS manifest, and the `analysis/` outputs — matching [storages.mdx §4](./storages.mdx).
4. Files marked **Add to IPFS (pin)** (the `sync` decision value) move between the user's computers over **IPFS**; the small committed manifest travels via
   **Git**; the repo may also mirror to **Google Drive / Dropbox** per its `clones` block.
5. The repo storage is surfaced through the **Repos** tab (long list + per-repo screen) and the **Repos**
   child of the Storages tab; it is **not** individually listed inside the Storages tab.
6. Each repo owns exactly **one TO DO Batch**, persisted local/disposable/uncommitted at
   `~/T/_large_files_bridge/_do_batches/repo_<repo-slug>_2_do.yaml`, recalculated-and-replaced each scan
   from the repo's classified files folded with the team-shared decisions ledger; it is the **richest
   batch** — **all five** categories apply (**`compress_video`, `compress_image`, `git_ignore`, `pin`,
   `pull_down`**) plus the on-demand transcribe batch; the **same** batch is the repo's yellow warning
   banner and the To-Do page slug (§6).

<!--
  Repo storage = any Git repo. Storage root = repo root (~/BGit/xyz/); tracking = ~/BGit/xyz/.lfbridge/
  (git-ignored): storage.yaml (name + type: repo + repo_root), files.yaml fingerprints, manifest.yaml,
  analysis/. Reuses the shared model from storages.mdx (don't restate it). UI = repos.mdx + one_repo.mdx.
  The Storages tab represents all repos with ONE "Repos" child that links to the Repos tab.
  §6 TO DO Batch: each repo owns ONE local/disposable/uncommitted batch repo_<slug>_2_do.yaml under
  ~/T/_large_files_bridge/_do_batches/, recalc-and-replace each scan from classified special files +
  folded team decisions ledger (to_do_batch_calc_engine.mdx, recommends-only, skips folded decisions).
  RICHEST batch — ALL FIVE categories apply incl. git_ignore. Same batch = the repo's yellow warning
  banner (warnings.mdx) AND the To-Do page slug (to_do.mdx §8, decision_toggles.mdx). Transcribe discovery
  via transcribe_calc_engine.mdx → transcribe slug. Code: todo-batch.engine.ts recalcForStorage,
  todo-batches.store.ts batchFileName, transcribe-scan.engine.ts.
-->

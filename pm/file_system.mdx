---
title: LargeFileBridge — File System (the left-bar hub + interesting-directory folder coloring)
description: The hub spec for the "File System" left-bar item (route /fs) — the page the user lands on when they click File System in the left nav. It owns three things that are NOT owned elsewhere: (1) INTERESTING-DIRECTORY FOLDER COLORING — a folder icon is tinted to make directories that contain something worth acting on stand out. A directory is "interesting" if it, or anything under it recursively, contains a big file (≥ the big-file threshold, default 100 MB), a video, or an uncompressed image ≥ 3 MB. The highest-priority thing present picks the color: BIG FILE → brown (light-brown fill, dark-brown outline) is the most powerful; else VIDEO → blue (medium-dark-blue outline, very-light-blue near-white fill); else UNCOMPRESSED IMAGE ≥3MB → blue outline only, no fill (the least powerful). A directory with none of these keeps the plain uncolored folder glyph. This coloring is applied to EVERY folder glyph across the app — the /fs column browser, View-one-directory child lists, storage directory rows, and repo rows — not just this page. (2) CLOUD-STORAGE ROOTS SURFACED AT THE TOP OF THE HOME COLUMN (§4, macOS) — Dropbox, each Google Drive account (GoogleDrive-<email>), and iCloud Drive live under ~/Library/CloudStorage/, not directly in ~, so the home column prepends one ☁ row per detected mount, sorted to the top, "as if they were in ~," letting the user browse and compress the big files inside them without drilling Library → CloudStorage. Each row is a REAL directory row (path = the mount) so badges, interest tint, and compress/git-ignore/track actions work unchanged; it is marked FsEntry.cloud only for the cloud glyph and top-of-column sort. This is a pure BROWSING feature, wholly independent of the Dropbox/Google-Drive Sync-List BACKBONES (dropbox.mdx/google_drive.mdx) — surfacing a mount never enables a backbone, writes a Sync List, or moves bytes. A legacy ~/Dropbox→CloudStorage/Dropbox symlink is de-duplicated to one cloud row; discovery is stat-only and macOS-guarded. (3) THE ACTION-LINKS ROW under the page title (delegates behavior to page_actions.mdx). The File System page's browsing mechanics (Finder-style column browser, code badges R/r·P·C/c·i, size/media filters, persisted view state) are owned by directories.mdx Part A, and the flat Full-paths tab (/fs/paths) by full_paths.mdx — this file references them and does not restate them. Sibling of directories.mdx, full_paths.mdx, page_actions.mdx, left_bar.mdx, repos.mdx, storages.mdx, compression.mdx, settings.mdx (the threshold), and codecs.mdx (what "uncompressed" means).
---

# LargeFileBridge — File System (the left-bar hub + interesting-directory folder coloring)

This is the page the user lands on when they click **File System** in the left nav
([left_bar.yaml](./left_bar.yaml) → `id: file_system`, `icon: Columns3`, `route: /fs`). It is the hub for
browsing the whole filesystem and finding the large files worth acting on.

The **browsing mechanics** are already specified and are **not** restated here:

* [directories.mdx](./directories.mdx) **Part A** — the Finder-style **column browser** at `/fs`: the
  Miller columns, the (optional) folder/file glyph, the code badges (R/r repo · P pin · C/c compress · i
  IPFS), the size/media header filters (Only Large · Videos / Images / Audio), and the persisted view state.
* [full_paths.mdx](./full_paths.mdx) — the **Full paths** tab at `/fs/paths`: the flat recursive TanStack
  table of large files under the current root. A tab strip switches `/fs` ↔ `/fs/paths`.

This file owns the three things those do **not**:

1. **§1–§3 — Interesting-directory folder coloring** (tinting the folder glyph by what's worth acting on).
2. **§4 — Cloud-storage roots surfaced at the top of the home column** — Dropbox, Google Drive, and
   iCloud lifted out of `~/Library/CloudStorage/` to the top level of the browser, "as if they were in
   `~`," so the user can browse and compress the huge files inside them without drilling down.
3. **§5 — The action-links row** under the page title (behavior delegated to
   [page_actions.mdx](./page_actions.mdx)).

Siblings: [directories.mdx](./directories.mdx) · [full_paths.mdx](./full_paths.mdx) ·
[page_actions.mdx](./page_actions.mdx) · [left_bar.mdx](./left_bar.mdx) · [repos.mdx](./repos.mdx) ·
[storages.mdx](./storages.mdx) · [compression.mdx](./compression.mdx) · [codecs.mdx](./codecs.mdx) ·
[settings.mdx](./settings.mdx) (the big-file threshold) · [dropbox.mdx](./dropbox.mdx) /
[google_drive.mdx](./google_drive.mdx) (the vendor-specific discovery for the surfaced roots, §4).

---

## 1. Interesting directories — the idea (LOCKED)

When a user opens the File System, most folders are noise — config, source, dot-directories. The few that
matter are the ones holding **big files, videos, or uncompressed images** — the files LFBridge exists to
pin and to help compress. So we **tint the folder icon** of those directories to make them stand out at a
glance, and we tint them **more strongly** the more important their contents.

A directory is **interesting** if — considering the directory itself **and everything under it,
recursively** — **any** of the following is true:

1. It contains at least one **video** file.
2. It contains at least one **uncompressed image** file that is **≥ 3 MB**.
3. It contains at least one file **at or over the big-file threshold** (the global
   `big_file.threshold_bytes`, default **100 MB** — [settings.mdx §1](./settings.mdx); a per-repo override
   may raise/lower it, [repo_settings.mdx](./repo_settings.mdx)).
4. It contains at least one **sub-directory (recursively) that is itself interesting** — i.e. interest
   **bubbles up**: a parent of an interesting folder is interesting.

Criterion 4 is automatic once interest is computed recursively: a directory is interesting iff **its whole
subtree** contains any of 1–3. Only **one** of the criteria has to hold — a folder needs just one video,
or just one big file, to qualify.

> **Why 3 MB for images and not the compress floor.** The compress-eligibility floor for images is 1 MiB
> ([compression.mdx](./compression.mdx)); interest uses a **higher 3 MB floor** so a folder of small
> uncompressed PNG icons does **not** light up. Videos have **no** size floor for interest — any video
> makes a folder interesting (a small already-compressed clip still counts). Big files use the threshold as-is.

---

## 2. The three colors of interesting (LOCKED)

A folder is **one** color, chosen by the **highest-priority** category present **anywhere in its subtree**.
Priority, strongest first: **big file → video → uncompressed image**.

| Level | Condition (anywhere in the subtree) | Folder glyph — outline | Folder glyph — fill | Meaning |
| --- | --- | --- | --- | --- |
| **1. Brown (strongest)** | ≥ 1 file **≥ big-file threshold** (default 100 MB) | **dark brown** | **light brown** | The important one — real large files live in here (incl. deep). |
| **2. Blue fill (medium)** | no big file, but ≥ 1 **video** | **medium-dark blue** | **very light blue** (near white) | Video content worth pinning/compressing, nothing over the threshold yet. |
| **3. Blue outline only (weakest)** | no big file and no video, but ≥ 1 **uncompressed image ≥ 3 MB** | **blue** | **none** (transparent) | Some heavy uncompressed images; the gentlest nudge. |
| **— Not interesting** | none of the above | default slate (unchanged) | none | Plain folder — no tint. |

Rules that make this unambiguous (all LOCKED):

* **Highest wins.** A folder that contains both a 4 GB `.mov` **and** loose PNGs is **brown**, not blue —
  the big file dominates. A folder with a small compressed `.mp4` and a 5 MB `.png` (no big file) is **blue
  fill** (video beats image). The big-file (brown) level explicitly requires a file **≥ threshold**; having
  only videos or only images — however many — is **not** enough for brown.
* **Recursive.** The color reflects the **whole subtree**, so a top folder shows **brown** when the only
  big files are several levels deep. The user sees the important branch from the root.
* **The base "interesting" look is the blue-fill treatment** (medium-dark-blue outline + very-light-blue
  fill) the user described — brown is the escalation for big files, blue-outline-only is the de-escalation
  for images-only.
* **Not interesting = no change.** A directory with none of 1–3 keeps today's plain folder glyph
  (`text-slate-500`, no fill). We do **not** color every folder — only the ones worth acting on.

The exact color tokens (added to the `:root` block in `styles.css`; the brown reuses the existing repo
badge ramp so the palette stays coherent):

```css
/* Interesting-directory folder glyph — outline (stroke) + fill */
--lfb-fold-big-line:    #5b3a1a;  /* dark brown  (= --lfb-badge-repo-root) */
--lfb-fold-big-fill:    #e7d3b1;  /* light brown */
--lfb-fold-video-line:  #2f6fed;  /* medium-dark blue (= --lfb-badge-ipfs) */
--lfb-fold-video-fill:  #eef4ff;  /* very light blue, near white */
--lfb-fold-image-line:  #2f6fed;  /* blue */
--lfb-fold-image-fill:  transparent; /* outline only, no fill */
```

The folder glyph is a single `lucide-react` `<Folder>`; **outline = its `color`/stroke, fill = its `fill`**.
So brown = `{ color: var(--lfb-fold-big-line), fill: var(--lfb-fold-big-fill) }`, video = the blue pair,
image = `{ color: var(--lfb-fold-image-line), fill: transparent }`, not-interesting = the current
`text-slate-500` with no fill.

---

## 3. Where the coloring applies + how it's computed

### 3.1 Everywhere a folder glyph is drawn (LOCKED)

This is **not** a File-System-page-only flourish. The user's rule: *"We'll do that pattern anywhere in the
file system where we show directories or even repos also."* So the interesting-folder coloring is a
**shared helper** applied to **every folder glyph across the app**:

* the **File System column browser** rows (`/fs`) — the primary place;
* **View one directory** child listings ([directories.mdx](./directories.mdx) Part B);
* **Storage detail** directory rows ([storages.mdx](./storages.mdx));
* **repo rows** where a repo is drawn as a folder — a repo is colored by the **same rule over its working
  tree** (a repo full of movies shows **brown**), layered under its existing R/r repo badge, so repos and
  plain folders read the same way.

The Full-paths tab (`/fs/paths`, [full_paths.mdx](./full_paths.mdx)) lists **files, never directory rows**,
so it has no folder glyph to color — it is unaffected.

### 3.2 The interest signal (LOCKED shape)

Each directory entry the UI renders carries an **`interest`** value:

```ts
type FolderInterest = "big" | "video" | "image" | null;
// "big"   → brown         (≥1 file ≥ threshold in the subtree)
// "video" → blue fill      (no big file, but ≥1 video)
// "image" → blue outline   (no big file/video, but ≥1 uncompressed image ≥ 3 MB)
// null    → not interesting (plain glyph)
```

It is computed by the **same bounded recursive walk** the charter rollup already does — extend
`buildDirRollup()` (which today emits `videosToCompress` / `imagesToCompress` / `bigNotIgnored` /
`bigIgnoredNotTracked`) to also emit `interest`, or compute it from those plus a broadened "any video /
any image ≥3MB" pass. The detection reuses the existing classifiers so the vocabulary never drifts:

* **video** = extension in `VIDEO_EXT` (`fs/badges.ts`) — for interest, **any** video counts (do not
  require `compressState === "should"`).
* **uncompressed image ≥ 3 MB** = extension in `IMAGE_UNCOMPRESSED_EXT` (`fs/badges.ts`) **and**
  `size >= IMAGE_INTEREST_FLOOR_BYTES` (3 MB = `3 * 1024 * 1024`).
* **big file** = `size >= getAppConfig().big_file.threshold_bytes` (default 100 MB; per-repo override
  honored), matching the size filter in [directories.mdx §1.4](./directories.mdx).

Determining each **column-browser** directory row's interest level (the primary place — the `/fs` tree) is computed in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/fs/badges.ts` — `computeDirInterest()` (the bounded subtree walk, mtime+TTL cache, early-exit on the first big file, and the truncation floor of §3.3), called per directory child by `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/fs/fs.service.ts` — `listDir()` (which shares **one** `interestBudget` across every child in the column so a huge sibling can't pin the thread).
The **entity-detail / View-one-directory** rollup computes the same value as a by-product of its category walk in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/entity/entity.service.ts` — `buildDirRollup()` (emits `interest`) · both paths share the classifiers `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/fs/badges.ts` — `compressInfo()` / `VIDEO_EXT` / `IMAGE_UNCOMPRESSED_EXT`. The value rides on each directory entry in the shared type `~/BGit/Bryan_git/LargeFileBridge/code/packages/shared/src/types.ts` — `FsEntry` (`interest?: FolderInterest`).

### 3.3 Performance — computed on the walk, cached, never blocking (LOCKED)

Interest is a **recursive** property, so a naive per-row recompute is expensive on a deep tree. Follow the
model already in [performance.mdx](./performance.mdx):

* Reuse the **bounded** walk (depth ≤ 8, `HARD_SKIP` dirs excluded). Interest is a by-product of that same
  pass — no extra traversal.
* **Fair per-child budget, not one greedy shared pool (LOCKED).** When a column lists its children, each
  directory child gets its **own** budget capped at **`INTEREST_PER_CHILD` (3,000 entries)**, drawn from a
  larger per-listing ceiling **`INTEREST_TOTAL` (120,000)**; only what a child actually consumes is drawn
  down, so cheap dirs leave budget for the rest. This is the fix for the **`_Mirror` starvation bug**: the
  old design shared **one** 40k budget consumed first-come, so huge early siblings (Dropbox/Library) drained
  it to **0 before `_Mirror` was even walked** — `_Mirror` got `undefined` → a **plain glyph** — and because
  `undefined` is never cached, every refetch re-drained the pool and `_Mirror` stayed plain **forever**. A
  per-child cap makes the walk fair and position-independent: every child, `_Mirror` included, gets its own
  slice and resolves (`_Mirror` finds its big file in ~35 entries).
* **The walk must never block the event loop (async policy, LOCKED).** `computeDirInterest` is a synchronous
  stat loop; a single heavy child can do thousands of stats. The listing loop counts those consumed entries
  toward its yield budget and hands the event loop back (`setImmediate`) so a cold multi-second walk stays
  **non-blocking** — concurrent requests interleave instead of hanging.
* **Cache** the result by directory + mtime/`scannedAt` (20 s TTL); serve the cached `interest` instantly.
* The column browser **renders immediately** with the plain glyph and **upgrades** the tint when the
  interest value arrives — the tree never blocks on the walk.

**Truncation returns the best-known FLOOR, it never discards a positive hit (LOCKED).** When the bounded
walk hits the budget cap, we do **not** throw away what the walk already saw. The rule is a **floor**:

* If the walk **already found a video** (or an uncompressed image ≥ 3 MB) before the cap, the directory is
  **definitely at least that** — return **`"video"`** (or `"image"`). It shows **blue**. The only thing
  truncation can hide is a **deeper big file** that would have **upgraded** the color to brown — so a
  truncated floor is always **safe** (it under-states at worst, never a false "not interesting"). Leave a
  truncated floor **uncached** so a later full-budget walk can still upgrade it to `"big"`.
* Only when the walk hits the cap **having found nothing yet** is the result **unknown** (`undefined` →
  plain glyph) — a big file could still be deeper, so we don't claim "not interesting".
* A **fully drained** walk that found nothing is a definite **`null`** (not interesting), which is cached.

So a huge directory like **`~/_Mirror/`** (≈20 k entries, a ≥100 MB file and videos several levels deep)
shows **brown** (it finds its big file in ~35 of its 3,000-entry slice); before both fixes it showed a
plain glyph. Even a `_Mirror` with only videos (no big file) is guaranteed **at least blue** by the floor
rule the moment a video is seen.

Sharing the per-listing budget as a per-child cap plus a total ceiling (so no single sibling can starve the rest), and yielding the event loop as heavy children consume it, is implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/fs/fs.service.ts` — `listDirectory()` (`INTEREST_PER_CHILD` / `INTEREST_TOTAL`; per-child `childBudget`, `interestPool` draw-down, and `sinceYield += consumed`).

Returning the best-known floor on budget exhaustion (yield the already-found video/image rather than discarding it, and leave that truncated floor uncached so a later full walk can upgrade it to `"big"`) is enforced in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/fs/badges.ts` — `computeDirInterest()` (the `truncatedFloor()` helper at each `budget.left <= 0` return) · mirrored for the entity-detail rollup in `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/entity/entity.service.ts` — `buildDirRollup()` (the `interestVideo`/`interestImage` branches taken before the `walkTruncated → undefined` branch).

The filesystem discovery scan that keeps the big-file / candidate data fresh runs **out-of-process and cooperatively async** (it yields to the event loop every few hundred entries so it never freezes the web app) on a schedule of **at least every 2 hours** (`scan_process.interval_hours`, default **2**), implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/scanner/scanner.service.ts` — `scanAll()` (the `yieldToLoop()` cadence) · scheduled via `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/schedule/schedule.service.ts` — `intervalFor()` → the launchd `StartInterval`. The default lives in `~/BGit/Bryan_git/LargeFileBridge/code/packages/shared/src/schemas.ts` — `scan_process.interval_hours` (default **2**).

Existing installs are migrated forward so the new cadence takes effect without user action. The old stored default (4h) is healed to 2h when config loads, in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/store-model/config.service.ts` — `getAppConfig()` (rewrites `scan_process.interval_hours === 4 → 2`; a value the user deliberately set is left alone). The already-installed launchd plist still fires on its baked-in `StartInterval` until re-rendered, so at boot any drift between the plist's interval and the configured interval is reconciled (the plist is rewritten and reloaded, and only for workers the user already installed) in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/schedule/schedule.service.ts` — `reconcileWorkerSchedules()` (reads the installed interval via `SchedulerInstaller.installedIntervalSeconds()`, implemented for macOS in `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/schedule/os/launchd.ts`), called once from `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/main.ts` — `bootstrapState()`.

Mapping the `interest` value to the folder glyph's `color`/`fill` is done in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/fs/FileSystemPage.tsx` — `FsRow` (the `<Folder>` at the folder-glyph line; replace the hardcoded `text-slate-500` with a `folderGlyphStyle(entry.interest)` lookup) · shared helper `~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/components/fs/folderInterest.ts` — `folderGlyphStyle()` (returns `{ color, fill }` from the token pairs in §2), reused by every other folder glyph listed in §3.1.

---

## 4. Cloud-storage roots surfaced at the top of the home column

### 4.1 The problem, and the product bet (LOCKED intent)

The files LFBridge exists for — the multi-gigabyte movies, the uncompressed image dumps — increasingly do
**not** live in the user's home tree at all. They live in **Dropbox**, in **Google Drive**, in **iCloud
Drive**. On macOS none of those mount in `~`; they mount **one level deep and out of sight** under
`~/Library/CloudStorage/` (Google Drive for Desktop, the Dropbox client, and the iCloud FileProvider all
publish their folders there). A user who opens **File System** and lands on `~` would have to know to drill
**`Library → CloudStorage → GoogleDrive-<email>`** — a path most people have never seen — before they could
reach the very content the product is built to compress and pin.

So the product bet, **LOCKED**: **we lift each cloud mount to the top of the home column, presented as if it
sat directly in `~`.** The user opens File System, and Dropbox / Google Drive / iCloud are simply *there*, at
the top, one click away — the same first-class starting points as any home folder. This is the difference
between "a tool that browses my disk" and "a tool that knows where my big files actually are."

> **Why this is a browsing feature, not a backbone feature (LOCKED).** Surfacing a mount here is **purely about
> visibility and navigation**. It is completely independent of the cloud **Sync-List backbones**
> ([dropbox.mdx](./dropbox.mdx) / [google_drive.mdx](./google_drive.mdx), which carry the Sync List YAML
> between the user's computers). Making Dropbox visible in the browser does **not** enable the Dropbox
> backbone, does **not** write a Sync List into Dropbox, and does **not** move a single byte. The two ideas
> share a vendor and a discovery path — nothing else. See §4.6.

### 4.2 The concept — each mount becomes a top-level row (LOCKED)

Only the **home column** (the leftmost column, the one rooted at `~`) is affected. Every deeper column is
untouched. When the browser lists the home directory, it **prepends one row per detected cloud mount**, above
every real home folder:

| What the user sees | What it really is |
| --- | --- |
| **☁ Dropbox** | a `dir` row whose `path` is `~/Library/CloudStorage/Dropbox` |
| **☁ Google Drive — bryan@act3ai.com** | a `dir` row whose `path` is `~/Library/CloudStorage/GoogleDrive-bryan@act3ai.com` |
| **☁ Google Drive — bryan@gmail.com** | one row **per signed-in account** (the user often has several) |
| **☁ iCloud Drive** | `~/Library/CloudStorage/iCloudDrive-…` (best-effort; only when it's a real dir) |

The rows are **real directory rows**, not a special widget: `kind: "dir"`, `path` = the true mount path. So
clicking one opens an **ordinary column** and **every downstream feature works unchanged** — the code badges,
the interesting-folder tint (§1–§3), the Full-paths tab, and above all the **compress / git-ignore / track**
action-links (§5). Nothing about "it came from the cloud shelf" changes what the user can do once inside.

They are marked (`FsEntry.cloud = { provider, account? }`) for **two** cosmetic reasons only: (1) a **cloud
glyph** instead of a folder glyph, so the row reads as a special shortcut; (2) they **sort to the very top**
of the column, above all folders and files. The mark drives presentation; it never gates behavior.

Surfacing the mounts on the home column — build one `dir` row per mount, sorted to the top — is implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/fs/fs.service.ts` — `listDirectory()` (the `isHomeDir(dirAbs)` block; `compareEntries()` floats `cloud` rows first) · the "am I the home column?" test is `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/fs/paths.ts` — `isHomeDir()` (raw-path then realpath compare, so a firmlinked home still matches).
The row's cloud glyph + friendly label render in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/fs/FileSystemPage.tsx` — `FsRow` (the `entry.cloud` branch draws a `<Cloud>` glyph, tinted by `interest` when set, else a sky accent) · the marker rides on the shared type `~/BGit/Bryan_git/LargeFileBridge/code/packages/shared/src/types.ts` — `FsEntry.cloud` (`CloudRootMark` / `CloudProvider`).

### 4.3 Discovery — where the mounts are, and how they're named (LOCKED shape)

Discovery is **macOS-specific** (the `~/Library/CloudStorage/` convention is Apple's FileProvider design) and
**stat-only** — Node `fs`, never the `find` shell (charter + the macOS-indexing rule). We enumerate
`~/Library/CloudStorage/` and classify each child by its vendor naming:

| Mount name pattern | Provider | Display label | Notes |
| --- | --- | --- | --- |
| `Dropbox`, `Dropbox (Personal)`, `Dropbox (Company)` | `dropbox` | the name verbatim | the suffix keeps two Dropbox accounts distinct ([dropbox.mdx §3](./dropbox.mdx)) |
| `GoogleDrive-<email>` | `google_drive` | `Google Drive — <email>` | **one row per account** — the account email is parsed out ([google_drive.mdx §2](./google_drive.mdx)) |
| `iCloudDrive-…` | `icloud` | `iCloud Drive` | best-effort; included only when it stats as a real directory |

Rules that make discovery robust (all LOCKED):

* **Only real directories.** Each candidate is `stat`-ed; anything that is not a live directory — an offline
  mount, a half-removed account, a broken iCloud placeholder — is **skipped**, so the shelf never shows a dead
  row.
* **Stable, vendor-grouped order.** Dropbox first, then each Google Drive account **sorted by email** (so two
  accounts never swap places between calls), then iCloud. Deterministic ordering keeps the home column from
  reshuffling under the user.
* **Non-macOS → empty.** On any other platform discovery returns `[]` and the home column is exactly as it was.
  The feature is additive and platform-guarded; it can never break the browser elsewhere.

Enumerating `~/Library/CloudStorage/` and classifying each mount by vendor naming (Dropbox / `GoogleDrive-<email>` / iCloud), skipping non-directories, with the deterministic vendor-grouped ordering, is implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/fs/cloud-roots.ts` — `detectCloudRoots()` (and `cloudStorageDir()` for the mount-point path).

### 4.4 De-duplication — the legacy symlink collision (LOCKED)

Some machines carry a **legacy home-level symlink** — `~/Dropbox → ~/Library/CloudStorage/Dropbox` — left by
older Dropbox installers. Without care the mount would then appear **twice**: once as the real `~/Dropbox`
symlink child, once as our surfaced Dropbox shortcut. That is confusing and wrong.

The rule, **LOCKED**: before adding the cloud rows we **drop any existing SYMLINK entry whose realpath is a
cloud mount**, then add that mount back as a proper cloud row. Only a **symlink** can alias a CloudStorage
mount up into `~`, so we `realpath` **only** the symlink entries (cheap — a home dir has a handful at most),
never every row. A genuine, unrelated file like `~/Dropbox alias` (a Finder alias *document*, not a symlink to
the mount) is left untouched. Net result: **exactly one** Dropbox row, presented as the cloud shortcut.

De-duplicating a legacy `~/Dropbox`-style symlink against the surfaced mounts (realpath the symlink entries only, drop the ones that resolve to a cloud mount, re-add as cloud rows) is implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/fs/fs.service.ts` — `listDirectory()` (the `cloudReal` set + the `kind === "symlink"` splice loop).

### 4.5 The interest tint carries straight through

A surfaced cloud root is a directory, so the **interesting-folder coloring of §1–§3 applies to it verbatim**:
its cloud glyph is tinted **brown** when its subtree holds a big file, **blue** for a video, and so on — the
user can see at a glance that *"my Dropbox is full of big files worth compressing"* before opening it. The
interest for each cloud row is computed by the **same bounded per-child walk** as every ordinary sibling
(§3.3): each mount draws from the same `INTEREST_PER_CHILD` slice of the shared `INTEREST_TOTAL` pool, early-
exits on the first big file, and honors the truncation **floor** — so a huge, deep Drive still resolves to at
least the level it has already proven, never a false plain glyph.

Computing each surfaced mount's `interest` from the shared per-child budget (so a huge cloud mount can't starve the ordinary siblings, and the truncation floor still applies) is implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/fs/fs.service.ts` — `listDirectory()` (the cloud-row `computeDirInterest()` call sharing `interestPool`) · the walk itself is `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/fs/badges.ts` — `computeDirInterest()`.

### 4.6 Independence from the Sync-List backbones (LOCKED separation)

This is the separation that keeps the product honest, and it is **LOCKED**. **Dropbox and Google Drive each
play two entirely separate roles**, and this section owns only the first:

1. **Browseable file-system root (this §4).** A place on disk the user can navigate and compress. Discovered
   from the mount, surfaced on the home column, always available whenever the mount exists — **regardless of
   any setting**.
2. **Sync-List backbone ([dropbox.mdx](./dropbox.mdx) / [google_drive.mdx](./google_drive.mdx)).** A transport
   that carries the small `LargeFilesBridge_SyncList.yaml` between the user's computers, gated by
   `large_files.settings.backbones.<vendor>.enabled`.

The invariants that follow (all LOCKED):

* Surfacing a mount in the browser **never** enables its backbone, **never** writes a Sync List into it, and
  **never** places a large file into it — the backbone's own "only the YAML, never the bytes" rule
  ([dropbox.mdx](./dropbox.mdx) top) is not even in play here, because §4 writes **nothing** to the cloud at
  all. It only **reads directory metadata** to display rows.
* The two share exactly one thing: the **discovery path** (`~/Library/CloudStorage/…` and, for Dropbox,
  `~/.dropbox/info.json`). The vendor specs own that discovery; §4 reuses it for browsing.
* A user with **all backbones disabled** still sees every cloud root on the home column. Visibility is a
  property of the **mount existing**, not of any backbone being turned on.

### 4.7 Performance & the network-mount hazard (CTO note)

Cloud mounts are **network-backed** and can be slow or partially materialized (Drive **Stream** mode and
Dropbox **online-only** files are placeholders whose bytes live in the cloud). The design keeps this from ever
hurting the browser:

* **Metadata only, no materialization.** Everything §4 does is `readdir` + `stat`. A `stat` on a placeholder
  returns local metadata **without** downloading the file — we never trigger a cloud fetch just to draw a row
  or compute interest.
* **The home column stays responsive.** Per-mount interest is **bounded** by the same `INTEREST_PER_CHILD` cap
  and **early-exits** on the first big file (a mount full of movies resolves in a few dozen stats), and the
  listing loop **yields the event loop** (`setImmediate`) as those stats accrue — a cold walk interleaves with
  other requests instead of blocking (§3.3).
* **Descending into a live mount is the mount's cost, not ours.** Listing *inside* a large streamed Drive is
  inherently slow — but that is the network mount, identical to a user hand-navigating there, and it is
  handled by the same protections every column has: the 5000-entry cap, cooperative yields, and the
  progressive react-query render. §4 does not add a new blocking path; it only makes the mount easier to find.

### 4.8 Security & confinement

Every surfaced mount sits **under the home root**, so it is already inside the browse allow-roots
([allow-root.ts](../code/packages/backend/src/modules/fs/allow-root.ts)) and inherits the same secret-denylist
protections as any home path. As defense-in-depth, the detected mounts are **also added explicitly** to the
allow-roots, so that a mount which happens to `realpath` **outside** home (a FileProvider firmlink, a relocated
Drive on another volume) still resolves when the user browses into it, rather than tripping confinement. The
feature **widens visibility, never authorization**: it exposes only what the user could already reach by
typing the path, and nothing under the secret denylist (`~/.ssh`, `~/.credentials`, Keychains, …) is ever
served — cloud mount or not.

Adding the detected cloud mounts to the browse allow-roots (so browsing into a mount that resolves outside home still passes confinement) is implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/fs/allow-root.ts` — `rawRoots()` (the `for (const cr of detectCloudRoots())` addition), consumed by `assertAllowedPath()`.

### 4.9 Growth opportunities (CPO/CTO backlog — not yet built)

Deliberately **not** in the first cut, recorded so the next iteration can pick them up:

* **A global setting to show/hide the shelf** (`settings.mdx`) — default **on**; some users may want only their
  own tree. Per-provider toggles (hide iCloud, keep Dropbox) are the natural next grain.
* **A per-mount uncompressed rollup** — surface the category counts ([the rollup table](./directories.mdx))
  right on the cloud row: *"Dropbox — 27 videos, 12 images can be compressed."* Turns the shelf from a
  shortcut into a dashboard.
* **Cross-platform discovery** — Windows (`%USERPROFILE%\Dropbox`, `Google Drive` drive-letter mounts) and
  Linux (`~/Dropbox`, `rclone`/`google-drive-ocamlfuse` mounts) so the feature is not macOS-only.
* **Server-mode parity** — a hosted LFBridge has **no** desktop CloudStorage mount, so the browseable-root
  idea would need the vendor **APIs** (Drive API, Dropbox API) behind the same `FsEntry` shape. Mirrors the
  server-mode note the backbone specs already make ([google_drive.mdx §7](./google_drive.mdx)).
* **"Compress in place vs. copy out" policy** for cloud files — compressing a streamed placeholder forces a
  full download first; the UI should make that cost explicit before it runs.

---

## 5. The action-links row (delegated to page_actions.mdx)

The File System page carries the standard **action-links row directly under its page title** — the visible
horizontal row of blue hyperlinks, each with a leading icon, that [page_actions.mdx](./page_actions.mdx)
now specifies for every file-list page (**not** the old `Actions ▾` dropdown). Its behavior — scope
(checked subset else all-recursive), skip-already-done, background queue, and the toast wording — is
inherited from [page_actions.mdx §1–§2](./page_actions.mdx) and **not** restated here.

The File System page's links (per the [page_actions.mdx §4](./page_actions.mdx) catalog), left → right:

**Create Transcriptions · Create AI descriptions · Compress videos · Compress images ·
Git ignore · Track / Pin this directory**

* The links render in **one measured row** that keeps what fits inline and rolls the rest into a
  right-pinned **`More ▾`** menu ([page_actions.mdx §3.1](./page_actions.mdx)) — never a second row.
* The producing pair (Create Transcriptions / Create AI descriptions) is present on every file-list page.
* Compress / Git ignore links are **red-tinted explicit offers with a confirm modal** ([page_actions.mdx
  §3](./page_actions.mdx), [menus.mdx §6.1](./menus.mdx)) — never a one-click mutation. **Compress videos /
  Compress images** open the **Compress Inside** dialog ([compress_inside.mdx](./compress_inside.mdx));
  **Git ignore** opens the **Git Ignore** dialog ([git_ignore.mdx](./git_ignore.mdx)), whose target is the
  **rightmost column that has a selection** (§5.1 generalized — [git_ignore.mdx §2](./git_ignore.mdx)).
* Scope is the **deepest (rightmost) open column's directory** walked recursively when nothing is checked,
  or the checked rows when the browser has a selection ([directories.mdx §1.2](./directories.mdx)) — with
  the **containment guarantee locked in §5.1** below.

The row is rendered in code (shared component, fed the File System scope):
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/components/menu/PageActions.tsx` — `PageActions` · consumed by `~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/fs/FileSystemPage.tsx` — `FileSystemPage` (passes the **deepest open column** — `currentDir = stack[stack.length - 1]` — as the scope `root`).

### 5.1 Scope containment — the deepest selected column, downward only (LOCKED)

This is the **safety guarantee** the whole column browser hangs on, and it is **LOCKED**. When the user
walks left → right through the columns (e.g. `Home → _Mirror → Politics → Charlie_Kirk_Mi`) and clicks a
producing action such as **Create Transcriptions** with **nothing checked**, the action's scope is the
**single deepest (rightmost) directory the user has opened** — and the recursive walk is **strictly
contained inside that directory**. It **descends only**; it **never ascends** to a parent column and
**never touches sibling directories**.

Concretely, with `Charlie_Kirk_Mi` as the deepest open column:

* ✅ The walk covers **`~/_Mirror/Politics/Charlie_Kirk_Mi/` and everything under it, recursively** — and
  nothing else.
* ❌ It does **not** walk `~/_Mirror/Politics/` (the parent column), `~/_Mirror/` (its parent), or the home
  root — even though each of those is an **open column to the left**. An open left column is **context the
  user drilled through**, not scope.
* ❌ It does **not** touch **siblings** of `Charlie_Kirk_Mi` (e.g. `~/_Mirror/Politics/Charlie_Kirk/`,
  `~/_Mirror/Politics/COVID/`) — only descendants of the deepest column.

The rules that make this unambiguous (all LOCKED):

1. **"Deepest" = the last opened column, not the last highlighted row.** The scope root is the directory
   whose **contents fill the rightmost column** — i.e. the last directory the user clicked open. Selecting
   (highlighting) a **file** inside that column does **not** change the scope root: it stays the containing
   directory. Selecting a file never widens or narrows the directory scope.
2. **A left column is never the scope.** Clicking a directory in an earlier column **replaces** every
   column to its right ([directories.mdx §1](./directories.mdx)), so at any moment the open-column chain is
   exactly the path from the root to the deepest directory. Only the **tail** of that chain is the scope;
   the ancestors are breadcrumbs.
3. **The walk is downward-only.** The recursive collection follows child directories out of the scope root
   and **never** calls `..`/parent — there is no code path by which a parent or sibling can enter the set.
4. **Checked subset overrides, still contained.** If the user has checked specific rows, the scope is
   exactly those checked paths (each of which lives at or below the deepest column) — never their parents.
5. **No-scope is a safe no-op, never a wide walk.** If somehow no column is open yet (root not resolved),
   the scope resolves to *nothing* and the enqueue endpoint returns a `400` — it must **never** fall back
   to walking the home directory or the filesystem root.

Determining the deepest-column scope root (the tail of the open-column stack) is computed in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/fs/FileSystemPage.tsx` — `FileSystemPage` (`currentDir = stack[stack.length - 1]`, passed as `{ root: currentDir }`; an unopened stack yields `{}`, which the server rejects rather than widening).

Enforcing downward-only containment of the recursive walk (descend into children only, skip hidden/tracking/heavy dirs, never ascend to a parent) is implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/transcribe/transcribe.service.ts` — `enqueueTranscribe()` → `resolveCandidates()` → `walkMedia()` (recurses into child directories of `root` only; the mirror walk for descriptions is `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/describe/describe.service.ts` — `enqueueDescribe()`).

### 5.2 Decision toggles on file rows — act on a big file without opening a repo (LOCKED reference)

The action-links row (§5) is a **batch** over the browser's scope. For acting on **one** big file, every
file/dir row across the File System surfaces also carries the two inline **decision toggles** — the
**editable** counterpart of the read-only `P` (pin) and `I` (git-ignored) code badges. This is how a
user pins a big file, or (under a repo) git-ignores it, **directly from the browser** — no need to open
the file's repo. The grammar, four render states, and click semantics are owned by
[decision_toggles.mdx](./decision_toggles.mdx) and [directories.mdx §3.6](./directories.mdx); this file
only notes that they ride on the rows it hubs.

* **Add-to-IPFS (pin)** toggle renders for **any large file**; **Add-to-git-ignore (⊘)** toggle is
  **repo-scoped** — it renders only for files/dirs **under a Git repo** (`repoRootFor()`), else **N/A**.
* Each renders OFF / ON (`#c2410c`) / **RECOMMENDED** (dashed orange — a calc-engine suggestion from
  [to_do_batch_calc_engine.mdx](./to_do_batch_calc_engine.mdx)) / N/A. A click writes **one** axis to
  the `decisions.yaml` ledger — in **Local Storage** at `~/T/_large_files_bridge/repos/<repoKey>/` (shared
  via the company sync repo when enabled, never in the working repo —
  [artifact_placement_policy.mdx](./artifact_placement_policy.mdx)) — performs that axis's effect (pin/unpin
  or `.gitignore` write), **never deletes local bytes**, and **stops propagation** so it never navigates.
* On a **directory** under a repo, turning ⊘ **ON** opens the [git-ignore pop-over](./git_ignore.mdx)
  rather than writing silently. The **cloud-root rows (§4)** are ordinary `dir` rows, so under a repo
  they carry the ⊘ toggle like any other directory.
* The **Full paths** tab ([full_paths.mdx](./full_paths.mdx), `/fs/paths`) lists file rows, so the same
  two toggle columns appear there — the editable counterpart of its `P` / `I` badge columns.

Rendering the two inline toggles is owned by
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/components/decision/DecisionToggles.tsx` — `DecisionToggles`; the under-a-repo test that gates the ⊘ toggle is `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/git/git.service.ts` — `GitService.repoRootFor()`.

---

## 6. Acceptance criteria

1. **Interesting definition (§1):** a directory is interesting iff its subtree (recursively) contains a
   video, an uncompressed image ≥ 3 MB, or a file ≥ the big-file threshold (default 100 MB). Interest
   bubbles up — a parent of an interesting folder is interesting.
2. **Three colors, highest wins (§2):** the folder glyph is **brown** (dark-brown outline, light-brown
   fill) when the subtree has a **big file**; else **blue fill** (medium-dark-blue outline, very-light-blue
   fill) when it has a **video**; else **blue outline only** (blue outline, no fill) when it has an
   **uncompressed image ≥ 3 MB**; else the plain uncolored glyph. Big beats video beats image.
3. **Recursive:** a top-level folder shows brown when its only big files are several levels deep; the color
   reflects the whole subtree, subject to the walk budget.
4. **Applied everywhere (§3.1):** the same coloring is used for every folder glyph — the `/fs` column
   browser, View-one-directory child lists, storage directory rows, and repo rows (a movie-filled repo
   reads brown) — via one shared `folderGlyphStyle()` helper. Full paths (`/fs/paths`) has no directory
   rows and is unaffected.
5. **Performance + truncation floor (§3.3):** interest is a by-product of the existing bounded walk (per-
   child in the column browser, sharing one budget), cached by directory + mtime/`scannedAt`; the tree
   renders immediately with plain glyphs and upgrades the tint when the value arrives — never blocking. A
   budget-capped walk that **already found a video/image returns that as a definite floor (at least blue),
   uncached so it can still upgrade to brown**; only a budget-capped walk that found **nothing** shows the
   plain glyph (unknown), never a false "not interesting". Concretely, `~/_Mirror/` (≈20 k entries, videos
   several levels deep) shows **at least blue** even though its walk truncates.
6. **Tokens:** the six `--lfb-fold-*` CSS variables from §2 exist in `styles.css` and drive the glyph
   `color`/`fill`; the brown reuses the existing repo-badge brown and the blue reuses the IPFS-badge blue.
7. **Cloud roots surfaced (§4):** on macOS, the **home column** shows one top-of-column ☁ row per detected
   `~/Library/CloudStorage/` mount — **Dropbox**, **each** `GoogleDrive-<email>` account, and **iCloud
   Drive** when it's a real dir — above every home folder, each a real `dir` row whose `path` is the mount so
   browsing/compress/interest all work unchanged. Only the home column is affected; deeper columns are
   untouched; a non-macOS host shows no change.
8. **Discovery, dedup & independence (§4.3/§4.4/§4.6):** mounts are found by `stat`-only enumeration (skipping
   non-directories), ordered Dropbox → Drive-by-email → iCloud; a legacy `~/Dropbox → CloudStorage/Dropbox`
   symlink is de-duplicated to **one** cloud row (an unrelated `~/Dropbox alias` file is left alone); the
   shelf appears **regardless of** `settings.backbones.*` and surfacing a mount **never** writes anything to
   the cloud (browsing role only, distinct from the Sync-List backbone).
9. **Cloud confinement & performance (§4.7/§4.8):** discovery and interest are metadata-only (no placeholder
   materialization); each mount's interest uses the shared per-child budget; detected mounts are added to the
   browse allow-roots so browsing in always resolves, while the secret denylist still applies.
10. **Action-links row (§5):** the File System page shows the standard action-links row under its title with
    the **short-label** catalog items above (**Compress videos · Compress images · Git ignore**), following
    [page_actions.mdx](./page_actions.mdx) — a **single measured row** with a right-pinned **`More ▾`**
    overflow (§3.1), not a dropdown, never a second row. Compress / Git ignore are red-tinted confirm-gated
    offers opening the [Compress Inside](./compress_inside.mdx) / [Git Ignore](./git_ignore.mdx) dialogs.
11. **Scope containment (§5.1):** with nothing checked, a producing action's scope is the **deepest
   (rightmost) open column's directory**, walked **recursively downward only**. Walking
   `Home → _Mirror → Politics → Charlie_Kirk_Mi` and clicking **Create Transcriptions** processes **only
   `~/_Mirror/Politics/Charlie_Kirk_Mi/` and its descendants** — never a parent column (`Politics`,
   `_Mirror`, home), never a sibling directory. Selecting a file inside the deepest column does not change
   the directory scope; an unopened stack is a `400`, never a fall-back walk of home or `/`.
12. **No overlap:** this file does not re-specify the column browser, badges, filters, or the Full-paths
    table — those stay owned by [directories.mdx](./directories.mdx) and [full_paths.mdx](./full_paths.mdx).
13. **Decision toggles on file rows (§5.2):** every file/dir row across the File System surfaces (column
    browser, cloud-root rows, and the Full-paths tab) carries the two inline **decision toggles** — the
    editable form of the `P` (pin) and `I` (git-ignored) badges. The **pin** toggle renders for any large
    file; the **⊘ git-ignore** toggle renders **only under a repo** (`repoRootFor()`), else N/A. A click
    writes **one** axis to the Local-Storage `decisions.yaml` (`~/T/_large_files_bridge/repos/<repoKey>/`,
    never the working repo), performs its effect, **never deletes local bytes**,
    and **stops propagation** (never navigates); turning ⊘ ON for a **directory** under a repo opens the
    [git-ignore pop-over](./git_ignore.mdx). Grammar/states owned by
    [decision_toggles.mdx](./decision_toggles.mdx) / [directories.mdx §3.6](./directories.mdx).

<!--
  HUB spec for the "File System" left-bar item (/fs). OWNS three things not owned elsewhere:
   (1) INTERESTING-DIRECTORY FOLDER COLORING (§1-§3): a dir is "interesting" if its SUBTREE (recursive)
       has a video, an uncompressed image >= 3 MB, or a file >= big-file threshold (default 100 MB). Color =
       highest-priority category present: BIG FILE -> brown (dark-brown outline / light-brown fill) is
       strongest; else VIDEO -> blue fill (medium-dark-blue outline / very-light-blue near-white fill);
       else UNCOMPRESSED IMAGE >=3MB -> blue OUTLINE ONLY (no fill) is weakest; else plain glyph. Big beats
       video beats image. Applied to EVERY folder glyph app-wide (fs column browser, view-one-dir child
       lists, storage dir rows, repo rows), NOT just /fs. Video interest = ANY video (no compressState/size
       floor). Image interest floor = 3 MB (higher than the 1 MiB compress floor). Tokens: six --lfb-fold-*
       vars (brown reuses --lfb-badge-repo-root, blue reuses --lfb-badge-ipfs).
       TRUNCATION FLOOR (§3.3, LOCKED): the walk is bounded (depth<=8, HARD_SKIP, a per-listing budget the
       column browser SHARES across every sibling — 40k in fs.service.ts listDir). On budget exhaustion we
       return the best-known FLOOR: if a video/image was already found, return "video"/"image" (>= blue),
       UNCACHED so a later full walk can upgrade to "big"; only truncation-with-nothing-found is undefined
       (unknown -> plain glyph); a fully-drained empty walk is a cached null (not interesting). This is the
       fix for ~/_Mirror/ (~20k entries, deep videos) showing a PLAIN glyph: it blew the shared budget and
       computeDirInterest() returned undefined, discarding the videos it had found. Now it returns "video".
       Computed by fs/badges.ts computeDirInterest() (truncatedFloor() helper) for the column browser, and
       mirrored in entity.service.ts buildDirRollup() (interestVideo/interestImage branches before the
       walkTruncated->undefined branch).
   (2) CLOUD-STORAGE ROOTS SURFACED AT THE TOP OF THE HOME COLUMN (§4, macOS): Dropbox / GoogleDrive-<email>
       (one row per account) / iCloud live under ~/Library/CloudStorage/, NOT in ~. On the HOME column ONLY
       we prepend one ☁ dir row per detected mount, sorted to the very top (compareEntries floats cloud
       first), each a REAL dir row whose path = the mount so badges/interest/compress/full-paths all work
       unchanged; marked FsEntry.cloud={provider,account?} only for the cloud glyph + top sort. Discovery =
       fs.service.ts listDirectory() isHomeDir() block calling cloud-roots.ts detectCloudRoots() (stat-only
       enumeration of ~/Library/CloudStorage/, classify Dropbox*/GoogleDrive-*/iCloud*, skip non-dirs, order
       Dropbox→Drive-by-email→iCloud, [] off-macOS). DEDUP (LOCKED): a legacy ~/Dropbox symlink ->
       CloudStorage/Dropbox is dropped (realpath the SYMLINK entries only) and re-added as the cloud row —
       exactly one Dropbox; an unrelated "~/Dropbox alias" file is untouched. Interest per cloud row uses the
       SAME shared per-child budget (computeDirInterest, interestPool). INDEPENDENCE (LOCKED): this is a
       BROWSING feature only — totally separate from the Sync-List BACKBONES (dropbox.mdx/google_drive.mdx,
       gated by settings.backbones.*). Surfacing a mount NEVER enables a backbone, writes a Sync List, or
       moves bytes; shelf shows regardless of settings. CONFINEMENT: detected mounts also added to browse
       allow-roots (allow-root.ts rawRoots()) so browsing in resolves even if a mount realpaths outside home;
       secret denylist still applies; metadata-only, never materializes a placeholder. Frontend: FsRow
       entry.cloud branch draws <Cloud> (FileSystemPage.tsx). Growth backlog (§4.9): settings toggle,
       per-mount rollup, Windows/Linux discovery, server-mode via vendor APIs.
   (3) ACTION-LINKS ROW (§5): delegates ALL behavior to page_actions.mdx (visible links row under the title,
       NOT the old Actions ▾ dropdown). Catalog: Create Transcriptions · Create AI descriptions · Compress
       videos · Compress images · Git ignore · Track / Pin this directory. Labels are the SHORT forms;
       the row is ONE measured line with a right-pinned "More ▾" overflow (page_actions.mdx §3.1). Compress
       links → compress_inside.mdx dialog; Git ignore → git_ignore.mdx dialog (target = the rightmost column
       that HAS a selection, §5.1 generalized).
       §5.1 SCOPE CONTAINMENT (LOCKED): with nothing checked, scope = the DEEPEST (rightmost) open column's
       dir = currentDir = stack[stack.length-1], walked RECURSIVELY DOWNWARD ONLY. Never ascends to a parent
       column (Politics/_Mirror/home) or touches a sibling dir. "Deepest" = last opened column, not last
       highlighted row; selecting a file doesn't change the dir scope. Checked subset overrides but stays at/
       below the deepest col. Unopened stack => {} => server 400, NEVER a fallback walk of home or /. Backend
       walkMedia() in transcribe.service.ts enqueueTranscribe()->resolveCandidates() recurses into children
       of root only (describe.service.ts enqueueDescribe() mirrors it).
   (bonus §5.2) DECISION TOGGLES on file rows: brief REFERENCE (not owned here) — every file/dir row
       across the FS surfaces (column browser, cloud rows, Full-paths tab) carries the two inline editable
       toggles = writable form of the P (pin) / I (git-ignored) badges. Pin toggle = any large file; ⊘
       git-ignore toggle = repo-scoped (renders only under a repo via git.service.ts repoRootFor(), else
       N/A). States OFF/ON(#c2410c)/RECOMMENDED(dashed, from to_do_batch_calc_engine.mdx)/N/A; click writes
       ONE axis to the Local-Storage decisions.yaml (repos/<repoKey>/, never the working repo), never deletes bytes, stops propagation (never navigates); a
       directory ⊘-ON opens git_ignore.mdx pop-over. Grammar owned by decision_toggles.mdx / directories.mdx
       §3.6; component DecisionToggles.tsx.
  Does NOT restate: the Miller column browser / badges R-r·P·C-c·i / size+media filters / persisted state
  (owned by directories.mdx Part A) or the flat Full-paths table /fs/paths (owned by full_paths.mdx).
  Code: interest computed in entity.service.ts buildDirRollup() (extend to emit `interest`) using
  fs/badges.ts compressInfo()/VIDEO_EXT/IMAGE_UNCOMPRESSED_EXT; rides on shared FsEntry.interest; frontend
  maps via components/fs/folderInterest.ts folderGlyphStyle(), applied at pages/fs/FileSystemPage.tsx FsRow
  <Folder> (replace text-slate-500) and every other folder glyph. Threshold = getAppConfig().big_file.
  threshold_bytes (settings.mdx §1). Keep consistent with directories.mdx, full_paths.mdx, page_actions.mdx.
-->
</content>

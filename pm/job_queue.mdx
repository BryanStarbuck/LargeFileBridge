---
title: LargeFileBridge — The Background Job Queue (async batch processing)
description: The server-side background queue that lets a page action enqueue MANY files and return immediately. When a page action (Create Transcriptions, Create AI descriptions — page_actions.mdx) resolves its eligible set, it does NOT run the files inline in the HTTP request; it PUSHES them onto this in-process FIFO queue and returns the plan. A pool of workers drains the queue in the background at a CORE-BUDGETED, MEDIA-AWARE concurrency (parallelization.mdx — the mass-compute budget is round(cores × 0.9), ~90% of cores; compress:image fans wide, compress:video fans narrow with capped ffmpeg threads, transcribe narrow, describe network-fixed) — calling the op's existing per-file runner (transcribeOne / describeOne / compressFile), each of which already registers a progress-dock job so the item shows a live card while it runs. Defines the task shape (compress tasks carry their media kind), dedup (never queue the same path+op twice), the core-budget media-aware admission buckets, how running items surface in the dock (via the existing job registry, webapp.mdx §12) and how the pending backlog is counted (a `queued` figure folded into GET /api/progress), and the crash/no-persistence posture (a queue that outlives a restart was never "in flight", mirroring the registry's rationale). Sibling of parallelization.mdx (the core-budget policy it draws), page_actions.mdx (who feeds it), webapp.mdx §11–§14 (the progress dock + registry + mapLimit it reuses), Transcribe.mdx / ai_description.mdx (the per-file runners it calls).
---

# LargeFileBridge — The Background Job Queue

A page action can add **many** items at once — "Create Transcriptions for this whole repo" might be 300
media files. Running those inline in the HTTP request would be wrong: the request would hang for an hour,
and closing the tab would lose the work. So LFBridge has a **server-side background queue**. A page action
**enqueues and returns**; a bounded pool of workers **drains the queue in the background**.

This spec owns the queue mechanism. It is deliberately small and reuses what already exists:

* [page_actions.mdx](./page_actions.mdx) — the **producer**. Its enqueue endpoints (§5 there) resolve the
  eligible set (scope + skip-already-done) and call `enqueue()`; they never wait for the work.
* [webapp.mdx §11–§14](./webapp.mdx) — the **progress dock**, the in-process **job registry**
  (`progress.registry.ts`), the operation **taxonomy** (kinds `transcribe` / `describe`), and the bounded
  `mapLimit` concurrency model. The queue is the missing piece **between** "a page action fires" and "the
  registry shows a running card": the registry shows what's **running now**; the queue holds what's
  **waiting to run**.
* [Transcribe.mdx §1/§5.1](./Transcribe.mdx) / [ai_description.mdx](./ai_description.mdx) — the **per-file
  runners** (`transcribeOne` / `describeOne`) the queue calls. Each already wraps its run in a
  `track("transcribe"/"describe", …)` registry job, so a queued item that starts **automatically gets a
  live dock card** — the queue does not need to touch the dock itself.

---

## 1. Why a queue and not just `mapLimit`

The browser-fan-out runner (`useProgress().run`, [webapp.mdx §12A](./webapp.mdx)) already does bounded
parallelism — but it lives **in the browser tab** and keeps one open request per item. That is right for a
handful of clicks; it is wrong for a 300-file batch, because:

* the batch must **survive navigation and tab close** — the user clicks "Create Transcriptions" and walks
  away;
* the server, not the browser, owns "how many are still to do";
* Whisper/ffmpeg are **heavy and local** — we want a **small, server-enforced** concurrency, not one
  request per file all racing.

So producing **page** actions (page_actions.mdx) go through **this server queue**; interactive **per-file**
and **small selected-set** clicks may still use the browser runner. The queue is the durable-within-the-
process path for "many".

---

## 2. The task shape & the queue

The queue is an in-process **FIFO** of small task records:

```ts
export type JobOp = "transcribe" | "describe" | "compress" | "ocr";

export interface QueueTask {
  op: JobOp;             // which per-file runner handles it
  path: string;          // the absolute media file path
  overwrite: boolean;    // re-do even if the output exists (default false)
  provider?: string;     // describe only — the chosen vision provider (or "auto")
  compress?: {
    deleteOriginal: "hard" | "trash";  // per-run originals disposition
    mediaKind: "image" | "video";      // which media-aware compress budget this task draws (§3)
  };
  batchId?: string;      // groups the per-file tasks of one bulk run (a "Compress inside" batch)
}
```

The `compress` task carries its **media kind** (image vs video) because the two draw **different**
concurrency budgets (§3) — image tools are single-threaded (fan wide), ffmpeg is multi-threaded (fan
narrow). The producer stamps it from the file's extension when it plans the batch
([compression.mdx §12.6](./compression.mdx)).

* **`enqueue(tasks: QueueTask[]): { queued: number; deduped: number }`** appends the tasks and **kicks the
  drain loop**, then returns immediately. `queued` is how many were actually added (after dedup); `deduped`
  is how many were dropped because an identical `op + path` was already **pending or running**.
* **Dedup (LOCKED).** The same file is never queued twice for the same op at the same time. Clicking
  **Create Transcriptions** twice in a row does not double-queue; the second call reports everything as
  `deduped`. (Skip-already-**done** is the producer's job, [page_actions.mdx §1.2](./page_actions.mdx);
  dedup here is about the same file being **in flight** twice.)
* **FIFO.** Items drain roughly in the order enqueued; ordering is best-effort, not a guarantee callers may
  rely on.

The FIFO queue, `enqueue()`, and the in-flight dedup set are implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/jobqueue/jobqueue.service.ts` — `enqueue()` / the module-level `pending[]` + `inflight` Set.

### 2.1 A task can leave the queue **without ever running** — `halted` (LOCKED)

A task leaves the queue **four** ways, not two: **`ok`**, **`rejected`**, **`failed`**, or **`halted`**
([processing_batches.mdx §4](./processing_batches.mdx) — the authoritative taxonomy).

**"Done" is not one of those four**, and must not be used as if it were. It is the name of **`ok` alone**.
All four exits are **settled** — the task is off the queue and will not be retried — but only `ok` is
**done**. A **`rejected`** task *ran*, and the provider **refused** it
([ai_description.mdx §2.3](./ai_description.mdx)): it is settled, it is **not** a failure (§4a), and it is
**not** a success. Letting it exit as "done" is the exact fold
[processing_batches.mdx §4.2](./processing_batches.mdx) exists to prevent.

The rest of this section is about the distinction between the two exits that are easiest to confuse:

* **`halted` ≠ `failed`.** A **failed** task **ran** and errored. A **halted** task was **NEVER ATTEMPTED**
  — the provider's **circuit opened** ([to_fix.mdx §2.4](./to_fix.mdx)) and the queue **drained** it out of
  `pending` rather than spend a round-trip it already knew would be rejected.
* **Why the distinction is load-bearing.** These two facts imply **opposite** user actions. A failure means
  the work was really lost; a halt means nothing was spent and the file is still exactly where it started.
  Collapsing `halted` into `failed` makes a user **re-run work believing it was attempted** and hides the
  actual fix — wait for the provider, then re-queue.
* **One-click re-queue (required).** Because a halted task was never tried, restoring it is pure re-enqueue.
  The surface must offer that in **one click**, grouped **one banner per circuit** — never a per-row retry
  ([processing.mdx §4.3.1](./processing.mdx)).
* **Terminal record.** A halt is recorded terminally like any other outcome, so the manifest and the
  transactions ledger show the batch **stopped**, not that N files broke.

Draining the doomed tasks and recording the distinct terminal state is in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/jobqueue/jobqueue.service.ts` — `haltDoomedTasks()` (splices `pending`, `recordFailure({ state: "halted" })`, `appendTerminal(id, "halted")`), called from `pump()`; the wire shape is `~/BGit/Bryan_git/LargeFileBridge/code/packages/shared/src/types.ts` — `FailedItemView.state?: "failed" | "halted"`.

---

## 3. The workers — the CORE BUDGET, media-aware (LOCKED)

A pool of **worker loops** drains the queue. Each loop repeatedly takes the next task whose **admission
bucket** has a **free slot**, runs it, and loops until the queue empties, then parks (no busy-wait).
Concurrency is **not** a fixed tiny constant — it is **derived from the machine's core count** so a many-core
box actually gets used. The policy is the canonical **Core Budget** ([parallelization.mdx](./parallelization.mdx)):
the **mass-compute** budget is `round(cores × max_core_fraction)`, `max_core_fraction` **default 0.9** — *use
up to ~90% of cores*.

The `compress` op is **media-aware** because the two media kinds have opposite tool profiles
([parallelization.mdx §2](./parallelization.mdx)), so it splits into **two admission buckets**:

| Bucket | Concurrency (on a `budget`-core machine) | Per-job threads | Why |
| --- | --- | --- | --- |
| `compress:image` | **wide** — `budget` (≈ 90% of cores) | **1** | oxipng/cwebp/mozjpeg/magick are ~single-threaded per file — one job per core saturates the box. |
| `compress:video` | **narrow** — `floor(budget / videoThreads)` (`videoThreads` default 4) | **`videoThreads`** | ffmpeg is internally multi-threaded; a few thread-capped jobs fill the budget without cores² thrash. |
| `transcribe` | **narrow** — `floor(budget / whisperThreads)` (min 1) | whisper-internal | Whisper is CPU/GPU-heavy and multi-threaded; a couple saturate the machine. |
| `describe` | fixed **generous — default ~24** (`LFB_DESCRIBE_CONCURRENCY`; network, not core-bound) | — | The heavy wait is a vision-API round-trip, not local CPU — **no hardware bottleneck**, so describe fans **much wider** than transcribe/compress (overlap many uploads, don't burn cores). Only ceiling is the provider's rate limit ([ai_description.mdx §12.6](./ai_description.mdx)). |

* **Anti-oversubscription (LOCKED, [parallelization.mdx §2](./parallelization.mdx)).** For every bucket
  `concurrentJobs × threadsPerJob ≈ budget`, **never `cores²`**. Image jobs fan wide at 1 thread each; video
  jobs fan narrow and the queue **passes a `-threads` cap into ffmpeg** for each batched job so N videos do
  not each grab every core. A **one-off** single-file Compress (the file/viewer menu) is **not** a batch and
  passes **no** cap — that lone file may use the whole machine.
* **Budgets are read live** from the shared helper at **admission time**, so changing
  `performance.max_core_fraction` in Settings ([parallelization.mdx §4](./parallelization.mdx)) takes effect
  on the **next** task with no restart. They are computed in **one** place, not hardcoded per op.
* **A task runs by calling its per-file runner** — `transcribeOne(path, overwrite)`,
  `describeOne(path, { overwrite, provider })`, or (for `compress`) `compressFile(path, { deleteOriginal,
  threads })` wrapped in `track("compress", …)`. Those runners **already** register a
  `track("transcribe"/"describe"/"compress", …)` job in the progress registry ([webapp.mdx §12](./webapp.mdx)),
  so the moment a queued item **starts**, a **live dock card** appears (determinate % for transcribe/compress,
  spinner for describe) — including in a tab that did not start the batch. The queue writes **no** dock code
  of its own. **Every op's heavy child-process work is `async` (`spawn`), never `spawnSync`** — the `compress`
  task's transcode ([compression.mdx §12.6](./compression.mdx)) **and** the `describe` task's compress-to-fit
  transcode + inline base64 read ([ai_description.mdx §3.3.1](./ai_description.mdx)) — so a worker **never
  blocks the event loop** while the batch drains. This is what keeps `GET /api/progress` answering (and the
  Processing page loading) while ~24 describe jobs or a wide compress batch run at once; a synchronous
  transcode here would freeze the single API server for the whole run ([performance.mdx P-27](./performance.mdx)).
* **Per-item isolation.** A task that throws is caught, logged to `error.err`
  ([ai_description.mdx §8](./ai_description.mdx)), and the worker moves on — one bad file never stalls the
  queue. Each runner already returns a truthful status rather than throwing for expected outcomes.

The media-aware, core-budgeted worker loops (take next runnable task by admission bucket, dispatch to the runner, catch-and-continue) are implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/jobqueue/jobqueue.service.ts` — `pump()` / `runTask()` / `capFor()` + `bucketOf()` (the live per-bucket caps drawn from `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/shared/concurrency.ts` — `coreBudget()`; buckets `transcribe` / `describe` / `compress:image` / `compress:video`); the dispatch maps `op` → `transcribeOne` / `describeOne` / `compressFile` (the last wrapped in `track("compress", …)`, passed the per-job `threads` cap, and on return `recordBatchResult()` against its `batchId` — §4a).

---

## 4. What the user sees — running cards + a queued count

The dock already shows **running** work (source B poll of `GET /api/progress`, [webapp.mdx §12](./webapp.mdx)).
The queue adds one thing: the **pending backlog count**, so the user knows work is still waiting, not just
the few cards currently running.

* **Running items** → ordinary dock cards, unchanged — they come from the registry the runners populate.
* **Pending backlog** → a small **`queued` integer** folded into the same `GET /api/progress` payload:

```
GET /api/progress → { jobs: Job[], queued: number }   // queued = tasks waiting (not yet started)
```

The dock shows a subtle footer like **"+ 24 queued"** under the live cards when `queued > 0`, and it clears
to nothing as the backlog drains. No card is fabricated for a not-yet-started item ([webapp.mdx §13](./webapp.mdx):
"a queued item simply has no card yet") — `queued` is just a number.

The `queued` count is exposed by the queue and folded into the progress payload in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/jobqueue/jobqueue.service.ts` — `queueDepth()`; read by `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/progress/progress.router.ts` — `GET /` (adds `queued` to `ProgressListResult`). The dock footer renders it in `~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/components/ProgressDock.tsx`.

---

## 4a. Batches — grouping one bulk run's tasks

A **batch** groups the per-file tasks of **one bulk run** — **one popup Confirm**
([processing_batches.mdx §1](./processing_batches.mdx)) — so a surface can show that run's **progress**, its
**outcome counters**, and its **error list**. Each enqueued task carries the run's `batchId` (§2).

**[processing_batches.mdx](./processing_batches.mdx) is the authoritative spec** for the batch **record**,
its **five-way outcome taxonomy**, the **rejection rate**, retention, and the batch actions. This section
owns only the **registry mechanism** the queue provides.

* **`createBatch({ batchId, kind, label, scope, total }): batchId`** — opens a batch when the bulk producer
  plans the set. **It adopts the `batchId` the batch manifest already minted** ([to_fix.mdx §4.1](./to_fix.mdx))
  rather than generating a second one, so **one id** spans the manifest on disk, the log context, the tasks,
  and the UI row.
* **`recordBatchResult(batchId, path, result)`** — called by `runTask()` as each task returns, routing the
  result into **exactly one** counter ([processing_batches.mdx §4](./processing_batches.mdx)):
  `ok` · `rejected` · `failed` · `halted`. **`rejected` gets its own counter — it is neither `ok` nor
  `failed`** ([ai_description.mdx §2.3](./ai_description.mdx) — a refusal is *"an answer, not a breakdown"*;
  it is **settled**, so it advances progress, but it is **not** a success and must never be folded into
  `done`). `failed` / `halted` also append to `errors[]` as `{ path, reason, state }`. A task that *throws*
  is caught (§3) and still recorded, so the batch always finishes and the identity
  `ok + rejected + failed + halted + running + pending === total` always holds.
* **`listBatches()`** — the batches, folded into `GET /api/progress` as `batches`
  ([processing.mdx §5](./processing.mdx)). **Retention is [processing_batches.mdx §5](./processing_batches.mdx)'s**
  — the **row** survives the **process lifetime** (pruned at 200 batches / 24 h) and `errors[]` caps at 200
  per batch. **This supersedes the 30-minute window** this section previously specified, which was written
  when a batch was only ever a "Compress inside" run whose sole post-run value was its error list.
* **`stopBatch(batchId)`** — drains that batch's **pending** tasks to `halted` (reusing `haltDoomedTasks()`),
  leaves **running** tasks to finish ([processing_batches.mdx §6.2](./processing_batches.mdx)).

The batch registry is in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/jobqueue/jobqueue.service.ts` — `createBatch()` / `recordBatchResult()` / `listBatches()` / `stopBatch()` (and the retention prune). The producers that open a batch are `compression.service.ts` — `enqueueCompressInside()` ([compression.mdx §12.6](./compression.mdx)) and the `transcribe` / `describe` / `ocr` enqueue endpoints ([page_actions.mdx §5](./page_actions.mdx)), each adopting its manifest's `batchId`; the surface that renders the batches is the [Processing page](./processing.mdx)'s batches table (processing.mdx §4.1).

## 4b. TO DO batch Apply is a queue producer (LOCKED)

Alongside the page actions (§5) and "Compress inside" (§4a), **Applying a [TO DO Batch](./to_do_batches.mdx)**
from the [To Do page](./to_do.mdx) is a producer that **reuses this same queue** — it enqueues and returns,
never running the effects inline in the apply request. When the user hits **Apply** in a batch popup, the
`apply` handler writes each row's decision(s) to the shared ledger and then **enqueues the effects** as
ordinary `QueueTask`s, so they drain against the **same** core-budgeted, media-aware worker pool (§3) and
surface as ordinary dock/Processing cards ([processing.mdx §4.6](./processing.mdx#46-applying-a-to-do-batch-is-a-processing-producer-locked)):

* **`compress_video` / `compress_image` recommendations** → **`compress`** tasks — identical to the
  Compress-inside producer's tasks, drawing the `compress:video` / `compress:image` admission buckets (§3),
  so they honour the same anti-oversubscription caps and can share a batch (`batchId`, §4a) for their
  progress bar + error list.
* **A transcribe slug's Apply** ("Transcribe N files", [transcribe_calc_engine.mdx §4](./transcribe_calc_engine.mdx))
  → **`transcribe`** tasks. Batch Apply is the **main volume producer** of `transcribe` work in the product
  (the ~80% default-checked list becomes the `transcribe` backlog); each runs via `transcribeOne`, so it
  gets a live dock card the moment it starts, exactly like a page action's transcriptions.
* **`pull_down` recommendations** → **pin/fetch** work: the file is pinned so its bytes arrive over IPFS
  ([to_do_batches.mdx §3](./to_do_batches.mdx#3-the-batch-yaml--every-field-locked-shape)). Like every other
  effect it registers a progress-dock job (the `pin` op, [webapp.mdx §11](./webapp.mdx)) so it shows as a
  Running-now card and counts into the pending backlog while queued.

**Dedup and skip-already-done still hold.** The queue's `op + path` dedup (§2) means re-Applying a batch, or
Applying its yellow-banner twin on a repo page, never double-queues an in-flight file; and because the
[calc engine recalculates-and-replaces](./to_do_batches.mdx#5-recalculate-and-replace-locked) after work
completes, an already-done file simply drops out of the next batch rather than being re-queued. A **mixed**
batch fans out **compress + transcribe + pin/fetch** tasks in one Apply — all into this one queue, all
watchable together on the Processing page.

The To-Do batch Apply that fans decisions out into `enqueue()` is served in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/todo/todo.router.ts` — `POST /api/todo/batches/:id/apply` (maps each checked recommendation to a `QueueTask` — `compress` / `transcribe` — or a pin/fetch, then calls `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/jobqueue/jobqueue.service.ts` — `enqueue()`), from `~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/todo/TodoPage.tsx` — `TodoBatchPopup` / `TranscribeBatchPopup`.

---

## 5. Persistence & crash posture (LOCKED)

The queue is **in-memory only**, exactly like the job registry ([webapp.mdx §12](./webapp.mdx)):

* The process **is** the single API server; a task that outlives a restart was **not** "in flight" anymore,
  so re-running it on boot would be wrong (it might duplicate work the user never re-requested).
* On restart the queue is simply **empty**. Nothing was lost that mattered: any output already written to
  disk stays; the user can re-invoke the page action, and **skip-already-done** ([page_actions.mdx §1.2](./page_actions.mdx))
  means only the genuinely-unfinished files re-queue. That idempotency is what makes no-persistence safe.
* There is **no** cross-process or on-disk queue file, and the queue never networks (it only calls the
  same local runners the per-file paths call — transcribe is fully local; describe's one deliberate upload
  is the runner's concern, [ai_description.mdx §7](./ai_description.mdx), not the queue's).

---

## 6. Acceptance criteria

1. `enqueue(tasks)` **appends** the tasks, **kicks** the drain, and **returns immediately** with
   `{ queued, deduped }`; the HTTP handler that called it also returns immediately (no waiting for the
   work).
2. **Dedup:** the same `op + path` already **pending or running** is not queued again; a double-click
   reports the second batch as `deduped`.
3. A pool drains the queue with **core-budgeted, media-aware** caps ([parallelization.mdx](./parallelization.mdx)):
   the mass-compute budget is `round(cores × max_core_fraction)` (default **0.9**), `compress:image` fans
   **wide** (`budget`, 1 thread/job), `compress:video` fans **narrow** (`floor(budget / videoThreads)`, threads
   capped), transcribe narrow, describe network-fixed. `concurrentJobs × threadsPerJob ≈ budget`, never
   `cores²`; a one-off single-file compress passes no cap. Extra tasks wait and start as slots free.
4. Each task runs by calling **`transcribeOne` / `describeOne` / `compressFile`** (the last wrapped in
   `track("compress", …)`, its heavy transcode **async** so it never blocks the event loop), so a started
   item shows a **live dock card** via the existing registry — including in a tab that did not start it; the
   queue adds no dock code.
5. A task that fails is **caught, logged, and skipped** — one bad file never stalls the queue.
6. `GET /api/progress` returns a **`queued`** count of not-yet-started tasks; the dock shows a
   **"+ N queued"** footer that drains to nothing. No fabricated cards for pending items.
7. The queue is **in-memory only** (no persistence); on restart it is empty, and re-invoking a page action
   re-queues only the still-unfinished files (skip-already-done makes this safe).
8. **Batches (§4a):** **every bulk run** — a "Compress inside", and each `transcribe` / `describe` / `ocr`
   popup Confirm — opens a batch (`createBatch`) that **adopts the manifest's existing `batchId`** (never
   mints a second) and whose per-file tasks carry it. `recordBatchResult` routes each result into **exactly
   one** counter — `ok` / **`rejected`** / `failed` / `halted` — so
   `ok + rejected + failed + halted + running + pending === total` always holds; **`rejected` is neither
   `ok` nor `failed`** ([processing_batches.mdx §4.2](./processing_batches.mdx)) and must never be folded into
   `done`. `failed`/`halted` also append to `errors[]` as `{ path, reason, state }`. `listBatches()` is folded
   into `GET /api/progress` for the [Processing page](./processing.mdx)'s batches table; **retention is
   [processing_batches.mdx §5](./processing_batches.mdx)'s** — the row lives the **process lifetime** (200
   batches / 24 h), superseding this section's old 30-min window. `stopBatch()` drains pending → `halted` and
   lets running tasks finish.
9. **TO DO batch Apply (§4b):** Applying a [TO DO Batch](./to_do_batches.mdx) is a producer that **reuses
   this queue** — it writes decisions then `enqueue()`s the effects and returns immediately;
   `compress_video`/`compress_image` → `compress` tasks, a transcribe slug's Apply → `transcribe` tasks (the
   main producer of transcribe work), and `pull_down` → pin/fetch work; dedup + skip-already-done hold, and a
   **mixed** batch fans out all kinds into the one queue watchable on Processing.

<!--
  The server-side background queue between "a page action fires" and "the registry shows a running card".
  LOCKED:
   - QueueTask { op: transcribe|describe|compress|ocr, path, overwrite, provider?,
     compress?{deleteOriginal, mediaKind}, batchId? }; enqueue(tasks) => {queued,deduped}, appends + kicks
     drain + RETURNS IMMEDIATELY. compress.mediaKind is LOAD-BEARING — it picks the §3 admission bucket.
     Producers = page_actions.mdx enqueue endpoints (transcribe/describe/ocr) + compression.mdx §12
     enqueueCompressInside() + todo.router apply (§4b).
   - A TASK LEAVES THE QUEUE **FOUR** WAYS (§2.1): ok | rejected | failed | halted (processing_batches.mdx §4
     = the authoritative taxonomy). ALL FOUR are SETTLED; only `ok` is DONE. "done" is NOT one of the four
     and must never name `rejected` (a rejected task RAN and the provider REFUSED — settled, not a failure,
     not a success; folding it into done is the exact defect processing_batches §4.2 exists to prevent).
   - DEDUP by op+path already pending/running (not the same as skip-already-DONE, which is the producer's).
   - CORE-BUDGETED, MEDIA-AWARE worker loops (parallelization.mdx): mass-compute budget = round(cores ×
     max_core_fraction), default 0.9 (~90% of cores). compress splits into buckets compress:image (WIDE =
     budget, 1 thread/job) and compress:video (NARROW = floor(budget/videoThreads), threads capped via
     ffmpeg -threads); transcribe narrow (floor(budget/whisperThreads)); describe network-fixed GENEROUS
     default ~24 via LFB_DESCRIBE_CONCURRENCY (not core-bound — no hardware bottleneck, fans WIDE,
     ai_description.mdx §12.6). Anti-oversubscription: concurrentJobs × threadsPerJob ≈ budget, NEVER cores². One-off
     single-file compress passes NO thread cap (may use all cores). Caps read LIVE at admission (Settings
     knob effective next task, no restart). Task runs by calling transcribeOne/describeOne/compressFile
     (passed the threads cap) — those already track() a registry job => started item = live dock card.
     compress's heavy transcode is ASYNC (spawn) so it never blocks the event loop. Queue writes NO dock
     code. Per-item try/catch: one bad file never stalls the queue.
   - BATCHES (§4a): createBatch/recordBatchResult/listBatches/stopBatch group ONE BULK RUN = ONE POPUP CONFIRM
     (compress-inside AND transcribe/describe/ocr — no longer compress-only). *** processing_batches.mdx is the
     AUTHORITATIVE spec for the RECORD + the COUNTING; this section owns only the registry MECHANISM. ***
     createBatch ADOPTS the manifest's existing batchId (to_fix §4.1) — NEVER mints a second (one id spans
     manifest + log context + tasks + UI row). recordBatchResult routes each result into EXACTLY ONE counter:
     ok | REJECTED | failed | halted, so ok+rejected+failed+halted+running+pending === total ALWAYS holds.
     *** rejected is NEITHER ok NOR failed *** (ai_description §2.3: a refusal is "an answer, not a breakdown"
     — SETTLED so it advances progress, but NOT a success => never fold into done). failed/halted also append
     errors[] {path,reason,state}. RETENTION IS NOW processing_batches.mdx §5's — row = PROCESS LIFETIME
     (prune 200 batches / 24h), errors[] cap 200/batch — SUPERSEDING the old 30-min window (written when a
     batch was only ever Compress-inside + its error list). Folded into GET /api/progress as `batches`.
   - TO DO BATCH APPLY (§4b): Applying a TO DO Batch (to_do.mdx) is a producer that REUSES this queue — writes
     decisions then enqueue()s effects and returns. compress_video/image => compress tasks (same buckets as
     Compress-inside); a transcribe slug's Apply => transcribe tasks (MAIN producer of transcribe work);
     pull_down => pin/fetch work (pin op dock card). Dedup + skip-already-done hold; recalc-and-replace drops
     done files from the next batch; a MIXED batch fans out compress+transcribe+pin into the one queue. Served
     by todo.router POST /api/todo/batches/:id/apply → enqueue(). Keep in sync with to_do.mdx, to_do_batches.mdx,
     transcribe_calc_engine.mdx, processing.mdx §4.6.
   - GET /api/progress gains `queued` (pending count); dock shows "+N queued" footer. No fake cards for
     pending items (webapp.mdx §13).
   - IN-MEMORY ONLY, no persistence (same rationale as the registry). Restart => empty queue; re-invoking a
     page action re-queues only unfinished files (skip-already-done makes no-persistence safe). Never networks.
  Code: modules/jobqueue/jobqueue.service.ts (enqueue/pump/runTask/queueDepth, capFor()/bucketOf() drawing
  shared/concurrency.ts coreBudget(), pending[] + inflight Set, createBatch/recordBatchResult/listBatches),
  shared/concurrency.ts (logicalCores/coreBudget/responsiveBudget), progress.router.ts (fold `queued`+`batches` into
  ProgressListResult), ProgressDock.tsx (footer). Keep in sync with page_actions.mdx (producer), compression.mdx §12
  (compress producer + batches), processing.mdx (batch surfaces), webapp.mdx §11–§14 (dock/registry/mapLimit),
  Transcribe.mdx + ai_description.mdx (the per-file runners).
-->

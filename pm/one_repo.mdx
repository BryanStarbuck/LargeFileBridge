---
title: LargeFileBridge — One repo
description: The per-repo screen (route /repos/$repoId) a Repos-table row opens. The header is two rows — the repo name (never wrapped, never squeezed) plus the width-measured task-tab strip and the smart "View recommendation ›" / "Scan now" primary on row one, and the repo's absolute directory path on its own full-width, never-wrapping row beneath. Under it the metrics strip runs the FULL page width on ONE row that never wraps (the hover-explanation moved to the left-bar hover-info panel), leading with the TWO decision tiles — Add to IPFS and Git Ignore — which replaced the single Undecided tile and absorbed "Big, not git-ignored". Below that is one TanStack Table — one row per large git-ignored file — where the user makes the core decision: Add to IPFS (pin) / Ignore / Undecided. The table has the standard search + icon-only filter/sort control row over an always-visible facet rail carrying TWO separate facets — Pin (IPFS) and Git-ignore, never merged and never named "Decision" (tables.mdx §2.4) — five leading icon control columns, per-row and bulk actions, size, CID, and which peers hold each file; ⌘-click / middle-click a row opens its viewer in a new tab. Defines the summary counts, the FileRow type, the decision model, bulk actions, Pin-now (an action-link, no longer the header primary), and empty/loading states.
---

# LargeFileBridge — One repo

The **One repo** screen is where the work happens. You reach it by clicking a repo on the
[Repos](./repos.mdx) page; it opens that repo at `/repos/$repoId`. It has two jobs:

1. **Show this repo's settings, progress, and status** — is per-repo pinning on? are the files pinned?
   which of your computers hold them? when did it last pin? — and let you **Pin now** (an action-link
   in the page action-links row; it is no longer the header primary, §3.1).
2. **Let you decide, file by file, what LFBridge should move** — the core interaction. For every
   large git-ignored file the repo contains, you choose **Add to IPFS (pin)** (the `sync` decision
   value), **Ignore**, or leave it **Undecided**. Most of the user's time here is spent scanning
   "how many will pin, how many will not, and which ones I still need to decide on."

The One-repo screen is rendered in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/OneRepoPage.tsx` — `OneRepoPage` component

This spec builds on the siblings and does not repeat them:

* [Task tabs](./task_tabs.mdx) — the **All / IPFS / Compress / Transcribe / AI descriptions / OCR** tab
  strip this screen's header now carries (§3.1 here) — **six** tabs, width-measured, with the ones that
  don't fit moving into a trailing overflow chevron ([task_tabs.mdx §1.4](./task_tabs.mdx#14-the-overflow-chevron--the-repo-name-never-loses-locked)).
  Selecting a tab re-tunes the metrics strip, the visible table columns, the default sort, and the row
  filter; that whole system — including the metrics panels, the per-metric popup + blue header-primary
  behavior, and the three-state Transcribe / Compress / Describe status icons — is defined there and only
  summarized here. The **explanation** text those tiles and icons publish is rendered by the **left-bar
  hover-info panel**, not by any region on this page (§3.2).
* [Repos](./repos.mdx) — the list this screen drills into; defines the LOCKED vocabulary
  **Repo → Large file → Decision** (§1 there), the `RepoStatus` enum, and the per-repo counts.
* [pin_process.mdx](./pin_process.mdx) — the **pin process** the Pin-now button triggers:
  every pin pass is a **full pass**, a repo trigger sets **priority** (not scope), the pass is parallel,
  and it is never a silent no-op. The button is one trigger of that pass.
* [Left bar](./left_bar.mdx) — the left navigation (source of truth `pm/left_bar.yaml`).
* [README](../README.md) — the product charter (git for small files; IPFS/p2p for the big ones).

---

## 1. The decision model (LOCKED)

Every large file in the repo is in exactly **one** of three **decision** states. This is the
vocabulary the whole app shares:

| Decision | Meaning | LFBridge behavior |
| --- | --- | --- |
| **Add to IPFS (pin)** | The user wants this file kept pinned across their computers. Stored decision value: `sync`. | LFBridge adds it to the manifest, pins it on IPFS, and transfers it to/from peers. |
| **Ignore** | The user deliberately does **not** want this file pinned. | LFBridge leaves it alone — never added to the manifest, never transferred. Stays a purely local file. |
| **Undecided** | LFBridge found the file but the user has not chosen yet. | LFBridge does **nothing** with the bytes yet; it just surfaces the file so the user can triage it. This is the queue of decisions to make. |

Independently of the decision, an **Add-to-IPFS (pin)** file (decision value `sync`) has a **transfer
status** — where its bytes are relative to the network (§4.2). A file's decision is user intent; its
transfer status is reality.

> **This table is the IPFS axis only — one of two.** The full decision the user makes has **two
> independent axes** — **Add to IPFS (pin)** *and* **Add to git ignore** — each on/off separately, and it
> is recorded (with who/when/the Storage ID) in a **repo-level, team-shared** ledger so a whole team
> shares one set of decisions. That is specified end to end in **[decisions.mdx](./decisions.mdx)**. The
> three-state enum here is the **frozen IPFS-axis projection** of that ledger (`ipfs:true → sync`,
> `ipfs:false → ignore`, no record → undecided) — this screen keeps rendering it verbatim; the wire value
> `sync` is never renamed. The git-ignore axis is applied through [git_ignore.mdx](./git_ignore.mdx).

```ts
// One row of the files table.
export interface FileRow {
  fileId: string;        // stable id (repoId + relative path)
  path: string;          // path relative to the repo root
  sizeBytes: number;     // on-disk (or expected) size
  cid: string | null;    // IPFS content id once added to the manifest; null while Undecided/Ignore
  decision: "sync" | "ignore" | "undecided";  // FROZEN wire value; "sync" renders as "Add to IPFS (pin)" in the UI
  transfer: TransferStatus;   // meaningful for decision === "sync" (see §4.2)
  peers: string[];       // ids of your computers currently pinning this file
  changedAt: string;     // ISO date-time the file last changed on disk
}

export type TransferStatus =
  | "pinned"     // present locally AND pinned on >=1 peer
  | "pending"    // queued to push out or fetch in
  | "fetching"   // transfer in progress (inbound)
  | "pushing"    // transfer in progress (outbound)
  | "missing"    // in the manifest but the bytes are not on this machine
  | "error"      // last transfer failed
  | "na";        // not applicable (decision is ignore/undecided)
```

---

## 2. Layout

Three regions stacked in the content column: the **header** (two rows — repo identity + tabs + the "View
recommendation" / "Scan now" primary on row one, the absolute path on its own full-width row, §3.1), the
**metrics strip** (full page width, one row, never wraps, §3.2), and the **files table** with its control
row. The **hover explanation** for anything on this page appears in the **left bar's** hover-info panel
(bottom-left, above the external links and the account slot) — there is **no** hover region beside the
tiles any more (§3.2).

```
┌────────────┬───────────────────────────────────────────────────────────────────────────┐
│            │  ‹ Repos                                                                  │
│  LEFT BAR  │  Our_Movies       ▦All ⌂IPFS ▤Compress ⌯Transcribe ⌗AI desc ⌸OCR ⌄ ⚙ ┌────┐│ ← row 1: name (never wraps) ·
│            │  ~/BGit/Bryan_git/Movies/Our_Movies/2026/renders/final                └View││   width-measured tabs + ⌄ overflow
│ Repos      │  ▲ row 2 = the absolute path: FULL width, ONE line, ellipsized, tooltip rec││   (task_tabs.mdx §1.4) · primary
│ Peers      ├───────────────────────────────────────────────────────────────────────────┤
│            │  ┌Add to┐ ┌ Git  ┐ ┌Pull ┐ ┌ Not  ┐ ┌Compr.┐ ┌Transc┐ ┌  AI  ┐            │ ← metrics strip (task_tabs.mdx §2)
│            │  │ IPFS │ │Ignore│ │Down │ │Backed│ │Videos│ │ribabl│ │Descr.│            │   FULL page width, ONE row,
│ Scans      │  │  5   │ │  12  │ │  0  │ │Up  3 │ │  12  │ │  71  │ │  40  │            │   flex-nowrap — NEVER wraps.
│ Settings   │  └──────┘ └──────┘ └─────┘ └──────┘ └──────┘ └──────┘ └──────┘            │   The 2 decision tiles LEAD + adjoin.
│            ├───────────────────────────────────────────────────────────────────────────┤
│            │  search…                            Sort: [1 Size ↓]        ⇅   ⛛   ⋮bulk │ ← control row (columns/sort per tab)
│            │  Pin (IPFS) ☑Pinned ☑Not pinned ☑Recommended ☑Undecided                    │ ← facet rail — TWO facets, never one
│            │  Git-ignore ☑Git-ignored ☑Not git-ignored ☑Recommended                     │   (tables.mdx §2.2/§2.4)
│            ├───────────────────────────────────────────────────────────────────────────┤
│            │  ☐ 📌 ⊘ ⌯ 👁 🔤  File               Size   Peers  Changed                   │ ← 5 leading icon cols
│            │  ☐ ● ● ● ○ ○   render/final_4k.mov  8.2GB  3      2h ago                 ⋮ │   Pin Ign Trn Desc OCR
│            │  ☐ ○ ○ ● ○ ○   render/take_07.mov   6.1GB  0      5m ago               ⚠ ⋮ │
│┌──────────┐│  ☐ ○ ● · · ·   scratch/proxy.mp4    220MB  0      1d ago                 ⋮ │ ← trailing ⋮ = row kebab (file catalog)
││hover-info││ ├──────────────────────────────────────────────────────────────────────────┤   ⌘/Ctrl/middle-click a row → new tab
││ panel    ││ │  173 files                                    page 1 / 1        [500 ▾]     │
│└──────────┘│ └──────────────────────────────────────────────────────────────────────────┘
│ JFK Social │ ← the hover explanation for a tile / row / icon lands in the LEFT BAR panel (§3.2),
│ ACT 3 Film │   the reserved white block above the external links and the account slot.
│ [account]  │
└────────────┴───────────────────────────────────────────────────────────────────────────┘
```

* The header's **‹ Repos** breadcrumb returns to the [Repos](./repos.mdx) list.
* **No warning banner on this page.** The **metrics-panel strip is the only warning surface**
  ([task_tabs.mdx §2.6](./task_tabs.mdx)): there is no `StatusBanner` / worst-first verdict banner between
  the header and the strip. Each condition a banner would have raised (undecided files, pull-down,
  not-backed-up, pending) reads off its own **narrow metric tile** — a small Title-Case label over a big
  number — and **clicking the tile opens that condition's educate-and-fix popup**
  ([task_tabs.mdx §2.4](./task_tabs.mdx), [warnings.mdx §4](./warnings.mdx)). **Every actionable tile opens a
  popup now — not just the decision tiles and Pull-down.** The task tiles (`Transcribable`, `AI Describable`,
  `Compressible Videos`, `Compressible Images`, `OCRable`) each got a `WarningDef` builder, so clicking one
  opens the scoped popup and **lets the user start that work** (transcribe / describe / compress / OCR)
  instead of the old no-op that merely re-selected the current tab
  ([task_tabs.mdx §2.4.1](./task_tabs.mdx#241-every-actionable-metric-tile-opens-a-popup--the-transcribe--compress--describe-builders-the-fix-locked)).
  **The strip takes the full page width and never wraps** — no region is docked beside it and nothing is
  reserved to its right; the explanation goes to the **left-bar hover-info panel** (§3.2,
  [task_tabs.mdx §3](./task_tabs.mdx#3-the-explanation-goes-to-the-left-bar-hover-info-panel-locked)).
  IPFS-down no longer raises a page banner here — it becomes the **top recommendation**, so the header
  primary shows a **red "View recommendation ›"** that opens the one-click *Start IPFS* popup (§3.1); it
  also stays reflected in the per-row pin toggles (§4.5.1).
* The files table is the same flat, chromeless TanStack `DataTable` house style as everywhere else.

---

## 3. Header + metrics strip

### 3.1 Header — TWO rows, and neither of them wraps (LOCKED)

The header is **two rows**, and the split is the point:

```
row 1 │ Our_Movies            ▦All ⌂IPFS ▤Compress ⌯Transcribe ⌗AI descriptions ⌄  ⚙  [View recommendation ›]
row 2 │ ~/BGit/Bryan_git/Movies/Our_Movies/2026/renders/final_delivery/master
        ▲ the absolute path — its OWN full-width row, ONE line, ellipsized, full text in the tooltip
```

* **Row 1 — the repo name + the actions slot.** The repo **name is the biggest, boldest text on the page**
  and never wraps; it truncates with the full name on hover. **Its sizing priority is inverted from the old
  layout:** the **title is sized to its content** (capped at **50%** of the row) and the **actions slot takes
  the remainder and shrinks first**. That ordering is what lets the tab strip **overflow into its chevron**
  ([task_tabs.mdx §1.4](./task_tabs.mdx#14-the-overflow-chevron--the-repo-name-never-loses-locked)) instead
  of crushing the repo name — with the old `flex-1` title the actions slot won every pixel it asked for and
  the name was the thing that shrank.
* **Row 2 — the absolute directory path, on its OWN full-width row (the fix).** It is deliberately **not**
  nested inside row 1's left column. **The bug:** the path was rendered as the header's `subtitle` *inside*
  the left column, sharing one flex row with a `shrink-0` actions slot (tabs + gear + primary); that squeezed
  it into a narrow box, so it **wrapped over several lines** while the right half of the row sat empty. **Now**
  it gets the full page width: **one line, ellipsized, full text in the `title` tooltip** — it never wraps.
* **Breadcrumb** — `‹ Repos` sits above row 1 and returns to the [Repos](./repos.mdx) list.
* **Task tabs** (in row 1's actions slot, a little **right of center**, between the name and the right-hand
  buttons) — the **All / IPFS / Compress / Transcribe / AI descriptions / OCR** strip — **six** tabs
  ([task_tabs.mdx §1](./task_tabs.mdx#1-the-tab-strip-locked)). **All** is the default. The strip is
  **width-measured**: whatever doesn't fit moves — always taking from the **right** — into a trailing
  **down-chevron overflow menu**, which renders **only** when something actually overflows
  ([task_tabs.mdx §1.4](./task_tabs.mdx#14-the-overflow-chevron--the-repo-name-never-loses-locked)).
  Selecting a tab re-tunes the **metrics strip**, the **visible table columns**, the **default sort**, and
  **which files the table shows** — all client-side, no refetch, no mutation. Everything below the header in
  §3.2 / §3.3 / §4 is presented *through* the active tab; the tab system, its metrics panels, and the three-state
  Transcribe / Compress / **Describe** status icons are defined in [task_tabs.mdx](./task_tabs.mdx).
* **Header primary — "View recommendation" / "Scan now"** (top-right, the SAME button on all six tabs).
  This one smart button replaces the old always-`Pin now` primary. It surfaces the single next thing to
  do, so a non-expert never has to guess which panel to open:
  * **"Scan now"** (leading `Search` icon) — shown **only** when the repo is **scan-stale**: it has never
    been scanned, or its `lastScanAt` is **older than 2 weeks**. A stale scan means the metrics can't be
    trusted, so re-scanning takes precedence over everything else. Clicking runs the same rescan worker;
    when the scan lands, `lastScanAt` refreshes and the button flips back to "View recommendation".
    When a repo's contents are known to have changed (the live watcher, scan.mdx §2.2), that also drives a
    fresh scan, after which the button returns to a recommendation.
  * **A pending recommendation** (no leading icon; a trailing right chevron `›`) — shown when a scan is
    **not** due and there is pending work. It opens the **most important** pending metric's educate-and-fix
    popup — the SAME popup its metric panel opens ([task_tabs.mdx §2.4](./task_tabs.mdx)) — resolved
    **worst-first across the full task set**:

    ```
    ipfs-down → pull-down → add-to-IPFS → transcribe → describe → compress → ocr
      (bad)      (bad)        (warn)        (warn)      (warn)     (warn)    (warn)
    ```

    (The third link is the **Add to IPFS** tile — the decision metric that used to be called *Undecided*,
    §3.2.2.)

    **The button's label names the recommendation** ([task_tabs.mdx §2.7](./task_tabs.mdx#27-the-per-metric-blue-header-primary--every-actionable-metric-can-be-the-recommendation-locked)):
    the three **integrity** conditions keep the generic **"View recommendation ›"** ("here's what's wrong —
    view it"), while the three **producing** tasks name the verb — **"Transcribe ›"**, **"Describe ›"**,
    **"Compress ›"** — so a non-expert reads the one action to take. Every one opens the identical popup its
    metric tile opens, and its Apply **starts the work** ([task_tabs.mdx §2.4.1](./task_tabs.mdx#241-every-actionable-metric-tile-opens-a-popup--the-transcribe--compress--describe-builders-the-fix-locked)).

    | Top pending recommendation | Label | Fill |
    | --- | --- | --- |
    | IPFS engine down / peer-pinned files not here | **View recommendation ›** | **red** (`--lfb-bad`) |
    | Files with no pin decision (the **Add to IPFS** tile) | **View recommendation ›** | **blue** (`--lfb-primary`) |
    | Audio/video with no transcript | **Transcribe ›** | **blue** (`--lfb-primary`) |
    | Image/video with no AI description | **Describe ›** | **blue** (`--lfb-primary`) |
    | Video/image that could be compressed | **Compress ›** | **blue** (`--lfb-primary`) |
    | Image/video/PDF whose text hasn't been read | **OCR ›** | **blue** (`--lfb-primary`) |

    The button tints **red** (`--lfb-bad`) only when the top recommendation is a Broken (`bad`) state
    (IPFS-down, pull-down), brand-**blue** (`--lfb-primary`) otherwise. A **"Transcribe ›"** / **"Describe ›"**
    primary shows whenever that work is pending even if the heavyweight model or AI key isn't ready — the
    **consent/provisioning gate fires when the popup's Apply runs**, then resumes (§3.1 gating mirrors the
    per-row status-icon path, [task_tabs.mdx §2.7](./task_tabs.mdx)).
  * **Neither** — when no scan is due and nothing is pending, there is **no** header primary: the metric
    panels (all green zeros) already say "all clear".

  Which button shows, and which popup it opens, is decided in code:
  `~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/metricWarningDefs.tsx` —
  `scanIsStale()` (the 2-week staleness gate) and `topRecommendation()` (the worst-first pick); wired into
  the header in `~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/OneRepoPage.tsx` —
  `OneRepoPage` (`scanDue` / `topRec`). `lastScanAt` is carried on `RepoDetail` from
  `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/store-model/units.service.ts` — `computeRepoDetail`.

* **Pin now** — no longer the header primary. It moves into the **page action-links row** (and stays in
  the `⋮` more menu). It still **prioritizes this repo but runs the full pin pass**: this repo is pinned
  first (push out every `sync`-decision file this machine has that peers lack, fetch every `sync`-decision
  file peers have that this machine lacks), then LFBridge continues the pass over every other known unit in
  the background ([pin_process.mdx §2/§3](./pin_process.mdx)). It never changes any decision, flips this
  repo's IPFS-pinning toggle on (§3.3), and must **never** be a silent no-op — its toast reports what
  happened, not a fixed "complete"
  ([pin_process.mdx §6](./pin_process.mdx#6-never-a-silent-no-op--report-truthfully-locked-learned-from-a-defect)).
  Handled in code: `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/repos/repos.router.ts` —
  `POST /api/repos/:repoId/pin` (calls `pinRepoFolder(folder, only, { manual: true })` then `pinAll({ priorityDone: folder })`).

* **Page action-links row** (directly under the page title — a visible horizontal row of blue text
  hyperlinks, each with a leading icon, [page_actions.mdx §0](./page_actions.mdx)) — the **page-action**
  surface that operates on this repo's **files** (the whole working tree, or the checked
  subset), as opposed to the `⋮` more menu which operates on the **repo entity**. It carries **Create
  Transcriptions · Create AI descriptions · Compress videos · Compress images · Git ignore · Pin now ·
  Rescan** ([page_actions.mdx §4](./page_actions.mdx)) — Pin now moved here from the header primary. The two
  producing actions (**Create Transcriptions** / **Create AI descriptions**) **open the unified batch-confirm
  popup** — the **same** "great pop-up" the `Transcribable` / `AI Describable` metric tile opens
  ([dialogs.mdx §5–§6](./dialogs.mdx), [task_tabs.mdx §5.1](./task_tabs.mdx)): a candidate list checked by
  default, uncheck, then a solid-blue **"Transcribe/Describe N files"** confirm. They still obey the LOCKED
  scope / skip-already-done / background-queue rules ([page_actions.mdx §1](./page_actions.mdx)) — with no
  rows checked they cover **all files recursively**, skipping already-done files — but the enqueue and the
  "{N} files will have their Transcriptions created" toast fire on the popup's **Confirm**
  ([page_actions.mdx §0.1](./page_actions.mdx)), never on the link's click and never as a `window.confirm`.
  Rendered by `PageActions` ([page_actions.mdx §3](./page_actions.mdx)).
* **The "more" menu** (top-right, next to the header primary) — on a single-entity page it renders as the
  word **More** plus a **down chevron**, with **no** leading `⋮` glyph and **no** bordered rounded rectangle
  ([menus.mdx §4.1](./menus.mdx) owns that presentation — this page does not restate it). Because this page
  **is one repo**, it carries the **repo entity menu**: the same catalog as a Repos-table row's kebab
  ([menus.mdx §4 / §5.1](./menus.mdx#4-the-entity--more-menu--single-entity-pages)) — Open in File
  System · View pinned on IPFS · Rescan · Pin now · Git ignore · Toggle Pinned · Repo
  settings… · Copy path · **Remove repo (unregister)** (destructive, last, never deletes local bytes).
  *Open repo* is omitted (you are already here). Pin now — no longer the header primary — appears in both
  the page action-links row and this **More ⌄** menu so the catalog stays complete.

The two-row header — the content-sized title that never loses, and the path on its own full-width line — is rendered in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/components/ui/PageHeader.tsx` — `PageHeader`
(row 1's title is `min-w-0 max-w-[50%] shrink truncate whitespace-nowrap`, so it is sized to its content
and capped at half the row while the actions slot shrinks first; row 2's path is
`mt-0.5 w-full truncate whitespace-nowrap` — its own full-width one-line row, ellipsized, with the full
text in the `title` tooltip).
The tab strip inside row 1's actions slot, and the overflow chevron that absorbs whatever does not fit, are rendered in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/TaskTabs.tsx` — `TaskTabs`.

### 3.2 The metrics strip never wraps — and the explanation lives in the left bar (LOCKED)

Beneath the header sits the **metrics-panel strip** — the active tab's "what could be done" tiles
([task_tabs.mdx §2](./task_tabs.mdx#2-the-metrics-panels--the-what-could-be-done-boxes-locked)). Two rules
govern how it occupies the page, and they replace the old width-budget arrangement:

* **The strip gets the FULL page width and is exactly ONE row.** The tiles are laid out `flex-nowrap` with
  every tile `shrink-0`, so **the strip can never break onto a second line**. On a pathologically narrow
  window the row **scrolls horizontally** rather than wrapping.
* **There is no docked hover-info region on this page any more.** The region that used to sit to the
  **right** of the tiles is **removed**. It reserved **≥25%** of the row width for prose, which is exactly
  what forced the tiles to flow onto a balanced second row; deleting the reservation deletes the wrapping.

**Where the explanation went — the left-bar hover-info panel.** The tiles stay deliberately terse (a label
over a number, no prose), so the words still have to live somewhere. They go to the **non-intrusive
hover-info panel at the BOTTOM OF THE LEFT BAR** — the reserved white block above the **JFK Social** /
**ACT 3 Filmmaking** external links and the account/login slot
([left_bar.mdx](./left_bar.mdx), [non_intrusive_tooltip.mdx](./non_intrusive_tooltip.mdx)).

* **Every hover source on this page still publishes the same way.** The metric tiles, the **File** cell, the
  **five icon control columns** (Pin · Ignore · Transcribe · AI Description · OCR, §4.3), and the **Compress**
  status icon all call the identical `setHoverInfo(text)` on enter and `setHoverInfo(null)` on leave — the
  publish API did not change, so no call site was rewritten.
* **One render-nothing component forwards it — mounted GLOBALLY in the app shell, not by this page.**
  `HoverInfoBridge` renders no markup and simply forwards the current text into the app-wide
  `HoverInfoProvider` that the left-bar panel reads. **It used to be mounted inside `MetricsStrip`**, which
  meant **only this page had a consumer**: every other table that uses the shared icon control columns
  ([tables.mdx §4c](./tables.mdx) — the IPFS pins table, the Storage detail table, …) published hover text
  into a store **nobody was listening to**, so hovering those icons explained nothing. Mounting the one
  bridge in the app shell makes the panel work on **every** page.
* **At rest the panel shows the active tab's `defaultHint`** — the per-tab one-liner declared in
  `TASK_TABS` ([task_tabs.mdx §7](./task_tabs.mdx)) — so the block is never a blank hole while the user's
  pointer is idle. **This at-rest hint is registered separately** from the bridge: `MetricsStrip` calls
  **`useHoverDefault(hint)`** for as long as it is mounted (and it clears on unmount, so stale text never
  lingers after navigating away). Two inputs — *hovered now* (`setHoverInfo`) and *at rest*
  (`setHoverDefault` / `useHoverDefault`) — one output panel.

The full-width, never-wrapping strip is rendered in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/MetricsStrip.tsx` — `MetricsStrip`
(the `flex w-full flex-nowrap … overflow-x-auto` row of `shrink-0` `MetricPanel`s).
The hover text is published and forwarded in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/HoverInfoRegion.tsx` —
`setHoverInfo()` (the module-level publish API), `setHoverDefault()` / `useHoverDefault()` (the at-rest
hint, called by `MetricsStrip`) and `HoverInfoBridge` (the render-nothing forwarder, mounted ONCE globally
in `~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/app/AppShell.tsx` — `AppShell`), into
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/components/hoverinfo/useHoverInfoPublisher.ts` —
`useHoverInfoPublisher` and rendered by
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/components/hoverinfo/HoverInfoPanel.tsx` —
`HoverInfoPanel` (the `"text"` block kind).

#### 3.2.1 Metric tile labels carry meaningful casing — and `Describable` is now `AI Describable` (LOCKED)

* **`Describable` → `AI Describable`** (both letters of **AI** upper-case, one space). The tile must say
  **who does the describing**, so it is never misread as *"files that have a description."* The paired
  done-tile is still **`Described`**. The **`MetricId` `describable` is unchanged** — this is a label
  change only.
* **Labels are no longer CSS-`capitalize`d.** The catalog stores **the exact string to render**, because
  labels like **"Add to IPFS"**, **"AI Describable"** and **"OCRable"** carry casing that a `capitalize`
  rule would fight.

Implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/metricWarnings.ts` —
`METRIC_CATALOG` (`describable.label`), rendered verbatim by
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/MetricsStrip.tsx` — `MetricPanel`.

#### 3.2.2 The Undecided tile is gone — two decision tiles, one per axis (LOCKED)

**Why it had to split.** A decision has **always been two independent axes** — **Add to IPFS** and
**git-ignore** ([decisions.mdx](./decisions.mdx)) — and the triage popup always wrote **both**. So a single
`Undecided` number could never tell the user **which axis was outstanding**. It is now two tiles, each with
its own count and its own scoped popup:

| Tile | `MetricId` | Counts | Opens |
| --- | --- | --- | --- |
| **Add to IPFS** | `addToIpfs` | Files with **no pin decision**, **excluding** rows whose bytes another IPFS tool already pinned on this node (`pinnedForeign`, §4.9). This is **exactly** the number the old `Undecided` tile showed. | `buildAddToIpfsWarning` — the renamed **two-axis triage popup** (its Apply still writes both axes; a big file usually wants both). |
| **Git Ignore** | `gitIgnore` | Large files **Git is still allowed to commit**: not already git-ignored, not owned by a rule we refuse to rewrite ([git_ignore.mdx §5.5](./git_ignore.mdx)), and not a small `analysisOnly` row. | `buildGitIgnoreWarning` — the **new** git-ignore popup. Its Apply writes **BOTH** axes, so flipping git-ignore **never clobbers an existing pin decision**. |

* **The old `bigNotIgnored` tile ("Big, not git-ignored") is FOLDED INTO "Git Ignore"** and **no longer
  exists as its own `MetricId`.** One question, one tile.
* **INVARIANT (LOCKED): the Git Ignore tile's count and its popup's target list come from ONE shared
  predicate — `gitIgnoreCandidates()`.** The number on the tile and the rows in the popup are therefore the
  **same set by construction**; they can never disagree. Any future surface that asks *"which files is Git
  still allowed to commit?"* calls that predicate rather than re-deriving the filter.
* **On the All tab the two decision tiles LEAD and sit ADJACENT** — they are two axes of **one** question,
  so they read as a pair. **On the IPFS tab only `Add to IPFS` appears** (that tab is the pin axis only,
  [task_tabs.mdx §1.3.1](./task_tabs.mdx)).
* **The worst-first header-recommendation chain now reads** `ipfs-down → pull-down → add-to-IPFS →
  transcribe → describe → compress → ocr` (§3.1, [task_tabs.mdx §2.7](./task_tabs.mdx)).
* **The FROZEN wire value `"undecided"` on the Decision enum is UNCHANGED.** Only the metric **tile** named
  *Undecided* is gone. Nobody may "clean up" `decision: "undecided"`, the Pin (IPFS) facet's **Undecided**
  value, or the `Decided by` facet's **Undecided** value — those are the data model (§1,
  [decisions.mdx §7](./decisions.mdx)), not the tile.
* **The compact summary line under the strip is also unchanged** — it still reads
  `N Pin · N Undecided · N Ignore · N Pinned locally` (§4.1).

Implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/metricWarnings.ts` — `MetricId`,
`METRIC_CATALOG`, `metricCount()` and `gitIgnoreCandidates()` (the shared predicate);
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/metricWarningDefs.tsx` —
`buildAddToIpfsWarning()`, `buildGitIgnoreWarning()`, `buildMetricWarning()`, `topRecommendation()`; the
per-tab tile lists in
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/taskTabs.config.ts` — `TASK_TABS`.

### 3.3 Status / settings strip

A single horizontal strip of the repo's live status (read) and its one per-repo setting:

| Field | Meaning |
| --- | --- |
| **Pin** (toggle) | This repo's **IPFS-pinning opt-in** — *not* a "managed vs. unmanaged" switch. When **off**, LFBridge still **knows, visits, and reconciles** this repo every pass ([pin_process.mdx §1/§5](./pin_process.mdx#1-lfb-knows-every-repo--there-is-no-lfb-managed-gate-locked)); it just does not add/pin/fetch its bytes during **background** pin passes (a manual **Pin now** flips this on). Decisions are preserved regardless. The only editable control in the strip. |
| **Status** | The rolled-up `RepoStatus` pill ([Repos §4.2](./repos.mdx)) — up to date / pinning / behind / needs review / error / never. |
| **Peers** | Count of your computers currently pinning this repo → clicking opens [Peers](./left_bar.mdx) filtered to this repo (peers page is out of scope here). |
| **Last pin** | Relative time of the last completed pin pass (absolute on hover). |
| **IPFS** | The local IPFS node health — `ok` (green) / `unreachable` (red). If unreachable, it becomes the top recommendation: the header primary is a **red "View recommendation ›"** opening the *Start IPFS* popup (§3.1), and the action-links row's **Pin now** is disabled with a tooltip. |

Note: the app-wide **background pin process** (installed? on/off? the every-15-min `launchd`
schedule) is the **Scans** left-bar page, not this strip. This strip's toggle is only *this repo's*
participation in it.

The repo's live status/settings strip values are computed by the backend in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/store-model/units.service.ts` — `computeRepoDetail`

---

## 4. The files table (TanStack, house style)

**One row per large git-ignored file** in the repo. Same flat `DataTable` and the same standard
control row as [Repos §3](./repos.mdx) — including **header-click sort** (click a column header to
sort; click again to toggle asc ↔ desc; [Repos §3.1](./repos.mdx)).

**Height — bounded (the content-below exception).** Unlike Repos, this page renders content **beneath**
the files table — the **Repo details** disclosure (§3.3 status strip, now a click-away disclosure at
the bottom of the page). Because something sits **below** the table, it is the **exception** to the
site-wide full-page-height rule ([Repos §3.3.1](./repos.mdx), [charter Tables rule](../CLAUDE.md)): the
table keeps a **bounded** height (it does **not** expand to fill the viewport) so the Repo-details
disclosure below it stays visible. In code this is `fillHeight={false}` on the `DataTable`.

### 4.1 Summary counts (above the control row)

A one-line summary the user reads first: **`128 Pin · 5 Undecided · 40 Ignore`** — `Pin` is the compact
label for the **Add to IPFS (pin)** decision (stored value `sync`). `Undecided` is accent-highlighted
whenever it is > 0.

> **These counts exclude `analysisOnly` rows.** The small, not-git-ignored media the scan surfaces purely
> so they can be OCR'd / described / transcribed (rule 5, [scan.mdx §4.1](./scan.mdx)) are **not** large-file
> decisions the user owes — they carry no Pin/Undecided/Ignore obligation. Counting a folder of thumbnails
> as *"Undecided"* would make every repo look like it has hundreds of pending decisions. So the summary
> counts, the repos-list counts ([repos.mdx §4.1](./repos.mdx)), and the large-file scan totals all count
> only the non-`analysisOnly` candidates. The `analysisOnly` rows appear only when **Large files only** is
> turned off (§4.2), and only the analysis metrics (`OCRable`, `AI Describable`, `Transcribable`) count
> them. The **Git Ignore** tile excludes them too — a thumbnail is never a "bad idea to check in" (§3.2.2).

> **These counts also exclude foreign-pinned rows from `Undecided` — the 4th fix in the pin-truth saga
> (2026-07-19).** An undecided file whose bytes are **already pinned on this node** under a foreign CID
> (`FileRow.pinnedForeign`, the green state — §4.9) must **not** be counted as `Undecided`: that tile and
> its triage popup are a *"pin these?"* ask, and re-asking it about a pinned file is exactly the "app says
> not pinned, CLI says pinned" defect again (a `charlie-kirk` repo showed **76 Undecided** of which **69**
> were already pinned). Those rows are counted apart as `RepoCounts.pinnedForeign` and the summary line
> appends **`· 69 Pinned locally`** (green) when > 0, so they never silently vanish from the readout —
> "pinned" means pinned on THIS computer by ANY local software ([ipfs.mdx §1.1](./ipfs.mdx)). The same
> exclusion applies to the **`Add to IPFS`** metric tile — the tile that replaced `Undecided` and counts
> exactly this number (§3.2.2, [task_tabs.mdx §2.5](./task_tabs.mdx)) —
> the triage popup's target list, the repos-list `Undecided` column, the `needs_review` rollup, and the
> to-do batch engine's `pin` recommendation (it recommends only the still-owed git-ignore axis for them).
> Counted in code:
> `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/store-model/units.service.ts` —
> `countDecisions` and `computeTaskMetrics` (popup filter mirrored in
> `~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/metricWarningDefs.tsx` —
> `buildAddToIpfsWarning` — the renamed two-axis triage popup, §3.2.2; batch seam in
> `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/todo/todo-batch.engine.ts`).

**Every segment is a quick filter on the Pin (IPFS) facet — and only that facet.** All three counts read
the **IPFS axis** ([tables.mdx §2.4.1](./tables.mdx#241-the-two-facets-fixed-vocabulary-locked)); none of
them says anything about git-ignore. Clicking a segment **sets the Pin (IPFS) facet to that one value** —
it unchecks every other Pin value and leaves the **Git-ignore** facet untouched (all-checked). Clicking the
active segment again re-checks the facet's values (back to the resting all-checked state,
[tables.mdx §2.1](./tables.mdx#21-a-facet-is-its-values-each-with-a-checkbox-all-checked-by-default-locked)):

| Segment | Sets the **Pin (IPFS)** facet to | Leaves **Git-ignore** |
| --- | --- | --- |
| `128 Pin` | ☑ Pinned only (uncheck Not pinned · Recommended · Undecided) | all-checked (untouched) |
| `5 Undecided` | ☑ Undecided only — the triage queue | all-checked (untouched) |
| `40 Ignore` | ☑ Not pinned only | all-checked (untouched) |

Because the segments drive the **rail facet** and not a private filter, the result is **visible**: after a
click the user sees which Pin chips went dark and can re-check them without hunting for a reset
([tables.mdx §2.2](./tables.mdx#22-the-facet-rail--filtering-without-opening-a-dropdown-locked)).

> **Recommendation — rename the third segment `Ignore` → `Not pinned`.** This label is the product's
> single live conflation risk: on this line `Ignore` means **"deliberately not pinned"** (the `ignore`
> wire value on the **IPFS** axis), while three columns to the right the **Ignore** toggle column means
> **git-ignored** — a different axis with a different source of truth
> ([tables.mdx §2.4](./tables.mdx#24-one-facet--one-axis--pin-and-git-ignore-are-never-the-same-filter-locked)).
> One word, two meanings, one screen. Reading `128 Pin · 5 Undecided · 40 Not pinned` makes the line
> self-evidently one axis, and it matches the **Pin (IPFS)** facet's own value label verbatim (§2.4.1), so
> the segment and the chip it drives read the same word. The stored value stays `ignore` — **frozen wire
> value**, never renamed ([decisions.mdx §7](./decisions.mdx#7-reconciliation--folding-the-shared-ledger-onto-this-computer-locked)).

> **The richer, tab-scoped surface is the metrics-panel strip** ([task_tabs.mdx §2](./task_tabs.mdx#2-the-metrics-panels--the-what-could-be-done-boxes-locked)),
> which sits above the control row and shows the **active tab's** "what could be done" panels (each a terse
> label + number, **no chevron** — the whole tile is the affordance; a light-green big-`0` when nothing is
> to be done). It runs the **full page width on one row and never wraps**, and its explanation text goes to
> the **left-bar hover-info panel** (§3.2, [task_tabs.mdx §3](./task_tabs.mdx#3-the-explanation-goes-to-the-left-bar-hover-info-panel-locked)).
> This one-line summary is the compact IPFS-decision readout; the metrics strip is the per-task overview.
> Note the deliberate overlap: the strip's **Add to IPFS** tile counts the same files this line calls
> **Undecided** (§3.2.2) — the tile is named for the axis, the line for the state.

### 4.2 Control row — LOCKED

Identical to the standard toolbar ([Repos §3.1](./repos.mdx)), following the shared grammar in
[tables.mdx §2–§3](./tables.mdx#2-filtering--the-value-visibility-checkbox-model-locked) — this page does
not restate it:

* **Search box** (left) — substring, case-insensitive, over the **file path**.
* **Facet rail** (directly beneath the control row, **always visible**) — the table's facets as labelled
  toggle-chip groups, **every value checked by default** = inactive
  ([tables.mdx §2.2](./tables.mdx#22-the-facet-rail--filtering-without-opening-a-dropdown-locked)). This
  table's facets are **Pin (IPFS)** and **Git-ignore** — **two facets, never one, and neither is named
  "Decision"** ([tables.mdx §2.4](./tables.mdx#24-one-facet--one-axis--pin-and-git-ignore-are-never-the-same-filter-locked))
  — and the two **promoted rail controls** every file table carries: the
  **`☑ Large files only`** toggle (default ON — hides the small `analysisOnly` media the scan surfaces;
  turn it off to reach a small checked-in JPG or short clip — [tables.mdx §2.9](./tables.mdx)) and the
  **File type** facet (`☑ Images ☑ Videos ☑ Audio ☑ PDFs ☐ Other` — [tables.mdx §2.10](./tables.mdx)). The
  `128 Pin · 5 Undecided · 40 Ignore` segments (§4.1) drive the Pin (IPFS) facet's chips.
  ```
  ☑ Large files only   ·   File type ☑ Images ☑ Videos ☑ Audio ☑ PDFs ☐ Other
  Pin (IPFS)  ☑ Pinned ☑ Not pinned ☑ Recommended ☑ Undecided   ·   Git-ignore  ☑ Git-ignored ☑ Not git-ignored ☑ Recommended
  ```
* **Filter icon** (`⛛`, icon-only, no box) — **the long tail only**, not the place filters live
  ([tables.mdx §2.3](./tables.mdx)): free-text `contains`
  (**File** path, **CID**), numeric thresholds (`Size ≥`, `Peers ≥`), and any facet that overflowed the
  rail at narrow widths. Colored whenever **any** filter — rail facets included — is active; resets to
  page 1.
* **Sort icon** (icon-only, no box) — the multi-level sort **editor**: sortable columns with asc/desc on
  the left, `1st`/`2nd`/`3rd` priority slots on the right, and **Clear sort**
  ([tables.mdx §3.1–§3.2](./tables.mdx#31-the-sort-dropdown-ui-locked)).
* **Sort chip run** (right of the control row, beside the sort icon) — the **active** sort, readable
  without opening anything: `Sort: [1 Size ↓] [2 File ↑]`. Chips toggle direction, remove a key, and
  drag to re-rank; **shift-click a header appends** a key
  ([tables.mdx §3.5](./tables.mdx#35-sorting-without-opening-the-dropdown--the-active-sort-chips-locked)).
  **Pin** and **Ignore** are two separate sort keys — there is no combined "Decision" key
  ([tables.mdx §3.6](./tables.mdx#36-sort-obeys-the-axis-split-too-locked)); their order is
  **On → Recommended → Off → N/A**.
* **Bulk menu** (`⋮`) — enabled when ≥1 row is checkbox-selected (§4.4).

### 4.3 Columns

The table leads with the **five icon control columns** ([tables.mdx §4c](./tables.mdx)) — **Pin · Ignore ·
Transcribe · AI Description · OCR** — immediately right of the select checkbox, then the **File** identity
column and the remaining data columns. The old **Pin decision** select, the **Decided by** / **Decided**
provenance columns, and the transfer **Status** column are **removed** (§4.3.1).

| # | Column | Kind | Sort | Filter | Notes |
| --- | --- | --- | --- | --- | --- |
| 0 | **☐ select** | — | — | — | Row checkbox; header checkbox selects the current page (for bulk decisions). |
| 1 | **Pin** *(icon)* | StatusActionIcon | ✓ | ✓ **Pin (IPFS)** facet | The **Add-to-IPFS decision toggle** ([decision_toggles.mdx](./decision_toggles.mdx)) rendered as the first leading icon column — **solid BLUE (`--lfb-pin`)** = pinned; dashed "could" edge = the [To-Do calc engine](./to_do_batch_calc_engine.mdx) recommends pinning; white/pen = off. Icon-only header; hover explains *"what an IPFS pin is."* Writes the `sync` axis on click (§4.5.1). **Carries the IPFS axis's one facet + one sort key** ([tables.mdx §2.4.1](./tables.mdx#241-the-two-facets-fixed-vocabulary-locked)). |
| 2 | **Ignore** *(icon)* | StatusActionIcon | ✓ | ✓ **Git-ignore** facet | The **Add-to-git-ignore decision toggle** (⊘, [decision_toggles.mdx](./decision_toggles.mdx)) — **solid ORANGE (`--lfb-decision-on`)** = git-ignored; dashed = recommended; white/pen = off. Icon-only header; hover explains *"what git-ignore does."* Repo-scoped (always present here). Writes the git-ignore axis on click (§4.5.1). **Carries the git-ignore axis's one facet + one sort key**, sourced from `git check-ignore`, never the ledger ([decisions.mdx §1.1](./decisions.mdx#11-this-ledger-records-intent--git-records-the-git-ignore-state-locked)). |
| 3 | **Transcribe** *(icon)* | StatusActionIcon | ✓ | ✓ | Three-state transcript status ([task_tabs.mdx §5](./task_tabs.mdx)) — **solid INDIGO (`--lfb-transcribe-done`)** = has transcript; "could" = audio/video with none (click to transcribe); n/a = not audio/video. Icon-only header; hover explains transcription. Present on **every** tab now, not only Transcribe. |
| 4 | **AI Description** *(icon)* | StatusActionIcon | ✓ | ✓ | Three-state describe status ([task_tabs.mdx §5.2](./task_tabs.mdx)) — **solid TEAL (`--lfb-describe-done`)** = described; "could" = image/video with none; n/a otherwise. Icon-only header; hover explains AI description. Present on every tab. |
| 5 | **OCR** *(icon)* | StatusActionIcon | ✓ | ✓ | Three-state OCR status ([ocr.mdx §11.2](./ocr.mdx)) — **solid VIOLET (`--lfb-ocr-done`)** = text read; "could" = image/video/PDF not yet read; n/a otherwise. Icon-only header; hover explains OCR. Present on every tab. |
| 6 | **File** | text | ✓ | ✓ (contains) | Path relative to the repo root; directory prefix muted, filename bold. |
| 7 | **Size** | int (bytes) | ✓ | ✓ | Human-readable (`8.2 GB`), right-aligned; sorts/filters on raw bytes. |
| 8 | **Peers** | int | ✓ | ✓ | How many of your computers hold this file. `0` on an **Add-to-IPFS (pin)** file renders red (not backed up yet). |
| 9 | **CID** | text | ✓ | ✓ | IPFS content id (monospace, middle-truncated, click to copy); blank until the file is an Add-to-IPFS (pin) file with a CID. |
| 10 | **Changed** | timestamp | ✓ | ✓ | `changedAt`, relative with absolute tooltip. |
| — | **`⋮`** | — | — | — | The row **kebab** — the **file** action catalog ([menus.mdx §5.3](./menus.mdx#53-file)). A control cell: opens the menu, never navigates. Right-click on the row opens the same menu ([menus.mdx §3.1](./menus.mdx#31-right-click-opens-the-same-menu-locked)). |

The five icon columns are **narrow with minimal padding** (the DataTable `tight` flag → `px-1`), each with
an **icon-only header** (a plain-text label is kept only for the Sort/Filter/Columns dropdowns), and
hovering **the header icon or any cell icon** both shows a `title` tooltip and fills the **left-bar
hover-info panel** (§3.2) with a plain-English explanation ([tables.mdx §4c.2–§4c.3](./tables.mdx)). All five share the one
**`StatusActionIcon`** grammar with **unique done colors** (Pin blue, Ignore orange, Transcribe indigo, AI
Description teal, OCR violet — [tables.mdx §4c.4–§4c.5](./tables.mdx)).

#### 4.3.1 Removed columns — the icons replace the old decision + status UI (LOCKED)

The Pin and Ignore icon cells **fully replace** the old spread-out decision UI, so these columns are **gone
from the file table**:

* **Pin decision** (the inline `[Pin ▾]` / old *Decision* select) — retired. The **Pin** icon (col 1) is
  now the per-row IPFS-axis control; setting the axis is a click on the icon, not a select. (A labelled
  one-of-N editor still lives off-table in the file kebab's *Set pin ▸* submenu, [menus.mdx §5.3](./menus.mdx).)
* **Decided by** and **Decided (when)** — the provenance **columns** are removed; *who decided and when*
  now surfaces **on hover** (the left-bar hover-info panel, §3.2, and the file page, §4.8).
* **Status** (the transfer pill) — dropped from the file table; the **Pin** icon's state already tells the
  user whether a file is pinned, and the richer transfer detail lives on the file's View-one-file page and
  the IPFS pins table ([ipfs_ui.mdx](./ipfs_ui.mdx)).

**The two decision axes still exist as data** ([decisions.mdx](./decisions.mdx)) and are still **set via the
Pin / Ignore icons** — only the extra *columns* are retired, nothing else. The File System page keeps its
own layout and is the one file table **without** this icon block ([tables.mdx §4c](./tables.mdx)).

Pagination: **default 500** ([Repos §3.3](./repos.mdx)); the **file count** shows left of the pager.

Each table row (one `FileRow` per discovered candidate, joined with its decision + manifest CID) is composed by the backend and served to this table in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/store-model/units.service.ts` — `composeFileRows` (served via `reposRouter` → `GET /api/repos/:repoId/files`)

The trailing per-row file kebab is rendered in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/OneRepoPage.tsx` — `OneRepoPage` `rowMenu` (mounts `EntityKebab`)

### 4.7 Row / cell navigation (per file) — LOCKED

**Clicking a file row opens that file's "View one file" experience.** The whole row is a deep link —
and the **File** column especially — so a single click on `render/final_4k.mov` takes the user straight
to the page for that one file. Only the interactive control cells opt out (the five leading icon columns —
**Pin** / **Ignore** flip the two decision axes in place, and **Transcribe** / **AI Description** / **OCR**
queue-or-view their task in place; the **CID** cell copies; the **`⋮`** kebab opens the menu). Each of those
stops click propagation so it never also fires the row navigation.

**Where a row click lands depends on the file's kind** — the same media-routing rule the File System
browser and the entity menus use ([media_viewer.mdx §1](./media_viewer.mdx), [menus.mdx §2](./menus.mdx#2-cell-navigation--clicking-a-cell-is-a-deep-link)):

| File kind | Route opened | Page |
| --- | --- | --- |
| **image** (`.png .jpg .webp .gif .heic .avif …`) | `/image?path=<abs>` | the **image viewer** ([media_viewer.mdx](./media_viewer.mdx)) |
| **video** (`.mp4 .mov .mkv .webm .m4v …`) | `/video?path=<abs>` | the **video viewer** ([media_viewer.mdx](./media_viewer.mdx)) |
| **audio** (`.mp3 .wav .flac .m4a .aac .ogg .opus …`) | `/audio?path=<abs>` | the **audio viewer** ([media_viewer.mdx](./media_viewer.mdx)) |
| anything else | `/file?path=<abs>` | the properties-first **View one file** ([files.mdx](./files.mdx)) |

The `path` passed is the file's **absolute** path (`<repo root>/<relative path>`), not the repo-relative
one, because every single-entity page keys off the absolute path.

Per-cell targets (all resolve to the row target above unless noted):

| Cell | Click target |
| --- | --- |
| **File** | The file's **View one file** experience (viewer for media, properties otherwise). |
| **Size / Changed** | Same as the row — the file's View one file page. |
| **Pin / Ignore** | Inline controls — the two axis toggles flip in place; neither navigates (§4.5.1); each stops propagation. |
| **Transcribe / AI Description / OCR** | Inline status icons — "could" queues the task (background job), "done" views the output, n/a is inert; none navigates; each stops propagation ([task_tabs.mdx §5](./task_tabs.mdx)). |
| **Peers** | Same as the row (peers detail lives on the file page / [Peers](./left_bar.mdx)). |
| **CID** | **Copies** the CID (click-to-copy); does not navigate away; stops propagation. |
| **`⋮`** | Opens the **file action menu** — never navigates (§4.3); stops propagation. |

**⌘-click / Ctrl-click / shift-click / middle-click opens the row in a NEW TAB (LOCKED).** A row navigates
via **JS**, not an `<a href>`, so those modified clicks used to be **swallowed** and navigate **in place** —
the opposite of what every other link on the web does. The house `DataTable` now takes an optional
**`rowHref(row)`**, and this table supplies it: **the same viewer route the row click uses**, as a URL. With
it present, ⌘/Ctrl-click, shift-click and middle-click open that URL in a **new tab/window**, while a plain
click still routes **in-page** through `onRowClick` — so routing and view state are unchanged. The rule
itself is owned by **[tables.mdx §4d](./tables.mdx)**; this page is one of its adopters. (It sits at §4d,
not §3.6, because §3.6 already means "Sort obeys the axis split too" and other specs cite that anchor.)

Implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/components/table/DataTable.tsx` — `DataTable`
(the `rowHref` prop; the row's `onClick` modifier branches and its `onAuxClick` middle-click branch), and
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/OneRepoPage.tsx` — `OneRepoPage`
(`rowHref`, building `<viewerRouteForName(name)>?path=<abs>`).

Row-click navigation — routing each file to its viewer (`/image` · `/video` · `/audio`) or the
properties page (`/file`) by kind, on the file's absolute path — is handled in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/OneRepoPage.tsx` — `OneRepoPage` `onRowClick` (uses `viewerRouteForName` from `@lfb/shared`)

The **File** row's kebab (and right-click) is the **file** catalog ([menus.mdx §5.3](./menus.mdx#53-file)):
View file · Open containing folder · Add to IPFS / Remove from IPFS · Set pin ▸ · Pin now ·
Compress… · Convert to compressible… · Git ignore · Find duplicates (fingerprint) · the two
sticky flags **Never IPFS** / **Do not compress** ([menus.mdx §6.6](./menus.mdx#66-the-two-sticky-flags--never-ipfs--do-not-compress-locked)) ·
View on IPFS · Copy CID / path · **Stop pinning (untrack/unpin)** (destructive, never deletes local
bytes). Compress/convert/ignore are **explicit-click offers with a confirm step**
([menus.mdx §6.1](./menus.mdx#61-never-act-on-files-without-an-explicit-click)).

### 4.4 Bulk actions

When rows are selected, the `⋮` bulk menu applies one decision to all of them at once — the fast
path for triaging many files:

* **Set to Add to IPFS (pin)** · **Set to Ignore** · **Set to Undecided** — bulk-writes the decision
  (the `sync`/`ignore`/`undecided` value).
* **Pin now (selected)** — queues transfers for the selected `sync`-decision files.

Selecting the header checkbox selects the whole current page; a "Select all N matching" affordance
appears when a filter is active, to act on the entire filtered set, not just the page.

Bulk-writing one decision across the selected rows is handled in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/OneRepoPage.tsx` — `OneRepoPage` bulk menu (mutated via `setDecision`)

### 4.5.1 The two decision-toggle columns — Pin + Ignore (LOCKED)

Every row shows the **two-axis decision as two inline toggle-checkboxes** — a **Pin** toggle (Add to IPFS)
and, to its right, an **Ignore** toggle (⊘, Add to git ignore). They are the always-visible, one-click way
to set each axis; the full visual grammar (four render states — OFF white/grey-edge · ON solid-orange ·
**RECOMMENDED** dashed-orange · N/A) and the single-axis write behavior are owned by
**[decision_toggles.mdx](./decision_toggles.mdx)**. Here on the One-repo table:

* Both toggles render on **every** row — the Ignore toggle is never N/A here, because every file in this
  table is under **this** repo ([decision_toggles.mdx §4](./decision_toggles.mdx#4-the-git-ignore-toggle-is-repo-scoped-locked)).
* A **RECOMMENDED** (dashed) toggle marks an **Undecided** file the [To-Do Batch Calc Engine](./to_do_batch_calc_engine.mdx)
  thinks should be pinned / ignored — one click confirms it.
* Clicking a toggle writes **that axis only** to the `decisions.yaml` ledger — which lives in **Local
  Storage** at `~/T/_large_files_bridge/repos/<repoKey>/` (and travels via the company sync repo when
  enabled), **never** in the working repo ([decisions.mdx §3](./decisions.mdx),
  [artifact_placement_policy.mdx](./artifact_placement_policy.mdx)) — via the same `setDecision` mutation the inline **Decision** select
  uses (§4.5), and carries out that axis's effect (pin/unpin; `.gitignore` write via [git_ignore.mdx](./git_ignore.mdx)).
  It never deletes local bytes and it stops click propagation (never navigates).

The Pin/Ignore toggle columns are rendered in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/OneRepoPage.tsx` — `OneRepoPage` (the `pin` and `ignore` `LfbColumn`s mounting `DecisionToggle`; write via `setDecision`).

### 4.5 The Decision select (per row) — SUPERSEDED by the Pin icon

> **The inline Decision/`Pin decision` select column is removed from the table (§4.3.1).** The per-row core
> interaction for the IPFS axis is now a click on the **Pin** icon (col 1, §4.5.1) — the icon cell writes
> the same three-state IPFS-axis decision the select used to. A labelled one-of-N editor still exists
> off-table in the file kebab's *Set pin ▸* submenu ([menus.mdx §5.3](./menus.mdx)). The state-transition
> semantics below are unchanged — they now fire from the Pin icon (and its "could"/recommended click)
> rather than a select.

Setting a row's IPFS-axis decision (via the Pin icon, or the off-table *Set pin ▸* editor) moves through
the same three states of the **IPFS axis only**. Its **Not pinned** state means *"deliberately not
pinned"* — **not** git-ignored, which is the separate **Ignore** icon (§4.5.1). Writing is immediate
(optimistic, with rollback on error):

* → **Add to IPFS (pin)** (decision value `sync`): LFBridge adds the file to the manifest,
  computes/records its **CID**, pins it, and queues transfers to peers. The row's Status moves to
  `pending` → `fetching`/`pushing` → `pinned`.
* → **Ignore**: removes it from the manifest (if present); Status becomes `—`; bytes stay local only.
* → **Undecided**: parks it back in the triage queue; no bytes move.

Changing a decision never deletes the user's local file — it only changes what LFBridge *does* with
it.

Writing a per-file decision (single or bulk) to the repo's `config.yaml` is handled in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/repos/repos.router.ts` — `reposRouter` → `PATCH /api/repos/:repoId/files`

### 4.6 Transfer status pill (LOCKED)

> **No longer a column in this table (§4.3.1).** The transfer **Status** column is **removed from the
> One-repo file table** — the **Pin** icon (col 1) already tells the user whether a file is pinned, so a
> separate pill column was redundant. The `transfer` value and this pill mapping still exist and still
> render on the file's **View-one-file** page and on the **IPFS pins table** ([ipfs_ui.mdx](./ipfs_ui.mdx));
> only the file-table *column* is gone. The mapping below governs those surviving surfaces.

| `transfer` | Meaning | Pill |
| --- | --- | --- |
| `pinned` | Present locally and pinned on ≥1 peer. | green |
| `pending` | Queued to push out or fetch in. | amber |
| `fetching` / `pushing` | Transfer in progress. | blue (accent), animated |
| `missing` | In the manifest but bytes not on this machine (fetch to restore). | red outline |
| `error` | Last transfer failed. | red |
| `na` | Decision is Ignore/Undecided — nothing to transfer. | `—`, muted |

Each file's `transfer` value (pinned / pending / na, derived from its decision, CID, and peer pins) is computed by the backend in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/store-model/units.service.ts` — `transferFor` (called by `composeFileRows`)

### 4.8 Decision provenance — who decided, and when (team-aware)

Because decisions are now a **team-shared, attributed** record ([decisions.mdx §3/§10](./decisions.mdx)),
this screen surfaces *who* decided each file and *when* — a decision the team can't inspect is barely
better than none:

* **Hover the Pin or Ignore icon** (the two leading decision icons, §4.3 — the old standalone *Decision*
  cell is gone, §4.3.1) → a non-intrusive tooltip ([non_intrusive_tooltip.mdx](./non_intrusive_tooltip.mdx)),
  also published to the left-bar hover-info panel (§3.2): *"Add to IPFS + git-ignored · decided by bob@acme.com ·
  Jul 12, 2:32 PM"*. A small person/clock glyph marks a file decided by **someone else on the team** (vs.
  you), so others' choices are visible at a glance. **Provenance moved from its own columns to the icon
  hover** — the *Decided by* and *Decided* columns are removed (§4.3.1), but the *who/when* data is intact
  and surfaces here and on the file page.
* **Filter/sort by decider — its own `Decided by` facet, a THIRD independent thing.** Provenance is
  neither the pin axis nor the git-ignore axis: *who* chose is orthogonal to *what* they chose (a
  teammate can have decided either axis, both, or — via the default decision policy
  ([decisions.mdx §9](./decisions.mdx#9-default-decision-policy--deciding-new-files-for-the-team-locked)) —
  neither by hand). So it
  gets its **own facet on the rail**, never a values-list bolted onto a pin/decision filter, which is
  exactly the merge [tables.mdx §2.4](./tables.mdx#24-one-facet--one-axis--pin-and-git-ignore-are-never-the-same-filter-locked)
  forbids:

  ```
  Decided by   ☑ Me   ☑ Teammate   ☑ Policy   ☑ Undecided
  ```

  All four checked by default = inactive; the values are a checkbox set because *"my decisions **and**
  the policy's"* is an ordinary ask a one-of-N select cannot express
  ([tables.mdx §2.1](./tables.mdx#21-a-facet-is-its-values-each-with-a-checkbox-all-checked-by-default-locked)).
  It **ANDs** with **Pin (IPFS)** and **Git-ignore** — *"undecided files a teammate hasn't touched"* is
  the two facets working together, not one facet doing two jobs. Its source is the folded ledger's
  `decided_by` ([decisions.mdx §3.3](./decisions.mdx#33-decided_by--attributed-never-anonymous)), a third
  source of truth alongside the ledger's IPFS axis and live `git check-ignore` — three axes, three facets.
  `Undecided` appears here because a file with **no** decision record has no decider; it is the facet's
  own no-provenance value, and it is **not** redundant with the Pin facet's `Undecided` (a file can be
  pin-undecided yet git-ignored by a hand-written rule, with no decider at all).
* **Sort by decided-at** — a normal sort key on `decidedAt` (most recently triaged first), taking a
  priority slot like any other ([tables.mdx §3.1](./tables.mdx#31-the-sort-dropdown-ui-locked)). It is a
  timestamp, not an axis, so it needs no facet.
* **Overrides are visible, not silent** — when a teammate's pulled-in decision changes an axis on a file
  this machine already acted on, the change shows immediately in the provenance and a **quiet, dismissible**
  dock note summarizes it ([decisions.mdx §11](./decisions.mdx)); it never deletes local or pinned bytes.

The per-row `decidedBy` / `decidedAt` (joined from the folded shared ledger) that this tooltip and filter read are added to each `FileRow` by the backend in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/store-model/units.service.ts` — `composeFileRows` (joins `foldLedger()` from `storage/decisions.service.ts`); rendered in `~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/OneRepoPage.tsx`.

The **Decided by** facet itself is a **new** surface (the rail has no provenance facet today); it is to be implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/OneRepoPage.tsx` — `OneRepoPage` (a `decidedBy` `LfbColumn` facet over `FileRow.decidedBy`, rendered on the shared facet rail in `~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/components/table/DataTable.tsx` — `DataTable`).

### 4.9 The Pin icon is THREE-STATE — decision × live pin reality (LOCKED)

The **Pin** icon (col 1) conveys **two facts at once**: the user's **decision** (do I want this file synced
over IPFS?) and the **reality on THIS computer right now** (is it actually pinned here?). A single on/off
toggle can only show intent; but a user syncing across three machines needs to see, per machine, whether a
file they *chose* to sync has actually **arrived and pinned here yet**. So the icon has **three states**
(the visual grammar — bare glyph, no rounded rectangle — is [tables.mdx §4c.4/§4c.4a](./tables.mdx)):

| State | Glyph | Condition | What it tells the user |
| --- | --- | --- | --- |
| **Not set to sync** | grey **outline** pin ("white background") | `decision !== "sync"` | This file is **not** being synced over IPFS. **Click to add it.** |
| **Synced here** | **blue filled** pin ("dark-blue fill") | `decision === "sync"` **and** `pinnedHere === true` | Chosen to sync **and** this computer's IPFS node actually holds the pin — in sync here. |
| **Set to sync, not here yet** | **RED filled** pin ("red fill") | `decision === "sync"` **and** `pinnedHere === false` | You chose to sync it, but **this** computer does not have it pinned yet. The pin pass will **pull it down**; the red flags the transient out-of-sync gap ([pin_process.mdx §2](./pin_process.mdx)). |
| **Pinned locally** (outside LFB) | **GREEN filled** pin | `decision !== "sync"` **and** `pinnedForeign === true` | The bytes are **already pinned on this node** under a foreign CID (a bare `ipfs add`, IPFS Desktop, another tool — **we don't care which local software pinned it**, [ipfs.mdx §1.1](./ipfs.mdx)). Discovered by the background pass ([foreign_pin_discovery.mdx §6](./foreign_pin_discovery.mdx#6-surfacing--the-file-shows-as-pinned-in-the-web-app-locked)). Reality without intent. **Click to have Large File Bridge sync it too.** |

* **`pinnedHere` is a LIVE, CANONICAL read of this node's pinset — never a per-file hash on the read path.**
  The blue↔red split is driven by `FileRow.pinnedHere`, computed by testing the file's recorded manifest CID
  against `ipfs.canonicalPinnedSet()` (this node's `ipfs pin ls`, canonicalized to CIDv1 base32 so a `Qm…`
  pin of a `bafy…` CID for the **same block** still counts — [knowledge/ipfs.mdx §5.1](../knowledge/ipfs.mdx)).
  The pinset is fetched **once per page load** and each row is an O(1) set lookup — **no full-file hashing**,
  honoring the read-path cost boundary this app keeps ([knowledge/ipfs.mdx §5.1 honest boundary](../knowledge/ipfs.mdx)).
* **`undefined` → intent-blue, NEVER a false red.** When the pinset can't be read (IPFS daemon down, or the
  caller didn't fetch it) `pinnedHere` is `undefined`; a decided file then shows **blue** (intent) and the
  icon **never cries red** on missing information. Red is only ever a **verified** "decided but absent here."
* **This directly fixes "the CLI says it's pinned but the web app shows it isn't"** — for any file the user
  has **opted into syncing**, the icon now reflects the node's real pinset, not just the stored decision.
* **The undecided-but-pinned case is the GREEN state — LEARNED FROM A DEFECT (the 3rd fix in this saga).**
  A file **pinned but never decided** (a legacy manual `ipfs add` under a foreign CID profile) is discovered
  by the **background** foreign-pin pass — never a read-path hash, so the honest boundary holds — and the row
  carries `pinnedForeign: true` ([foreign_pin_discovery.mdx §6](./foreign_pin_discovery.mdx#6-surfacing--the-file-shows-as-pinned-in-the-web-app-locked)).
  History of the bug ("CLI says pinned, this page says not pinned", e.g. `charlie-kirk/videos/1965884012565766172.mp4`
  pinned as `QmT5j43eo…`): fix 1 canonicalized CID encodings (v0↔v1); fix 2 built foreign-pin discovery, which
  correctly **recorded** the pin in `foreign-pins.json` and shipped `pinnedForeign` on the row — but the pin
  cell here still rendered only `decision × pinnedHere`, so the recorded reality was **never drawn** and an
  undecided pinned file stayed grey. Fix 3 is this fourth state: **recorded must mean rendered**. Any new
  surface that shows pin state must consume `pinnedForeign`, not just `pinnedHere`. Fix 4 (2026-07-19)
  extended the same rule to the **counting** surfaces: the `Undecided` summary segment / metric tile /
  triage popup and the to-do batch `pin` recommendation all exclude `pinnedForeign` rows (§4.1 blockquote) —
  a pin-nag about a pinned file is the same defect wearing a number instead of an icon.
* **Toggling writes the decision to the shared ledger, which travels to the owning Personal / Company
  storage.** Clicking the Pin icon — one file, or a bulk popup for hundreds — writes the IPFS axis to the
  repo's `decisions.yaml` ledger (§4.5.1). That ledger lives under the repo's tracking area and is committed
  and union-merged by the git backbone, so a decision made on one computer travels to the user's other
  computers; when the repo is owned by a **company** storage with a sync repo, it travels to the whole team
  ([decisions.mdx §3](./decisions.mdx), [repo_company_mapping.mdx](./repo_company_mapping.mdx),
  [repo_owner_propagation.mdx](./repo_owner_propagation.mdx)). Turning the axis **off** (grey) is the same
  path in reverse. On each other computer the pass then reconciles reality → the icon there shows **red**
  until that machine pulls the bytes, then **blue**.

Computed in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/store-model/units.service.ts` —
`composeFileRows` (sets `pinnedHere` from the threaded canonical pinset, and `pinnedForeign` from the
recorded foreign-pin index via `foreignPinByAbsPath`), fed by
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/repos/repos.router.ts` — `repoDetailWithPins`
(fetches `ipfs.canonicalPinnedSet()` once per detail response). Rendered by
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/OneRepoPage.tsx` — `OneRepoPage`
(the `pin` `LfbColumn`, four-state via `TaskIconCell` with red/green `doneColor` overrides), through the shared
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/components/StatusActionIcon.tsx` — `StatusActionIcon`.

---

## 5. Empty & loading states

* **No large files:** the table is replaced by a card — "No large files found in this repo. LFBridge
  looks for git-ignored files under `large/` and the ignored media extensions."
* **All decided:** the summary shows `Undecided 0`; the quick-filter for Undecided is disabled.
* **Loading:** skeleton rows sized to the columns; no layout shift.
* **IPFS node unreachable:** a banner at the top of the content; Pin now and per-row transfers are
  disabled, but decisions can still be edited (they queue for when the node returns).

---

## 6. Acceptance criteria

1. The screen opens at `/repos/$repoId` from a [Repos](./repos.mdx) row click. The header is **two rows**:
   row 1 = the `‹ Repos` breadcrumb above the **repo name** (biggest/boldest text; **never wraps**, sized to
   its content and capped at **50%** of the row) with the width-measured **tab strip**, the **gear**, and the
   smart header primary in the actions slot (which **shrinks first**); row 2 = the **absolute directory
   path** on its **own full-width row**, **one line, ellipsized, full text in the tooltip** — it never wraps
   (§3.1). **Pin now is NOT a header button** — it lives in the page action-links row (and the **More ⌄**
   menu) and still **prioritizes this repo (pinned first) then runs the full pin pass**
   ([pin_process.mdx §2/§3](./pin_process.mdx)), never changes a decision, and is never a silent no-op
   (reports what it actually did).
2. The **status/settings strip** (§3.3) shows Pin (the one editable per-repo on/off toggle), Status pill,
   Peers, Last pin, and IPFS node health; when the node is unreachable, Pin now is disabled.
3. The **summary counts** line reads `N Pin · N Undecided · N Ignore` (recommended: `N Not pinned` for
   the third, §4.1); each segment is a quick-filter that sets the **Pin (IPFS)** facet to that one value
   and **leaves the Git-ignore facet untouched** — no segment ever touches both axes. Undecided is
   accent-highlighted when > 0.
4. The **files table** is one TanStack table, one row per large git-ignored file, flat/chromeless,
   with the standard control row (search left; icon-only filter `⛛` and sort dropdowns right), the
   always-visible **facet rail** beneath it, and columns in order: select, then the **five leading icon
   control columns Pin · Ignore · Transcribe · AI Description · OCR** ([tables.mdx §4c](./tables.mdx)),
   then File, Size, Peers, CID, Changed, and a trailing **`⋮`** kebab. The old **Pin decision** select,
   **Decided by**, **Decided**, and transfer **Status** columns are **removed** (§4.3.1).
5. **Pagination defaults to 500**; the **file count** shows left of the pager.
6. Each row shows the **two decision-icon columns** — **Pin** (Add to IPFS, solid **blue** done) and
   **Ignore** (⊘, Add to git ignore, solid **orange** done) — as the first two of the five leading icon
   columns, rendered through the shared **`StatusActionIcon`** ([tables.mdx §4c](./tables.mdx),
   [decision_toggles.mdx](./decision_toggles.mdx); the "could"/recommended edge and n/a hairline are shared).
   Both write immediately per §4.5.1, write only their own axis to the team-shared ledger, and never delete
   the user's local file. The labelled **Pin decision** select column is **removed** (§4.3.1) — the Pin icon
   is the per-row IPFS-axis control now.
6b. **The two axes are never one control.** The rail carries **Pin (IPFS)** and **Git-ignore** as **two
   facets** with the [tables.mdx §2.4.1](./tables.mdx#241-the-two-facets-fixed-vocabulary-locked)
   vocabulary, and **no facet on this page is named "Decision"**
   ([tables.mdx §2.4](./tables.mdx#24-one-facet--one-axis--pin-and-git-ignore-are-never-the-same-filter-locked));
   **Pin** and **Ignore** are two separate sort keys with no combined "Decision" key (§4.2,
   [tables.mdx §3.6](./tables.mdx#36-sort-obeys-the-axis-split-too-locked)). The old **Pin decision** select
   column is **removed** (§4.3.1) — the **Pin** icon carries the IPFS axis's one facet and one sort key.
   Provenance is a **third, separate** `Decided by` **rail facet** (☑ Me ☑ Teammate ☑ Policy ☑ Undecided,
   §4.8) — a filter, not a column, so it survives the column removal — never folded into either axis. The
   stored `sync` / `ignore` / `undecided` values are unchanged — this is a **column + facet** change, not a
   data change.
7. **Bulk actions** (`⋮`, enabled on selection) set Add to IPFS (pin) / Ignore / Undecided across
   selected rows and can Pin-now the selected `sync`-decision files; the header checkbox selects the
   page, with a "select all matching" affordance under an active filter.
8. The transfer **Status** column is **removed** from this table (§4.3.1) — the **Pin** icon shows pinned
   state; the LOCKED transfer-status pill mapping (§4.6) now applies only on the file page and the IPFS pins
   table. An Add-to-IPFS (pin) file with Peers = 0 still renders the **Peers** count red.
9. Empty / all-decided / loading / node-unreachable states render per §5.
10. The header carries a top-right **"more" menu**, rendered as the word **More** + a **down chevron** with
    **no** leading `⋮` glyph and **no** bordered rounded rectangle ([menus.mdx §4.1](./menus.mdx)) = the
    **repo** entity catalog ([menus.mdx §5.1](./menus.mdx#51-repo)), with *Open repo* omitted and *Pin now*
    present in it **and** in the action-links row (it is **not** the header primary, §3.1). Each files-table
    **data cell is a deep link** per §4.7 (File/Size/Changed →
    View one file; Peers → Peers filtered to the file; CID copies); the row **`⋮` kebab** (and
    right-click) opens the **file** catalog ([menus.mdx §5.3](./menus.mdx#53-file)) and never navigates,
    with compress/convert/ignore gated behind an explicit-click confirm.
11. The header carries the **task tab strip** (right of center) — **six** tabs: **All** (default) / **IPFS**
    / **Compress** / **Transcribe** / **AI descriptions** / **OCR** ([task_tabs.mdx](./task_tabs.mdx));
    selecting a tab re-tunes the
    metrics-panel strip, the visible columns, the default sort, and the row filter client-side. **All** is
    metrics-forward (light-green big-`0` panels when nothing is to be done); the **Transcribe** tab shows the
    three-state Transcribe status icon, the **Compress** tab the Compress status icon, and the **AI
    descriptions** tab the three-state Describe status icon. The strip is **width-measured**: tabs that don't
    fit move (from the **right**) into a trailing **down-chevron overflow menu** that renders **only** when
    something overflows, and the **active** tab is always kept visible
    ([task_tabs.mdx §1.4](./task_tabs.mdx#14-the-overflow-chevron--the-repo-name-never-loses-locked)).
11a. **The metrics strip takes the FULL page width, is ONE row, and NEVER wraps** (`flex-nowrap`, `shrink-0`
    tiles; horizontal scroll on a pathologically narrow window). There is **no hover-info region beside the
    tiles** — that ≥25% reservation, and the balanced second row it forced, are **gone**. Every hover source
    (metric tile, **File** cell, the five icon control columns, the **Compress** status icon) publishes via
    the same `setHoverInfo(text)`, and `HoverInfoBridge` forwards it to the **left-bar hover-info panel** —
    the reserved white block above the JFK Social / ACT 3 Filmmaking links and the account slot — which
    shows the active tab's `defaultHint` at rest (§3.2).
11b. **Two decision tiles, not one "Undecided" tile** (§3.2.2): **Add to IPFS** (`addToIpfs`; files with no
    pin decision, excluding foreign-pinned rows — exactly the old Undecided number; opens
    `buildAddToIpfsWarning`) and **Git Ignore** (`gitIgnore`; large files Git may still commit — not
    git-ignored, not rule-locked, not `analysisOnly`; opens the new `buildGitIgnoreWarning`, whose Apply
    writes **both** axes so an existing pin decision is never clobbered). The old **`bigNotIgnored`** tile is
    **folded into Git Ignore** and no longer exists. The Git Ignore tile's **count and its popup's target
    list come from the one shared `gitIgnoreCandidates()` predicate**, so they can never disagree. On **All**
    the two tiles **lead and sit adjacent**; on **IPFS** only **Add to IPFS** shows. The frozen wire value
    `"undecided"` and the `N Pin · N Undecided · N Ignore · N Pinned locally` summary line are **unchanged**.
11c. **The metric tile reads `AI Describable`, not `Describable`** (both letters of AI upper-case, one
    space); the paired done-tile is still `Described` and the `MetricId` `describable` is unchanged. Tile
    labels are **not** CSS-`capitalize`d — the catalog stores the exact string, because "Add to IPFS",
    "AI Describable" and "OCRable" carry meaningful casing (§3.2.1).
12. **The header primary is labeled per metric** and resolves worst-first **ipfs-down → pull-down →
    add-to-IPFS → transcribe → describe → compress → ocr** (§3.1): the integrity trio keeps **"View
    recommendation ›"** (red for `bad`, blue otherwise), the task set names the verb — **"Transcribe ›"**,
    **"Describe ›"**, **"Compress ›"**, **"OCR ›"** (blue) — and each opens the same educate-and-fix popup
    its metric tile opens. **Every actionable metric tile opens a popup** and lets the user start the work
    (not only the decision tiles / Pull-down);
    the transcribe/describe applies are model/consent-gated, interposing then resuming.
12a. **⌘-click / Ctrl-click / shift-click / middle-click a files-table row opens its viewer in a NEW
    tab/window** — the `DataTable` `rowHref(row)` this page supplies (the same route the plain row click
    uses); a plain click still routes in-page. Owning rule:
    [tables.mdx §4d](./tables.mdx#4d-modified-click-on-a-row-opens-a-new-tab-locked-site-wide) (§4.7) —
    **not** §3.6, which means "Sort obeys the axis split too".
13. **The five leading icon control columns** ([tables.mdx §4c](./tables.mdx)) — **Pin · Ignore · Transcribe
    · AI Description · OCR**, in that order right of the select checkbox — lead the table on **every** tab
    (§4.3): narrow (`tight`/`px-1`), **icon-only headers** (text label kept only for Sort/Filter/Columns
    dropdowns), each rendered through the shared **`StatusActionIcon`** with **unique done colors** (Pin blue,
    Ignore orange, Transcribe indigo, AI Description teal, OCR violet). Hovering the header icon **or** any
    cell icon shows a `title` tooltip **and** fills the **left-bar hover-info panel** with a plain-English
    explanation of the icon (§3.2, §4.3, §4.8). The **Pin decision**, **Decided by**, **Decided**, and **Status**
    columns are removed (§4.3.1); the two decision axes remain as data, set via the Pin/Ignore icons.

<!--
  Keep this page aligned with repos.mdx (the list it drills into) and left_bar.yaml (the nav).
  LOCKED across specs: the three-state DECISION model — Add to IPFS (pin) / Ignore / Undecided (user
  intent; the pin decision's stored wire value stays `sync`) vs the TransferStatus (reality); the
  transfer-status pill colors (§4.6, now shown on the file page + IPFS pins table, NOT this file table);
  the standard control row (search left; icon-only sort/filter right); pagination default 500 with the
  count shown. Clicking the Pin icon (the per-row IPFS control) never deletes local bytes.
  HEADER (§3.1, LOCKED): TWO rows. Row 1 = repo NAME (biggest/boldest, never wraps, sized to CONTENT and
  capped at 50%) + the actions slot (tabs · gear · primary) which takes the remainder and SHRINKS FIRST —
  that inversion is what lets the tab strip overflow instead of crushing the name. Row 2 = the absolute
  path on its OWN full-width row, ONE line, ellipsized, tooltip carries the full text. (BUG FIXED: the path
  used to be the subtitle INSIDE row 1's left column beside a shrink-0 actions slot, so it wrapped over
  several lines while the right half sat empty.) PIN NOW IS NOT A HEADER BUTTON — action-links row + the
  More menu; the header primary is "View recommendation ›" / "Scan now" / the per-metric verb. The
  single-entity "more" button renders as the word More + a down chevron, NO ⋮ glyph, NO bordered box
  (menus.mdx §4.1 owns it). SIX tabs: All · IPFS · Compress · Transcribe · AI descriptions · OCR.
  METRICS STRIP (§3.2, LOCKED): FULL page width, ONE row, NEVER wraps (flex-nowrap + shrink-0 tiles;
  overflow-x scroll is the only escape). The docked hover-info region to the RIGHT is DELETED — its ≥25%
  width reservation was what forced the balanced second row. The explanation now goes to the LEFT-BAR
  hover-info panel (the reserved white block above the JFK Social / ACT 3 links and the account slot):
  every source still calls setHoverInfo(text) and a render-nothing HoverInfoBridge forwards it into the
  app-wide HoverInfoProvider; at rest it shows the active tab's defaultHint.
  DECISION TILES (§3.2.2, LOCKED): the "Undecided" TILE is GONE, split into TWO tiles — Add to IPFS
  (addToIpfs = files with no pin decision, minus foreign-pinned = exactly the old number, opens
  buildAddToIpfsWarning) and Git Ignore (gitIgnore = large files Git may still commit: !gitignore &&
  !gitignoreLocked && !analysisOnly, opens the NEW buildGitIgnoreWarning whose Apply writes BOTH axes so a
  pin decision is never clobbered). The old bigNotIgnored tile is FOLDED IN and no longer exists as a
  MetricId. INVARIANT: the tile count and the popup's target list share ONE predicate,
  gitIgnoreCandidates(), so they can never disagree. All tab = the two tiles LEAD and are ADJACENT; IPFS
  tab = Add to IPFS only. Worst-first chain: ipfs-down → pull-down → add-to-IPFS → transcribe → describe →
  compress → ocr. THE WIRE VALUE "undecided" IS UNCHANGED — only the TILE is gone; do not "clean up" the
  Decision enum, the Pin facet's Undecided value, or the summary line (N Pin · N Undecided · N Ignore ·
  N Pinned locally), all of which stand.
  TILE LABELS (§3.2.1): "Describable" → "AI Describable" (MetricId `describable` unchanged); labels are NOT
  CSS-capitalized — METRIC_CATALOG stores the exact string ("Add to IPFS", "AI Describable", "OCRable").
  ROW MODIFIED-CLICK (§4.7): DataTable's optional rowHref(row) makes ⌘/Ctrl-click, shift-click and
  middle-click open a NEW TAB; plain click still routes in-page. Owning rule = tables.mdx §4d (NOT §3.6,
  which is "Sort obeys the axis split too" and is cited by other specs — never renumber it).
  ICON COLUMNS (tables.mdx §4c, §4.3/§4.3.1, LOCKED): the table LEADS every tab with FIVE narrow icon control
  columns after the select checkbox — Pin·Ignore·Transcribe·AI Description·OCR — icon-only headers, left-bar
  hover-info explanation, shared StatusActionIcon, UNIQUE done colors (Pin blue, Ignore orange, Transcribe
  indigo, AI Description teal, OCR violet). REMOVED from this file table: the "Pin decision" select column,
  the Decided by + Decided provenance columns, and the transfer Status column. The two decision axes still
  exist as DATA and are set by clicking the Pin/Ignore icons; provenance surfaces on icon hover (§4.8).
  AXIS SPLIT (tables.mdx §2.4, LOCKED): Pin (IPFS) and Git-ignore are TWO facets on the rail and TWO sort
  keys — never one, and NO facet/column filter is named "Decision". The removed col-3c select used to carry
  the IPFS-axis one-of-N choice; that choice is now a click on the Pin icon (col 1), which carries the IPFS
  axis's one facet + one sort key. §4.1's segments drive the Pin (IPFS) facet
  only and never touch Git-ignore (recommend relabeling `Ignore` → `Not pinned` there — same word, two
  axes, one screen). Provenance is a THIRD facet, `Decided by` (Me/Teammate/Policy/Undecided, §4.8), not a
  values-list on any axis. Labels + facets only — sync/ignore/undecided stay FROZEN wire values
  (decisions.mdx §7). The app-wide background pin process (launchd
  install / on-off / 15-min schedule) is the Scans left-bar PAGE, not this repo's strip toggle. All
  tables are TanStack tables. The left bar lives in ./left_bar.mdx + pm/left_bar.yaml — never redefine
  it here.
-->

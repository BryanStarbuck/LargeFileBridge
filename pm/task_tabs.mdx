---
title: LargeFileBridge — Task Tabs (the per-repo file screen's All / IPFS / Compress / Transcribe / AI descriptions / OCR tabs)
description: The tab strip that sits a little right of center in the One-repo header's first row (the repo name on the left — the biggest, boldest text on the page, which never wraps and is never squeezed; the gear + the smart "View recommendation" / "Scan now" primary on the right), with SIX tabs — All, IPFS, Compress, Transcribe, AI descriptions, OCR — one selected at a time. The strip is width-measured: tabs that don't fit move, always from the RIGHT, into a trailing down-chevron overflow menu that renders only when something overflows, and the active tab is always swapped back into the visible set. Selecting a tab re-tunes EVERYTHING below it: the metrics-panel strip, the visible table columns, the default sort, and which files the table shows. The All tab is the overview and is metrics-forward, leading with the two adjacent decision tiles — Add to IPFS and Git Ignore — which replaced the single Undecided tile and absorbed the old "Big, not git-ignored". Each metric panel is deliberately NARROW — as wide as its widest word — a small label (rendered with the catalog's exact casing; it wraps onto as many lines as it needs) stacked over a big centered number; a light-green rounded rectangle with a big 0 when nothing is to be done. THE METRIC PANELS ARE THIS PAGE'S ONLY WARNING SURFACE — there is no separate warning banner: clicking a panel opens that metric's educate-and-fix popup. The strip takes the FULL page width, is ONE row, and NEVER wraps — the region that used to be docked to its right is gone, so nothing reserves width from it. The IPFS / Compress / Transcribe / AI descriptions / OCR tabs narrow the file set and columns to that one task. The Transcribe tab adds a three-state Transcribe status icon (filled indigo = done · orange-edge/99%-white = could be transcribed · thin-light-grey edge = not applicable). The explanation for whatever the user hovers (a panel, a row, a status icon, an action link) is published to the hover-info panel at the BOTTOM OF THE LEFT BAR so the panels themselves stay terse. Remote-only rows — files a peer computer has pinned that this one does not have yet (storage_company.mdx §8.5) — belong on the All and IPFS tabs only; they are never shown on Compress / Transcribe / AI descriptions / OCR, because their analysis status is `na` and padding those tabs with un-runnable work would be a lie (§4.8). Companion to one_repo.mdx (the page that hosts the tabs), warnings.mdx (the metric popups), decision_toggles.mdx (the toggle grammar the status icons echo), Transcribe.mdx / compression.mdx / ipfs_ui.mdx / ai_description.mdx / ocr.mdx (the per-tab tasks), special_files.mdx (the classifier that scopes each tab), left_bar.mdx (the panel the hover text lands in), and non_intrusive_tooltip.mdx (the hover-info content model).
---

# LargeFileBridge — Task Tabs

The **One-repo** screen ([one_repo.mdx](./one_repo.mdx)) is where the user does the work on a repo's large
files. It now carries a **task tab strip** in the **first row** of its header — **a little right of
center**, with the repo **name** still on the left (the biggest, boldest text on the page — it never wraps
and is never squeezed, [one_repo.mdx §3.1](./one_repo.mdx#31-header--two-rows-and-neither-of-them-wraps-locked))
and the settings **gear** + the smart **"View recommendation" / "Scan now"** header primary (§1.1) still on
the right. There are **six tabs**:

| Tab | The one question it answers |
| --- | --- |
| **All** *(default)* | *"Across every task, what could I do on this repo right now?"* — the overview. |
| **IPFS** | *"Which large files are pinned across my computers, and which still need a decision or a peer?"* |
| **Compress** | *"Which videos and images could be compressed, and how big are they?"* |
| **Transcribe** | *"Which audio/video files have no transcript yet?"* |
| **AI descriptions** | *"Which images and videos have no AI description yet?"* |
| **OCR** | *"Which images, videos and PDFs haven't had their on-screen text read yet?"* |

> **The three analysis tabs — Transcribe, AI descriptions, OCR — mirror each other point-for-point** (their
> own could/done metric tiles, a three-state status icon, the same metric-tile → popup and blue
> header-primary behavior). AI descriptions is specified in §4.5 (tuning), §5.2 (the Describe status icon),
> §5.2.1 (the Describe confirm flow), cross-linking to [ai_description.mdx](./ai_description.mdx) (the
> feature) and [describe_calc_engine.mdx](./describe_calc_engine.mdx) (the bulk-discovery engine); OCR is
> §4.6 here and [ocr.mdx](./ocr.mdx) end to end. Everywhere this spec says "Transcribe / Compress," read
> "and Describe, and OCR" too — the task tabs share one grammar.

**One tab is selected at a time, and selecting it re-tunes the whole surface below it** — the
**metrics-panel strip**, the **visible table columns**, the **default sort**, and **which files the table
shows**. This spec owns that tab system end to end; the sibling specs own their own tasks and only point
here for how the tab presents them.

The tab strip and the per-tab tuning are rendered in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/OneRepoPage.tsx` — `OneRepoPage`
(mounts `TaskTabs` from `~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/TaskTabs.tsx` and
selects the active tab's column set + sort + filter + metrics from
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/taskTabs.config.ts` — `TASK_TABS`).

This spec builds on the siblings and does not repeat them:

* [one_repo.mdx](./one_repo.mdx) — the per-repo screen whose header hosts the tabs and whose files table
  the tabs re-tune. The decision model (Add to IPFS (pin) / Ignore / Undecided), the files table, and the
  header (the "View recommendation" / "Scan now" primary, gear) are defined there; this spec adds the tab
  layer over them.
* [warnings.mdx](./warnings.mdx) — the **educate-and-fix popup** clicking a metric panel opens (§2.4).
  On the One-repo page these panels **replace the warning banner** (§2.6) — the popup is the same
  educate-and-fix surface, just launched from the panel instead of a banner arrow button.
* [decision_toggles.mdx](./decision_toggles.mdx) — the four-state toggle grammar the **Transcribe** /
  **Compress** status icons echo (§5, §6) — same box, different "done" color because these are *status*
  icons, not two-axis decisions.
* [Transcribe.mdx](./Transcribe.mdx) · [compression.mdx](./compression.mdx) · [ipfs_ui.mdx](./ipfs_ui.mdx)
  — the three tasks the task tabs surface.
* [special_files.mdx](./special_files.mdx) — the classifier (`large` · `video/image/audio` · `compressible`
  · `transcribable`) that scopes which files each tab shows (§4).
* [non_intrusive_tooltip.mdx](./non_intrusive_tooltip.mdx) — the hover-info content model, here rendered by
  the panel at the BOTTOM OF THE LEFT BAR (§3) — there is no region beside the metrics panels any more.

---

## 1. The tab strip (LOCKED)

### 1.1 Position and look

* **Where.** In the **first row** of the One-repo header, **a little right of center**. The repo **name**
  stays **left** (the `‹ Repos` breadcrumb sits above it); the **gear** (Repo settings) and the smart header
  primary — **"View recommendation ›"** (or **"Scan now"** when a scan is overdue, or the per-metric verb),
  the SAME button on every tab — stay **right**
  ([one_repo.mdx §3.1](./one_repo.mdx#31-header--two-rows-and-neither-of-them-wraps-locked)). The tabs sit
  between them, inside the header's **actions slot** — the slot that **shrinks first**, which is precisely
  what makes the overflow chevron (§1.4) fire instead of the repo name being crushed. (**Pin now is not a
  header button**; it lives in the page action-links row and the **More ⌄** menu.)
* **The absolute path is NOT in this row.** It gets its own full-width row **beneath** the title row, one
  line, ellipsized ([one_repo.mdx §3.1](./one_repo.mdx#31-header--two-rows-and-neither-of-them-wraps-locked)) —
  so the tab strip never shares a line with it and never squeezes it.
* **Look.** The house tab style already used by the File System tabs
  (`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/fs/FsTabs.tsx` — `FsTabs`): a
  horizontal row of tab buttons, each a small **leading icon + label**, with a **2px underline** in
  `--lfb-primary` under the **active** tab (transparent under the others), active label at font-weight 500.
* **Icons.** All → `LayoutGrid`; IPFS → `Pin` (the pin used everywhere for the Add-to-IPFS axis);
  Compress → `Archive`; Transcribe → `Captions`; AI descriptions → `ScanText`; OCR → `TextSelect`.

```
┌────────────────────────────────────────────────────────────────────────────────────────────┐
│  ‹ Repos                                                                                   │
│  Our_Movies      ▦ All  ⌂ IPFS  ▤ Compress  ⌯ Transcribe  ⌗ AI descriptions  ⌸ OCR   ⚙  ┌───┐│ ← all six fit: NO chevron
│                        ▔▔▔▔▔  (All active: 2px primary underline)                       │Vie││
│  ~/BGit/Bryan_git/Movies/Our_Movies/2026/renders/final_delivery                         └───┘│ ← row 2 = path, full width
└────────────────────────────────────────────────────────────────────────────────────────────┘

… and the same header once the window narrows (§1.4):

┌──────────────────────────────────────────────────────────────────────┐
│  ‹ Repos                                                             │
│  Our_Movies     ▦ All  ⌂ IPFS  ▤ Compress  ⌯ Transcribe  ⌄  ⚙ ┌─────┐│ ← the two rightmost tabs moved
│                       ▔▔▔▔▔                              ▲    │View ││   into the ⌄ overflow menu
│  ~/BGit/Bryan_git/Movies/Our_Movies/2026/renders        │    └─────┘│   (the repo name never shrinks)
│                                          ┌──────────────┴────┐      │
│                                          │ ⌗ AI descriptions │      │
│                                          │ ⌸ OCR             │      │
│                                          └───────────────────┘      │
└──────────────────────────────────────────────────────────────────────┘
```

### 1.2 Selecting a tab re-tunes four things (LOCKED)

Switching tabs is a **client-side view change** — it never refetches the repo and never mutates
anything. The active tab drives, in one place ([taskTabs.config.ts](./one_repo.mdx) — `TASK_TABS`):

1. **The metrics-panel strip** — which "what could be done" panels show (§2, §4).
2. **The visible table columns** — a tab-scoped column set (§4, §7).
3. **The default sort** — what is most interesting for that task floats to the top (§4).
4. **The row filter** — which files are appropriate for that task (§4); files outside the tab's scope are
   hidden (still reachable on **All**).

### 1.3 Active-tab state (LOCKED)

* **Default = All** on every fresh open of a repo.
* The active tab is **ephemeral view state** (React state on `OneRepoPage`), **not** written to
  `config.yaml`. It is *not* a decision and *not* a per-user table preference — the per-user **sort /
  filter** persistence ([tables.mdx §4](./tables.mdx)) still applies *within* whatever tab is active.
* Switching tabs is **instant** (no spinner) — the file set is already loaded; the tab only re-projects it.

#### 1.3.1 A tab's `rowFilter` is scope — it is NOT a facet (LOCKED)

A tab hides rows (§4), and so does a filter facet ([tables.mdx §2](./tables.mdx#2-filtering--the-value-visibility-checkbox-model-locked)).
They are different things and must never be merged:

* **The tab's `rowFilter` is scope, chosen by the tab.** It never appears on the **facet rail**
  ([tables.mdx §2.2](./tables.mdx#22-the-facet-rail--filtering-without-opening-a-dropdown-locked)), it has
  no checkboxes, and the user does not un-set it — they **switch tabs** (or go to **All**, which has no
  `rowFilter`). It is honest despite being invisible on the rail because **the tab itself is the visible
  control**: a lit "Compress" tab is a plain statement that you are looking at compressible files.
* **The two compose by AND.** The tab picks the row scope; the rail's facets then cut within it. A facet
  never widens a tab's scope — unchecking nothing on the rail still shows only the tab's rows.
* **The tab's `defaultSort` is a default, not a lock.** It seeds the multi-level sort for that tab; the
  user's own re-rank persists and wins ([tables.mdx §3.4](./tables.mdx#34-the-default-sort--bookmarks-first-locked-default-user-changeable)),
  and it displays in the active-sort **chip run** ([tables.mdx §3.5](./tables.mdx#35-sorting-without-opening-the-dropdown--the-active-sort-chips-locked))
  like any other sort — so a tab's opinion about ordering is always readable, never hidden.
* **The IPFS tab is the pin axis only.** Its `rowFilter` and its urgency sort concern **Add-to-IPFS**
  state; they say nothing about git-ignore, and no tab may filter on both axes at once
  ([tables.mdx §2.4](./tables.mdx#24-one-facet--one-axis--pin-and-git-ignore-are-never-the-same-filter-locked)).

### 1.4 The overflow chevron — the repo name never loses (LOCKED)

**The rule this protects.** The repo **NAME is the biggest, boldest text on the page**. It must **never
wrap** and must **never be squeezed** by the tab strip
([one_repo.mdx §3.1](./one_repo.mdx#31-header--two-rows-and-neither-of-them-wraps-locked)). Six tabs plus a
gear plus a primary button will not fit beside a long name on a narrow window, so **something has to give —
and it is never the name.**

* **The strip is width-measured.** It keeps the tabs that **fit** and moves the rest into a trailing
  **down-chevron (`⌄`) overflow menu**.
* **Overflow is always taken from the RIGHT.** Tabs are dropped in reverse order (OCR first, then AI
  descriptions, …), so the leftmost tabs — the ones the user reaches for most — stay put and the strip's
  order never reshuffles.
* **The chevron renders ONLY when something overflows.** When all six fit there is **no chevron and no
  chrome at all** — not a greyed-out button, not a reserved gap. A user on a wide window never knows the
  mechanism exists.
* **An ACTIVE tab that would land in the overflow is swapped back into the visible set** (trading places
  with the last visible tab), so **the underline is never hidden inside the menu**. The user can always see
  which tab they are on.
* **Selecting from the overflow menu is identical in every way to clicking a visible tab** — same
  `onChange`, same client-side re-tune of metrics / columns / sort / filter (§1.2), no refetch, no mutation.
  The menu is a placement detail, never a second behavior.

**Measurement technique.** A **hidden, off-screen copy of the strip with identical markup** is rendered
alongside the real one; each measured tab's **right edge** (`offsetLeft + offsetWidth`) and the **chevron's
width** are read from it, and the fit walks left→right keeping tabs while they fit, reserving room for the
chevron. A **`ResizeObserver`** on the wrapper refits on every width change. This is the **same technique
the page action-links row uses for its "More ▾"** ([page_actions.mdx §3](./page_actions.mdx)) — one measured
strip, one overflow menu, no layout thrash.

Implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/TaskTabs.tsx` — `TaskTabs` (the
hidden measurement layer, the `useLayoutEffect` fit + `ResizeObserver`, the `visibleCount` split, the
active-tab swap-back, and the `ChevronDown` menu).

---

## 2. The metrics panels — the "what could be done" boxes (LOCKED)

Above the table sits a horizontal strip of **metric panels**. Each panel is a **terse label + a number**
answering *"how many of this thing could I do here?"* — e.g. `Add to IPFS 5`, `Git Ignore 12`,
`Compressible videos 12`, `Transcribable 71`. **This is the surface the warnings live on** for this screen:
on the **All** tab the panels are the whole focus (an at-a-glance overview of everything actionable); on a
task tab only that task's panels show.

**The strip takes the FULL page width and is ONE row that never wraps** — `flex-nowrap` with `shrink-0`
tiles; on a pathologically narrow window the row **scrolls horizontally** rather than breaking a line
([one_repo.mdx §3.2](./one_repo.mdx#32-the-metrics-strip-never-wraps--and-the-explanation-lives-in-the-left-bar-locked)).
Nothing is docked beside it and nothing reserves width from it — the explanation goes to the **left-bar
hover-info panel** (§3).

The metrics strip is rendered in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/MetricsStrip.tsx` — `MetricsStrip`
(one `MetricPanel` per active-tab metric in a single `flex w-full flex-nowrap … overflow-x-auto` row;
counts from `RepoDetail` per §2.5).

### 2.1 Anatomy of one panel (LOCKED)

A panel is a small **rounded rectangle**, kept **as narrow as possible** — **only as wide as its widest
word** (CSS `width: min-content`) — holding a vertical stack, top to bottom:

* a **terse label** — *what it is*, no sentence, no description text (the explanation goes to the left-bar
  hover-info panel, §3). One to three words: `Add to IPFS`, `Git Ignore`, `Not Backed Up`, `Pull Down`,
  `Compressible Videos`, `Transcribable`, `AI Describable`, `OCRable`. The label is **small** and **wraps**
  onto as many lines as the narrow box needs — the label wraps *inside* the tile; the **strip** never wraps.
* **Casing is EXACT, never CSS-`capitalize`d (LOCKED).** The catalog stores **the exact string to render**,
  because labels carry meaningful casing a `capitalize` rule would fight: **`Add to IPFS`**, **`AI
  Describable`**, **`OCRable`** ([one_repo.mdx §3.2.1](./one_repo.mdx)). No presentational transform is
  applied to a metric label anywhere.
* the **number** — **big and centered**, `tabular-nums`, sitting under the wrapped label. The number is
  the visual center of the panel; the label words sit above it.

The panel is a **whole-panel button** — clicking anywhere on it opens that metric's warning popup (§2.4);
there is **no chevron** (the narrow box has no room for one and needs none — the whole tile is the
affordance).

```
┌────────────┐   ┌────────────┐   ┌────────────┐   ┌────────────┐   ┌────────────┐
│  Add to    │   │    Git     │   │    Not     │   │Compressible│   │Compressible│
│   IPFS     │   │   Ignore   │   │  Backed Up │   │  Videos    │   │  Videos    │
│     5      │   │     12     │   │     3      │   │    12      │   │     0      │  ← green + big 0
└────────────┘   └────────────┘   └────────────┘   └────────────┘   └────────────┘
  amber            amber           red (at risk)    amber (offer)    light-green
 (decision)       (decision)                                          (all good)
 ▲ the two decision tiles LEAD and sit ADJACENT on the All tab (§2.5.1)
```

(Each box is only as wide as its widest word — `Ignore`, `Backed`, `Compressible` — with the rest of
the label wrapping under it and the number centered below. The **strip** holding them is one non-wrapping
full-width row.)

### 2.2 The green-zero state (LOCKED)

When a panel's count is **0** — there is **nothing to do** for that metric — the panel is a **light-green
filled rounded rectangle** (a faint green fill, the health "good" tint) showing a **big `0`**. This is the
reassurance state: a wall of green zeros means the repo is fully handled. Clicking it still opens the
metric's popup in its **all-clear** state, but the panel reads as *done*.

### 2.3 Non-zero panels are health-tinted (LOCKED)

A count `> 0` tints the panel by how much it wants attention, reusing the app health tokens
(`components/ui/health.ts`):

| Metric shape | Tint when `> 0` |
| --- | --- |
| **Not backed up** (sync files with 0 peers), **Missing / pull-down** | **red** — data is at risk / owed. |
| **Add to IPFS**, **Git Ignore** (the two decision axes, §2.5.1) | **accent / amber** — action needed, not an emergency. |
| **Compressible videos / images**, **Transcribable**, **AI Describable**, **OCRable** | **amber** — an offer, at the user's leisure. |

No panel ever carries a paragraph of description — **terse label + number only**; §3 supplies the words
(in the left bar).

### 2.4 Clicking a panel opens the metric's warning popup (LOCKED)

**Clicking a metric panel** is the same affordance as the blue arrow button elsewhere
([warnings.mdx §4](./warnings.mdx)): it opens the **educate-and-fix popup** for that metric, scoped to the
exact files the number counts. The panels are this page's warning surface; there is **no banner** to carry
an arrow button (§2.6), so the tile itself is what the user presses. Examples:

* **Add to IPFS** → the two-axis triage popup listing the files with **no pin decision**, with the two
  decision toggles
  ([decision_toggles.mdx §7](./decision_toggles.mdx#7-in-the-warning--to-do-batch-popups--three-axes-per-row-locked)).
  The count and the popup's list both **exclude foreign-pinned rows** (`FileRow.pinnedForeign` — already
  pinned on this node outside Large File Bridge): this metric is a *"pin these?"* ask, and it must never
  nag about a file whose bytes are already pinned ([one_repo.mdx §4.1 blockquote / §4.9 fix 4](./one_repo.mdx)).
  Counted in code:
  `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/store-model/units.service.ts` —
  `computeTaskMetrics` (popup list in
  `~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/metricWarningDefs.tsx` —
  `buildAddToIpfsWarning`).
* **Git Ignore** → the git-ignore popup over the large files Git is still allowed to commit
  ([git_ignore.mdx](./git_ignore.mdx)); its Apply writes **both** axes so an existing pin decision survives
  (§2.5.1, `buildGitIgnoreWarning`). It replaces the old **Big Not Git-ignored** tile.
* **Pull Down** → the "pull them down" popup ([warnings.mdx §10.8.12](./warnings.mdx)) of peer-pinned
  files missing locally.
* **Not Backed Up** → the pin/peer popup for `sync` files with 0 peers.
* **Compressible Videos** / **Compressible Images** → the compress popup with the compress axis
  ([compression.mdx](./compression.mdx)).
* **Transcribable** → the transcribe popup ([transcribe_calc_engine.mdx §4](./transcribe_calc_engine.mdx)).
* **AI Describable** → the describe popup ([describe_calc_engine.mdx §4](./describe_calc_engine.mdx)).
* **OCRable** → the OCR popup ([ocr.mdx §12](./ocr.mdx)).

A panel with a **green 0** opens the same popup in its **all-clear** state ("Nothing to do here"), or —
for a metric that has no dedicated popup yet — simply re-tunes the view to the tab where the user acts on
it (the spec backlog: only a few of the catalogued warnings are wired as popups today).

The panel → popup wiring opens `WarningPopup` directly (the same popup the banner arrow button opens),
driven per-metric by a `WarningDef` built from the live `RepoDetail`, in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/MetricsStrip.tsx` — `MetricsStrip`
(opens `WarningPopup` on click) and
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/metricWarningDefs.tsx` —
`buildMetricWarning` (maps a `MetricId` + `RepoDetail` to the popup's `WarningDef`; returns null ⇒ the
panel falls back to its tab-switch `onOpen`). The same module's `topRecommendation` resolves the
worst-first popup the header **"View recommendation ›"** primary opens ([one_repo.mdx §3.1](./one_repo.mdx)).

### 2.4.1 Every actionable metric tile opens a popup — the transcribe / compress / describe builders (the fix) (LOCKED)

**The bug this fixes.** Historically only the `undecided` (now `addToIpfs`), `pullDown`, and `ipfs-down` metrics had a
`WarningDef` builder. So when the user clicked the **`Transcribable`**, **`Compressible videos`**,
**`Compressible images`**, or **`AI Describable`** tile, `buildMetricWarning()` returned `null` and the tile
**did nothing useful** — it fell through to its `onOpen` fallback, which merely **re-selects the tab the
user is already on** (e.g. clicking `Transcribable` on the Transcribe tab re-selected Transcribe — a
visible no-op). The header, likewise, only ever offered `IPFS-down → pull-down → undecided`, so a repo
whose *only* pending work was "58 files could be transcribed" showed **no** blue primary at all — just the
gear. The task tiles looked clickable but led nowhere. **This is the reported defect.**

**The fix — a builder for every actionable metric.** Each task metric now has its own educate-and-fix
`WarningDef` builder, so clicking its tile opens the **same** `WarningPopup` the wired metrics already open,
scoped to exactly the files the number counts, and lets the user **start the work** from there:

| Metric tile | Builder | Popup it opens | Apply enqueues |
| --- | --- | --- | --- |
| **`Transcribable`** | `buildTranscribeWarning` | the transcribe popup — the counted candidates **checked by default** (§5.1) | background `transcribe` jobs, model-gated (§5.1, [transcribe_engine.mdx §3](./transcribe_engine.mdx)) |
| **`AI Describable`** | `buildDescribeWarning` | the describe popup — the counted candidates **checked by default** (§5.2.1) | background `describe` jobs, provider/model-gated ([ai_description.mdx §2](./ai_description.mdx)) |
| **`Compressible Videos`** | `buildCompressWarning` (`kind: "video"`) | the compress popup over the counted videos ([compression.mdx](./compression.mdx)) | background `compress` jobs |
| **`Compressible Images`** | `buildCompressWarning` (`kind: "image"`) | the compress popup over the counted images ([compression.mdx](./compression.mdx)) | background `compress` jobs |

* **`buildMetricWarning()` gains those cases.** Its `switch` now returns the transcribe / describe /
  compress-video / compress-image / OCR `WarningDef`s in addition to `addToIpfs` / `gitIgnore` / `pullDown`;
  it still returns `null` only for the genuinely popup-less metrics (`notBackedUp`, `pending`,
  `alreadyCompressed`, `transcribed`, `described`, `ocred`) whose tiles keep their tab-switch fallback.
  (`bigNotIgnored` is not on that list any more — it no longer exists as a `MetricId`, §2.5.1.)
* **The `>0` tint is unchanged** (§2.3): these are amber "offer" tiles, so the popup is an *offer to start*,
  never a data-at-risk alarm.
* **A green-`0` tile still opens the all-clear popup** (§2.2) — "Nothing to transcribe / describe / compress
  here."
* **Apply is gated exactly as the single-file path is.** The transcribe and describe applies run behind the
  same **model-readiness / consent** gate the per-row status icon uses (`withModelReady` for transcribe,
  the provider-key + model check for describe) — so a first click that needs the heavyweight model or an AI
  key **interposes the permission/provisioning flow, then resumes** the batch (§5.1,
  [transcribe_engine.mdx §3.6](./transcribe_engine.mdx), [ai_description.mdx §2](./ai_description.mdx)).

The new per-metric builders and their wiring into the tile-popup resolver are added in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/metricWarningDefs.tsx` —
`buildTranscribeWarning()` / `buildDescribeWarning()` / `buildCompressWarning()` (each maps `RepoDetail` +
`repoId` → a `WarningDef` whose `targets` are the counted files and whose `apply` enqueues the matching
background jobs), wired into `buildMetricWarning()`'s `switch` (the `transcribable` / `describable` /
`compressibleVideos` / `compressibleImages` cases) and ranked in `topRecommendation()` (§2.7). The tiles
still mount through `~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/MetricsStrip.tsx`
— `MetricsStrip` (unchanged: it already opens whatever `WarningDef` the resolver returns, else falls back to
`onOpen`).

### 2.6 The metric panels replace the warning banner on this page (LOCKED)

On the One-repo page there is **no warning banner** ([warnings.mdx §2](./warnings.mdx)) and **no
worst-first verdict banner** — the metric panels **are** the warning surface. Everything a banner would
have said is expressed by the panels: the count is the headline, the health tint (§2.3) is the severity,
the left-bar hover-info panel (§3) carries the one-sentence explanation, and clicking the panel opens the same
educate-and-fix popup a banner's arrow button would have opened (§2.4). This keeps the top of the page
quiet — a wall of narrow tiles, mostly green zeros — instead of a stack of banners.

* **No StatusBanner / RepoVerdict on this page.** The old worst-first diagnosis banner (IPFS down →
  pinned-but-no-peers → undecided → pending) is **removed**; each file-work condition now reads off its
  own panel (`Pending`, `Not Backed Up`, `Add to IPFS`, `Git Ignore`, `Pull Down`) and opens its own popup.
* **IPFS-down has no metric tile, so it rides the header primary.** The one condition that isn't a
  per-file count — the local IPFS engine being down — is surfaced by the header **"View recommendation ›"**
  primary ([one_repo.mdx §3.1](./one_repo.mdx)), which opens the worst-first recommendation
  (`metricWarningDefs.topRecommendation`: IPFS-down → pull-down → **add-to-IPFS** → …, §2.7). It is still
  also reflected in the disabled per-row pin toggles; it no longer raises a page banner here.
* This is **specific to the One-repo page.** Other pages (IPFS, Devices, File/Directory) still use the
  banner surface ([warnings.mdx §2](./warnings.mdx)); only this page swaps it for the metric panels.

### 2.5 Where the numbers come from (LOCKED)

Every metric count is derived from the **`RepoDetail`** the backend already returns for the screen
([one_repo.mdx §3.2](./one_repo.mdx)) plus the **per-file task classification** (§4.4). The backend joins
each `FileRow` against the special-files classifier and the storage index so the counts are exact:

* `counts.undecided`, `counts.pending`, `counts.ignored`, `counts.pinned` — already on `RepoDetail.counts`.
  `taskMetrics.undecided` (undecided **minus** foreign-pinned) is the number the **Add to IPFS** tile shows.
* `missingPinned.length` → **Pull down**.
* `sync` files with `peers.length === 0` → **Not backed up**.
* per-file `compress` / `transcribe` / `describe` / `ocr` state (§4.4) rolled up → **Compressible videos /
  images**, **Transcribable**, **AI Describable**, **OCRable** (and their done twins).
* **Git Ignore** is the one metric counted **on the client**, from the rows the page already holds, by the
  shared `gitIgnoreCandidates()` predicate (§2.5.1) — `!gitignore && !gitignoreLocked && !analysisOnly`.
  Its admitting scan rule is the **checked-in threshold** (50 MB default), **not** the 100 MB payload
  threshold (scan.mdx §4.1 rule 4 / §4.2), so the table and this number agree by construction. Before rule
  4 existed no row could have `gitignore === false` and the old `bigNotIgnored` count was **always 0**.

The per-tab metric counts are computed by the backend in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/store-model/units.service.ts` —
`computeRepoDetail` (which now attaches `detail.taskMetrics`, joining `composeFileRows` with
`classifySpecial` from `modules/scanner/special-file.service.ts` and `readStorageIndex`), and resolved per
tile in
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/metricWarnings.ts` —
`metricCount()` (with `gitIgnoreCandidates()` for the Git Ignore tile).

### 2.5.1 The two DECISION tiles — Add to IPFS + Git Ignore (LOCKED; there is no "Undecided" tile)

A decision has **always been two independent axes** — **Add to IPFS** and **git-ignore**
([decisions.mdx](./decisions.mdx)) — and the triage popup always wrote **both**. One number named
*Undecided* therefore could never say **which axis was outstanding**. It is now **two tiles**:

| Tile | `MetricId` | Counts | Popup |
| --- | --- | --- | --- |
| **Add to IPFS** | `addToIpfs` | Files with **no pin decision**, excluding rows whose bytes another IPFS tool already pinned on this node (`pinnedForeign`). **Exactly the number the old Undecided tile showed.** | `buildAddToIpfsWarning` — the renamed two-axis triage popup (Apply still writes both axes). |
| **Git Ignore** | `gitIgnore` | **Large files Git is still allowed to commit**: not already git-ignored, not owned by a rule we refuse to rewrite ([git_ignore.mdx §5.5](./git_ignore.mdx)), not a small `analysisOnly` row. | `buildGitIgnoreWarning` — **new**. Apply writes **BOTH** axes, so flipping git-ignore **never clobbers an existing pin decision**. |

* **The old `bigNotIgnored` tile ("Big, not git-ignored") is FOLDED INTO `Git Ignore`** and **no longer
  exists as a `MetricId`**.
* **ONE shared predicate (LOCKED).** The Git Ignore tile's **count** and its popup's **target list** both
  come from **`gitIgnoreCandidates()`**, so the number and the list are the **same set by construction** and
  can never disagree.
* **Placement.** On the **All** tab the two decision tiles **LEAD and sit ADJACENT** — two axes of one
  question, read as a pair. On the **IPFS** tab **only `Add to IPFS`** appears (that tab is the pin axis
  only, §1.3.1).
* **The FROZEN wire value `"undecided"` is UNCHANGED.** Only the metric **tile** named *Undecided* is gone —
  the `Decision` enum value, the Pin (IPFS) facet's `Undecided` value, and the One-repo summary line
  `N Pin · N Undecided · N Ignore · N Pinned locally` all stand exactly as specified
  ([one_repo.mdx §1 / §4.1](./one_repo.mdx)). Nobody may "clean up" the wire value.

Implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/metricWarnings.ts` — `MetricId`,
`METRIC_CATALOG`, `metricCount()`, `gitIgnoreCandidates()`;
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/metricWarningDefs.tsx` —
`buildAddToIpfsWarning()` / `buildGitIgnoreWarning()` (wired into `buildMetricWarning()`); tile order per tab
in `~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/taskTabs.config.ts` — `TASK_TABS`.

### 2.7 The per-metric blue header primary — every actionable metric can be the recommendation (LOCKED)

Because every actionable metric now has a popup (§2.4.1), every actionable metric can also be the single
**header primary** — the smart button top-right ([one_repo.mdx §3.1](./one_repo.mdx#31-header--two-rows-and-neither-of-them-wraps-locked)) that a
non-expert reads as *"the one thing to do next."* Before the fix that button only knew three
recommendations (`IPFS-down → pull-down → undecided`) and wore one generic label, **"View recommendation
›"**. The fix widens it to the whole task set and **labels the button per metric** so the verb is explicit.

**Worst-first order (LOCKED).** `topRecommendation()` walks the pending work in this fixed severity order
and returns the **first** one that is pending; that becomes the header primary:

```
ipfs-down  →  pull-down  →  add-to-IPFS  →  transcribe  →  describe  →  compress  →  ocr
  (bad)       (bad)          (warn)          (warn)         (warn)       (warn)      (warn)
```

* **The first three are data-integrity first**: a paused IPFS engine, then peer-pinned files not here yet,
  then files that still need a **pin** decision — the **`addToIpfs`** metric (§2.5.1), which is the tile
  formerly called *Undecided*. **`gitIgnore` is a tile, not a recommendation link** — it is an offer at the
  user's leisure, and it never outranks a backup problem.
* **Then the producing tasks**, in the order **transcribe → describe → compress → ocr** — transcription and
  description are content the user is usually chasing (searchable text, alt-text) and rank above the
  space-reclaiming compress offer; **OCR ranks last** because missing OCR text is a *search convenience*,
  the cheapest of the set to fix and the least costly to lack ([ocr.mdx §12.3](./ocr.mdx)). It must never
  outrank a backup problem — but on a repo where it is the only outstanding item it is, correctly, the whole
  recommendation.

**Per-metric label + color (LOCKED).** The button is **blue** (`--lfb-primary`) for every `warn`-state
recommendation and **red** (`--lfb-bad`) only for a `bad` one (IPFS-down, pull-down). Its **label names the
verb**, white text, with a **trailing right chevron `›`**:

| Top pending recommendation | Button label | Fill |
| --- | --- | --- |
| IPFS engine down | **View recommendation ›** (Start IPFS) | **red** (`--lfb-bad`) |
| Peer-pinned files not here | **View recommendation ›** (Pull down) | **red** (`--lfb-bad`) |
| Files need a pin decision (**Add to IPFS**) | **View recommendation ›** | **blue** (`--lfb-primary`) |
| Audio/video with no transcript | **Transcribe ›** | **blue** (`--lfb-primary`) |
| Images/videos with no AI description | **Describe ›** | **blue** (`--lfb-primary`) |
| Videos/images that could be compressed | **Compress ›** | **blue** (`--lfb-primary`) |
| Images/videos/PDFs whose text hasn't been read | **OCR ›** | **blue** (`--lfb-primary`) |

> **Why the first three keep the generic "View recommendation ›" label** and the task three get a verb: the
> integrity conditions are diagnoses ("here's what's wrong — view it"), whereas the task recommendations are
> a **single clear action** the user can name and start ("Transcribe ›"). Clicking any of them opens the
> **same** educate-and-fix popup its metric tile opens (§2.4 / §2.4.1) — the button is just the worst-first
> shortcut to it.

**Precedence and the "nothing" state (LOCKED, unchanged from [one_repo.mdx §3.1](./one_repo.mdx)):**

* **Scan-stale wins over everything.** If the repo is scan-stale (never scanned, or `lastScanAt` older than
  2 weeks) the button is **"Scan now"** — the metrics can't be trusted until a rescan, so no task
  recommendation shows.
* **All-clear = no button.** When no scan is due and `topRecommendation()` returns `null` (every task tile is
  a green `0`), there is **no** header primary; the wall of green zeros already says "all clear."
* **The button is model-gated on click, not on display.** A **"Transcribe ›"** / **"Describe ›"** primary
  shows whenever that work is pending, even if the heavyweight model or AI key isn't ready yet; the
  **consent/provisioning gate fires when the popup's Apply runs**, then resumes (§2.4.1, §5.1).

The worst-first pick (now including the transcribe / describe / compress builders), the per-metric label,
and the blue/red fill are resolved in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/metricWarningDefs.tsx` —
`topRecommendation()` (returns `{ metricId, warning, label }`, the order above) and the header render in
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/OneRepoPage.tsx` — `OneRepoPage`
(the `scanDue ? "Scan now" : topRec ? <primary label={topRec.label} …> : null` ternary, fill from
`topRec.warning.state === "bad" ? var(--lfb-bad) : var(--lfb-primary)`).

---

## 3. The explanation goes to the LEFT-BAR hover-info panel (LOCKED)

The metric panels are deliberately **terse** (§2.1), so their explanations have to live somewhere. They go
to the **non-intrusive hover-info panel at the BOTTOM OF THE LEFT BAR** — the reserved white block above
the **JFK Social** / **ACT 3 Filmmaking** external links and the account/login slot
([left_bar.mdx](./left_bar.mdx), [non_intrusive_tooltip.mdx](./non_intrusive_tooltip.mdx)).

> **There is NO hover-info region on this page any more.** The region that used to be **docked to the right
> of the metrics strip** is **removed**. It reserved **≥25%** of the row width for prose, and that
> reservation is exactly what forced the tiles onto a balanced second row. Deleting the region deletes the
> wrapping: the strip now takes the **full page width** on **one** row (§2,
> [one_repo.mdx §3.2](./one_repo.mdx#32-the-metrics-strip-never-wraps--and-the-explanation-lives-in-the-left-bar-locked)).

* **What drives it — every existing hover source, unchanged.** Hovering (or keyboard-focusing) **any** of
  these publishes text; each calls the **same `setHoverInfo(text)`** on enter and `setHoverInfo(null)` on
  leave, so **no call site had to be rewritten**:
  * a **metric panel** → what that metric means and what the popup will do (*"Large files that aren't set
    to sync over IPFS yet. Add them so every one of your computers keeps a copy."*);
  * the **File cell** of a table row → a one-line summary of that file for the active task (*"take_07.mov ·
    6.1 GB · video, no transcript yet — could be transcribed."*);
  * the **five icon control columns** — Pin · Ignore · Transcribe · AI Description · OCR — header icon **or**
    cell icon ([tables.mdx §4c](./tables.mdx));
  * the **Compress status icon** (§6);
  * an **action link** (the page-action row, [page_actions.mdx](./page_actions.mdx)) → what that action
    does and its current scope.
* **One render-nothing forwarder.** **`HoverInfoBridge`** is mounted once by the page; it renders no markup
  and forwards the current text into the app-wide **`HoverInfoProvider`** that the left-bar panel reads,
  clearing it on unmount so stale text never lingers after navigating away.
* **Default (nothing hovered).** The panel shows the **active tab's `defaultHint`** — the per-tab one-liner
  declared on `TaskTabDef` (§7): *"Transcribe: audio and video files. Files with no transcript yet are on
  top."*
* **It is the non-intrusive tooltip surface, in its one canonical home.** Same content model as
  [non_intrusive_tooltip.mdx](./non_intrusive_tooltip.mdx) — the panel renders a **`"text"` block** — so it
  never covers the row the user is reading and the panels need no inline prose.

The publish API and the forwarder are rendered in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/HoverInfoRegion.tsx` —
`setHoverInfo()` (module-level store, unchanged signature) and `HoverInfoBridge` (the render-nothing
component), publishing through
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/components/hoverinfo/useHoverInfoPublisher.ts` —
`useHoverInfoPublisher`, and rendered by
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/components/hoverinfo/HoverInfoPanel.tsx` —
`HoverInfoPanel` (the `"text"` block kind). The strip that mounts the bridge is
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/MetricsStrip.tsx` — `MetricsStrip`.

---

## 4. Per-tab tuning (LOCKED) — filter · columns · sort · metrics

Each tab declares four things. **Deep-thought defaults below** — every choice is "what is most useful for
this task, first."

> **The five icon control columns lead EVERY tab (LOCKED — supersedes the per-tab "single leading icon"
> language below).** [tables.mdx §4c](./tables.mdx) makes **Pin · Ignore · Transcribe · AI Description ·
> OCR** a **fixed leading block** on every file table, right of the select checkbox. So on **every** task
> tab, each tab's `columnIds` now **begin with all five** (`pinned`, `gitignore`, `transcribe`, `describe`,
> `ocr`) before the tab's own content columns — the Transcribe / Describe / OCR status icons are no longer
> *only* on their own tab, and no tab shows a single lone status icon as its one leading column anymore.
> Where a per-tab section below still says *"the `Transcribe` icon is the LEADING, header-less control
> column to the LEFT of `File`,"* read it as: that tab's icon is **one of the five** leading icon columns
> (in the fixed Pin·Ignore·Transcribe·AI Description·OCR order), each an **icon-only header** (not
> header-less — the header is the icon, [tables.mdx §4c.2](./tables.mdx)) whose hover fills the left-bar
> hover-info panel (§3). The **Decision** (`Pin decision` select), **Decided by**, **Decided**, and transfer
> **Status** columns are **removed from every tab** ([one_repo.mdx §4.3.1](./one_repo.mdx)); the two decision
> axes still exist as data and are set via the Pin/Ignore icons. The one thing that stays a **content**
> column is the **Compress status icon** (green) on the Compress tab (§4.3, §6) — Compress is not one of the
> five leading icons.

**Every tab carries the two promoted rail controls** ([tables.mdx §2.9/§2.10](./tables.mdx)) on top of its
own per-tab tuning:

* **`☑ Large files only`** — pinned at the head of the rail, **default ON** on every tab **except OCR**
  (§4.6), where it seeds **OFF**. When on it hides the small, not-git-ignored **analysis media** the scan
  now surfaces (`analysisOnly` rows, [scan.mdx §4.1](./scan.mdx)); the user turns it **off** to reach files
  **below** the large-file threshold — a small checked-in JPG, a short screen clip. The tab's `rowFilter`
  (scope, §1.3.1) and this toggle **compose by AND**: turning Large-files-only off never widens the tab's
  media scope, it only stops hiding small files within it.
* **File type facet** — `☑ Images ☑ Videos ☑ Audio ☑ PDFs ☐ Other` (Other off by default), inside the
  **Filter ⛛ dropdown** with an **All kinds** reset and a **More types…** disclosure ([tables.mdx §2.10](./tables.mdx)).
  The four common kinds always list without expanding; on a task tab the tab scope narrows what actually has
  rows (OCR → Images, Videos, **PDFs**), and the user isolates one class in a click.

### 4.1 All — the overview

* **Filter:** none of its own — but the **Large files only** toggle (default ON) means a fresh **All** tab
  shows every **large** file in the repo; turning it off reveals the small analysis media too
  ([one_repo.mdx §4](./one_repo.mdx)).
* **Columns:** the five leading icon columns `pinned`, `gitignore`, `transcribe`, `describe`, `ocr`
  ([tables.mdx §4c](./tables.mdx)), then `File`, `Size`, `Peers`, `CID`, `Changed`. The old `Decision`,
  `Decided by`, `Decided`, and `Status` columns are gone ([one_repo.mdx §4.3.1](./one_repo.mdx)). (The
  responsive-priority rules still drop the least-important content column as it narrows,
  [repos.mdx §3.2.1](./repos.mdx#321-responsive-column-priority--drop-the-least-important-column-as-the-table-narrows-locked).)
* **Default sort:** **most-recently-changed first** (`Changed` desc) — the overview tab leads with what
  moved. (Files still owing a decision are reached from the two decision tiles, §2.5.1, or the Pin (IPFS)
  facet.)
* **Metrics (all of them):** the two DECISION tiles LEAD and sit ADJACENT — `Add to IPFS` · `Git Ignore`
  (§2.5.1) — then `Pull down` · `Not backed up` · `Compressible videos` · `Compressible images` ·
  `Transcribable` · `AI Describable`. This is the "everything actionable" wall — mostly green zeros on a
  healthy repo. There is no `Undecided` tile and no `Big not git-ignored` tile: the first is now `Add to
  IPFS`, the second folded into `Git Ignore`.

### 4.2 IPFS — pinning across your computers

* **Filter:** IPFS-relevant files — **large OR media** (video/image/audio), matching the classifier's
  `large`/`video`/`image`/`audio` categories ([special_files.mdx](./special_files.mdx)). (Small non-media
  files that were never candidates don't appear.)
* **Columns:** the five leading icon columns ([tables.mdx §4c](./tables.mdx)), then `File`, `Size`, `Peers`,
  `CID`, `Changed`. The transfer **Status** column is removed ([one_repo.mdx §4.3.1](./one_repo.mdx)) — the
  **Pin** icon shows pinned state; the Compress content column is not shown here.
* **Default sort:** **by transfer urgency**, worst first — `missing`/`error` → `pending` → `undecided`
  (needs a decision) → `pinned`; secondary **Size desc** so the biggest at-risk bytes lead. A `sync` file
  with **0 peers** sorts above a comfortably-pinned one.
* **Metrics:** `Add to IPFS` · `Pull down` · `Not backed up` · `Pending`. **No `Git Ignore` tile here** —
  this tab is the pin axis only (§1.3.1, §2.5.1).

### 4.3 Compress — reclaim space

* **Filter:** compressible-relevant files — **videos and images** (`compressible` true, i.e. the
  extension-based `compressState === "should"`, plus already-`done` ones shown muted so the user sees the
  whole picture). Audio and non-media never appear (audio is never compressible — charter).
* **Columns:** the **five leading icon columns** (`pinned`, `gitignore`, `transcribe`, `describe`, `ocr` —
  [tables.mdx §4c](./tables.mdx)), then `File`, then **`Compress`** (the green compress status icon, §6) **as
  a content column** to the right of `File`, then `Size`, `Kind` (video/image), `Changed`. Compress is **not**
  one of the five leading icons (it is not a per-file yes/no artifact axis in the same sense), so it rides as
  the Compress tab's own status **content** column — using the same `StatusActionIcon` box grammar as the
  five, green done ([tables.mdx §4c.6](./tables.mdx)). The user can still pin what they compress via the
  leading `Pin` icon. (An **est. savings** column is a later add, driven by the learned size baseline —
  [compression.mdx](./compression.mdx) — and is out of scope until the baseline exists.)
* **Default sort:** **could-compress first**, then **Size desc** — the biggest reclaimable file at the top,
  because that is the biggest win.
* **Metrics:** `Compressible videos` · `Compressible images` · `Already compressed`.

### 4.4 Transcribe — get transcripts

* **Filter:** transcribable files only — **audio and video** (`transcribable` true;
  [special_files.mdx §3](./special_files.mdx#3-the-transcribable-sub-flag--video-has-audio-and-audio)).
  Images and non-media never appear.
* **Columns:** **`Transcribe`** (the three-state icon, §5) **as the LEADING control column — to the LEFT of
  `File`, with NO header title (icon only)** — then `File`, `Size`, `Kind` (video/audio), `Changed`. The
  product owner's instruction (LOCKED): *"move that transcribe icon over to the left of the file icon and
  get rid of the title of it — do it like the pin icons for the other tabs and the IPFS icon."* So the
  Transcribe icon occupies the **same leading, header-less control slot** the `Pin` / IPFS icons occupy on
  their tabs, not a labeled column after `File`. (A **duration** column is a later add once probed durations
  are indexed.)
* **Default sort:** **could-transcribe first** (the orange "not yet" rows), then **Size desc**.
* **Metrics:** `Transcribable` (could) · `Transcribed` (done).

### 4.5 AI descriptions — get AI descriptions

* **Filter:** describable files only — **images and videos** (`describable` true; the visual media
  [ai_description.mdx §1](./ai_description.mdx) can send to a vision model — image always, video only via
  Gemini). Audio and non-media never appear (there is nothing to *see* to describe).
* **Columns:** **`Describe`** (the three-state icon, §5.2) **as the LEADING control column — to the LEFT of
  `File`, with NO header title (icon only)** — then `File`, `Size`, `Kind` (image/video), `Changed`. This is
  the **identical leading, header-less control slot** the `Transcribe` icon occupies on its tab (§4.4) — the
  Describe tab mirrors Transcribe exactly. IPFS/decision columns are hidden.
* **Default sort:** **could-describe first** (the amber "not yet" rows), then **Size desc**.
* **Metrics:** `AI Describable` (could) · `Described` (done).

> **This mirrors §4.4 point-for-point.** The only differences from the Transcribe tab are the classifier
> flag (`describable` = image/video, vs `transcribable` = audio/video), the icon glyph and done-color
> (§5.2), and the target feature ([ai_description.mdx](./ai_description.mdx) rather than
> [Transcribe.mdx](./Transcribe.mdx)). Everything else — the leading header-less column, could-first sort,
> two metric tiles, metric-tile → popup (§2.4.1), and blue **"Describe ›"** header primary (§2.7) — is the
> same grammar.

> **The per-file task fields these tabs read are new on `FileRow`.** Today `FileRow` carries only the
> IPFS/decision axes. This feature adds **three** per-file task-status fields — `transcribe`, `compress`,
> and `describe` — each a `TaskStatus` (`could` / `done` / `na`), composed by the backend by joining each row
> against the classifier and the storage index (§2.5). See §5.2 and §7.

### 4.6 OCR — read the text out of images, videos & PDFs (LOCKED — the default-OFF exception)

* **Filter:** OCR-relevant files only — **images, videos, and PDFs** (`ocr` is `could` or `done`; audio has
  no pixels — [ocr.mdx §11.1](./ocr.mdx)). A **superset** of the AI-descriptions media axis (§4.5): the same
  images + video, **plus PDFs**, which are read by rasterizing each page ([ocr.mdx §1.7.1](./ocr.mdx)).
* **`Large files only` seeds OFF here — the one exception** ([tables.mdx §2.9](./tables.mdx),
  [ocr.mdx §11.4](./ocr.mdx)). OCR's real targets are a **screenshot, a receipt, a slide, a contract PDF, a
  downloaded JPG** — almost always **under** the large-file threshold. Opening the OCR tab with
  Large-files-only ON would hide exactly the files the user came to read, so the OCR tab opens with the toggle
  **off** and all image/video/PDF sizes visible. The toggle stays on the rail; the user can turn it **on** to
  focus on big files. **This is what makes "go to the OCR tab, turn off Large files, and OCR a small PDF" work
  out of the box** — the small file is now a scan candidate ([scan.mdx §4.1](./scan.mdx)) and nothing hides it.
* **Columns:** **`OCR`** (the three-state icon, [ocr.mdx §11.2](./ocr.mdx)) **as the LEADING, header-less
  control column — to the LEFT of `File`** (like Transcribe/Describe/Compress) — then `File`, `Size`,
  `Kind` (image/video/**pdf**), `Changed`.
* **Default sort:** **could-OCR first** (files whose text hasn't been read yet), then **Size desc**.
* **Metrics:** `OCRable` (could) · `OCRed` (done).
* **Filter the list to just PDFs** via the File-type facet in the Filter ⛛ dropdown ([tables.mdx §2.10](./tables.mdx))
  — check **PDFs** alone to OCR a folder of contracts.

### 4.7 Every file tab reaches small files — the reachability guarantee (LOCKED)

The scan now admits **image/video/audio AND PDF at any size** as `analysisOnly` candidates
([scan.mdx §4.1](./scan.mdx)) precisely so that turning **Large files only OFF** on **any** tab reveals
the small files the tab is about — a small clip on Transcribe/Compress, a small image on
Describe/OCR/Compress, a **small PDF on OCR**. `analysisOnly` rows are **never bridge payload**: they are excluded from auto-pin
exactly as the checked-in-nudge rows are ([scan.mdx §4.1](./scan.mdx), the `nudgeOnly` exclusion), so
surfacing a thumbnail for OCR never silently pins it to IPFS.

### 4.8 Remote-only rows belong on All and IPFS only (LOCKED)

A row in the file list is no longer always a local file. A **remote-only** row — a file another of the
user's computers has pinned and this one does not have yet, composed from the reconciled manifest with no
local bytes behind it — is defined and locked in
[storage_company.mdx §8.5](./storage_company.mdx#85-remote-only-rows--a-file-in-the-list-that-is-not-on-this-disk-locked)
and rendered by [one_repo.mdx §4.10](./one_repo.mdx#410-remote-only-rows--a-file-in-this-list-that-is-not-on-this-computer-locked).
This section says only where the **tabs** put it:

| Tab | Shows remote-only rows? | Why |
| --- | --- | --- |
| **All** | **Yes** | All is the overview — *"everything this repo has"* now honestly includes files that exist on the user's other computer. Hiding them here is what made the two-computer case invisible. |
| **IPFS** | **Yes** | The pin axis is exactly what a remote-only row is about: decided to sync, not here yet. It sorts to the **top** of the tab's worst-first transfer-urgency sort (§4.2) — a file that is *not on this machine at all* is the worst case of "missing". |
| **Compress** | **No** | Nothing to compress — there are no local bytes. |
| **Transcribe** | **No** | `transcribe` is `na`. |
| **AI descriptions** | **No** | `describe` is `na`. |
| **OCR** | **No** | `ocr` is `na`. |

* **The three analysis tabs must not be padded.** Each of those tabs' `rowFilter` selects files whose task
  status is `could` or `done`; a remote-only row is **`na`** on all three ([one_repo.mdx §4.10](./one_repo.mdx)),
  so it is filtered out **by the existing rule, with no special case** — the tab keeps its promise that every
  row on it is work that can actually be started. Showing a row the user cannot act on would inflate the tab
  and hand them a disabled icon as an answer.
* **The metrics strip follows the same line.** A remote-only row raises the **`Pull down`** tile (always) and
  the **`Add to IPFS`** tile (while this computer has not decided it), and raises **no** `Compressible
  videos` / `Compressible images` / `Transcribable` / `AI Describable` / `OCRable` / `Git Ignore` count
  (§2.5, §2.5.1). So on a laptop that is simply behind, the analysis tiles stay honest green zeros while
  `Pull down` carries the number.
* **The worst-first header recommendation is unchanged** (§2.7): `ipfs-down → pull-down → add-to-IPFS →
  transcribe → describe → compress → ocr`. Remote-only rows feed the **pull-down** link — already the second
  and one of the two `bad`-state links — so the laptop's header primary correctly reads a **red "View
  recommendation ›"** that opens the existing Pull-down popup. **No new recommendation link and no new
  ordering is introduced.**
* **`Large files only` does not hide them** on any tab ([tables.mdx §4e](./tables.mdx#4e-remote-only-rows-in-a-file-table-locked-site-wide)):
  a remote-only row is a large file, not an `analysisOnly` row. It is unaffected by the OCR tab's default-OFF
  exception (§4.6) because the OCR tab does not show it at all.

The tab that admits or excludes a remote-only row is the tab's own `rowFilter` in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/taskTabs.config.ts` — `TASK_TABS`
(the analysis tabs' `could`/`done` predicates already exclude `na`), over the rows composed by
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/store-model/units.service.ts` — `composeFileRows()`,
whose per-tile counts come from `computeTaskMetrics()`.

---

## 5. The Transcribe status icon — three states (LOCKED)

The Transcribe tab shows a **Transcribe status icon** per row, in a **leading, header-less control column to
the LEFT of `File`** (§4.4 — the same slot the `Pin` / IPFS icons occupy; the product owner: *"move it to
the left of the file icon and get rid of the title, like the pin and IPFS icons"*). It uses the lucide
**`Captions`** glyph (transcript/subtitles) in a 16×16 box, and has **three** states — echoing the
[decision-toggle](./decision_toggles.mdx) box grammar but with a **blue** "done" fill, because this is a
**status/action** indicator, not a stored two-axis decision. **It toggles between the two live states:**
"could" (a warm red/orange edge — see the color note) ⇄ "done" (blue fill, white glyph) as a file gains its
transcript.

| State | When | Fill | Edge | Glyph |
| --- | --- | --- | --- | --- |
| **Done** | The file **has a transcript** ([Transcribe.mdx §3](./Transcribe.mdx) — a `.transcription` exists under `.lfbridge/`). | **solid blue** (`--lfb-pin` / the pin blue) | none — **white** glyph/caption lines | **white** |
| **Could be transcribed** | The file is **audio or video and has no transcript yet** — it *could* be transcribed. | **~99% white / ~1% tint** (a barely-there wash) | **normal-thickness "actionable" edge** (`--lfb-decision-on`, the warm red/orange the product owner calls **red**) | red/orange |
| **Not applicable** | The file is **not** audio/video (or otherwise not transcribable). | **white** | **very thin, very-light-grey** (`--lfb-toggle-off-edge` at hairline weight) | faint grey |

> **Color note (reconciled).** The "could" edge uses the shared **`--lfb-decision-on`** token — a warm
> burnt color (`#c2410c`) the code and earlier revisions called **orange**. The product owner describes the
> same swatch as **red** (*"a red outline / red edge, no fill or a very very light red fill — that means it
> can be transcribed; after transcribing make it blue fill with white edge lines"*). These are the **same
> token and the same behavior** — a red/orange edge with a near-white fill for "could", toggling to solid
> blue with a white glyph for "done". No color change is needed in code beyond keeping this token; the Done
> state is already solid blue with a white glyph. The Compress icon (§6) uses the identical grammar with a
> **green** done color.

* The **orange "could" state is the actionable one** — it marks exactly a file the
  [What-Can-Be-Transcribed engine](./transcribe_calc_engine.mdx) would pick up (audio, or video **with**
  audio, no sidecar).
* **Click behavior** (a control cell — stops propagation, never navigates the row):
  * **Could →** offers/queues a transcription for that one file (an explicit-click action, background job,
    watchable on [Processing](./processing.mdx)) — the same enqueue the transcribe popup uses.
  * **Done →** opens/views the transcript.
  * **Not applicable →** inert (no hover affordance beyond the left-bar hover-info text).
* On the **Transcribe tab** the icon is the **leading, header-less control column** (§4.4); the tab's
  default sort puts the red/orange "could" rows first.

The Transcribe status icon (three states, click-to-transcribe / click-to-view) is rendered in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/components/TranscribeStatusIcon.tsx` —
`TranscribeStatusIcon` (state from `FileRow.transcribe`; done-blue token `--lfb-pin` in
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/styles.css`, via the shared `StatusActionIcon`).

### 5.1 The Transcribable metric → popup → Confirm → Processing flow (LOCKED)

Clicking the **`Transcribable`** metric panel (§2.4) — or the tab's "how many could be transcribed" number —
opens the **transcribe popup**, and from there the user drives a confirmed batch. The full path, exactly as
the product owner walked it (they were in the **charlie_kirk** repo's Transcribe tab, which read **58 ready
to transcribe**):

1. **Open.** Clicking the Transcribable metric opens the **educate-and-fix / transcribe popup** — the
   ~80%-of-screen list ([transcribe_calc_engine.mdx §4](./transcribe_calc_engine.mdx)) — listing all **58**
   candidates **checked by default**.
2. **Continue / adjust.** The user hits **Continue** into the checklist; they may **uncheck** any files to
   exclude. Whatever stays checked is the set.
3. **Confirm (the solid blue button).** They press the **solid-blue Confirm** button — labelled **"Transcribe
   N files"** (N = the checked count, e.g. 58). This is the commit.
4. **Model gate (may interpose).** If the heavyweight model is missing/partial, the permission popup +
   provisioning of [transcribe_engine.mdx §3](./transcribe_engine.mdx) runs first; because the user
   **confirmed**, the flow **resumes into the transcription** once provisioning finishes
   ([transcribe_engine.mdx §3.6](./transcribe_engine.mdx)).
5. **Async + the Processing tab appears.** The transcriptions run **asynchronously and in parallel**
   ([transcribe_engine.mdx §5](./transcribe_engine.mdx) — calibrated to the machine's cores/RAM). The
   conditional **Processing** surface appears ([processing.mdx](./processing.mdx)) so the user can **watch
   progress live** — which files are running in parallel, which are pending in the queue, and which failed
   (§processing.mdx). The popup can be closed immediately; the work keeps draining.
6. **Self-clearing.** As each file completes it gains its `.transcription` (its row's icon toggles to solid
   **blue** "done"), and it drops out of the next discovery run.

This confirm flow (default-checked 58 → uncheck → solid-blue "Transcribe N files" → enqueue → Processing) is
rendered in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/todo/TodoPage.tsx` — `TranscribeBatchPopup`
and `~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/MetricsStrip.tsx` — the
`transcribable` panel's `onOpen` (opens the same popup, gated by `withModelReady()` from
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/lib/transcribe.ts`).

> **The `Create Transcriptions` page action-link opens this SAME popup (LOCKED — [dialogs.mdx §6.1](./dialogs.mdx)).**
> On the One-repo page the **Create Transcriptions** / **Create AI descriptions** links in the action-links
> row ([page_actions.mdx §0.1](./page_actions.mdx)) are **not** a separate fire-and-forget enqueue — clicking
> either opens the **exact popup** the `Transcribable` / `AI Describable` metric tile opens (this §5.1 flow),
> scoped to the checked rows else the whole repo. The metric tile and the action-link are two triggers for
> **one** popup, so the user gets the same checked-by-default list, the same **"Transcribe N files"**
> confirm, and the same model-gated background enqueue whichever they click. (The tile builds its targets
> from the loaded `RepoDetail.files`; the action-link fetches the identical candidate set from the
> `/plan` preview endpoint over the same scope — [dialogs.mdx §5.2](./dialogs.mdx).)

### 5.2 The Describe status icon — three states, mirrors the Transcribe icon (LOCKED)

The **AI descriptions** tab (§4.5) shows a per-row **Describe status icon** built on the **same
`StatusActionIcon` grammar** as the Transcribe icon (§5) and the Compress icon (§6) — a 16×16 box in the
**leading, header-less control column to the LEFT of `File`** (§4.5), three states, echoing the
[decision-toggle](./decision_toggles.mdx) box but with its own **status** color. It uses the lucide
**`ScanText`** glyph (a "read the picture" mark, distinct from the Transcribe `Captions` glyph). It
**toggles could ⇄ done** as a file gains its `.ai_description` artifact ([ai_description.mdx §2](./ai_description.mdx)).

| State | When | Fill | Edge | Glyph |
| --- | --- | --- | --- | --- |
| **Done** | The file **has an AI description** — a `.ai_description` artifact exists under the owning root's `.lfbridge/` ([ai_description.mdx §2](./ai_description.mdx), same placement rule transcripts use — [Transcribe.mdx §3.4](./Transcribe.mdx)). | **solid teal** (`--lfb-describe-done` — the AI-description "done" color, distinct from the Transcribe blue and the Compress green) | none — white glyph | **white** |
| **Could be described** | The file is an **image or video with no description yet** — it *could* be described. | **~99% white / ~1% tint** wash | **normal-thickness "actionable" edge** (`--lfb-decision-on`, the shared warm red/orange "could" token) | red/orange |
| **Not applicable** | The file is **not** an image/video (audio, plain files), or a **video with no vision provider that supports video** (only Gemini describes video — [ai_description.mdx §4](./ai_description.mdx)). | **white** | **very thin, very-light-grey** (`--lfb-toggle-off-edge` hairline) | faint grey |

* The **"could" state is the actionable one** — it marks exactly a file the
  [What-Can-Be-Described engine](./describe_calc_engine.mdx) would pick up (image, or video the configured
  provider can describe, with no `.ai_description`).
* **Click behavior** (a control cell — stops propagation, never navigates the row):
  * **Could →** offers/queues an AI description for that one file (explicit-click, background `describe`
    job, watchable on [Processing](./processing.mdx)), **gated behind the AI provider-key + model
    readiness** exactly as the single-file describe path ([ai_description.mdx §2](./ai_description.mdx)) —
    the direct parallel of the transcribe icon's `withModelReady` gate (§5). A missing key opens the
    credentials setup ([warnings.mdx §10.6.3](./warnings.mdx)); a video with only image-capable keys is N/A
    with a "needs Gemini" reason ([warnings.mdx §10.6.4](./warnings.mdx)).
  * **Done →** opens/views the AI description.
  * **Not applicable →** inert (left-bar hover-info text only).
* On the **AI descriptions tab** the icon is the **leading, header-less control column** (§4.5); the tab's
  default sort puts the red/orange "could" rows first.

The Describe status icon (three states, click-to-describe / click-to-view) is rendered in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/components/DescribeStatusIcon.tsx` —
`DescribeStatusIcon` (state from the new `FileRow.describe` field; done-teal token `--lfb-describe-done`,
via the shared `StatusActionIcon` — the same component `TranscribeStatusIcon` and `CompressStatusIcon` use).
Its click is wired through `~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/OneRepoPage.tsx`
— `OneRepoPage` `onDescribeActivate()` (the describe analog of `onTranscribeActivate()`; the "could" branch
runs behind the describe consent gate, then enqueues).

### 5.2.1 The AI Describable metric → popup → Confirm → Processing flow (LOCKED)

Clicking the **`AI Describable`** metric tile (§2.4.1) — or pressing the blue **"Describe ›"** header primary
when describe is the top recommendation (§2.7) — opens the **describe popup**, the exact mirror of the
transcribe confirm flow (§5.1):

1. **Open.** Clicking `AI Describable` opens the **educate-and-fix / describe popup** — the ~80%-of-screen
   list ([describe_calc_engine.mdx §4](./describe_calc_engine.mdx)) — listing every image/video candidate
   **checked by default**.
2. **Continue / adjust.** The user may **uncheck** any files to exclude; whatever stays checked is the set.
3. **Confirm (the solid blue button).** They press **"Describe N files"** (N = the checked count). This is
   the commit.
4. **Provider / model gate (may interpose).** If no AI vision provider key resolves, or a video needs Gemini
   specifically, the credentials/setup gate interposes ([warnings.mdx §10.6.3 / §10.6.4](./warnings.mdx),
   [ai_description.mdx §2](./ai_description.mdx)); because the user **confirmed**, the flow **resumes into
   the descriptions** once a usable provider exists. If the file is owned by no storage yet, the
   first-time-storage wizard interposes ([Transcribe.mdx §3.5](./Transcribe.mdx), shared placement).
5. **Async + Processing.** The descriptions run **asynchronously and in parallel** (the same background
   `describe` jobs the page action uses, [ai_description.mdx §2](./ai_description.mdx)); the conditional
   **Processing** surface appears ([processing.mdx](./processing.mdx)) so the user watches progress live
   (running / pending / failed). The popup can close immediately; the work keeps draining.
6. **Self-clearing.** As each file completes it gains its `.ai_description` (its row's Describe icon toggles
   to solid **teal** "done"), and it drops out of the next discovery run.

This confirm flow (default-checked candidates → uncheck → solid-blue "Describe N files" → provider/model
gate → enqueue → Processing) is rendered in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/todo/TodoPage.tsx` — `DescribeBatchPopup`
(the describe analog of `TranscribeBatchPopup`) and `~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/metricWarningDefs.tsx`
— `buildDescribeWarning()` (the `AI Describable` tile's / header primary's `WarningDef`, gated by the describe
consent check in `~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/lib/describe.ts`).

### 5.3 The three status icons at a glance (LOCKED)

The Transcribe (§5), Describe (§5.2), and Compress (§6) icons are **one shared component in three
skins** — same box, same three states, same could ⇄ done toggle and click contract; only the **glyph** and
the **done-color** differ. This is deliberate: a user who learns one icon knows all three.

| Icon | Tab | Glyph | Classifier flag | "Done" color | Feature spec |
| --- | --- | --- | --- | --- | --- |
| **Transcribe** | Transcribe (§4.4) | `Captions` | `transcribable` (audio/video) | **blue** (`--lfb-pin`) | [Transcribe.mdx](./Transcribe.mdx) |
| **Describe** | AI descriptions (§4.5) | `ScanText` | `describable` (image/video) | **teal** (`--lfb-describe-done`) | [ai_description.mdx](./ai_description.mdx) |
| **Compress** | Compress (§4.3) | `Archive` | compressible (video/image) | **green** (health "good") | [compression.mdx](./compression.mdx) |

The **"could"** edge and the **N/A** hairline are **identical across all three** (`--lfb-decision-on` /
`--lfb-toggle-off-edge`); only the **filled "done"** swatch tells them apart.

---

## 6. The Compress status icon — same grammar (LOCKED)

The Compress tab shows a parallel **Compress status icon** per row, lucide **`Archive`** glyph, same
three-state box grammar as §5 with its own "done" color:

| State | When | Fill | Edge | Glyph |
| --- | --- | --- | --- | --- |
| **Done** | Already compressed (extension is already-lossy, or the in-file "LFBcompressed" marker is present). | **solid green** (health "good") | none | white |
| **Could be compressed** | A video/image whose `compressState === "should"`. | 99% white / 1% orange | medium-orange, normal thickness | orange |
| **Not applicable** | Not a compressible media type. | white | very thin, very-light-grey | faint grey |

Click: **Could →** offers compression (explicit-click confirm, background job, [compression.mdx](./compression.mdx));
**Done →** shows the compression record; **Not applicable →** inert. The Compress tab's default sort puts
the orange "could" rows first, then Size desc.

The Compress status icon is rendered in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/components/CompressStatusIcon.tsx` —
`CompressStatusIcon` (state from `FileRow.compress`).

---

## 7. How a tab re-tunes the table (mechanism) (LOCKED)

The whole tab system is **data-driven** from one config, so a tab is defined once and the page reads it:

```ts
// pages/repos/taskTabs.config.ts
export type TaskTabId = "all" | "ipfs" | "compress" | "transcribe" | "ai-descriptions" | "ocr";

export interface TaskTabDef {
  id: TaskTabId;
  label: string;
  icon: LucideIcon;
  columnIds: string[];                 // which of OneRepoPage's LfbColumns to show, in order
  defaultSort: SortingState;           // the tab's "most interesting first" sort
  rowFilter: (f: FileRow) => boolean;  // which files belong to this task
  metrics: MetricId[];                 // which MetricPanels the strip shows
  defaultHint: string;                 // what the LEFT-BAR hover panel shows at rest on this tab (§3)
  largeOnlyDefault: boolean;           // seed for the "Large files only" rail toggle — false on OCR (§4.6)
}

// The strip's render order; TaskTabs measures against it and overflows from the RIGHT (§1.4).
export const TASK_TAB_ORDER: TaskTabId[] =
  ["all", "ipfs", "compress", "transcribe", "ai-descriptions", "ocr"];
```

* **Columns** — `OneRepoPage` defines the union of all columns once (including the five icon control columns
  `pinned`, `gitignore`, `transcribe`, `describe`, `ocr` and the Compress-tab `compress` content column); **every**
  tab's `columnIds` **begin with all five icon columns in the fixed order** `pinned`, `gitignore`, `transcribe`,
  `describe`, `ocr` ([tables.mdx §4c](./tables.mdx)), then the tab's own content columns after `File`. No
  tab includes a `decision`, `decidedBy`, `decided`, or transfer `status` column anymore
  ([one_repo.mdx §4.3.1](./one_repo.mdx)). The
  responsive-priority drop rule ([repos.mdx §3.2.1](./repos.mdx#321-responsive-column-priority--drop-the-least-important-column-as-the-table-narrows-locked))
  still runs on whatever subset is visible.
* **Sort** — the tab's `defaultSort` becomes the table's `defaultSort`; the user can still re-sort (header
  click / dropdown) within the tab, and that choice persists per user per page
  ([tables.mdx §4](./tables.mdx)).
* **Filter** — `rowFilter` is applied to `detail.files` before the table sees them; the table's own
  search/filter still narrow further within the tab.
* **Metrics** — `metrics` selects which `MetricPanel`s the strip renders, **in the listed order**; each maps
  to a count (§2.5) and a `WarningDef` (§2.4). On **All** the list **starts with the two decision tiles,
  adjacent** (`addToIpfs`, `gitIgnore`); on **IPFS** it carries `addToIpfs` only (§2.5.1).
* **Hover default** — `defaultHint` is what the **left-bar hover-info panel** shows on that tab when
  nothing is hovered (§3).

The active-tab column/sort/filter/metric selection is applied in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/OneRepoPage.tsx` — `OneRepoPage`
(reads `TASK_TABS[activeTab]`, passes the projected `columns` + `defaultSort` to `DataTable` and the
filtered `data`, and the tab's `metrics` to `MetricsStrip`).

---

## 8. Acceptance criteria

1. The One-repo header's **first row** shows a **tab strip a little right of center** — **six** tabs:
   **All** (default), **IPFS**, **Compress**, **Transcribe**, **AI descriptions**, **OCR** — with the repo
   **name** on the left (never wrapped, never squeezed) and the **gear** + the smart header primary on the
   right; **Pin now is NOT in the header** (it is an action-link / More-menu item). The active tab shows a
   2px `--lfb-primary` underline. The **absolute path** is on its own full-width second row, never wrapped
   ([one_repo.mdx §3.1](./one_repo.mdx#31-header--two-rows-and-neither-of-them-wraps-locked)).
1a. **Tab overflow (§1.4):** the strip is **width-measured**; tabs that don't fit move — **always from the
   RIGHT** — into a trailing **down-chevron overflow menu**, which renders **only** when something
   overflows (no chevron, no chrome, no reserved gap when all six fit). An **active** tab that would land
   in the overflow is **swapped back** into the visible set so the underline is never hidden. Choosing from
   the menu behaves **identically** to clicking a visible tab. Measurement is a hidden off-screen copy with
   identical markup plus a `ResizeObserver` refit — the same technique the action-links row uses for
   "More ▾".
2. Selecting a tab re-tunes, in one client-side change (no refetch, no mutation), the **metrics strip**,
   the **visible columns**, the **default sort**, and the **row filter** per §4.
3. **All** is metrics-forward: the strip shows every actionable metric; a metric with **0** renders a
   **light-green rounded rectangle with a big `0`**; a metric `> 0` is health-tinted per §2.3.
4. Every metric panel is **narrow (as wide as its widest word)** with a **small label** (wrapping *inside*
   the tile, rendered in the catalog's **exact casing** — never CSS-`capitalize`d, so "Add to IPFS",
   "AI Describable" and "OCRable" read correctly) stacked over a **big centered number**, **no chevron** and
   no description prose;
   **clicking the panel opens that metric's warning popup** ([warnings.mdx §4](./warnings.mdx)) scoped to
   the counted files (or its all-clear state at 0). **There is no warning banner on this page** — the
   panels are the only warning surface (§2.6).
5. **The metrics strip takes the FULL page width, is ONE row, and NEVER wraps** (`flex-nowrap`, `shrink-0`
   tiles; horizontal scroll on a pathologically narrow window). **There is no hover-info region beside the
   panels** — the docked region and its ≥25% width reservation (and the balanced second row that
   reservation forced) are **gone**. The explanation instead appears in the **hover-info panel at the bottom
   of the left bar**, above the JFK Social / ACT 3 Filmmaking links and the account slot: every source —
   metric tile, **File** cell, the five icon control columns, the **Compress** status icon, the action links
   — calls the same `setHoverInfo(text)`, and the render-nothing **`HoverInfoBridge`** forwards it into the
   app-wide `HoverInfoProvider`. At rest the panel shows the active tab's **`defaultHint`** (§3).
5a. **Two decision tiles, no "Undecided" tile (§2.5.1):** **Add to IPFS** (`addToIpfs` — files with no pin
   decision, foreign-pinned rows excluded; exactly the old Undecided number; opens `buildAddToIpfsWarning`)
   and **Git Ignore** (`gitIgnore` — large files Git may still commit: `!gitignore && !gitignoreLocked &&
   !analysisOnly`; opens the new `buildGitIgnoreWarning`, whose Apply writes **both** axes so a pin decision
   is never clobbered). The old **`bigNotIgnored`** tile is **folded into Git Ignore** and no longer exists
   as a `MetricId`. The Git Ignore tile's **count and its popup's list share the one `gitIgnoreCandidates()`
   predicate** and can never disagree. On **All** the two lead and are **adjacent**; on **IPFS** only
   **Add to IPFS** shows. The frozen wire value `"undecided"` and the One-repo summary line are unchanged.
6. The **IPFS** tab filters to large/media files, shows the IPFS columns, and sorts by transfer urgency
   (worst first) then Size desc (§4.2).
7. The **Compress** tab filters to compressible videos/images, shows the **Compress status icon** as a
   **leading, header-less control column to the LEFT of `File`** (§4.3/§6), and sorts could-compress-first
   then Size desc.
8. The **Transcribe** tab filters to audio/video, shows the **three-state Transcribe status icon** (§5) **as
   a leading, header-less control column to the LEFT of `File`** (like the Pin/IPFS icons) — **solid blue =
   done (white glyph)**, **red/orange-edge / ~99%-white fill = could be transcribed** (audio/video, no
   transcript; the shared `--lfb-decision-on` token the product owner calls "red"), **thin very-light-grey
   edge = not applicable** — and sorts could-transcribe-first then Size desc. The icon **toggles could ⇄
   done** as a file gains its transcript.
8a. **Confirm flow (§5.1):** clicking the **`Transcribable`** metric opens the ~80% popup with all
    candidates **checked by default** (58 in the worked case); **Continue** → the user may **uncheck** some;
    the **solid-blue "Transcribe N files"** button **confirms**; the model-provisioning gate may interpose
    ([transcribe_engine.mdx §3](./transcribe_engine.mdx)) and then **resume**; transcriptions run **async and
    in parallel**, and the **Processing** surface appears so progress (running / pending / failed) is
    watchable.
9. The Transcribe/Compress status icons are **control cells** (stop propagation, never navigate): the
   red/orange "could" state click **queues** the action (background job); the done state **views** the
   output; the N/A state is inert.
10. The per-file `transcribe`, `compress`, and `describe` task-status fields are composed on `FileRow` by
    the backend (§2.5, §7) by joining the classifier + storage index; the per-tab metric counts come from the
    same `RepoDetail`.
11. **The metric-tile fix (§2.4.1):** clicking an actionable **task** tile — `Transcribable`,
    `AI Describable`, `Compressible Videos`, `Compressible Images`, `OCRable` — opens that metric's educate-and-fix popup
    (via `buildTranscribeWarning` / `buildDescribeWarning` / `buildCompressWarning`), scoped to the counted
    files, and lets the user **start the work**; it no longer no-ops into re-selecting the current tab. A
    green-`0` tile opens the all-clear popup. The transcribe/describe applies are **model/consent-gated**
    (withModelReady / provider key), interposing then resuming, exactly like the single-file path.
12. **The per-metric blue header primary (§2.7):** the header primary is labeled **per metric** — generic
    **"View recommendation ›"** for the integrity trio (the third being the **Add to IPFS** tile), then
    **"Transcribe ›"**, **"Describe ›"**, **"Compress ›"**, **"OCR ›"** for the task set — resolved
    worst-first **ipfs-down → pull-down → add-to-IPFS → transcribe → describe → compress → ocr**, **red** (`--lfb-bad`) for a `bad` recommendation and **blue**
    (`--lfb-primary`) otherwise, with a trailing `›`. Scan-stale still wins ("Scan now"); all-clear shows no
    button; the model/consent gate fires on the popup's Apply, not on display.
13. **The AI descriptions tab (§4.5, §5.2, §5.2.1):** the tab beside Transcribe filters to describable
    **images/videos**, shows the **three-state Describe status icon** (`ScanText` glyph; **teal** done ·
    red/orange-edge "could" · thin-grey N/A) as the **leading, header-less control column to the LEFT of
    `File`**, sorts could-describe-first then Size desc, exposes `AI Describable` / `Described` metric tiles,
    and drives the same metric-popup + **"Describe ›"** header-primary + confirm-flow behavior as Transcribe
    (§5.2.1). The Transcribe / Describe / Compress icons are **one shared `StatusActionIcon` in three skins**
    (§5.3) — same states/toggle/click contract, differing only in glyph and done-color.
14. **The five icon control columns lead EVERY tab** ([tables.mdx §4c](./tables.mdx), §4 preamble): every
    tab's `columnIds` begin with **Pin · Ignore · Transcribe · AI Description · OCR** in that fixed order,
    ahead of the tab's content columns; each is narrow (`tight`/`px-1`) with an **icon-only header** whose
    hover fills the **left-bar hover-info panel** (§3), all through the shared **`StatusActionIcon`** with unique done colors (Pin
    blue, Ignore orange, Transcribe **indigo**, AI Description teal, OCR violet). The **Decision**,
    **Decided by**, **Decided**, and transfer **Status** columns are **removed from every tab**
    ([one_repo.mdx §4.3.1](./one_repo.mdx)); the two decision axes still exist as data, set via the Pin/Ignore
    icons. The **Compress** tab additionally shows its **green Compress status icon** as a **content** column
    (§4.3, §6) — Compress is not one of the five leading icons.
15. **Remote-only rows appear on All and IPFS, and on no other tab (§4.8).** A row with
    `presence: "remote-only"` ([storage_company.mdx §8.5](./storage_company.mdx#85-remote-only-rows--a-file-in-the-list-that-is-not-on-this-disk-locked),
    [one_repo.mdx §4.10](./one_repo.mdx#410-remote-only-rows--a-file-in-this-list-that-is-not-on-this-computer-locked))
    shows on **All** and on **IPFS** (where it sorts to the **top** of the worst-first transfer-urgency
    sort), and is excluded from **Compress / Transcribe / AI descriptions / OCR** by those tabs' existing
    `could`/`done` predicates because its task status is **`na`** — no special case, and no tab is ever
    padded with work that cannot run. It raises the **`Pull down`** tile (always) and **`Add to IPFS`**
    (while undecided here) and **no** compression / analysis / `Git Ignore` count, so the analysis tiles stay
    green zeros on a merely-behind computer. The worst-first chain and the header primary are **unchanged** —
    remote-only rows feed the existing **pull-down** link.

<!--
  Task Tabs = the SIX-tab strip (All / IPFS / Compress / Transcribe / AI descriptions / OCR) in ROW 1 of
  the One-repo header (right of center; gear + header primary stay right, the repo NAME stays left and is
  never wrapped or squeezed; the absolute path has its OWN full-width row 2). Selecting a tab re-tunes FOUR
  things: metrics strip, visible columns, default sort, row filter (client-side; no refetch, no mutation).
  Default = All (ephemeral state, NOT config.yaml).
  §1.4 OVERFLOW CHEVRON (LOCKED): the strip is WIDTH-MEASURED — tabs that don't fit move, ALWAYS FROM THE
  RIGHT, into a trailing ⌄ overflow menu that renders ONLY when something overflows (no chrome at all when
  all six fit). An ACTIVE tab that would overflow is SWAPPED BACK into the visible set so the underline is
  never hidden. Selecting from the menu == clicking a visible tab. Technique = hidden off-screen copy with
  identical markup (each tab's right edge + the chevron width) + ResizeObserver refit — same as the page
  action-links "More ▾". This works because PageHeader inverts the priority: title sized to CONTENT (capped
  50%), the ACTIONS SLOT takes the remainder and SHRINKS FIRST. Code: TaskTabs.tsx, PageHeader.tsx.
  METRIC PANELS: NARROW (w-min, as wide as widest word), small wrapping label over a BIG centered number,
  NO chevron; labels render in the catalog's EXACT casing — NOT CSS-capitalized ("Add to IPFS",
  "AI Describable", "OCRable"); clicking the whole panel opens that metric's warning popup (warnings.mdx
  §4); count 0 => LIGHT-GREEN rounded rect w/ big 0;
  >0 = health-tinted (red=at-risk, amber/accent=action). THE PANELS REPLACE THE WARNING BANNER on this
  page (§2.6) — no StatusBanner/RepoVerdict. IPFS-down has no tile, so it rides the header "View
  recommendation ›" primary (metricWarningDefs.topRecommendation, one_repo §3.1); Pin now is an
  action-link + a More-menu item, never a header button (the single-entity More button = the word "More" +
  a down chevron, NO ⋮ glyph, NO bordered box — menus.mdx §4.1 owns that).
  §3 NO HOVER-INFO REGION ANY MORE: the region docked to the RIGHT of the strip is DELETED — its >=25%
  width reservation was exactly what forced the tiles onto a balanced 2nd row. The strip now takes the FULL
  page width on ONE flex-nowrap row of shrink-0 tiles and NEVER wraps (overflow-x scroll is the only
  escape). The text goes to the LEFT-BAR hover-info panel (the reserved white block above the JFK Social /
  ACT 3 Filmmaking links and the account slot): every source (metric tile, File cell, the 5 icon control
  columns, the Compress icon, action links) still calls the SAME setHoverInfo(text), and the render-nothing
  HoverInfoBridge forwards it into the app-wide HoverInfoProvider; at rest it shows the tab's defaultHint.
  Code: MetricsStrip.tsx, HoverInfoRegion.tsx (setHoverInfo + HoverInfoBridge),
  components/hoverinfo/HoverInfoPanel.tsx ("text" block kind), useHoverInfoPublisher.ts.
  §2.5.1 TWO DECISION TILES, NO "Undecided" TILE: Add to IPFS (addToIpfs = no pin decision, minus
  foreign-pinned = EXACTLY the old Undecided number; buildAddToIpfsWarning, the renamed two-axis triage
  popup) + Git Ignore (gitIgnore = large files Git may still commit: !gitignore && !gitignoreLocked &&
  !analysisOnly; the NEW buildGitIgnoreWarning whose Apply writes BOTH axes so a pin decision is never
  clobbered). The old bigNotIgnored tile FOLDED IN — it is no longer a MetricId. INVARIANT: the tile count
  and the popup's target list share ONE predicate, gitIgnoreCandidates(), so they can never disagree.
  All tab = the two lead + are ADJACENT; IPFS tab = Add to IPFS only. THE WIRE VALUE "undecided" IS
  UNCHANGED (Decision enum, Pin facet value, and the "N Pin · N Undecided · N Ignore · N Pinned locally"
  summary line all stand) — only the TILE is gone; never "clean up" the wire value.
  TILE RENAME: Describable → "AI Describable" (MetricId `describable` unchanged); done-twin stays
  "Described".
  ROW MODIFIED-CLICK: DataTable's rowHref(row) makes ⌘/Ctrl/shift/middle-click open a NEW TAB; plain click
  still routes in-page (tables.mdx §3.6, one_repo §4.7).
  PER TAB (§4): All=all files, full cols, changed-desc; IPFS=large|media, IPFS cols, transfer-urgency
  sort; Compress=compressible video/image, Compress icon col, could-first+size-desc; Transcribe=audio/video,
  Transcribe 3-state icon, could-first+size-desc. TRANSCRIBE ICON is now a LEADING, HEADER-LESS control col
  to the LEFT of File (like Pin/IPFS — owner: "move it left of the file icon, drop the title"). States:
  Done=solid BLUE fill+white glyph · Could(audio/video, no transcript)=red/orange normal-edge
  (--lfb-decision-on #c2410c = the "red" the owner means) + ~99%-white/~1%-tint fill · N/A=white + very-thin
  very-light-grey edge. TOGGLES could<->done. §5.1 CONFIRM FLOW: Transcribable metric → ~80% popup all-checked
  (58) → Continue → uncheck → solid-blue "Transcribe N files" Confirm → (model-provision gate transcribe_engine
  §3 may interpose+resume) → async PARALLEL transcribe (transcribe_engine §5) → Processing surface appears.
  COMPRESS ICON (§6, Archive glyph) same grammar + leading header-less col, GREEN done. New FileRow
  fields: transcribe, compress (backend join classifySpecial + readStorageIndex). Code: TaskTabs.tsx,
  taskTabs.config.ts, MetricsStrip.tsx/MetricPanel (opens WarningPopup directly), metricWarningDefs.tsx
  (buildMetricWarning + topRecommendation + scanIsStale), HoverInfoRegion.tsx, TranscribeStatusIcon.tsx,
  CompressStatusIcon.tsx; units.service.ts computeRepoDetail attaches taskMetrics + per-file task status.
  Keep aligned with one_repo.mdx (host), warnings.mdx (popups + metric-panel presentation), decision_toggles.mdx
  (toggle grammar the icons echo), Transcribe.mdx/compression.mdx/ipfs_ui.mdx (tasks), special_files.mdx
  (classifier), left_bar.mdx + non_intrusive_tooltip.mdx (the hover panel + its content model), tables.mdx (per-tab column visibility + sort persistence).
  ── THE FIX + AI DESCRIPTIONS (this revision) ──
  §2.4.1 THE BUG FIX: previously only undecided/pullDown/ipfs-down had WarningDef builders, so clicking
  Transcribable/Compressible*/AI Describable tiles did NOTHING (fell through to onOpen = re-select current tab)
  and the header showed no task primary. FIX adds buildTranscribeWarning/buildDescribeWarning/buildCompressWarning
  in metricWarningDefs.tsx, wired into buildMetricWarning()'s switch (transcribable/describable/compressibleVideos/
  compressibleImages cases) — every actionable metric tile now opens the scoped WarningPopup + lets user START
  the work; transcribe/describe applies are model/consent-gated (withModelReady / provider key), interpose+resume.
  Still returns null (tab-switch fallback) for notBackedUp/pending/alreadyCompressed/transcribed/described/ocred
  (bigNotIgnored is NOT on that list — it no longer exists as a MetricId; addToIpfs + gitIgnore both HAVE builders).
  §2.7 PER-METRIC HEADER PRIMARY: topRecommendation() worst-first order LOCKED = ipfs-down → pull-down →
  ADD-TO-IPFS → transcribe → describe → compress → ocr (gitIgnore is a TILE, never a recommendation link);
  label per metric — generic "View recommendation ›" for the integrity trio (ipfs-down/pull-down red
  --lfb-bad; add-to-IPFS blue), then "Transcribe ›" / "Describe ›" / "Compress ›" / "OCR ›" blue
  (--lfb-primary), trailing › chevron. Scan-stale → "Scan now" wins; all-clear → no button;
  gate fires on Apply not display. §4.5 "AI descriptions" TAB (beside Transcribe): filter=describable
  image/video, LEADING header-less Describe icon col left of File, could-first+size-desc sort, metrics
  "AI Describable"/Described. §5.2 DESCRIBE STATUS ICON (ScanText glyph, TEAL --lfb-describe-done done): could(image/
  video no desc)=red/orange edge+99%-white · na=hairline grey (incl. video w/ no video-capable provider).
  DescribeStatusIcon.tsx via shared StatusActionIcon; onDescribeActivate() in OneRepoPage. §5.2.1 DESCRIBE
  CONFIRM FLOW mirrors §5.1: AI Describable tile → ~80% popup checked-by-default → "Describe N files" →
  provider/model/storage gate interpose+resume → async parallel describe jobs → Processing. §5.3 the three
  icons = ONE StatusActionIcon in 3 skins (Transcribe=Captions/blue, Describe=ScanText/teal, Compress=Archive/
  green); could-edge + N/A hairline identical across all three. New FileRow.describe (TaskStatus); TaskMetrics
  gains describable/described; TaskTabId = all|ipfs|compress|transcribe|ai-descriptions|ocr. Owns the task-tab surface for transcribe AND describe;
  ai_description.mdx owns the describe feature, describe_calc_engine.mdx the bulk-discovery engine.
  §4.8 (NEW, LOCKED) REMOTE-ONLY ROWS ON THE TABS: a FileRow may be presence:"remote-only" (a peer computer
  pinned it, this one doesn't have the bytes) — defined+LOCKED in storage_company.mdx §8.5, rendered by
  one_repo.mdx §4.10; this file only places it. SHOWN on All (the overview must honestly include what your
  other computer has) and IPFS (it is the purest "decided to sync, not here" case and sorts to the TOP of
  the tab's worst-first transfer-urgency sort). NOT shown on Compress/Transcribe/AI descriptions/OCR — its
  transcribe/describe/ocr are `na`, so those tabs' EXISTING could/done rowFilters already exclude it: NO
  special case, and no analysis tab is ever padded with work that cannot run (a disabled icon is not an
  answer). Metrics follow: raises Pull down ALWAYS + Add to IPFS while undecided here, raises NO
  compressible/transcribable/describable/OCRable/Git Ignore count — so on a merely-behind laptop the
  analysis tiles stay green zeros while Pull down carries the number. The worst-first chain (§2.7) is
  UNCHANGED: remote-only rows feed the existing pull-down link (already a `bad` state, already 2nd), so no
  new recommendation and no re-ordering. Large-files-only never hides them (tables.mdx §4e); the OCR
  default-OFF exception is irrelevant since OCR doesn't show them. AC 15 added.
-->

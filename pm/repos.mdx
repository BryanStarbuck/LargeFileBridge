---
title: LargeFileBridge — Repos
description: The landing screen (route /) — one TanStack Table listing every git repo on THIS computer that LargeFileBridge manages. One row per repo, with a leading bookmark (favorite) toggle, the repo name, local path, large-file counts (Synced / Pending / Undecided / Ignored), peer count, last-sync time, and a rolled-up sync status. Above the table sits the standard control row (search box + icon-only filter and sort dropdowns). Clicking a row opens that repo's per-file sync screen (one_repo.mdx). Defines the table columns, the leading bookmark toggle (persisted per-repo in config.yaml), the RepoRow type, the add-repo action, the row → one_repo navigation, pagination at 500, and empty/loading states.
---

# LargeFileBridge — Repos

The **Repos** page is the landing view of LargeFileBridge and the screen users live in. It answers
one question at a glance: *for every repo on this computer, are my large files in sync across my
other machines?* It is a header over **one TanStack Table** — **one row per repo, for every git repo on
this computer** (LFB discovers and knows them all automatically — there is no "managed" opt-in;
[sync_process.mdx §1](./sync_process.mdx#1-lfb-knows-every-repo--there-is-no-lfb-managed-gate-locked)) —
with the standard search / filter / sort control row above it.

The left bar's **Repos** item (order 1, icon `FolderGit2`, route `/` — source of truth
`pm/left_bar.yaml`) lands here. Clicking any repo row opens that repo's per-file screen
([One repo](./one_repo.mdx)).

The Repos page is loaded and rendered as one TanStack table in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/ReposPage.tsx` — `ReposPage` component

This spec builds on the siblings and does not repeat them:

* [README](../README.md) — the product charter: Git tracks the small files; LFBridge moves the big,
  git-ignored files peer-to-peer over IPFS.
* [Left bar](./left_bar.mdx) — the left navigation (data source of truth `pm/left_bar.yaml`); this
  document does not redefine it and only shows where it sits.
* [One repo](./one_repo.mdx) — the per-file sync screen a row click opens.

---

## 1. Vocabulary — Repo → Large file → Decision (LOCKED)

Three terms, used consistently across every spec and every screen:

| Term | Definition |
| --- | --- |
| **Repo** | **Any** git repository on this computer. LFBridge **discovers and knows every repo automatically** — a repo is never "unmanaged" ([sync_process.mdx §1](./sync_process.mdx#1-lfb-knows-every-repo--there-is-no-lfb-managed-gate-locked)); IPFS pinning of its bytes is the only opt-in. It has a local **path**, a **name** (the folder name), a git-ignore that excludes the big files, and a **manifest** (checked into git) listing the large files by path + IPFS CID. |
| **Large file** | A big, git-ignored file inside a repo (video, image, model weights, dataset, render output). LFBridge — not git — is responsible for moving its bytes between your computers. |
| **Decision** | Per large file, whether LFBridge should move it: **Sync**, **Ignore** (deliberately not synced), or **Undecided** (LFBridge found it but the user has not yet chosen). Defined in full on the [One repo](./one_repo.mdx) screen; the Repos table only shows the **counts** per decision. |

```ts
// The per-repo summary the backend returns for each Repos-table row.
export interface RepoRow {
  repoId: string;        // stable id (hash of the absolute path)
  bookmarked: boolean;   // user favorite — persisted per-repo in config.yaml (§8); drives the leading ribbon toggle
  name: string;          // folder name
  path: string;          // absolute local path
  counts: {
    synced: number;      // large files in sync (CID present + pinned locally) — INCLUDES files
                         //   that were already IPFS-pinned and got imported (ipfs.mdx §4)
    pending: number;     // decided "Sync" but not yet transferred (fetch/push queued)
    undecided: number;   // found by LFBridge, no Sync/Ignore decision yet
    ignored: number;     // decided "Ignore" — deliberately not synced
  };
  peerCount: number;     // how many of your computers currently pin this repo
  lastSyncAt: string | null;  // ISO date-time of the last completed sync, or null
  status: RepoStatus;    // rolled-up sync status (see §4.2)
}

export type RepoStatus =
  | "up_to_date"   // every "Sync" file present locally and on >=1 peer
  | "syncing"      // a transfer is in progress
  | "behind"       // pending > 0 (files owed in or out)
  | "needs_review" // undecided > 0 (user has decisions to make)
  | "error"        // last sync failed / IPFS node unreachable
  | "never";       // repo added, never synced
```

The per-repo summary row (`RepoRow`) each table row shows is assembled by the backend in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/store-model/units.service.ts` — `computeRepoRow`

---

## 2. Layout

The screen has four regions: the **left bar** (see [Left bar](./left_bar.mdx)), the **brand
header**, the **top-right Add repo button**, and the **Repos table** with its control row.

```
┌────────────┬───────────────────────────────────────────────────────────────────────────┐
│            │  Repos                                                   ┌──────────────┐   │
│  LEFT BAR  │  (large + bold, NORMAL letter spacing)                  │ + Add repo   │   │
│            │                                                         └──────────────┘   │
│ Repos ◀    ├───────────────────────────────────────────────────────────────────────────┤
│ Peers      │  search…                                                      ⇅   ⛛        │ ← control row
│ Sync       ├───────────────────────────────────────────────────────────────────────────┤
│ Settings   │     Repo        Path             Synced Pending Undec Ignored Peers Last     │
│            │  ▮  Our_Movies  ~/BGit/…/Movies   128    2       5     40      3   09:14 › ⋮│ ← ▮ = solid yellow (bookmarked)
│            │  ▯  render_farm ~/work/render       0    311      0      0      1   —     › ⋮│ ← ▯ = gray outline (not bookmarked)
│            │  ▮  photos_2026 ~/Pictures/2026    980    0      12      0      2   06:30 › ⋮│ ← trailing ⋮ = row kebab (repo catalog)
│            ├───────────────────────────────────────────────────────────────────────────┤
│  [account] │  3 repos                                        page 1 / 1        [500 ▾]   │ ← count + pagination
└────────────┴───────────────────────────────────────────────────────────────────────────┘
```

* The page renders inside the standard app frame: 256px left bar, scrolling content, a centered
  content column.
* Every row is a whole-row click target that opens that repo ([One repo](./one_repo.mdx), §5).
* The table is a **flat data surface** — no card box, no colored header fill; rows separated by a
  single 1px bottom rule (the house table style, §3).

---

## 3. The table (TanStack, house style)

**All tables are TanStack tables** (charter rule). This is one flat, chromeless `DataTable`:

The rows are served to the table by the backend endpoint in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/repos/repos.router.ts` — `reposRouter` → `GET /api/repos`

### 3.1 Control row (above the table) — LOCKED

A single control row sits above the table, matching the standard toolbar:

* **Search box** (left) — a free-text input; substring match, case-insensitive, across **Repo name
  + path**. Filters the current data set live.
* **Filter icon** (right, icon-only `⛛` funnel — **no text label, no button box**) — opens a
  dropdown listing the filterable columns; each filter row is an **operator select + value input**;
  multiple filters are **ANDed**; Clear / Apply, `Enter` applies. The icon is **colored** when any
  filter is active (with an optional count). Applying a filter resets to page 1.
* **Sort icon** (right, icon-only sort glyph — **no text label, no button box**) — opens a dropdown
  listing the sortable columns, each with asc/desc; the icon is **colored** when a sort is active.
* **Header-click sort (LOCKED).** In addition to the sort dropdown, **clicking a column header**
  sorts the table by that column. Clicking the **same** header again **toggles ascending ↔
  descending** (it does not cycle to "unsorted" — the header always leaves a sort applied; clear it
  from the dropdown's **Clear sort**). This adds **no new UI** — the header text *is* the control; the
  active sort column shows a small **caret** (`↑` asc / `↓` desc) beside its label, plus a pointer
  cursor / hover on sortable headers. Header click and the dropdown share **one** sort state, so a
  header-driven sort shows as active in the dropdown and vice-versa. **Only sortable columns respond**
  — every column the sort dropdown lists (including the leading **Bookmark**, whose header sorts
  bookmarked-first exactly like the dropdown) is clickable; a column with `sortable: false` and the
  **non-data** trailing chevron / kebab cells do nothing on header click.
`DataTable` toolbar (search left; icon-only sort/filter popovers right; rounded fills reserved for
the floating popovers, never the data surface).

### 3.2 Columns

Column order, left to right. Numeric columns are right-aligned, `tabular-nums`.

| # | Column | Kind | Sort | Filter | Notes |
| --- | --- | --- | --- | --- | --- |
| 0 | **`▮` Bookmark** | toggle | ✓ | ✓ | **Leading** column — a ribbon **favorite** toggle (§8). A **control cell**: clicking it flips `bookmarked` and **never opens the repo**. Solid **yellow** when on, thin **gray outline** (white fill) when off. Sort surfaces bookmarked repos first; filter = Bookmarked yes/no. |
| 1 | **Repo** | text | ✓ | ✓ (contains) | Folder name, bold. |
| 2 | **Path** | text | ✓ | ✓ (contains) | Absolute local path, muted, middle-truncated with a tooltip of the full path. |
| 3 | **Synced** | int | ✓ | ✓ | `counts.synced`. Green metric. **Counts already-pinned files too:** any large file that was **already IPFS-pinned** on this computer and got **imported** into tracking ([ipfs.mdx §4](./ipfs.mdx#4-import--the-load-bearing-feature)) is present-and-pinned, i.e. **Synced** — so `Synced` reflects both LFB-synced and adopted-already-pinned files. The word is **Synced** (a state), never "Sync." |
| 4 | **Pending** | int | ✓ | ✓ | `counts.pending`. Amber when > 0. |
| 5 | **Undecided** | int | ✓ | ✓ | `counts.undecided`. Accent-highlighted when > 0 (there are decisions to make). |
| 6 | **Ignored** | int | ✓ | ✓ | `counts.ignored`. Muted. |
| 7 | **Peers** | int | ✓ | ✓ | `peerCount` — how many of your computers pin this repo. `0` renders red (nothing backing it up). |
| 8 | **Last sync** | timestamp | ✓ | ✓ | `lastSyncAt`, relative (`2h ago`) with an absolute tooltip; `—` if never. |
| 9 | **Status** | enum | ✓ | ✓ | `status` (§4.2) as a colored pill. `filterOptions` = the `RepoStatus` values. |
| — | *(chevron)* | — | — | — | Trailing **› right chevron** affordance (opens the repo, same as the row click). |
| — | **`⋮`** | — | — | — | The row **kebab** — the **repo** action catalog ([menus.mdx §3 / §5.1](./menus.mdx#3-the-row--kebab--every-table-has-one)). A control cell: it opens the menu, never the row. Right-click on the row opens the same menu ([menus.mdx §3.1](./menus.mdx#31-right-click-opens-the-same-menu-locked)). |

### 3.3 Pagination — LOCKED

* **Default page size 500.** Do **not** default to a small page size. Show the **item count**
  (`N repos`) to the left of the pager. Page-size options: `500 / 1000 / All`.

### 3.3.1 Height — fill the page (LOCKED)

The Repos table has **nothing beneath it** (the table is the last thing on the page), so it takes the
**full-page-height** house behavior:

* The table's **body scroll region grows to the bottom of the viewport** — no fixed short height, no
  dead white space below it when the browser window is tall. Bryan runs a **tall** window; a fixed
  `max-h` table would leave a big empty block at the bottom, which this removes.
* **The page itself does not scroll.** The **control row** (§3.1) and the **count/pagination footer**
  (§3.3) stay **pinned**; only the **rows** scroll, inside the table (the `thead` is already
  `sticky`). The body is the single scroll container the windowing (`performance.mdx` P-01) already
  reads.
* **Mechanism.** The scroll container is `flex-1 min-h-0` (fills its flex-column parent) instead of a
  fixed `max-h`; the page's outer element is a full-height flex column (`flex flex-1 flex-col
  min-h-0`) with the page header `shrink-0`, so the table is the one element that flexes to fill. The
  app frame's content column is already `min-h-full` so this height is available on every route.
* **This is the site-wide default** ([charter Tables rule](../CLAUDE.md)). The **only exception** is a
  page with content **below** the table — see [one_repo.mdx §3.5](./one_repo.mdx) and
  [directories.mdx](./directories.mdx) — where the table keeps a **bounded** height so what sits under
  it stays visible. Content **above** a table (headers, banners, stat tiles) never triggers the
  exception.

### 3.4 `ColumnDef` shape

```ts
// pages/repos/columns.ts — kinds drive the filter operators & alignment.
import type { ColumnDef } from "@tanstack/react-table";

export const repoColumns: ColumnDef<RepoRow>[] = [
  // Leading control cell — a favorite/ribbon toggle. Not a navigable cell (see §5.1, §8).
  { id: "bookmark",  header: "",          kind: "toggle", align: "center", accessor: r => r.bookmarked },
  { id: "name",      header: "Repo",      kind: "text",      accessor: r => r.name },
  { id: "path",      header: "Path",      kind: "text",      accessor: r => r.path },
  { id: "synced",    header: "Synced",    kind: "int",  align: "right", accessor: r => r.counts.synced },
  { id: "pending",   header: "Pending",   kind: "int",  align: "right", accessor: r => r.counts.pending },
  { id: "undecided", header: "Undecided", kind: "int",  align: "right", accessor: r => r.counts.undecided },
  { id: "ignored",   header: "Ignored",   kind: "int",  align: "right", accessor: r => r.counts.ignored },
  { id: "peers",     header: "Peers",     kind: "int",  align: "right", accessor: r => r.peerCount },
  { id: "lastSync",  header: "Last sync", kind: "timestamp",  accessor: r => r.lastSyncAt },
  { id: "status",    header: "Status",    kind: "enum", filterOptions: STATUS_OPTIONS, accessor: r => r.status },
];
```

---

## 4. Status

### 4.1 The counts, at a glance

Each row's four counts are the whole point of the page — they let the user decide, per repo, what
needs attention: **Undecided > 0** means there are files to triage; **Pending > 0** means bytes are
owed in or out; **Peers = 0** means nothing is backing this repo up.

> **These counts include git-ignored media of *any* size.** The scan's payload rule
> ([scan.mdx §4.1](./scan.mdx#41-what-counts-as-a-candidate--the-payload-rule-locked)) treats every
> git-ignored video/audio/image as a large file **regardless of the 100 MB threshold** — so a repo of
> 5–100 MB clips (e.g. `charlie-kirk`: 46 files, `Intel_Murder_Docus`: 27) shows its *real* Undecided
> count, not just the two or three files over 100 MB. A previous scanner bug gated everything on the
> size threshold and made these repos look nearly empty; that is fixed.

### 4.2 Status pill colors (LOCKED)

| `status` | Meaning | Pill |
| --- | --- | --- |
| `up_to_date` | Every "Sync" file present locally and on ≥1 peer. | green |
| `syncing` | A transfer is in progress right now. | blue (accent), animated |
| `behind` | `pending > 0` — files owed in or out. | amber |
| `needs_review` | `undecided > 0` — the user has decisions to make. | accent outline |
| `error` | Last sync failed, or the IPFS node is unreachable. | red |
| `never` | Repo added but never synced. | grey |

Precedence when several could apply: `error` > `syncing` > `behind` > `needs_review` > `up_to_date`
> `never`.

---

## 5. Row & cell navigation

### 5.1 Whole-row and per-cell targets

**Clicking a bare part of a row opens that repo** — it navigates to the per-file sync screen
([One repo](./one_repo.mdx)) at `/repos/$repoId`. The whole row is the default hit target
(`cursor-pointer`, hover highlight), routed via TanStack Router; the trailing **›** chevron is the
visual affordance and goes to the same place.

Whole-row click navigation to the repo's per-file screen is handled in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/ReposPage.tsx` — `ReposPage` `onRowClick` (navigates to `/repos/$repoId`)

**On top of that, each data cell is a deep link** to the most specific view for that cell
([menus.mdx §2](./menus.mdx#2-cell-navigation--clicking-a-cell-is-a-deep-link)) — so a click lands the
user exactly where they meant to go instead of opening the repo and re-filtering by hand:

| Cell | Click target |
| --- | --- |
| **`▮` Bookmark** | **Nothing — toggles the favorite** (§8). A control cell like `⋮`: it flips `bookmarked` and **never navigates**. |
| **Repo** | The repo page (`/repos/$repoId`) — same as the row. |
| **Path** | The [File System](./directories.mdx) browser revealed at that repo path. |
| **Synced** | The repo's files table filtered to **transfer = synced**. |
| **Pending** | The repo's files table filtered to **Pending**. |
| **Undecided** | The repo's files table filtered to **Undecided** (the triage queue). |
| **Ignored** | The repo's files table filtered to **decision = Ignore**. |
| **Peers** | [Peers](./left_bar.mdx) filtered to this repo (which of your computers pin it). |
| **Last sync** | The repo page (its status strip / recent activity). |
| **Status** | The repo page. |
| **`⋮`** | Opens the **repo action menu** — **never navigates** (§5.2). |

A linking cell shows a pointer cursor + hover emphasis; the counts read as clickable numbers.
`⌘`/middle-click opens the target in a new context where supported. Cell clicks **never mutate**.

### 5.2 The row `⋮` menu

The trailing **`⋮` kebab** opens the **repo** action catalog
([menus.mdx §5.1](./menus.mdx#51-repo)) — Open / Open in File System / View pinned on IPFS / Rescan /
Sync now / Git-ignore big files… / Toggle Synced / Repo settings… / Copy path / **Remove repo**
(destructive, last). Right-clicking the row opens the same menu
([menus.mdx §3.1](./menus.mdx#31-right-click-opens-the-same-menu-locked)). The kebab is a control cell:
it opens the menu and never opens the repo.

The trailing per-row repo kebab (and its action catalog) is rendered in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/components/menu/RowKebabs.tsx` — `RepoKebab` component (mounted as the table's `rowMenu` in `ReposPage`)

---

## 6. Add repo (top-right)

A **+ Add repo** button, top-right of the header. It opens a small dialog to register a git repo on
this computer with LFBridge:

* **Pick a folder** — a path field (the repo's git root). LFBridge validates it is a git working
  tree.
* On confirm: LFBridge reads the repo's `.gitignore` + manifest, scans for large git-ignored files,
  and adds a row. New large files it finds start as **Undecided** (the user triages them on the
  [One repo](./one_repo.mdx) screen).
* A repo already registered is rejected with an inline message (no duplicate rows).

Registering a repo by folder path (and kicking a background rescan) is handled in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/repos/repos.router.ts` — `reposRouter` → `POST /api/repos` (calls `registerRepo`)

### 6.1 Page actions — the action-links row

Directly under the page title — with the **+ Add repo** primary button (§6) staying in the header — the
page carries the page-level **action-links row** ([page_actions.mdx](./page_actions.mdx)): the visible
horizontal row of blue hyperlinks (each with a leading icon, **not** the old `Actions ▾` dropdown), the
third action surface, distinct from the per-row `⋮` repo kebab ([menus.mdx §5.1](./menus.mdx#51-repo)).
Because **Repos rows are repos, not files**, this page does **not** carry the file-producing actions
(Create Transcriptions / Create AI descriptions live on the file-list pages). Instead its links are the
"Repos list" row of the per-page catalog ([page_actions.mdx §4](./page_actions.mdx)) — the **whole-list**
operations:

* **Rescan all** — re-run the scan across **every** repo on this computer (the per-repo Rescan
  [menus.mdx §5.1](./menus.mdx#51-repo), lifted to all rows).
* **Sync all** — kick a sync on every repo that has pending work (the per-repo **Sync now**, lifted to all
  rows).

Scope resolves as usual ([page_actions.mdx §1.1](./page_actions.mdx)) — the **checked** repos if any rows
are checked, else **every** repo in the list; both run through the background queue and the progress dock,
so the header returns immediately.

implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/ReposPage.tsx` — `ReposPage` (mounts `PageActions` from `code/packages/frontend/src/components/menu/PageActions.tsx` — `PageActions`, scoped to the repo rows).

---

## 7. Empty & loading states

* **No repos:** the table is replaced by a dashed card — "No repos yet. Add your first repo →"
  (opens the Add-repo dialog).
* **Loading:** skeleton rows sized to the columns, so there is no layout shift.
* **Refetch:** existing rows stay visible until new data replaces them; a repo mid-sync shows the
  animated `syncing` pill without blocking interaction.

---

## 8. Bookmark (favorite) — the leading ribbon toggle

Each row's **leading cell** is a **bookmark** (favorite) toggle: a ribbon-shaped icon the user clicks
to mark repos they care about so they float to the top of a long list.

### 8.1 The icon and its two states (LOCKED)

Use the **ribbon bookmark** glyph — lucide **`Bookmark`** (the classic banner/ribbon shape), sized to
the row (≈16px), vertically centered in the leading cell.

| State | `bookmarked` | Rendering |
| --- | --- | --- |
| **On** (favorite) | `true` | **Solid yellow** — filled *and* stroked in the warning/yellow token (e.g. `#EAB308`). It reads as a fully-colored ribbon. |
| **Off** (default) | `false` | A **thin gray outline** with a **white** (empty) fill — just the 1px ribbon stroke in a muted gray, no fill. |

Implementation note: one `<Bookmark>` with `fill` toggled — `fill="currentColor"` + yellow color when
on; `fill="none"` + `stroke` muted-gray when off. On hover of an **off** star, preview a faint yellow
so it's discoverable. No layout shift between states (same box).

### 8.2 Interaction (LOCKED)

* **Click toggles** `bookmarked` for that repo. It is a **control cell** — exactly like the `⋮` kebab
  (§5.2): the click **stops propagation** and **never opens the repo** or triggers cell navigation.
* **Optimistic**: the icon flips immediately; the write (§8.3) happens in the background; on failure it
  reverts and shows a toast.
* **Accessible**: render as a `<button aria-pressed={bookmarked} aria-label="Bookmark this repo">`;
  it is keyboard-focusable and toggles on `Enter`/`Space`. Tooltip: *"Bookmark"* / *"Bookmarked"*.
* The same toggle is also offered in the repo **`⋮` menu** as *"Bookmark / Remove bookmark"*
  ([menus.mdx §5.1](./menus.mdx#51-repo)) so it is reachable without hunting for the tiny icon.

The leading ribbon toggle and its optimistic flip are implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/ReposPage.tsx` — `BookmarkToggle` (mutated via `ReposPage` `toggleBookmark`)

### 8.3 Persistence — it lives in the repo's `config.yaml` (LOCKED)

The bookmark is a **local, per-computer** preference (a favorite *this machine's* user set), so it
persists in that repo's **sync-unit `config.yaml`** — the same file that holds `synced`,
`big_file_override`, etc. — **not** in the shared manifest and **not** synced to peers.

```yaml
# sync/r/<repo>/config.yaml  (excerpt)
schema_version: 1
repo: { name: charlie-kirk, path: /Users/…/charlie-kirk, remote: … }
synced: false
bookmarked: true          # ← the favorite flag (default false). Written by the bookmark toggle.
big_file_override: { … }
```

* **Default `false`.** A newly discovered/added repo is not bookmarked.
* **The scan never touches it.** Discovery writes `status.yaml` and reconciles `config.yaml` **without
  clobbering** `bookmarked` (or any other user field) — re-registering an existing repo preserves it.
* **API:** `POST /api/repos/:repoId/bookmark` with `{ bookmarked: boolean }` writes the flag to
  `config.yaml` and returns the updated `RepoRow`. Idempotent; unknown `repoId` → 404.

Persisting the bookmark flag to the repo's `config.yaml` is handled in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/repos/repos.router.ts` — `reposRouter` → `POST /api/repos/:repoId/bookmark`

### 8.4 Sort & filter — the shared model ([tables.mdx](./tables.mdx))

The bookmark's sort/filter behavior is the **cross-cutting** one defined once in
[tables.mdx](./tables.mdx) (bookmarks, promoted filter, multi-level sort, per-user persistence) — Repos
is its **reference implementation**. In short:

* **Sort is multi-level** (primary / secondary / tertiary — [tables.mdx §3](./tables.mdx#3-multi-level-sort--primary--secondary--tertiary-locked)).
  The **default** here is **Bookmarked descending first, then repo name** — favorites float to the top
  of a 100-repo list. The sort dropdown's **1st/2nd/3rd** priority slots let the user re-rank; the
  choice is **saved per user for this page** and restored next visit.
* **Filter** — the filter dropdown **promotes a "Bookmarked only" checkbox** to the top
  ([tables.mdx §2](./tables.mdx#2-the-promoted-bookmarked-only-filter-locked)); the per-column
  *Bookmarked: yes / no* filter still exists in the standard list (§3.1).
* Bookmark state is **independent** of `status` — a bookmarked repo still shows its true status pill.
* **Persistence** — this page's sort + filter are saved in `users/<email>/config.yaml` `tables.views`
  ([tables.mdx §4](./tables.mdx#4-persistence--sort--filter-saved-per-user-per-page-locked)).

---

## 9. Acceptance criteria

1. The brand header shows **"Repos"** as normal text; the left-bar Repos item routes here (`/`).
2. The page is **one TanStack Table**, flat/chromeless, **one row per repo — every git repo on this
   computer** (all known automatically, no "managed" opt-in), with columns: a
   leading **bookmark toggle**, then Repo, Path, Synced, Pending, Undecided, Ignored, Peers, Last sync,
   Status (in that order), plus a trailing **›** chevron.
3. The **control row** above the table has a left **search box** and **icon-only** filter (`⛛`) and
   sort dropdowns on the right — no text labels, no button boxes; the icon colors when active;
   filters AND together and reset to page 1.
4. **Pagination defaults to 500** items per page; the **repo count** shows to the left of the pager;
   options are 500 / 1000 / All.
5. Each numeric count renders per §4.1 (Peers = 0 → red; Undecided > 0 → accent; Pending > 0 →
   amber) and the **Status** column is a colored pill per the locked §4.2 mapping and precedence.
6. **Clicking a bare part of a row** opens that repo at `/repos/$repoId` ([One repo](./one_repo.mdx));
   the trailing chevron does the same. **Each data cell is additionally a deep link** per §5.1 (counts
   → the repo's files filtered to that slice; Path → File System; Peers → Peers filtered to the repo).
   The **`⋮` kebab** (and right-click) opens the **repo** action menu ([menus.mdx §5.1](./menus.mdx#51-repo))
   and never navigates. The **Synced** count includes files that were **already IPFS-pinned and imported**
   ([ipfs.mdx §4](./ipfs.mdx#4-import--the-load-bearing-feature)).
7. **+ Add repo** registers a git repo by folder path, validates it is a git working tree, scans its
   large git-ignored files (new ones start Undecided), and rejects duplicates.
8. Empty / loading / refetch states render per §7 with no layout shift.
9. The **leading bookmark toggle** (§8) renders **solid yellow** when `bookmarked`, **thin gray
   outline / white fill** when not; clicking it flips the flag **without opening the repo** (a control
   cell), persists to the repo's `config.yaml` (`bookmarked`, default `false`, survives rescans), and
   is exposed as a sort (bookmarked first) and a Bookmarked yes/no filter.

<!--
  Keep this page aligned with one_repo.mdx (the per-file screen a row opens) and left_bar.yaml (the
  nav). LOCKED across specs: vocabulary Repo → Large file → Decision (Sync / Ignore / Undecided);
  the four per-repo counts (Synced / Pending / Undecided / Ignored); the RepoStatus enum + pill
  colors + precedence (§4.2); the standard control row (search left; icon-only sort/filter right);
  the leading bookmark toggle (§8) — solid-yellow/gray-outline ribbon, control cell, persisted in the
  repo's config.yaml; pagination default 500 with the item count shown. All tables are TanStack
  tables. The left bar lives in ./left_bar.mdx + pm/left_bar.yaml — never redefine it here.
-->

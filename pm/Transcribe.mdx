---
title: LargeFileBridge — Transcribe (audio & video → text)
description: The transcription feature. LFBridge transcribes AUDIO files (MP3, WAV, FLAC, M4A, AAC, OGG, …) and VIDEO files (MP4, MOV, MKV, WEBM, … — the audio track is demuxed to MP3 with ffmpeg first), turning speech into a plain-text transcript. Entry points span every scale: transcribe ONE file (its ⋮ kebab / right-click "Transcribe…", or the Transcribe button on the open MP3/MP4 in the Media Viewer), a FEW selected files, or ALL audio/video under a DIRECTORY, a REPO, or a whole STORAGE ("Transcribe all files…"). Transcripts are NOT stored in the local state dir and there is NO extra .transcribe/ directory — the transcript is written INSIDE the owning root's committed tracking area, path-mirrored, under the media's FULL filename (extension kept) with .transcription APPENDED as a second extension. WHERE that tracking area sits is keyed on the root's STORAGE KIND (artifact_placement_policy.mdx §0, the owner of the rule): in a WORKING GIT REPO it is the repo's hidden .lfbridge/ (repo file a/b/talk.mp3 → <repo>/.lfbridge/a/b/talk.mp3.transcription), because LFB is a guest there and quarantines its files in one hidden corner; in a DEDICATED LFB FILE REPO (a Personal/Company/Community SDL) there is NO .lfbridge/ AT ALL — the SDL repo root IS the .lfbridge area, so the mirror hangs straight off the root (~/BGit/Bryan_git/personal_large_files_bridge/_Mirror/…/x.mp4.transcription, NOT .lfbridge/_Mirror/…). Because both areas are committed, transcripts travel with the repo across the user's computers; appending (not replacing) the extension means talk.mp3 and talk.wav no longer collide. The transcription engine is COPIED into this repo's code tools directory (we do NOT reuse the ACT3-internal copy) and is called IN-PROCESS as a class from the web app — never by shelling out to `node Transcribe.js`. It drives the local `whisper` (+ `ffmpeg`) binaries, entirely on-machine (no network). Sibling of storages.mdx (the storage roots + the .lfbridge tracking area), media_viewer.mdx (the open-one-file view whose action bar gains Transcribe), menus.mdx (the ⋮ catalogs that gain Transcribe / Transcribe all files), and compression.mdx (the sibling "drive a local media tool over a file" engine this mirrors).
---

# LargeFileBridge — Transcribe (audio & video → text)

> **AUTHORITY NOTE (dominant).** The transcription engine **preference order** is **Apple SpeechAnalyzer →
> Whisper Small → Qwen3-ASR**, and the legacy **`SFSpeechRecognizer` is never used**. See
> [transcribe_engine.mdx §2](./transcribe_engine.mdx) for the full engine spec. This decision **takes
> precedence** over any earlier wording here that framed Qwen3-ASR as primary or Whisper at `base`.

LFBridge can **transcribe** the speech in a media file to a **plain-text transcript**. It works on
**audio** (MP3, WAV, FLAC, M4A, AAC, OGG, …) and on **video** (MP4, MOV, MKV, WEBM, … — the audio
track is demuxed to MP3 first), so any file whose "what was said" matters becomes searchable, quotable
text.

Two things make this spec worth pinning down: **where transcripts are stored** (a parallel hidden
hierarchy under the storage's own root — §3), and **how the engine runs** (a copy of our `Transcribe`
tool called **in-process** as a class, never shelled out — §5).

Siblings this builds on and does not repeat:

* [storages.mdx](./storages.mdx) — the storage roots a transcript hierarchy hangs under, and the
  `.lfbridge/` tracking area (§6 media analysis). Transcribe is the **authoritative** spec for how the
  transcript **files** are laid out; where `storages.mdx §6` sketched a `transcript.yaml` under
  `.lfbridge/analysis/`, **this file supersedes that for transcripts** (§3.3).
* [media_viewer.mdx](./media_viewer.mdx) — the open-one-file view; its action bar gains a **Transcribe**
  button for audio/video (§2).
* [menus.mdx](./menus.mdx) — the ⋮ kebab / right-click catalogs; the **file** catalog gains
  **Transcribe…**, the **directory/repo/storage** catalogs gain **Transcribe all files…** (§2).
* [compression.mdx](./compression.mdx) — the sibling engine that drives a local media tool over a file
  (offer → confirm → run → report). Transcribe mirrors its shape (a `tools`/`file`/`batch` REST surface,
  in-process spawning of a brew/pip binary).
* [webapp.mdx](./webapp.mdx) §10–§14 — the **progress dock** + in-process **job registry** every
  long-running job routes through. A transcription run registers here (kind `transcribe`) so a live,
  determinate card shows while Whisper runs (§5.1).
* [special_files.mdx](./special_files.mdx) — the classifier that sets the **`transcribable`** sub-flag
  (video — because it carries an audio track — and audio), i.e. *which* files this engine may run over.
* [Task tabs](./task_tabs.mdx) — the per-repo screen's **Transcribe tab**, where audio/video files are
  surfaced with a three-state **Transcribe status icon**; opening the tab filters the file table to
  transcribable files and sorts the not-yet-transcribed ones to the top. The tab's **`Transcribable` metric
  tile** opens the transcribe popup, and a blue **"Transcribe ›"** header primary appears when transcription
  is the top recommendation (§9.1).
* [placement_radios.mdx](./placement_radios.mdx) · [repo_settings.mdx](./repo_settings.mdx) — where the
  **placement radio** that chooses the transcript's artifact root lives. This spec describes the resolved
  path *shape* (§3); those specs own *choosing the root* (§9.2 — cross-link only, not re-specified here).
* [repo_tracking_scheme.mdx](./repo_tracking_scheme.mdx) — a completed transcription appends a
  **`transcribe` event** to the file's per-file sidecar (`output:` → the `.transcription` path) and a
  `TRANSCRIBE` line to the per-computer history log, and increments `repo_storage.yaml → counts.transcribed`.
* [use_case_two_computers.mdx](./use_case_two_computers.mdx) — the tour where a video's transcript is
  produced on one computer and travels with the repo (the sidecar event travels separately, via the
  company sync repo, since it is Local-Storage tracking state — [artifact_placement_policy.mdx](./artifact_placement_policy.mdx)).

---

## 1. What it does (the loop)

For one media file, transcription is:

1. **Classify** the file as audio or video by name ([media.ts `mediaKindForName`](../code/packages/shared/src/media.ts)).
   Anything else is skipped with a clear reason.
2. **Demux if video** — a video/container is not directly digestible by Whisper, so its audio track is
   extracted to a temporary MP3 with **ffmpeg** (`-vn -acodec libmp3lame`). A video with **no audio
   stream** writes a short "no audio to transcribe" placeholder instead of failing.
3. **Transcribe** the audio with the **preferred engine for this machine** — Apple **SpeechAnalyzer** on
   macOS 26+, otherwise **Whisper Small** (`whisper --model small --language en`, Apple-Silicon **MPS** with
   an automatic **CPU fallback** on garbage), otherwise the **Qwen3-ASR** third choice — following the fixed
   order and auto-fallback of [transcribe_engine.mdx §2](./transcribe_engine.mdx). The legacy
   `SFSpeechRecognizer` is never used. The engine is spawned **asynchronously** and (for Whisper) its stdout
   is streamed to derive a **determinate percentage** (segment end-timestamp ÷ media duration) — see §5.1.
4. **Write the transcript** — a plain-text file with a small metadata header (source name, timestamp,
   engine/model, source duration) — **inside the owning root's tracking area**, path-mirrored, under the
   media's full filename with `.transcription` **appended** (§3). In a **working repo** that area is the
   repo's `.lfbridge/`: `<repo>/a/b/talk.mp3` → `<repo>/.lfbridge/a/b/talk.mp3.transcription`. In a
   **dedicated LFB file repo (SDL)** it is the **root itself** — no `.lfbridge/` segment
   ([artifact_placement_policy.mdx §0](./artifact_placement_policy.mdx)).
5. **Report truthfully** — word count on success; an honest skip/error otherwise (never a silent
   "done"; the [pin_process.mdx §6](./pin_process.mdx#6-never-a-silent-no-op--report-truthfully-locked-learned-from-a-defect)
   rule applies here too).

Transcription is **always an explicit user action** — like compression, LFBridge **offers**, it never
transcribes a file on its own. It is **local-only**: no account, API key, or Google Cloud credential is
involved (§5.2).

The per-file loop (classify → ffmpeg demux → whisper → write transcript → report), wrapped in a
progress-registry job so the dock shows a live card, is implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/transcribe/transcribe.service.ts` — `transcribeOne()` (async; wraps the run in `track("transcribe", …)` from `modules/progress/progress.registry.ts`).

---

## 2. Entry points (every scale)

Transcription is reachable everywhere a media file, or a bag of them, is visible. The same backend
serves all of them.

### 2.1 One file — the Media Viewer button

Open an MP3 / MP4 (or any audio/video) in the **[Media Viewer](./media_viewer.mdx)**; its action bar
(band 3) shows a **Transcribe…** button (for `kind` audio or video), and the **Transcription** analysis
tab (band 6) shows a **Generate transcript** pane. Clicking either flips the control to a disabled
**Transcribing…** spinner **instantly** (the click never looks like a no-op — the defect this fix
addressed), and a live **progress card** appears in the dock. On completion the transcript tab fills in
and a toast reports the word count; when a transcript already exists the button reads **Re-transcribe**.

When the local tools are missing, the Transcription tab shows a **setup card** with the exact install
commands and a **Re-check** button that re-queries `GET /api/transcribe/tools` and re-enables the run
once fixed — no page reload (§5.2).

The instant pending state, the progress-dock nudge, and the setup card are implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/lib/useTranscribeFile.ts` — `useTranscribeFile()` (react-query mutation; toasts the honest outcome, invalidates `["transcript"]` + `["progress"]`) · `~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/entity/MediaViewer.tsx` — `ActionBar` / `MediaAnalysis` (calls `api.transcribeFile` / `api.transcript`) · `~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/components/TranscribeSetupCard.tsx` — `TranscribeSetupCard`.

### 2.2 One file — the ⋮ kebab / right-click

Every audio/video row (File System, repo files, storage files) and its single-entity page carry a
**Transcribe…** item in the **Work** group of the entity ⋮ catalog
([menus.mdx §5.3](./menus.mdx#5-the-per-entity-action-catalogs-locked)).

### 2.3 A few files — selection

A multi-select of media rows transcribes the selected set (the batch endpoint), mirroring the bulk
Compress path.

### 2.4 All files under a directory / repo / storage — "Transcribe all files…"

* **Directory** — the directory ⋮ catalog gains **Transcribe all files…**, which walks the directory for
  audio/video and transcribes each.
* **Repo** — the repo row / one-repo page ⋮ ([menus.mdx §5.1](./menus.mdx)) gains **Transcribe all
  files…** over the repo's working tree.
* **Storage** — the storage detail page ([storages.mdx §2](./storages.mdx)) gains a **Transcribe all**
  action over the whole storage.

The kebab/menu items (`Transcribe…` on a file, `Transcribe all files…` on a directory / repo / storage) are implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/components/menu/EntityMenu.tsx` — `buildActions()` (file + directory) · `~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/components/menu/RowKebabs.tsx` — `RepoKebab` · `~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/storages/StorageDetailPage.tsx` — `StorageDetailPage`.

### 2.5 The page action — "Create Transcriptions" (selected-or-all, background-queued)

The primary bulk entry point is the **`Create Transcriptions`** page action-link in the blue action-links row
under the title of every file-list page ([page_actions.mdx §0](./page_actions.mdx) — the hub that owns this
convention). Unlike
the "Transcribe all files…" kebab on a single directory/repo/storage (§2.4, which walks one entity), the
page action honours the **whole page's set** and the three LOCKED page-action rules:

* **Scope** ([page_actions.mdx §1.1](./page_actions.mdx)) — with rows **checked**, it transcribes exactly
  the checked set; with **nothing** checked, it transcribes **every media file on the page, recursively**.
* **Skip-already-done** ([page_actions.mdx §1.2](./page_actions.mdx)) — files that **already have a
  transcript** (§4 idempotency) and non-audio/-video files are dropped **before** anything is queued.
* **Background queue** ([job_queue.mdx](./job_queue.mdx)) — the eligible remainder is pushed onto the
  server-side queue and the request **returns immediately**; the queue drains in the background, each file
  surfacing a determinate `transcribe` dock card as it runs.

The instant it is invoked, one toast states the eligible count:
**"{N} files will have their Transcriptions created"** (or, when every candidate already had a transcript,
**"All {M} files already have transcriptions — nothing to do"**). `N` is the `willProcess` figure the
enqueue endpoint returns (§6, `POST /api/transcribe/enqueue`) — the eligible remainder, never the raw page
count. This is the worked example in [page_actions.mdx §1](./page_actions.mdx): 40 files, 12 already
transcribed, none checked → **28** queued → *"28 files will have their Transcriptions created"*.

The "Create Transcriptions" page action, its plan/skip/queue, and the toast are implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/lib/pageActions.ts` — `createTranscriptions()` · `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/transcribe/transcribe.service.ts` — `planTranscribe()` / `enqueueTranscribe()`.

---

## 3. Where transcripts are stored — inside the owning root's tracking area, path-mirrored, extension APPENDED (LOCKED — supersedes the earlier beside-media layout)

Transcripts do **not** go in the Local state dir, and — as of this revision — they do **not** sit loose
next to the media either. They live **inside the owning root's own tracking area**, in a **path-mirrored
hierarchy**, under the media's **full filename (extension kept)** with **`.transcription` appended** as a
**second** extension.

> **Which tracking area — the storage-KIND rule (owned by [artifact_placement_policy.mdx §0](./artifact_placement_policy.mdx)).**
> `.lfbridge/` exists **only inside a working git repo**, where LFB is a **guest** and quarantines its files
> under one hidden directory. A **dedicated LFB file repo** — the Personal / Company / Community **SDL
> storage** — has **no `.lfbridge/` directory at all**: the SDL repo root **is** the `.lfbridge` area, so the
> mirror hangs straight off the root. That policy spec is the **owner** of this rule; this spec only applies
> it and shows the resolved path shape.
>
> | Storage kind | Example root | The transcript lands at |
> | --- | --- | --- |
> | **`repo`** — a working git repo (Git Storage) | `~/BGit/Bryan_git/charlie-kirk/` | `<root>/.lfbridge/<rel-dir>/<name.ext>.transcription` |
> | **`personal` / `company` / `community`** — an SDL file repo | `~/BGit/Bryan_git/personal_large_files_bridge/` | `<root>/<rel-dir>/<name.ext>.transcription` (**no** `.lfbridge/`) |

> **This supersedes the previous LOCKED "beside the media, extension replaced" layout.** An earlier
> revision wrote `render/talk.mp3` → `render/talk.transcription` next to the media. The product owner's
> current, authoritative instruction replaces that: *"because I'm in the repo and what I'm transcribing is
> in the repo, it'll have that `.lfbridge` directory in the repo. Say a file is two directories deep in the
> repo — it creates those two directories inside the `.lfbridge`, then creates the same file name including
> that extension, and we add a second dot extension `.transcription`, and puts the transcription output
> there."* So the transcript is a **tracking artifact under `.lfbridge/`**, not a sibling of the media, and
> its name **appends** `.transcription` rather than **replacing** the original extension.

### 3.1 The layout — under the kind-resolved base, mirror the path, append `.transcription` (LOCKED)

For a **working-repo** file at `<repo>/a/b/talk.mp3`, the transcript is written to
`<repo>/.lfbridge/a/b/talk.mp3.transcription`:

```
WORKING GIT REPO (LFB is a guest → one hidden .lfbridge/ corner)
<repo>/
├── a/
│   └── b/
│       └── talk.mp3                       ← the real media file (untouched, still in the working tree)
└── .lfbridge/
    └── a/
        └── b/
            └── talk.mp3.transcription     ← the transcript: same relative path under .lfbridge/,
                                              full filename (talk.mp3) with .transcription APPENDED

SDL FILE REPO (a repo that exists ONLY for LFB → the root IS the .lfbridge area)
~/BGit/Bryan_git/personal_large_files_bridge/
└── _Mirror/
    └── Politics/
        └── talk.mp3.transcription         ← NO .lfbridge/ segment — the mirror hangs off the root
```

Three rules, together, define the path — **all LOCKED**:

1. **Root = `trackingBaseDir(<owning root>)`.** The transcript lands under the owning root's tracking base —
   **not** beside the media and **not** in `~/T/_large_files_bridge/`. That base is keyed on **storage kind**
   ([artifact_placement_policy.mdx §0](./artifact_placement_policy.mdx)): `<root>/.lfbridge/` for a **working
   repo**, and `<root>` **itself** for a **dedicated LFB file repo (SDL)**, which has no `.lfbridge/` at all.
   Note that the working repo's committed `.lfbridge/` now holds **only** the two content artifacts
   (transcripts and AI descriptions); LFB's tracking state — `repo_storage.yaml`, the per-file sidecars, and
   `history/` — lives in **Local Storage** at `~/T/_large_files_bridge/repos/<repoKey>/`, never the working
   repo ([repo_tracking_scheme.mdx](./repo_tracking_scheme.mdx),
   [artifact_placement_policy.mdx](./artifact_placement_policy.mdx)).
2. **Mirror the media's relative directory hierarchy.** A file two directories deep (`a/b/talk.mp3`)
   **creates `a/b/` inside the tracking base** and writes there. The media's path relative to the root is
   reproduced verbatim under that base (under `.lfbridge/` in a working repo; under the root itself in an
   SDL). **The mirroring is unchanged by the storage-kind rule** — only the base it hangs off differs.
3. **Keep the full filename, APPEND `.transcription`.** The transcript filename is the media's **entire
   filename including its original extension**, with **`.transcription` appended** — `talk.mp3` →
   `talk.mp3.transcription` (**not** `talk.transcription`). The `.transcription` token is a single constant
   — `TRANSCRIPTION_EXT` — **appended**, never substituted.

> **Why the tracking area (and not beside the media).** The working repo's `.lfbridge/` — and, in an SDL,
> the repo root — is **committed and travels with the repo**
> ([repo_tracking_scheme.mdx](./repo_tracking_scheme.mdx); the working tree's own
> `git status` shows the committed `.lfbridge/…/talk.mp3.transcription` artifact tracked). Putting the
> transcript there means it is **version-controlled and
> synced across the user's computers by git** — exactly the *"the transcript is produced on one computer and
> travels with the repo"* behavior of [use_case_two_computers.mdx](./use_case_two_computers.mdx) — while the
> media working tree stays **clean** (no loose `.transcription` files scattered next to videos), and every
> derived tracking artifact lives in **one** hidden place. Transcripts are small UTF-8 text, so committing
> them is cheap and desirable (unlike the multi-GB media, which stays git-ignored and rides IPFS).

### 3.2 Append-not-replace resolves the old collision caveat (LOCKED)

Because the original extension is **kept** and `.transcription` is **appended** (not substituted), two
media files that differ only by extension in the same folder **no longer collide**: `talk.mp3` →
`.lfbridge/…/talk.mp3.transcription` and `talk.wav` → `.lfbridge/…/talk.wav.transcription` are **distinct
files**. The earlier layout's accepted "`talk.mp3` and `talk.wav` overwrite each other" caveat is therefore
**retired** — appending the extension is precisely what fixes it.

The `.transcription` body is plain text and opens in the ordinary `/file` viewer like any other text file.
A tool that wants a media file's transcript computes the path deterministically:
`join(trackingBaseDir(root), relDir, mediaFilename + ".transcription")` — where `trackingBaseDir()` yields
`<root>/.lfbridge` for a working repo and `<root>` for an SDL (§3.1 rule 1). It **never** hard-codes
`".lfbridge"` into the join, which would put a phantom segment into every SDL path.

### 3.3 Relationship to `.lfbridge/analysis/`

[storages.mdx §6](./storages.mdx#6-per-file-media-analysis-transcription--description--visuals-by-time)
sketched transcripts as `transcript.yaml` under `.lfbridge/analysis/<relpath>/`. **This spec is
authoritative for the transcript files.** Transcripts live **inside the owning root's tracking area** (§3.1)
— the same area storages.mdx §6 pointed at — but as a **path-mirrored, human-openable plain-text
`<name.ext>.transcription` file placed directly under that base** (mirroring the media's relative
directory), **not** as a `transcript.yaml` and **not** under an `analysis/` sub-directory. When the
storage/repo file table needs to note that a transcript exists (the `analysis: ["transcript"]` indicator),
it detects the presence of `<trackingBase>/<rel-dir>/<name.ext>.transcription`, computed deterministically
from the media path. In an **SDL**, note that both this transcript mirror **and** the `analysis/` tree hang
off the **repo root** rather than a `.lfbridge/` — the `analysis/` family is one of the SDL metadata trees
the storage-kind rule relocates ([artifact_placement_policy.mdx §0.1](./artifact_placement_policy.mdx)).

### 3.4 Resolving where the parallel hierarchy lives — the ordered rule (LOCKED)

The transcript (and the `.ai_description` of ai_description.mdx §2) is written into a **placement root**
that does **not** always match the media file's nearest storage-carrying ancestor. A media file very often
lives **out in the user's wider filesystem** (e.g.
`~/_Mirror/Politics/Charlie_Kirk_Mi/CIA/FINAL_CIA_Drones.mp4`) — not inside a Git repo and not inside a
storage root. For those files we want the transcript to travel with the storage that owns them (often a
dedicated LFB file repo), mirroring the media's relative hierarchy under that root's tracking base.
Placement is therefore resolved by an **ordered rule** — the first case that matches wins. This is the
**authoritative** resolution for *every* derived-artifact producer (transcription and AI description share
it). In every case the transcript lands at **`<trackingBase>/<rel-dir>/<name.ext>.transcription`** — the
resolved `root` run through `trackingBaseDir()` (`<root>/.lfbridge` for a **working repo**, `<root>` itself
for an **SDL** — §3.1 rule 1), the media's mirrored relative directory, and the full filename with
`.transcription` **appended**:

**A. Containing storage root (walk-up).** Walk **up** from the media file's directory to the filesystem
root. The **nearest ancestor** carrying a `storage.yaml`, a `.lfbridge/`, or a `.git/` (a repo *is* a
storage — [storages.mdx](./storages.mdx)) is the containing root. This is the "repo root" walk the
product owner described: we are usually in some subdirectory *under* a repo, so we climb until we find the
`.git/`. When found:
* `root` = that ancestor · `rel` = the media file's path **relative to it** · the transcript lands at
  **`<trackingBase(root)>/<rel-dir>/<name.ext>.transcription`**. This is the **common, primary case** the
  product owner described: *"I'm in the repo and what I'm transcribing is in the repo,"* so the transcript
  is written **inside the repo's `.lfbridge/`**, path-mirrored, extension appended (§3.1).
* **Classify the found root by KIND before assuming it is a working repo.** Every SDL **also** carries its
  own `.git/`, so a walk-up that stops at a `.git/` and concludes "working repo → `.lfbridge/`" would put a
  phantom `.lfbridge/` segment inside an SDL. The **kind wins over the presence of `.git/`**: resolve
  `resolveStorageType(root)` **first**, and only fall back to "has `.git/` → working `repo`" for a root that
  resolves to no SDL type ([artifact_placement_policy.mdx §0.2](./artifact_placement_policy.mdx)).
* **No `*.transcription` git-ignore nudge is needed** anymore. Because the transcript lives **inside
  `.lfbridge/`** — which is **committed and travels with the repo** ([repo_tracking_scheme.mdx](./repo_tracking_scheme.mdx)) —
  it is version-controlled and synced by git across the user's computers on purpose (small text; the
  multi-GB media stays git-ignored and rides IPFS). This **removes** the earlier revision's need to inject a
  `*.transcription` line into a plain repo's `.gitignore`. A repo's own per-repo config UI is
  [repo_settings.mdx](./repo_settings.mdx) — reached *because* we detected we are inside a repo.

**B. Owning storage by mapping — routed to the dedicated SDL file repo (the fix, LOCKED).** When walk-up
finds nothing (the file is out in the wider filesystem), the file is still **owned** by a storage:
* **Which storage owns it.** The **most specific** company/personal storage whose **mapped source
  directory** ([syncable_data_location.mdx §3](./syncable_data_location.mdx), storage_settings.mdx §4a)
  contains the file — matched against this computer's **graft** local paths ([devices.mdx §4](./devices.mdx)).
  If no explicit mapped dir contains it, the **Personal** storage is the **default catch-all** owner for
  anything under the user's home tree ([storage_personal.mdx](./storage_personal.mdx)).
* **Where the artifact goes.**
  * If the owning storage has its **dedicated Git repo** backing **enabled** (storage_settings.mdx §4.1),
    the artifact is written **under that dedicated repo's ROOT — with NO `.lfbridge/` segment**, mirroring
    the file's hierarchy — `<dedicatedRepo>/<rel-dir>/<name.ext>.transcription`. That dedicated repo **is an
    SDL** (Personal / Company / Community), and an SDL has **no `.lfbridge/` at all**: the repo exists only
    to hold LFB's files, so hiding them under `.lfbridge/` inside a repo containing nothing else buys no
    separation and only adds a meaningless path segment
    ([artifact_placement_policy.mdx §0](./artifact_placement_policy.mdx)). `rel` is the file's path relative
    to its **mapped-dir root** (prefixed by the mapped-dir key), or relative to **`~`** for the personal
    default catch-all. The transcript is **committed** — the dedicated repo **exists to hold and back up
    these artifacts** (the product owner: *"it won't need to add the `.gitignore` if you have a dedicated git
    repo — it'll just put it in there"*).
  * If the owning storage has **no** dedicated repo enabled, the artifact goes under the storage's own
    **root** — `<storageRoot>/<rel-dir>/<name.ext>.transcription` for an SDL-kind storage (again **no**
    `.lfbridge/` segment), or `<storageRoot>/.lfbridge/<rel-dir>/…` when the resolved root is a working repo.

**C. First-time experience (no storage yet).** If the file is owned by **no** storage **and** the user has
**no Personal storage configured at all**, we do **not** silently write beside the media. The action is
gated by the **first-time setup wizard** (§3.5).

**D. Last resort.** Personal exists but the file is genuinely outside every mapped hierarchy and no
dedicated repo is enabled → the media file's **own directory** is the placement root. That directory is not
an SDL, so the working-repo branch of the kind rule applies: a `.lfbridge/` is created **in that directory**
and the transcript lands at `<mediaDir>/.lfbridge/<name.ext>.transcription`. This is the narrow fallback, not
the common path — the tracking-base + append-extension invariant (§3.1) still holds, so a transcript is
**never** written loose beside the media.

> **Worked example — the two kinds side by side (the reported case).** The contrast *is* the rule
> ([artifact_placement_policy.mdx §0](./artifact_placement_policy.mdx)):
>
> **SDL file repo (no `.lfbridge/`).** With the Personal storage's **dedicated Git repo** turned ON at
> `~/BGit/Bryan_git/personal_large_files_bridge/`, transcribing
> `~/_Mirror/Politics/Charlie_Kirk_Mi/CIA/FINAL_CIA_Drones.mp4` writes to
> `~/BGit/Bryan_git/personal_large_files_bridge/_Mirror/Politics/Charlie_Kirk_Mi/CIA/FINAL_CIA_Drones.mp4.transcription`
> — mirrored straight off the dedicated repo's **root**, with **no `.lfbridge/` segment** (that path was the
> old, now-**wrong** layout), the full filename with **`.transcription` appended**, **committed** (travels
> with the repo), and **no** `.transcribe/` directory.
>
> **Working git repo (keeps `.lfbridge/`).** Transcribing `~/BGit/Bryan_git/charlie-kirk/videos/_QwGo0LyZ3I.mp4`
> writes to `~/BGit/Bryan_git/charlie-kirk/.lfbridge/videos/_QwGo0LyZ3I.mp4.transcription` — LFB is a guest
> in the user's own project, so it quarantines the artifact under the hidden `.lfbridge/`. Likewise, in-repo,
> a file two directories deep at `<repo>/CIA/FINAL_CIA_Drones.mp4` writes to
> `<repo>/.lfbridge/CIA/FINAL_CIA_Drones.mp4.transcription`.

The ordered resolution (walk-up → owning-storage/dedicated-repo → first-time signal → last-resort), the
kind-resolved path-mirrored placement, the **append**-`.transcription` naming, and the home-`~`
expansion of pasted paths are implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/storage/artifact-placement.service.ts` — `resolveArtifactPlacement()` (transcript path via `lfbridgeArtifactPath(root, rel, ext)` = `join(trackingBaseDir(root), rel) + TRANSCRIPTION_EXT`, where `TRANSCRIPTION_EXT` is **appended**, not substituted) and `artifactPathForPlacement(root, rel, ext, placement)`.
Deciding the base that path hangs off — `<root>/.lfbridge` for kind `repo`, `<root>` itself for an SDL — is the single choke point every LFB writer resolves through (none joins `LFBRIDGE_DIR` directly), implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/storage/storage-type.service.ts` — `trackingBaseDir(root, type?)` / `usesLfbridgeDir(type)` / `resolveStorageType(root)`.
Migrating a pre-existing SDL `.lfbridge/` up to the root (with a read-fallback to the old path until it is migrated, so no transcript is reported missing or regenerated mid-migration) is implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/config/migrate-sdl-lfbridge.ts` — `migrateSdlLfbridge()`.
The transcript path (`<root>/.lfbridge/<rel-dir>/<name.ext>.transcription`) and the owning-root resolution wrap it in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/transcribe/transcribe.service.ts` — `resolveTranscriptPath()` / `resolveStorageRoot()`. The per-case `gitIgnore` flag is **retired** — transcripts live in the committed `.lfbridge/` area, so no `*.transcription` ignore line is ever injected.

### 3.5 The first-time setup wizard — actions that trigger it (LOCKED)

A derived-artifact action must know **where its output will land before it runs**. When placement rule C
fires — the file is owned by no storage and **no Personal storage exists yet** — the action is redirected
to a **first-time setup wizard** instead of producing a file in a surprising place.

* **Triggering actions.** Any derived-artifact action taken on a file/directory that resolves to
  `needsSetup`:
  * **Transcribe** (this spec).
  * **Get AI details** / **AI description** ([ai_description.mdx §2](./ai_description.mdx)).
  (The list grows as new per-file analysis actions are added; every one consults placement first.)
* **What the wizard does.** It walks the user through the minimum configuration for the situation:
  1. **In a repo?** If the file *is* under a repo (rule A would have matched) we never reach the wizard —
     repo configuration is [repo_settings.mdx](./repo_settings.mdx). The wizard is only for the
     no-storage case.
  2. **Company vs personal.** If a **company** storage already covers the path, use it. Otherwise
     **default to Personal** — create the one Personal storage.
  3. **Where the Personal storage's artifact root lives.** Default to creating it under the **home (`~`)
     directory** ([storage_personal.mdx §1](./storage_personal.mdx)); or, if the user prefers, point it at
     a **dedicated Git repo** (`~/BGit/Bryan_git/personal_large_files_bridge/`) — in which case artifacts
     store **under that repo's root** per rule B, with **no `.lfbridge/` segment** (it is an SDL —
     [artifact_placement_policy.mdx §0](./artifact_placement_policy.mdx)).
* **After setup.** The originally-clicked action re-resolves placement (now a storage exists) and runs —
  the user lands back where they were, no reload.

The backend reports `needsSetup` from the same placement resolver; every derived-artifact launcher checks it
and, when set, opens the wizard (via a global request bus, so no entry point needs bespoke wiring) and
re-runs the original action once a Personal storage exists. The wizard offers exactly the two choices above
— **Dedicated Git repo** (recommended; artifacts tracked + backed up, placement rule B) or **Plain folder** —
and commits by creating the one Personal storage at its canonical root. Implemented in code:
* `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/storage/artifact-placement.service.ts` — `resolveArtifactPlacement()` (the `needsSetup` flag).
* `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/storage/storage.router.ts` — `GET /api/storages/placement` (what-would-this-do) · `POST /api/storages/personal/create` (the wizard's commit).
* `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/storage/storage.service.ts` — `createPersonalStorage()` (creates the root, optional `git init`, enables the dedicated-repo backing).
* `~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/components/FirstTimeStorageWizard.tsx` — `FirstTimeStorageWizardProvider` (the modal) · `~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/lib/setupWizard.ts` — `requestStorageSetup()` (the bus the Transcribe / Get-AI-details launchers call).

### 3.6 Pasted paths are `~`-expanded (LOCKED)

Users paste paths that begin with `~` (e.g. `~/BGit/Bryan_git/personal_large_files_bridge`). Every entry
point home-expands the input **before** any resolution, so `~/…` and the absolute form behave identically
everywhere — walk-up, ownership matching, and dedicated-repo routing all operate on the expanded absolute
path. Home-expansion of every inbound path is handled in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/fs/badges.ts` — `expandHome()` (applied at each service entry point and inside `resolveArtifactPlacement()`).

---

## 4. What a transcript file contains

A transcript is plain UTF-8 text: a short metadata header, a divider, then the transcript body. The header
records the **actual engine + model** used ([transcribe_engine.mdx](./transcribe_engine.mdx)) — the preferred
Apple **SpeechAnalyzer** (`apple-speechanalyzer`), the **Whisper Small** second choice (`whisper-small`), or
the heavyweight **Qwen3-ASR** third choice (`qwen3-asr-1.7b-mlx`) — and the **source duration**, so a reader
can confirm the transcript covers the whole file (§critical: the full 1–2h+ duration,
[transcribe_engine.mdx §4](./transcribe_engine.mdx)):

```
Transcription of: talk.mp3
Generated on: 2026-07-05 09:41:00
Engine: apple-speechanalyzer (device: on-device, language: en)
Source duration: 01:47:12   ·   Transcript covers: 01:47:12  ✓ full
============================================================

Welcome back to the show. Today we're talking about …
```

A video with **no audio stream** yields a one-line placeholder (`[No audio stream — nothing to
transcribe]`) so a re-run over a tree can skip it cheaply. Results are **idempotent by path**: an existing
transcript at `<trackingBase>/<rel-dir>/<name.ext>.transcription` (§3) is reported as "already
transcribed" unless the caller asks to overwrite (Re-transcribe). A transcript whose header shows
**`Transcript covers` < `Source duration`** (a truncated run) is treated as **incomplete** and is a
candidate to re-transcribe, never a silent success ([transcribe_engine.mdx §4](./transcribe_engine.mdx)).

---

## 5. The engine — copied in, called in-process (LOCKED)

> **Deep engine spec: [transcribe_engine.mdx](./transcribe_engine.mdx).** This section defines the
> *in-process, non-blocking* discipline (the load-bearing §5.1 rule). The **three engines** in their
> preference order (Apple **SpeechAnalyzer** preferred → **Whisper Small** → heavyweight **Qwen3-ASR**; never
> the legacy `SFSpeechRecognizer`), the **macOS-update warning** when the hardware supports SpeechAnalyzer but
> the OS is too old, the **on-demand model download/install/configure with the user's permission** for the
> Qwen third choice (disk estimate, health-check/repair, the two progress UIs, and the flow-resume after
> install), the **critical full-duration / no-truncation requirement for 1–2h+ media**, and the **per-machine
> parallel calibration** all live in [transcribe_engine.mdx](./transcribe_engine.mdx). This file references it
> and does not restate it.

The transcription engine is our existing **`Transcribe.js`** tool (canonically
`~/BGit/all/tools/Transcription/Transcribe.js`, and a copy in the ACT3-internal web app). LFBridge does
**not** reuse the ACT3-internal copy and does **not** bounce through the command line or spawn a separate
`node` process to run it. Instead:

* The engine is **copied into this repo's own code `tools/` subdirectory** and adapted to TypeScript as a
  **class** with a callable method (no `process.argv`, no `process.exit`, no top-level auto-run). We own
  our copy; upstream changes are pulled in by re-copying, not by importing across repos.
* The web-app backend **imports the class and calls it directly, in-process** — one function call, a
  structured result back. This is the same discipline as
  [compression.mdx](./compression.mdx): drive the local media binary from inside the Node process.

* **It still drives the local `whisper` and `ffmpeg` binaries** — those are the actual
  transcription/demux tools (Whisper is a Python CLI). "In-process" means *we don't shell out to our own
  Node script*; invoking the underlying `whisper`/`ffmpeg` executables is expected and mirrors how
  `compression.service.ts` invokes `ffmpeg`/`magick`.
* **Entirely local at run time — no phone-home.** Transcription runs **on-machine**; nothing about the
  **media or the transcript** ever leaves the computer (charter alignment: only-our-content, no phone-home).
  The **one** networked step is the **one-time provisioning download of open-source model weights** (the
  heavyweight Qwen3-ASR weights) — this is analogous to `brew install` fetching a formula, sends **none
  of the user's content**, happens **only with explicit permission**, and never recurs once installed. See
  [transcribe_engine.mdx §3](./transcribe_engine.mdx).
* **Binaries vs. model weights are two different "missing" cases.**
  * A **missing CLI binary** (`whisper`, `ffmpeg`) is handled by the **Tools preflight**
    ([tools.mdx](./tools.mdx)) / the **setup card** (§5.2): a clear `tool_missing` result with the exact
    install command and a **Re-check** — an **explicit-click** install, never silent.
  * A **missing / half-installed / outdated heavyweight model** is a **separate, larger provisioning step**
    that LFBridge **can download, install, and configure for the user — with their explicit permission and
    a disk-space warning** — showing a **download progress UI** then an **install/configure progress UI**
    ([transcribe_engine.mdx §3](./transcribe_engine.mdx)). This is the piece the earlier "does not
    auto-install behind a web request" wording did **not** cover; it is now allowed **because the user
    approves it** (charter consent posture preserved).

The copied in-process engine (ffmpeg demux + Whisper with the MPS→CPU fallback and English sanity check, returning a structured result instead of logging/exiting) lives in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/tools/transcribe/Transcribe.ts` — class `Transcriber`, methods `toolStatus()` / `transcribeToFile()`.

### 5.1 Async & non-blocking — the "Generate transcript didn't work" fix (LOCKED)

The standalone `Transcribe.js` uses `spawnSync`/`execSync`. Copied verbatim that would be a **defect in a
web server**: `spawnSync` **blocks the Node event loop for the entire Whisper run**. On a 6-minute video
that froze the whole API — `GET /api/health` and `GET /api/progress` stopped responding, the open
`POST /api/transcribe/file` request looked dead, and the button appeared to "do nothing." This was the
reported bug.

The in-process engine therefore spawns **every** child process (`ffprobe`, `ffmpeg`, `whisper`)
**asynchronously** (`child_process.spawn`, awaited) so the event loop stays free to serve other requests
while a transcription runs. Two behaviours ride on that:

* **Determinate progress.** Whisper streams each decoded segment to stdout as `[start --> end]`; the
  engine parses the **end timestamp** and divides by the media **duration** (from `ffprobe`) to emit a
  0–1 fraction. `transcribeOne()` maps that to a `{ done, total: 100, unit: "%" }` update on its
  progress-registry job, so the dock card fills a real bar. When duration is unreadable the card stays a
  spinner rather than lying.
* **Cross-tab visibility.** Because the run registers in the server-side job registry, the dock card
  shows even in a browser tab that did **not** start it (a launchd tree run, another window).

The async spawn (with the streamed stdout progress parse and the duration probe) is implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/tools/transcribe/Transcribe.ts` — class `Transcriber`, private `spawnAsync()` / `runWhisper()` / `probeDurationSec()` / `parseSegmentEndSec()` (the public `transcribeToFile()` takes an `onProgress` sink).

### 5.2 No credentials, no cloud — the setup card (LOCKED)

Transcription needs **no Google Cloud credential, API key, or account of any kind** — it is *local
Whisper*. (The only credential file LFBridge reads, `~/.credentials/large_files_bridge.json`, is for
Google **OAuth sign-in** and the AI-**description** Gemini path — see
[security.mdx](./security.mdx) / [ai_description.mdx](./ai_description.mdx) — and is unrelated to
transcription.) The repo therefore ships **no credentials file for transcription**, and the copied
engine directory contains only the code.

What *can* be missing is the two free binaries. When `GET /api/transcribe/tools` reports `whisper` (or
`ffmpeg` for video) absent, the Transcription tab renders a **setup card** that mirrors the
credentials-setup pattern used elsewhere: it names each tool, its status, the exact install command
(`pipx install openai-whisper`, `brew install ffmpeg`), and a **Re-check** button. Pressing Re-check
re-queries the tools endpoint; once the tool appears the run is re-enabled **without a page reload** —
the "fix it, then hit the button to run again" flow the product owner asked for.

The setup card + Re-check are implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/components/TranscribeSetupCard.tsx` — `TranscribeSetupCard` (Re-check calls the `["transcribe-tools"]` query's `refetch`). The full install
preflight also lists `whisper` (alongside `ffmpeg`/`ffprobe`) under **Settings → Tools**, implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/settings/ToolsPage.tsx` — `buildRows()` (reads `GET /api/transcribe/tools` via `api.transcribeTools()` for the `whisper` row; `GET /api/compress/tools` for `ffmpeg`/`ffprobe`, shared with the Compress feature since both drive the same binary).

#### 5.2.1 Detection detail — why a genuinely-installed tool must never be reported missing (LOCKED)

The setup card's entire premise — "install what's missing, then Re-check" — only works if the **detection**
is trustworthy. A false "missing" for a tool that is actually on the machine sends the user chasing a
problem that doesn't exist (the exact complaint this hardening pass answers: *"I clicked Transcribe and it
failed — I suspect it didn't install what it needs,"* when in fact `whisper`, `ffmpeg`, and the Qwen3-ASR
model were all present).

* **How detection works.** `commandExists(cmd)` shells out to `which` (`where` on Windows) — never a shell
  alias, never `command -v` (which can see builtins/aliases a spawned child can't run). A tool is "installed"
  exactly when `which` finds it **on the searched `PATH`**.
* **The PATH gap this closes.** Both `whisper` and `mlx-qwen3-asr` are installed by **pipx**, which places
  its shims in **`~/.local/bin`** — a directory that is on an **interactive shell's** `PATH` (via `.zshrc` /
  `.bash_profile`) but is **not** guaranteed to be on the `PATH` of every process that might run this code:
  a `launchd` agent with no `EnvironmentVariables` block gets macOS's bare minimum
  (`/usr/bin:/bin:/usr/sbin:/sbin`), and even a hand-rolled `EnvironmentVariables` PATH (see the IPFS
  daemon's autostart plist, which hard-codes `/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin`) still omits
  `~/.local/bin`. Measured on a real machine with all three tools genuinely installed, a minimal/launchd-style
  `PATH` finds **none** of `whisper`, `ffmpeg`, or `mlx-qwen3-asr`; a Homebrew-only `PATH` finds `ffmpeg` but
  still misses the two pipx tools. So the risk is real, not hypothetical: a background-launched or
  restricted-environment process can see the **same machine** as "missing everything" while an interactive
  terminal sees it as fully set up.
* **The fix — search a WIDENED path, not the bare inherited one.** Tool detection (`commandExists()`) and
  tool **execution** (`spawnAsync()`, so a bare command name like `"whisper"` resolves the same way it was
  detected) both search `process.env.PATH` **plus** the well-known install locations pipx and Homebrew use:
  `~/.local/bin`, `/opt/homebrew/bin`, `/opt/homebrew/sbin`, `/usr/local/bin`, `/usr/local/sbin`, in addition
  to the standard system dirs. This is **additive only** — an already-correct interactive `PATH` is untouched
  — so it only ever makes a genuinely-installed tool **more** likely to be found, never less.
* **Detection and execution must agree.** It would be a subtler version of the same bug to detect a tool
  successfully (widened `PATH`) but then fail to actually run it (bare inherited `PATH`) — the button would
  look enabled and then fail on click. Both `commandExists()` and `spawnAsync()` search the identical widened
  `PATH` for exactly this reason.

The widened tool-search `PATH` (shared by detection and execution, so the two never disagree) is implemented
in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/tools/transcribe/audio-prep.ts` — `toolSearchPath()` / `toolEnv()` / `commandExists()` / `spawnAsync()`, reused by
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/tools/transcribe/qwen-asr.ts` and
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/transcribe/model-provision.service.ts`
(pipx/brew/ffmpeg detection during provisioning, §3 below). The Whisper engine
(`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/tools/transcribe/Transcribe.ts` — private
`commandExists()` / `spawnAsync()`) keeps its own copy of the engine logic (deliberately not merged into
`audio-prep.ts`, to avoid churn on the working engine) but draws the **same** `toolEnv()` so the two engines
never disagree on what's installed.

#### 5.2.2 Every entry point gates identically — no bypass (LOCKED)

Transcription has two frontend launchers that must apply the **exact same** gates before calling the
backend: the **Media Viewer** button/tab (`useTranscribeFile()`, §2.1) and the **⋮ kebab / row actions**
(`runTranscribeFile()` in `lib/transcribe.ts`, §2.2). Both funnel through `withModelReady()`
([transcribe_engine.mdx §3.2](./transcribe_engine.mdx#32-the-first-time-permission-popup-locked)) before
ever calling `api.transcribeFile()`, so the first-time heavyweight-model consent popup interposes **no
matter which button the user clicked** — a real defect this hardening pass fixed: the Media Viewer hook
previously called the transcribe API directly, silently skipping the consent gate every time (the model
would then always resolve straight to whichever fallback was available, and the user was never offered the
higher-quality engine on their first run from that button). The two launchers are implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/lib/useTranscribeFile.ts` — `useTranscribeFile()` (wraps its `run()` in `withModelReady()` before `mutate()`) ·
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/lib/transcribe.ts` — `runTranscribeFile()` / `withModelReady()`.

> **The heavyweight model is provisioned separately — with permission — not by this setup card.** The
> setup card covers only the small **CLI binaries**. The **heavyweight model weights** (multi-GB,
> [transcribe_engine.mdx §3](./transcribe_engine.mdx)) are a distinct provisioning flow: on the **first**
> transcribe attempt where the model is absent, LFBridge shows a **permission popup** with a **disk-space
> estimate** and, on approval, **downloads → installs → configures** it (each with its own progress UI),
> then **resumes the original action**. If the model is already present the popup is **skipped**; if it is
> half-installed or outdated, a **health check** finishes/repairs it. That flow is owned by
> [transcribe_engine.mdx](./transcribe_engine.mdx), not restated here.

---

## 6. REST surface

Allow-list-gated like every data route, mirroring `compression.router.ts`:

| Method + path | Body / query | What |
| --- | --- | --- |
| `GET /api/transcribe/tools` | — | Which binaries are present: `swiftc` (for the preferred `speech` engine), `whisper` + `ffmpeg` (Whisper Small), `mlx-qwen3-asr` (Qwen). Drives the disabled state + install hints. |
| `GET /api/transcribe/engine` | — | **Engine + model readiness** ([transcribe_engine.mdx §2/§3](./transcribe_engine.mdx)): which engine is **active** (`speech` / `mac` / `qwen`); the **`speech`** readiness block (`available`, `osMajor`, **`needsOsUpdate`** — hardware supports SpeechAnalyzer but OS < 26, §2.2 — and `hardwarePossible`); and, for the heavyweight **`qwen`** third engine, whether its model is `installed` / `partial` / `missing` / `outdated`, its on-disk size, and the disk estimate for a fresh install. Drives the macOS-update notice and the Qwen permission popup. |
| `POST /api/transcribe/engine/provision` | `{ approve: true }` | **Download → install → configure** the heavyweight model (explicit consent). Registers a `download` then an `install` progress job ([transcribe_engine.mdx §3.3](./transcribe_engine.mdx)); returns immediately, watchable on [Processing](./processing.mdx). |
| `GET /api/transcribe/file?path=<abs>` | — | The existing transcript for a media file (text + transcript path), or `null`. |
| `POST /api/transcribe/file` | `{ path, overwrite? }` | Transcribe ONE file → the `<trackingBase>/<rel-dir>/<name.ext>.transcription` transcript under the owning root — `.lfbridge/` in a working repo, the root itself in an SDL (§3). |
| `POST /api/transcribe/batch` | `{ paths[], overwrite? }` | Transcribe a selected set. |
| `POST /api/transcribe/tree` | `{ path, overwrite? }` | Transcribe ALL audio/video under a directory or repo. |
| `POST /api/transcribe/storage/:id` | `{ overwrite? }` | Transcribe ALL audio/video in a storage. |
| `POST /api/transcribe/enqueue` | `{ paths?, root?, overwrite? }` | **Plan + background-queue** (the page action-link, §2.5). Resolves the eligible set (checked `paths` or the recursive `root`, minus already-transcribed), pushes it onto the [background queue](./job_queue.mdx), and **returns the plan immediately** (`EnqueuePlan` — [page_actions.mdx §5](./page_actions.mdx)), not the results. |

The transcription REST surface (tools / read-transcript / file / batch / tree / storage / **enqueue**) is implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/transcribe/transcribe.router.ts` — `transcribeRouter`, mounted at `/api/transcribe` in `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/main.ts`. The `POST /enqueue` handler calls `enqueueTranscribe()` and returns without awaiting the queue.

> **Long-running note.** Whisper is slow (minutes per file), so the request can stay open for a while —
> but the engine is **async and non-blocking** (§5.1), so a slow run no longer freezes the server, and
> every run registers a **progress-registry job** (kind `transcribe`) that the dock surfaces via
> `GET /api/progress`. A single-file run reports a **determinate %**; a tree/storage run reports one
> card per file as it works through the set. Each file still reports its own result, so a tree run
> returns honest per-file counts (transcribed / skipped / failed). The progress registry + the dock are
> specified in [webapp.mdx](./webapp.mdx) §10–§14 (code: `modules/progress/progress.registry.ts`,
> `modules/progress/progress.router.ts`, and the frontend `progress/ProgressContext.tsx` /
> `components/ProgressDock.tsx`).

---

## 7. Acceptance criteria

1. LFBridge transcribes **audio** (MP3, WAV, FLAC, M4A, AAC, OGG, …) and **video** (MP4, MOV, MKV,
   WEBM, … — audio demuxed via ffmpeg first) to a **plain-text transcript**; non-media files are skipped
   with a reason; a video with no audio yields a placeholder.
2. **Entry points at every scale:** the **Media Viewer** Transcribe button on an open audio/video; a
   **Transcribe…** item in the file ⋮ kebab / right-click / more-menu; a selected-set batch; and
   **Transcribe all files…** on a **directory**, a **repo**, and a **storage**.
3. Transcripts are stored **inside the owning root's committed tracking area** (not the Local state
   dir, and no `.transcribe/` directory), **mirroring the media file's relative directory path** under that
   base, keeping the media's **full filename including its extension** with **`.transcription`
   appended**. Because that area is **committed**, transcripts are version-controlled and travel with the
   repo across the user's computers; **no `*.transcription` git-ignore line is injected**. Appending (not
   replacing) the extension means `talk.mp3` and `talk.wav` no longer collide.
3a. **The base is keyed on storage KIND** ([artifact_placement_policy.mdx §0](./artifact_placement_policy.mdx),
   the owner of the rule), resolved through the single choke point `trackingBaseDir(root)` — **no** code path
   joins `.lfbridge` directly:
   * **Working git repo** (kind `repo`, the default) → **keeps** `.lfbridge/`:
     `<repo>/a/b/talk.mp3` → `<repo>/.lfbridge/a/b/talk.mp3.transcription`.
   * **Dedicated LFB file repo** (an SDL — kind `personal` / `company` / `community`) → **no `.lfbridge/`
     at all**, the mirror hangs off the **root**: `~/_Mirror/…/x.mp4` →
     `~/BGit/Bryan_git/personal_large_files_bridge/_Mirror/…/x.mp4.transcription` (**not**
     `…/personal_large_files_bridge/.lfbridge/_Mirror/…`).
   * An SDL's own `.git/` **never** demotes it to a working repo — the walk-up of §3.4 rule A classifies by
     **type first**, and only then falls back to "has `.git/` → working repo".
4. The **owning root** is the nearest ancestor with `storage.yaml` / `.lfbridge/` / `.git/`; when none
   exists, a `.lfbridge/` is created in the file's own directory as the base (§3.4 rules A–D).
5. The engine is **copied into this repo's `tools/` directory** (the ACT3-internal copy is **not**
   reused) and is **called in-process as a class** — never by shelling out to `node Transcribe.js`; it
   still invokes the local `whisper` + `ffmpeg` binaries and runs **entirely on-machine** at run time (the
   only networked step is the one-time, permissioned model-weights download, §5 / [transcribe_engine.mdx](./transcribe_engine.mdx)).
6. Missing `whisper`/`ffmpeg` **binaries** produce a clear **tool-missing** result with an install hint
   (explicit-click install, no silent no-op); a missing / half-installed / **outdated heavyweight model**
   triggers the **permission-gated provisioning** flow ([transcribe_engine.mdx §3](./transcribe_engine.mdx))
   — a disk-space warning, then download → install → configure with progress UIs — not a bare error. Every
   run **reports truthfully** (word count on success, honest skip/error otherwise).
7. Transcription is **always an explicit user action** — LFBridge offers, never transcribes on its own; and
   the heavyweight model is **never downloaded/installed without explicit permission** (charter consent).
8. The engine is **async / non-blocking**: a running transcription never freezes the API (health &
   progress keep responding), the button flips to a **Transcribing…** pending state the instant it's
   clicked, and a **live progress card** (determinate % for one file) shows in the dock — including for a
   run this tab did not start.
9. Transcription needs **no credentials and no cloud** (local engine); the repo ships **no transcription
   credentials file**. When the local **binaries** are missing, a **setup card** shows the install commands
   and a **Re-check** button that re-enables the run once fixed, **without a page reload**; when the
   **model** is missing/partial/outdated, the permission popup + provisioning flow of
   [transcribe_engine.mdx](./transcribe_engine.mdx) handles it and then **resumes the original action**.
9a. **Three engines (preference order) + long-media robustness** ([transcribe_engine.mdx](./transcribe_engine.mdx)):
    Apple **SpeechAnalyzer** (preferred, macOS 26+, on-device — Apple's NEW engine, never the legacy
    `SFSpeechRecognizer`) → **Whisper Small** (second) → heavyweight **Qwen3-ASR** (third); a Mac whose
    hardware supports SpeechAnalyzer but runs an OS **older than macOS 26** gets a **macOS-update warning that
    names the Whisper-Small fallback** and still transcribes (§2.2). Whichever runs, the settings are
    **triple-checked to transcribe the FULL duration of 1–2h+ media without truncating** — the run is
    verified to cover the source duration end-to-end (a short/truncated result is a failure, never a silent
    success), and the machine's **parallel-job count is calibrated** to its cores/RAM/model (low-power
    boxes = 1 at a time; a ~24-core box runs several).
10. **Bulk discovery via the To-Do transcribe slugs:** the on-demand **What Can Be Transcribed Calc
    Engine** ([transcribe_calc_engine.mdx](./transcribe_calc_engine.mdx)) — triggered only by the
    **"Show what could be transcribed"** link on the To Do page, never by the scheduled scan — walks every
    storage for transcribable audio/video **lacking a `.transcription` sidecar** and writes one per-storage
    **transcribe batch**; each renders as a **transcribe slug** whose ~80%-of-screen popup lists candidates
    **checked by default**, and **Apply** ("Transcribe N files") **enqueues background jobs** (this spec's
    §5 engine, unchanged) watchable on [processing.mdx](./processing.mdx). Completed files gain a sidecar
    and drop out of the next run.
11. **Transcribe-tab metric tile + header primary (§9.1):** clicking the **`Transcribable`** metric tile
    opens the transcribe popup (candidates checked by default → **"Transcribe N files"** confirm → model
    gate may interpose → async parallel → Processing), instead of the old no-op that re-selected the tab.
    When transcription is the repo's top pending recommendation, the header primary is a **blue**
    (`--lfb-primary`) **"Transcribe ›"** button (trailing `›`) that opens the same popup; it sits at the
    **transcribe** slot of the worst-first order **ipfs-down → pull-down → undecided → transcribe → describe
    → compress**, is **model-gated on Apply not on display**, and disappears (green-`0` tile) once every
    audio/video file has a complete transcript. **Where** the transcript lands is not decided here — it is
    the placement radio owned by [placement_radios.mdx](./placement_radios.mdx) / [repo_settings.mdx](./repo_settings.mdx)
    (§9.2, cross-link only).

## 8. Bulk discovery — "Show what could be transcribed" (the To-Do transcribe slugs) (LOCKED)

Every entry point in §2 acts on media the user has **already found** — a file they opened, a directory
they right-clicked, a page they scoped. This section adds the **discovery** direction: a single, on-demand
sweep that answers *"across all my storages, what could still be transcribed?"* and lands the answer on the
**To Do** page as clickable **transcribe slugs**. It **produces nothing** on its own — it only lists
candidates. The actual transcription is **this spec's §5 engine, wholesale** — nothing about the engine,
the sidecar layout (§3), or the truthful reporting (§1) changes; discovery is purely a **fan-out that
feeds Apply**.

### 8.1 The engine — on-demand, never the scheduled scan (LOCKED)

The **What Can Be Transcribed Calc Engine** ([transcribe_calc_engine.mdx](./transcribe_calc_engine.mdx))
is **explicitly on-demand**: it runs **only** when the user clicks the **"Show what could be
transcribed"** action link on the To Do page — it is **never** part of the periodic background scan
([scan_process.mdx](./scan_process.mdx) / the launchd tree run). Discovery walks are expensive over cloud
mounts and whole-home trees, so they happen when the user asks, not on a timer.

When invoked it walks **each storage** — Personal, Dropbox, every Google Drive, and every repo
([storages.mdx](./storages.mdx)) — for **VIDEO and AUDIO** files that are:

* **Transcribable** — audio, or **video that carries an audio track**. This is the same
  `transcribable` flag defined in [special_files.mdx §3](./special_files.mdx#3-the-transcribable-flag)
  (a silent video is **not** a candidate — it would only ever yield the "no audio" placeholder of §4), and
* **Not yet transcribed** — they have **no `.transcription` transcript** at the resolved
  `<trackingBase>/<rel-dir>/<name.ext>.transcription` path (§3), **or** an existing transcript whose header shows
  it **did not cover the full source duration** (a truncated run, §4 / [transcribe_engine.mdx §4](./transcribe_engine.mdx)).
  This is exactly the **skip-already-done** test the page action uses
  (§2.5 / [page_actions.mdx §1.2](./page_actions.mdx)): *absence of a complete transcript* is the candidate test, so a
  file transcribed by any earlier path never re-appears.

It writes **one "transcribe batch" per storage** into `~/T/_large_files_bridge/_do_batches/` (`kind:
transcribe`) — the shared To-Do batch area whose schema is
[to_do_batches.mdx §4](./to_do_batches.mdx#4-the-transcribe-batch-schema) (`transcribe_video` /
`transcribe_audio` totals + the per-file `items`).

The on-demand per-storage / all-storage walk (transcribable-and-no-sidecar candidate test, one
`kind: transcribe` batch written per storage) is implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/todo/transcribe-scan.engine.ts` — class `WhatCanBeTranscribedCalcEngine`, methods `scanStorage()` / `scanAll()`.
The link that triggers it posts a scan route, implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/todo/todo.router.ts` — `POST /api/todo/transcribe-scan`.

### 8.2 The transcribe slug + the ~80% popup (Apply → background jobs → Processing) (LOCKED)

On the **To Do** page ([to_do.mdx §7](./to_do.mdx#7-the-transcribe-slugs)), each transcribe batch renders
as a **transcribe slug** (one per storage). Its **blue chevron** opens an **~80%-of-screen popup** that
lists the batch's candidates with **checkboxes checked by default** (checked = transcribe), so the common
"transcribe everything found" case is one click; the user unchecks anything to exclude. An **Apply** button
labelled **"Transcribe N files"** (N = the checked count) confirms.

Apply **does not transcribe inline.** It **enqueues background transcription jobs** — the same
[background queue](./job_queue.mdx) / progress-registry `transcribe` jobs the §2.5 page action uses — so
the popup returns immediately and the work drains in the background, **watchable on the
[Processing](./processing.mdx) page** (one determinate `%` card per file, §5.1). As each file completes it
gains a **`.transcription` sidecar** (§3) and therefore **drops out of the next discovery run** — the To-Do
transcribe slugs are self-clearing as the user works through them.

The transcribe slug, its default-checked ~80% popup, and the "Transcribe N files" Apply that enqueues the
background jobs are implemented in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/todo/TodoPage.tsx` — `TranscribeBatchPopup` (the popup + Apply) and `ShowTranscribableLink` (the "Show what could be transcribed" trigger).

## 9. The Transcribe status icon & the Transcribe tab (LOCKED)

On the per-repo screen ([one_repo.mdx](./one_repo.mdx)) the **Transcribe tab**
([task_tabs.mdx §4.4](./task_tabs.mdx#44-transcribe--get-transcripts)) narrows the files table to
**transcribable** files (audio, or video with audio — [special_files.mdx §3](./special_files.mdx#3-the-transcribable-sub-flag--video-has-audio-and-audio))
and adds a per-row **Transcribe status icon** (lucide `Captions`) with **three** states — the authoritative
definition is [task_tabs.mdx §5](./task_tabs.mdx#5-the-transcribe-status-icon--three-states-locked):

* **Done** — the file has a `.transcription` transcript (§3) → **solid blue fill, white edge/glyph**.
* **Could be transcribed** — audio/video with **no** transcript yet (exactly this engine's candidate) →
  **red/orange normal-thickness edge, 99%-white / 1%-tint fill** (the token is the shared "actionable"
  warm color the product owner refers to as **red** — the same `could` wash the Compress icon uses;
  see [task_tabs.mdx §5](./task_tabs.mdx#5-the-transcribe-status-icon--three-states-locked)). Clicking it
  **queues** a transcription for that one file (a background job, [processing.mdx](./processing.mdx)) — the
  same enqueue the transcribe popup uses.
* **Not applicable** — not audio/video → **white fill, very-thin very-light-grey edge** (inert).

* **Position (LOCKED — [task_tabs.mdx §4.4/§5](./task_tabs.mdx#5-the-transcribe-status-icon--three-states-locked)).**
  On the Transcribe tab the icon is a **leading control column to the LEFT of the File column** — the same
  slot the **Pin** and **IPFS** icons occupy on their tabs — and its column header carries **no title text**
  (an icon-only control column). The product owner's instruction: *"move that transcribe icon over to the
  left of the file icon and get rid of the title of it — like the pin icons and the IPFS icon."*

The tab's default sort puts the red/orange "could" rows first (most work-to-do at the top), then Size desc.
The icon is a control cell — it stops click propagation and never navigates the row.

The Transcribe status icon is rendered in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/components/TranscribeStatusIcon.tsx` — `TranscribeStatusIcon` (state from the new `FileRow.transcribe` field composed in `~/BGit/Bryan_git/LargeFileBridge/code/packages/backend/src/modules/store-model/units.service.ts` — `composeFileRows`).

### 9.1 The `Transcribable` metric tile → popup, and the blue "Transcribe ›" header primary (LOCKED)

On the Transcribe tab the **`Transcribable`** metric tile ([task_tabs.mdx §2](./task_tabs.mdx#2-the-metrics-panels--the-what-could-be-done-boxes-locked))
counts the audio/video files with no complete transcript yet. **Clicking it opens the transcribe popup** —
the same ~80%-of-screen confirm flow §5.1 walks (candidates checked by default → uncheck → solid-blue
**"Transcribe N files"** → model gate may interpose → async parallel → Processing). This is part of the
metric-tile fix ([task_tabs.mdx §2.4.1](./task_tabs.mdx#241-every-actionable-metric-tile-opens-a-popup--the-transcribe--compress--describe-builders-the-fix-locked)):
before it, clicking `Transcribable` did nothing (it merely re-selected the Transcribe tab you were already
on); now it opens the popup and lets the user **start the transcription**.

* **The blue "Transcribe ›" header primary.** When transcription is the repo's **top pending
  recommendation**, the header primary button ([one_repo.mdx §3.1](./one_repo.mdx#31-header)) is a **blue**
  (`--lfb-primary`) **"Transcribe ›"** (white text, trailing right chevron) — the verb-named variant of the
  smart header button. It opens the **same** transcribe popup the `Transcribable` tile opens. Its place in
  the worst-first order is **ipfs-down → pull-down → undecided → transcribe → describe → compress**
  ([task_tabs.mdx §2.7](./task_tabs.mdx#27-the-per-metric-blue-header-primary--every-actionable-metric-can-be-the-recommendation-locked)):
  transcription outranks describe and compress, but a data-integrity condition (a paused IPFS engine,
  peer-pinned files not here, or files still needing a decision) always shows first.
* **Model-gated on Apply, not on display.** The **"Transcribe ›"** primary (and the tile's popup) shows
  whenever transcription is pending even if the heavyweight model isn't installed; the permission +
  provisioning gate (§5 / [transcribe_engine.mdx §3](./transcribe_engine.mdx)) fires only when the popup's
  **"Transcribe N files"** Apply runs, and then **resumes** the batch — identical to the per-row "could"
  status-icon path (§9, `withModelReady`).
* **Green-zero.** When every audio/video file already has a complete transcript, the `Transcribable` tile is
  a **light-green big `0`** and no **"Transcribe ›"** header primary shows — the Transcribe tab reads as done.

The `Transcribable` tile's and the **"Transcribe ›"** header primary's shared `WarningDef` (the same
transcribe popup, scoped to the counted candidates, gated by `withModelReady`) is built in code:
`~/BGit/Bryan_git/LargeFileBridge/code/packages/frontend/src/pages/repos/metricWarningDefs.tsx` —
`buildTranscribeWarning()`, wired into `buildMetricWarning()` (the `transcribable` case) and ranked in
`topRecommendation()` (the transcribe slot of the worst-first order).

### 9.2 Where the transcript lands is chosen elsewhere — cross-links only (LOCKED)

Nothing on the Transcribe tab decides **where** a transcript file is written — that is owned by the
**placement radios** and the **repo settings**, not this feature:

* **Placement radios** — the repo/storage setting that picks the artifact home (the `lfbridge` choice, a
  dedicated Git repo, or beside-the-media as a last resort) is specified in
  **[placement_radios.mdx](./placement_radios.mdx)** and surfaced per repo in
  **[repo_settings.mdx](./repo_settings.mdx)**. §3 of this spec describes the resolved *path shape*
  (`<trackingBase>/<rel-dir>/<name.ext>.transcription`); the **radio that chooses the root** lives there.
  Note the radio option is still **named `lfbridge`** — a frozen wire literal — even in an **SDL**, where it
  resolves to the **root** with no `.lfbridge/` segment
  ([artifact_placement_policy.mdx §0](./artifact_placement_policy.mdx)).
* **AI descriptions share the identical placement.** The `.ai_description` artifact
  ([ai_description.mdx §2](./ai_description.mdx)) resolves through the **same** ordered rule (§3.4) and the
  **same** placement radios — so the Transcribe and AI-descriptions tabs behave identically on *where the
  output goes*, differing only in the artifact and the engine.

This spec does **not** re-specify placement configuration — it only points at it, so the two surfaces stay
in one place.

<!--
  Transcribe = audio+video → plain-text transcript. LOCKED:
  (§3) transcripts live INSIDE the owning root's COMMITTED tracking area — NO .transcribe/ dir, NOT beside
       the media, NOT the local state dir: <trackingBase>/<rel-dir>/<name.ext>.transcription — mirror the
       media's relative dir path UNDER that base, keep the FULL filename incl. extension and APPEND
       .transcription as a 2nd ext. TRANSCRIPTION_EXT is APPENDED, never substituted
       (artifact-placement.service.ts lfbridgeArtifactPath/artifactPathForPlacement). The area is COMMITTED
       so transcripts travel with the repo across computers — NO *.transcription git-ignore line is injected
       (gitIgnore flag retired). Appending (not replacing) the ext means talk.mp3 & talk.wav no longer
       collide (old caveat RETIRED). SUPERSEDES the earlier beside-media/replace-ext LOCKED layout AND
       storages.mdx §6's transcript.yaml sketch.
  (§3.1/§3.4) THE BASE IS KEYED ON STORAGE KIND — rule OWNED by artifact_placement_policy.mdx §0, referenced
       here, never re-litigated. trackingBaseDir(root, type?) / usesLfbridgeDir(type) in
       storage-type.service.ts is the SINGLE CHOKE POINT; NO writer joins LFBRIDGE_DIR directly.
       - WORKING GIT REPO (kind `repo`, the DEFAULT) → KEEPS .lfbridge/ (LFB is a guest → quarantine in one
         hidden corner): ~/BGit/Bryan_git/charlie-kirk/.lfbridge/videos/_QwGo0LyZ3I.mp4.transcription
       - DEDICATED LFB FILE REPO / SDL (personal|company|community) → NO .lfbridge/ AT ALL, the root IS the
         .lfbridge area: ~/BGit/Bryan_git/personal_large_files_bridge/_Mirror/…/x.mp4.transcription
         NOT …/personal_large_files_bridge/.lfbridge/_Mirror/… (the OLD, now-WRONG pattern).
       An SDL always has its own .git/ — the KIND WINS over the presence of .git/, so §3.4 rule A's walk-up
       MUST classify by TYPE BEFORE falling back to "has .git/ → working repo". A pre-existing SDL .lfbridge/
       is migrated to the root by migrate-sdl-lfbridge.ts (migrateSdlLfbridge) with a READ-FALLBACK until
       migrated. UNCHANGED by that decision: the APPEND-the-ext naming, the path-MIRRORING itself, and the
       placement radio's 3 choices — only what `lfbridge` RESOLVES TO in an SDL changes (the root).
  (§5) engine = our Transcribe.js COPIED into code/packages/backend/src/tools/transcribe/ as TS classes,
       called IN-PROCESS (no `node Transcribe.js`, no reuse of ACT3-internal copy). PREFERENCE ORDER
       (DOMINANT): Apple SpeechAnalyzer (macOS 26+, on-device, Swift helper speech/SpeechAnalyzerCLI.swift —
       NEVER legacy SFSpeechRecognizer) → Whisper Small (whisper-small, not base) → Qwen3-ASR (third). §2.2:
       Mac hardware supports SpeechAnalyzer but OS<26 → warn + name Whisper-Small fallback, still transcribe.
       Spawns local swiftc/whisper/mlx-qwen3-asr + ffmpeg; entirely on-machine, no network (SpeechAnalyzer
       model ships in OS). Missing tools → clear tool_missing.
  (§5.1) ASYNC/non-blocking is LOCKED: use child_process.spawn (awaited), NOT spawnSync — spawnSync froze
       the whole server for the whole Whisper run (the "Generate transcript didn't work" bug). Stream
       whisper stdout `[start --> end]` ÷ ffprobe duration → determinate % on a track("transcribe",…) job.
  (§5.2) NO credentials / NO cloud — local Whisper only. large_files_bridge.json is OAuth+Gemini, NOT
       transcription. Missing binaries → TranscribeSetupCard with install cmds + Re-check (refetch tools).
  (§2) entry points: MediaViewer button + Transcription tab (instant Transcribing… pending), file ⋮
       Transcribe…, batch, dir/repo/storage "Transcribe all files…". Always explicit user action.
  (§8) BULK DISCOVERY: on-demand What Can Be Transcribed Calc Engine (transcribe-scan.engine.ts
       WhatCanBeTranscribedCalcEngine.scanStorage/scanAll; POST /api/todo/transcribe-scan) — triggered ONLY
       by To-Do "Show what could be transcribed" link, NEVER the scheduled scan. Walks each storage for
       transcribable (special_files.mdx §3) audio/video with NO .transcription sidecar → one kind:transcribe
       batch per storage in _do_batches/ (to_do_batches.mdx §4). Renders as a transcribe slug (to_do.mdx §7);
       blue chevron → ~80% popup, list checked-by-default, Apply "Transcribe N files" ENQUEUES background
       jobs (§5 engine wholesale, not re-implemented) watchable on processing.mdx; done files gain a sidecar
       and drop out next run. Frontend TodoPage.tsx TranscribeBatchPopup/ShowTranscribableLink.
  Code pointers: transcribe.service.ts (async transcribeOne/transcribeTree; wraps track() from
  progress.registry.ts), transcribe.router.ts (async /api/transcribe tools|file|batch|tree|storage),
  tools/transcribe/Transcribe.ts (Transcriber.transcribeToFile + spawnAsync/runWhisper/probeDurationSec/
  parseSegmentEndSec), frontend lib/useTranscribeFile.ts (pending+toast+progress invalidation),
  components/TranscribeSetupCard.tsx (setup+Re-check), MediaViewer ActionBar/MediaAnalysis, EntityMenu
  buildActions, RowKebabs RepoKebab, StorageDetailPage. Mirrors compression.mdx's engine+router shape.
  Keep consistent with media_viewer.mdx §4/§6, menus.mdx §5, and webapp.mdx §10–§14 (progress dock/registry).
-->
